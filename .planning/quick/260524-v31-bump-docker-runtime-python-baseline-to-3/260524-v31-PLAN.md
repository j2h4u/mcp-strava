---
quick_id: 260524-v31
slug: bump-docker-runtime-python-baseline-to-3
status: ready
created: 2026-05-24
---

# Quick Task 260524-v31 - Bump Docker/runtime Python baseline to 3.14

## Goal

Move the declared project runtime and Docker runtime image from Python 3.13 to Python 3.14, then verify that the MCP server still builds, starts, smokes, and satisfies the read-model p95 gate.

## Tasks

| Task | Files | Action | Verify |
|------|-------|--------|--------|
| 1 | `pyproject.toml`, `uv.lock`, `deploy/Dockerfile` | Change runtime baseline to Python 3.14. | Lock metadata and Docker build agree on 3.14. |
| 2 | `tests/test_phase01_validation.py`, `tests/test_docker_runtime.py`, docs/context files | Update version assertions and repo context that still names 3.13. | Targeted tests pass in Docker test container. |
| 3 | Docker runtime | Rebuild and restart MCP image, then run smoke and p95 gate. | `just test` and `just mcp-read-model-perf` pass. |

## Acceptance

- Runtime image uses `python:3.14-slim`.
- Package metadata requires Python `>=3.14`.
- Tests no longer assert the old 3.13 baseline.
- Docker MCP smoke passes.
- Warm MCP p95 gate remains below 500 ms for every product tool.
