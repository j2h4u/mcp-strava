---
quick_id: 260524-v31
slug: bump-docker-runtime-python-baseline-to-3
status: complete
completed: 2026-05-24
---

# Quick Task 260524-v31 - Summary

## Outcome

Bumped the project and Docker runtime baseline from Python 3.13 to Python 3.14.

## Changes

- `deploy/Dockerfile` now uses `python:3.14-slim`.
- `pyproject.toml` and `uv.lock` now declare `requires-python = ">=3.14"`.
- Version assertions in runtime tests now expect Python 3.14.
- Codebase stack context and `AGENTS.md` no longer describe Python 3.13 as the active runtime baseline.

## Verification

| Command | Result |
|---------|--------|
| `docker run --rm -v "$PWD":/src:ro -w /tmp/mcp-strava python:3.14-slim ... python -m pytest -q ...` | `Python 3.14.5`, `115 passed in 41.68s` |
| `just test` | Passed; Docker image rebuilt from `python:3.14-slim`, container became healthy, MCP smoke returned the five product tools and called `list_workouts`. |
| `docker compose -f deploy/docker-compose.yml exec -T mcp-strava python --version` | `Python 3.14.5` |
| `just mcp-read-model-perf` | Passed; p95 ms: `get_fitness_state=19.021`, `list_workouts=17.371`, `get_workout_detail=13.448`, `compare_periods=26.662`, `project_fitness_state=12.079`. |
| `docker compose -f deploy/docker-compose.yml exec -T mcp-strava python -m mcp_strava admin db-check --json` | Passed; `user_version=5`, `integrity_result=ok`, `metric_dirty_activities=0`, read-model fact counts unchanged. |

## Notes

No live data migration was required. The live container was recreated on Python 3.14 and continued using the existing v5 read-model database.
