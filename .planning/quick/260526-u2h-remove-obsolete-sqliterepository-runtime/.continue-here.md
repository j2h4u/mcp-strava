---
context: quick
quick_task: 260526-u2h-remove-obsolete-sqliterepository-runtime
phase: 08-duckdb-primary-storage-aggregate-analytics-surface
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-05-26T16:56:17.608Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

- [ ] CONSTRAINT: No backward-compatibility archaeology by default. The user explicitly rejected special removed-command warnings, legacy aliases, retired code, obsolete tests, and "keep it just in case" shims. If you feel tempted to preserve an old path for compatibility, stop and ask the user for explicit approval.

Do not proceed until the box is checked.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Removed-command special casing | A deleted CLI command was still detected only to print a historical message: `_usage_error("admin db-migrate was removed; DuckDB is the only runtime storage.")`. | blocking | Deleted commands must fall through to the normal unknown-command path unless the user explicitly approves a compatibility notice. |
| Tests preserving obsolete behavior | Many tests still import SQLite adapter modules and cutover commands even after DuckDB became the only runtime storage. | blocking | Delete or rewrite those tests so they verify current DuckDB behavior only. |
| Source modules kept as fossils | Old report, analytics, trends, SQLite repository, and cutover modules kept old architecture reachable or testable. | blocking | Remove the modules and update callers to current application services or DuckDB repository factories. |

<current_state>
Paused during quick task `.planning/quick/260526-u2h-remove-obsolete-sqliterepository-runtime/`.

Trigger: the user saw a targeted CLI usage error for removed `admin db-migrate` and objected that this is backward compatibility archaeology. The task expanded from one string removal into a cleanup of the whole class of stale SQLite/cutover/runtime compatibility paths.

Current branch: `feat/phase-8-duckdb-primary-storage`.

Current GSD project state says Phase 09 is complete, Phase 08 plan 08-08 remains open because the 100 ms p95 performance gate previously failed. This quick task is local cleanup before continuing performance/validation work.
</current_state>

<completed_work>

Completed or partially completed in this session:

- Created quick plan: `.planning/quick/260526-u2h-remove-obsolete-sqliterepository-runtime/260526-u2h-PLAN.md`.
- Removed runtime SQLite routing from `src/mcp_strava/db.py`; repository factories now route to DuckDB only.
- Removed old SQLite migration/preflight/cutover paths from deploy and refresh bootstrap code.
- Deleted `src/mcp_strava/adapters/sqlite/` files.
- Deleted `src/mcp_strava/adapters/duckdb/migrations.py`, which was one-shot SQLite to DuckDB cutover tooling.
- Deleted old `src/mcp_strava/analytics.py`, `src/mcp_strava/report.py`, and `src/mcp_strava/trends.py`.
- Removed the explicit special-casing for removed admin commands from `src/mcp_strava/cli.py`; `db-migrate` and `duckdb-cutover` are no longer in `ADMIN_COMMANDS`.
- Removed old CLI fallback rendering for non-bundle daily/weekly data; daily and weekly render current product bundles only.
- Simplified `src/mcp_strava/training.py` to Banister/EWMA and forward simulation only. Removed old weekly plan/progressive signal helpers.
- Removed old weekly-plan/report dataclasses from `src/mcp_strava/types.py`.
- `tests/test_read_model_queries.py` had already been partly rewritten toward DuckDB fixture helpers before the pause.

No tests have been run after the latest source edits.
</completed_work>

<remaining_work>

- Run marker search:
  `rg -n "SQLiteRepository|adapters\\.sqlite|sqlite3|run_preflight|run_migrations|run_duckdb_cutover|DuckDBMigrationError|duckdb-cutover|db-migrate|legacy|retired|mcp_strava\\.analytics|mcp_strava\\.report|mcp_strava\\.trends|daily_report\\(|weekly_digest\\(|compute_trends\\(" src tests docs README.md Justfile`
- Delete or rewrite SQLite-only tests. Do not keep tests for removed behavior.
- Update docs to describe DuckDB-only runtime. Remove `admin db-migrate`, `admin duckdb-cutover`, `sqlite3`, and old `strava.db` runtime instructions unless they are clearly marked as external historical data notes. Prefer removing historical runbooks entirely.
- Re-run the marker search and make it clean except for legitimate Strava `gear.retired` field references.
- Run the test suite and fix fallout. Start with `uv run pytest -q` or the project gate used locally, then run the expected `just test` gate if available.
- Update quick SUMMARY/STATE if the task is completed.

Likely test files requiring deletion or rewrite:

- `tests/test_sqlite_safety.py`
- `tests/test_phase01_validation.py`
- `tests/test_full_fidelity_mirror.py`
- `tests/test_load_status.py`
- `tests/test_duckdb_migration.py`
- `tests/test_repository_boundary.py`
- `tests/test_phase4_e2e.py`
- `tests/test_read_model_materialization.py`
- `tests/test_application_services.py`
- `tests/test_metric_services.py`
- `tests/test_cli_surface.py`
- `tests/test_docker_runtime.py`
- `tests/test_refresh_runtime.py`
- `tests/test_smoke.py`
- `tests/test_metric_registry.py`
- `tests/test_security_guards.py`

Important dependency notes:

- `tests/test_metric_services.py` and `tests/test_mcp_surface.py` import helpers/constants from `tests/test_read_model_queries.py`; preserve current DuckDB helper exports if still useful.
- `tests/test_docker_runtime.py` and `tests/test_cli_surface.py` import fixture helpers from SQLite-only tests; replace with DuckDB fixture helpers or remove those cases.
- Do not reintroduce SQLite modules to satisfy old tests.
</remaining_work>

<decisions_made>

- DuckDB is now the only runtime storage. SQLite is not a live adapter.
- Removed commands should not have tailored "this command was removed" messaging. They should be unknown commands.
- Tests must verify current product/runtime behavior only.
- MCP and CLI remain thin surfaces over application/core logic. Do not add admin/sync/migration concepts to MCP.
- Existing untracked `references/linkedin-post-notes.md` is not part of this cleanup and should not be touched.
</decisions_made>

<blockers>

- No technical blocker. Work is paused only because the user asked to preserve context before context compaction.
- The worktree is intentionally in a broken intermediate state: source modules were deleted and tests/docs still reference them.
</blockers>

## Required Reading (in order)

1. `.planning/quick/260526-u2h-remove-obsolete-sqliterepository-runtime/260526-u2h-PLAN.md` - the active quick task.
1. `.planning/HANDOFF.json` - machine-readable current state.
1. `src/mcp_strava/cli.py` - verify removed command behavior and product CLI surface.
1. `src/mcp_strava/db.py` and `src/mcp_strava/adapters/duckdb/repository.py` - current repository path.
1. `tests/test_read_model_queries.py` - current DuckDB fixture helper work in progress.
1. `.planning/STATE.md` - broader GSD state, especially Phase 08 08-08 performance gate.

## Critical Anti-Patterns (do NOT repeat these)

- Do not preserve old command names, aliases, tailored removal errors, retired modules, or old tests unless the user explicitly approves it.
- Do not use a test failure as a reason to revive deleted SQLite code. Rewrite or delete the obsolete test instead.
- Do not touch `references/linkedin-post-notes.md`; it was already untracked and unrelated.

## Infrastructure State

- No background processes were started in this pause turn.
- No tests were run after the latest source edits.
- Current uncommitted source diff is large and incomplete. `git diff --stat` at pause showed 29 files changed, about 279 insertions and 7184 deletions.
- Existing runtime/docker state was not inspected during this pause.

<context>
The mental model for resuming:

This is not a compatibility migration. It is cleanup after a completed technology stack shift. The code should express the current system, not teach future readers about removed SQLite/cutover paths. If a source file, test, or doc mentions SQLite runtime or old migration commands, decide whether it has a current product/runtime purpose. If not, remove it.

The user specifically objected to `_usage_error("admin db-migrate was removed; DuckDB is the only runtime storage.")`. Treat that as a representative bug: special handling of removed features is the thing to eliminate.
</context>

<next_action>
Start with the marker `rg` command from Remaining Work, then patch tests and docs until no obsolete SQLite/cutover/removed-command references remain in current code paths.
</next_action>
