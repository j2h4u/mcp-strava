# Quick Task 260526-u2h: Remove obsolete SQLiteRepository runtime/test paths

**Date:** 2026-05-26
**Status:** In progress

## Task

After the DuckDB cutover, remove product/runtime/test paths that keep `SQLiteRepository` alive as a compatibility layer. Keep SQLite code only where it is part of the current data-import/cutover mechanism or adapter-specific tests.

## Plan

1. Remove stale product modules and smoke checks that still call old SQLite-backed report/analytics/trend paths.
2. Refactor current CLI/admin paths to use the active repository factory or remove commands that only exist for old SQLite behavior.
3. Add/strengthen boundary tests so direct `SQLiteRepository` use cannot return to product modules.
4. Run focused tests plus the full repository test gate.
