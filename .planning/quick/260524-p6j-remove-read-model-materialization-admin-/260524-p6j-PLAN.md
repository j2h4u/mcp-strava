---
quick_id: 260524-p6j
status: planned
---

# Remove read-model materialization admin surface

## Scope

- Remove the local admin CLI command for manual read-model materialization.
- Keep read-model materialization owned by automatic refresh/backfill runtime paths.
- Replace redundant MCP forbidden-name checks with an exact five-tool product allowlist contract.
- Update tests and deployment docs to assert the exact MCP tool surface instead of named operational exclusions.

## Verification

- Targeted tests for CLI, MCP, gateway, smoke, refresh runtime, and Docker runtime.
- Full pytest suite.
- Docker `just test` live MCP smoke.
