---
context: default
phase: 08-duckdb-primary-storage-aggregate-analytics-surface
task: null
total_tasks: null
status: paused
last_updated: 2026-05-27T12:48:37.229Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: No hidden athlete-physiology defaults. HR_REST has NO default; zone/TRIMP
  computation must FAIL FAST (clear MCP_STRAVA_HR_REST message) when it is unset. Do NOT
  reintroduce `_DEFAULT_BOUNDS`, `_FALLBACK_HR_MAX`, or literal `191`/`53`/`177` fallbacks —
  the planner tried this and it was explicitly rejected. Bounds come from (running max-HR-to-date
  from DB, HR_REST from config) or the path fails loudly.
- [ ] CONSTRAINT: No backward-compatibility archaeology. DuckDB is the ONLY storage; SQLite
  adapter/cutover are gone. Do not revive removed modules, aliases, or "just in case" shims.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Hidden default fallback | Relocating a hardcoded athlete value (191/53/177) into a `_DEFAULT_*`/`_FALLBACK_*` constant instead of removing it | blocking | Every zone/TRIMP path derives bounds from (max-HR-to-date, config HR_REST) and raises when HR_REST is None; no module-level fallback constants |
| Skipping the full suite after doc edits | Committed a docs stage without re-running pytest; two doc-content tests were actually broken | advisory | Run `uv run pytest -q` before every commit, including docs-only changes (tests assert doc content) |

<current_state>
Paused at a clean, fully-committed stop point. No active phase PLAN.md in flight — recent work
was a sequence of quick tasks + cleanup on branch `feat/phase-8-duckdb-primary-storage` (NOT pushed).
Full test suite GREEN: 300 passed. Working tree clean except untracked `references/linkedin-post-notes.md`
(intentional personal draft — do not touch).
</current_state>

<completed_work>

- DuckDB-only cleanup (quick 260526-u2h): removed SQLite adapter, SQLite→DuckDB cutover tooling,
  legacy analytics/report/trends from source, tests, and docs.
- Residual SQLite traces removed: `deploy/service.py` dual-mode→single-owner DuckDB, freshness
  docstring, README, AGENTS.md rewrite.
- Removed SQLite-reappearance detector tests + sqlite `.gitignore` patterns; rewrote `SKILL.md`
  (1053→97 lines) into an agent-facing operational guide; purged legacy `references/` notes;
  regenerated `coach-review.md` + `sports-medicine-review.md` via expert panel (implementation-agnostic).
- HR zones on the fly (quick 260527-nbq, commits cc1513d..1d06439): isolated `src/mcp_strava/hr_zones.py`
  zone model (Karvonen, swappable, identifier persisted on facts); `MCP_STRAVA_HR_REST` (no default) +
  `MCP_STRAVA_HR_ZONE_MODEL` settings; materializer computes bounds per-activity from running
  max-HR-to-date + config HR_REST; persists per-activity `min_hr/max_hr/hr_zone_model/hr_max_used/hr_rest_used`
  (additive `ADD COLUMN IF NOT EXISTS`); removed `Config.Athlete`, literal `Config.Zones.BOUNDS`,
  frozen `Config.SQL.*`. TRIMP byte-identical; fail-fast verified at all 3 sites.
- Scrubbed athlete name/DOB/age PII from 4 `references/` domain reviews (commit 31fff2e).
</completed_work>

<remaining_work>

- git-history PII filter — DEFERRED to post-merge. Name "[REDACTED-NAME]"/"[REDACTED-NAME]" + DOB [REDACTED-DOB] still live
  in git history (baseline commit `354dcb9`, present on `main`). Plan: after this branch merges, run
  `git filter-repo --replace-text` over consolidated history (filter-repo NOT installed — `uv tool install git-filter-repo`).
- Phase 08 plan 08-08 performance acceptance — NOT started. `just mcp-read-model-perf 20 2 100` (100ms p95)
  previously failed: get_fitness_state 100.07ms, list_workouts 118.11ms, get_workout_detail 131.93ms.
  Milestone v1.1 is not honestly complete until this gate passes and 08-08 gets a SUMMARY.
</remaining_work>

<decisions_made>

- HR zones computed on the fly via an isolated swappable model (Karvonen now); model id stored on facts so old data is never retroactively recomputed.
- HRmax = running max-observed-HR-to-date (per activity, from DB); HRrest from config only, no default, fail-fast.
- Rejected all hidden-default fallbacks (no 191/53/177 baked anywhere).
- git-history PII filter deferred to post-merge (PII is on main/baseline; rewrite once after consolidation).
- `references/` treated as historical archive; `linkedin-post-notes.md` left untracked/untouched.
</decisions_made>

<blockers>
- Phase 08 08-08 100ms p95 perf gate previously failed — no workaround yet; needs investigation/optimization.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state for this pause.
2. `.planning/STATE.md` — broader project state + Quick Tasks Completed table.
3. `.planning/quick/260527-nbq-wire-hr-zone-computation-on-the-fly-into/260527-nbq-SUMMARY.md` — HR-zone refactor details.
4. `src/mcp_strava/hr_zones.py` + `src/mcp_strava/adapters/duckdb/read_model_materializer.py` — current zone/TRIMP computation path.

## Critical Anti-Patterns (do NOT repeat these)
- Reintroducing a hardcoded athlete HR default (191/53/177) anywhere → keep fail-fast; bounds derive from data+config.
- Committing without running the full suite (doc tests assert doc content).

## Infrastructure State
- Branch `feat/phase-8-duckdb-primary-storage`, NOT pushed. Working tree clean except untracked `references/linkedin-post-notes.md`.
- Tests: `uv run pytest -q` → 300 passed. `tests/conftest.py` autouse sets `MCP_STRAVA_HR_REST=53` for the suite.
- Live runtime requires `MCP_STRAVA_HR_REST` to be set explicitly (no default) — operator action.
- No background processes running.

<context>
This was a very long session. The throughline: make the service alienable and honest — strip SQLite
archaeology, remove the owner's hardcoded physiology, and compute HR zones from data+config behind a
swappable interface, all while keeping TRIMP byte-identical. Everything is committed and green. The two
real open items (git-history PII filter, 08-08 perf gate) are intentionally deferred, not forgotten.
</context>

<next_action>
Start with: merge `feat/phase-8-duckdb-primary-storage` to main; then run the git-history PII filter on
the consolidated history; then attack the Phase 08 08-08 100ms p95 perf gate (`just mcp-read-model-perf 20 2 100`).
</next_action>
