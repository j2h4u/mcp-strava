---
context: default
phase: none (v1.1 milestone closed + archived)
task: null
total_tasks: null
status: paused
last_updated: 2026-06-11T15:11:58.009Z
---

# Continue Here — current docs/state cleanup

No active phase. Milestone **v1.1 "DuckDB Analytics"** is closed, archived, tagged
(`v1.1` pushed). Current work is Enji cleanup and pipeline-boundary cleanup; nothing is
pending in Phase 16.

<current_state>
Current docs/state work is in progress. Phase 16 remains shipped, but it is not the
active focus. Live runtime details should be checked directly before any deploy/state
assertion.
</current_state>

<completed_work>
- Phase 16 remains complete and archived.
- Current documentation cleanup is tracking stale architecture/state claims.
</completed_work>

<remaining_work>
All optional:
- Continue Enji cleanup if additional stale state/docs claims show up.
- Start milestone v1.2 via `/gsd-new-milestone` when there is new scope.
</remaining_work>

<decisions_made>
- Keep documentation state aligned with the live tree and current workstream names.
- Verify live runtime details directly before updating deployed-state claims.
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Check live container, DB, and smoke status directly when needed.
- Do not assume the archived Phase 16 runtime details are still current without live verification.

## Critical Anti-Patterns (do NOT repeat)
- Keep operational assertions tied to current evidence, not archived milestone prose.

<context>
Long session: executed + verified phase 16, closed the milestone, then discovered the
"value" wasn't actually deployed (container on old code, DB on old schema). Did the real
deploy + migration, found/fixed a tz regression via live smoke, then chased a "9.6 min
recompute feels slow" thread to a numpy rewrite of cardiac drift. Everything is now real,
deployed, green, and pushed.
</context>

<next_action>
Nothing required. If continuing: `/gsd-new-milestone` for v1.2, OR re-profile materialize
(cProfile tottime, ~30 activities on a stopped-container DB copy) to confirm whether
`_upsert_facts_batch` is the next real hot spot before optimizing it.
</next_action>
