---
context: default
phase: null
task: 17
total_tasks: null
status: in_progress
last_updated: 2026-05-29T09:50:07.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Protected backup must not be deleted.** `~/backups/mcp-strava-safe/` (read-only) holds the only verified-good copy of the ~2-day-to-refetch Strava data. Do NOT delete/overwrite until prod refresh is back to normal.
- [ ] CONSTRAINT: **Do not assert a root cause without the decisive measurement.** This session burned hours on plausible-but-wrong causes (reopen amplification, fact-table churn, summary churn), each refuted by the user. Form hypothesis → run the measurement that would disprove it → THEN conclude.
- [ ] CONSTRAINT: **Growth experiments must start from a COMPACTED (free_blocks=0) copy.** A bloated file has a free-block pool that masks growth — every experiment looked flat until run from a compacted baseline.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Glib root-cause | Asserted reopen-amplification / fact-table ART / summary-churn as the cause; all empirically false | blocking | Design the disproving measurement before stating a cause |
| Experiment on bloated DB | free-pool absorbed churn → false "flat" results | blocking | Compact to free=0 first, then measure deltas |
| Prod restart storm | ~10+ owner restarts/snapshots polluted the experiment + interrupted refresh; also wrongly blamed restarts | advisory | Snapshot once; observe untouched; minimize restarts |
| Background wait-loops | `while kill -0 …; do sleep 5; done &` left running >1h, latched onto pytest | advisory | Never background wait-loops; use foreground bounded sleep or task notifications |
| Grepped only success events | Missed a 6h-old FATAL by grepping refresh_ok/mirror_storage, not errors | advisory | Grep for error/fatal events first when investigating |

<current_state>
Milestone v1.1 complete. This session shipped post-v1.1 admin-CLI cleanup (tasks 12+13: `admin catchup`, `admin compact`) and per-cycle storage observability — all committed (7 commits, 9f11af5..c4eed2e, working tree clean, 321 tests pass).

Then a disk-bloat investigation uncovered a more serious live incident: **PROD refresh has been BROKEN since ~09:17 Almaty (2026-05-29) by DuckDB ART-index corruption** — not caused by us (broke ~4h before the session). Mirror is stale ~6h+ but **reads work and DATA is intact** (only the index is corrupt). A verified safe backup was created. Paused pending the user's remediation decision.
</current_state>

<completed_work>
- Task 12 (9f11af5): `admin catchup` merge + runtime renames; removed `admin mirror-refresh`.
- Task 13 (3571eba/fdffbd7): `admin compact` via COPY FROM DATABASE + human-readable sizes; deliberately NO auto-compact in catchup.
- Task 14 (cd5319d): compaction runbook + ADMIN_COMMANDS doc fix.
- Task 15 (5bf21a4/c4eed2e): `mirror_storage` indicator every cycle (free_blocks + delta); `refresh_ok` now carries activity/stream/detail/kudos counts.
- Task 16: protected verified backup at `~/backups/mcp-strava-safe/` (read-only).
</completed_work>

<remaining_work>
- Task 17 (NEXT, decision): remediate broken refresh — (a) compact prod to unbreak + watch for recurrence; (b) durable = remove ART index/PK on hot tables (fixes fatal AND bloat); (c) wait for unreleased DuckDB #21986.
- Task 18 (P1): refresh-HEALTH observability — healthcheck fails on staleness; failures written to sync_log; Grafana alert.
- Task 19 (P2): disk bloat cadence (tools exist).
- Task 20 (tech debt): slow test `test_repository_methods_cover_activity_stream_zone_kudos_and_synclog` = 52s of ~187s; consider pytest-xdist.
- Task 21 (optional): safe backup offsite/Borg.
</remaining_work>

<decisions_made>
- **Upgrade does NOT fix the refresh fatal** — tested DuckDB 1.5.3 / 1.5.4.dev2 / 1.6.0.dev12 against a prod snapshot; all FATAL on the failing upsert. 1.5.3 is latest stable; the RevertCommit index-corruption fix is unreleased (DuckDB PR #21986).
- **Compaction (COPY FROM DATABASE) rebuilds the ART index clean and fixes the fatal** — proven: a compacted copy survives full row-by-row re-upsert. But prod re-corrupted ~30min after the 13:45 compact → recurring trigger.
- Root cause of BOTH refresh fatal and disk bloat = ART indexes (PRIMARY KEY) on hot tables.
- No auto-compact in catchup (orthogonal: catchup=network, compact=disk).
</decisions_made>

<blockers>
- PROD refresh broken (DuckDB ART corruption, activity 18694535582). Reads OK, mirror stale. Awaiting remediation decision (task 17).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable companion (full task/decision/blocker detail).
2. `src/mcp_strava/maintenance/compact.py` — `compact_database` (the index-rebuild fix) + `storage_stats` (indicator).
3. `src/mcp_strava/refresh/_sync_ops.py` `sync_summaries` + `src/mcp_strava/adapters/duckdb/repository.py` `upsert_activity_summary` — the failing upsert path.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: deleting/overwriting `~/backups/mcp-strava-safe/` → loss of ~2-day-refetch data. Mitigation: read-only; leave until normal refresh restored.
- ANTI-PATTERN: blind DuckDB upgrade as a fix → already disproven; wastes restarts.
- ANTI-PATTERN: running risky ops on prod without confirming the safe backup is intact.

## Infrastructure State
- Prod container `mcp-strava`: healthy, **reads work, refresh broken** (ART corruption), interval reverted to 3600.
- Prod DB `/opt/docker/mcp-strava/data/strava.duckdb`: ~92M, **corrupt ART index** for activity 18694535582; data intact. In-dir backup `strava.pre-compact-20260529T084527Z.duckdb` (241M, raw).
- Safe backups (read-only): `~/backups/mcp-strava-safe/strava-clean-20260529T094705Z.duckdb` (verified: 604 activities, 2,684,891 streams, healthy index) + `strava-raw-…`.
- Repro tooling: `/tmp/repro.py` may be gone (tmpfs); recreate from HANDOFF if needed.

<context>
The data is safe and backed up; this is not an emergency (reads serve, just stale). The decision is durability: compaction is a proven but possibly non-holding fix; removing the ART index is the durable root fix but invasive (schema + repository + migration + tests). User leaned toward verifying the upgrade hypothesis first (done — ruled out). User explicitly wants the backup protected.
</context>

<next_action>
Start with: confirm the safe backup at `~/backups/mcp-strava-safe/` is intact, then get the user's remediation decision for task 17 (compact-to-unbreak + watch, vs ART-index removal). Do not run destructive prod ops before confirming the backup.
</next_action>
