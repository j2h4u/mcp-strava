---
context: default
phase: 15-self-invalidating-read-model-source-fingerprint-auto-recompu
task: null
total_tasks: null
status: milestone_complete
last_updated: 2026-06-04T20:27:47.705Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Hallucinated backward-compat | Built a preflight "fix" for a pre-Phase-15-DB crash-loop that cannot happen (single dev instance, one DB already migrated, no backups). Reverted. | advisory | The instant "backward-compat / shim / migrate old DB / restore backup / legacy" appears in your reasoning → STOP, investigate the urge. Rule is in `~/.claude/memory/feedback_no_backward_compat.md` + `~/AGENTS.md` + project memory `no-backward-compat-ever.md`. NEVER backward compat on ANY project. |
| `git add` aborts on a bad pathspec | `git add f1 f2 deleted_file` failed on the deleted pathspec and left f1/f2 UNSTAGED, so a commit captured only a partial change (2b76d73). | advisory | After `git add`, run `git status --short` and confirm intended files are staged BEFORE committing; or stage with separate calls. |

<current_state>
Phase 15 is COMPLETE and verified (21/21 must-haves). Milestone v1.1 is at 100% (STATE `status: complete`) but NOT yet formally archived. Working tree is clean; local == origin/main (ahead 0 / behind 0). CI green; 389 tests pass; gates green (ruff, `basedpyright src` reportAny=0 with NO baseline file, `lint-imports` 3 contracts, pytest). Nothing is in-progress or uncommitted.
</current_state>

<completed_work>
This session (all committed + pushed):
- Executed + verified Phase 15; gap-closure WR-01..04; milestone v1.1 → 100%.
- Fixed a real prod bug: refresh worker kudos-sync `TypeError` (container was unhealthy 257+ cycles) → durable typed repo method `activities_missing_kudos()`. Container healthy.
- Paid down reportAny **463 → 0** across the codebase (typed the DuckDB row boundary, fact-read TypedDicts, devtools/deploy JSON boundaries); removed the basedpyright baseline → reportAny is now hard-enforced repo-wide.
- Added CI gates: `import-linter` (3 layer contracts) + `basedpyright` reportAny; swapped pyright→basedpyright (bundles 1.1.410); widened CI to lint the whole repo (`.`).
- Removed dead back-compat/migration cruft: `ensure_provenance_columns` + helper, `schema_migration_log` table, unused sport aliases, `deploy/smoke.py` compat wrapper.
- Expert-panel decision implemented: sidecar seeding is now fail-loud (catch only table-absent); dropped the redundant MAX-fallback in `current_metric_version` and the dual-seed chokepoint self-heal.
- `health.py`: corrupt health file now fails loud (→ unhealthy) instead of reading as fake-healthy.
- Ran a read-only fallback audit (subagent) — its top finds were either things we'd just built or dubious "SQL NULL can't happen" claims; the genuinely-removable borderline (health.py) is done.
</completed_work>

<remaining_work>
1. **`/gsd-complete-milestone`** — formally archive milestone v1.1. This is the clean next step. Everything is green and committed.
2. OPTIONAL (deferred, low value): remove the provably-redundant `start_time_local` read-time fallback in `metric_services._activity_payload`. It re-parses `summary.start_date_local` with the SAME helper the materializer already used, so it can only return the same None — but `tests/test_metric_services._repo_with_facts` inserts facts with NULL `start_time_local`, so removing it requires migrating that fixture to materialize the column. Attempted + reverted this session (marginal benefit vs fixture surgery at low context).
</remaining_work>

<decisions_made>
- **NO BACKWARD COMPATIBILITY, EVER, ON ANY PROJECT.** Single dev instance, one DB, no backups, no external consumers. Recorded globally (`~/.claude/memory/`, `~/AGENTS.md`) and per-project. Fail-loud > silent-wrong; delete dead insurance.
- Subagents run on `model="sonnet"` to save Opus tokens (reserve Opus for subtle architecture). Recorded in memory.
- `basedpyright` reads `[tool.pyright]`, NOT `[tool.basedpyright]` (verified empirically) — config lives in `[tool.pyright]`.
- Verify panel/subagent/reviewer findings against the actual code + git history before acting — paid off repeatedly (the PEP 758 false-BLOCKER; a subagent flagging code we'd just deliberately built).
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Container `mcp-strava` (compose project `deploy`): healthy. Self-recompute fired on last rebuild (608 activities enqueued) — expected.
- Branch `main`, local == origin. CI (GitHub Actions: CI + CodeQL) green on HEAD `5ef2fde`.
- Python 3.14 (`.venv/bin/python` / `uv run`). `except A, B:` is valid PEP 758 syntax here — do NOT "fix" it.

<context>
The phase work is finished; this is a milestone boundary, not a paused mid-task. Paused only because context filled up (~73%) after a long, productive maintenance sweep. The throughline all session: the operator wants ruthless removal of anything defensive that guards a scenario that cannot occur on a single dev instance — no migrations, no shims, no fallbacks-for-mythical-states, no backward compat. Fail loud instead.
</context>

<next_action>
Start with: `/gsd-complete-milestone` to archive milestone v1.1 (everything is committed, pushed, and green — no cleanup needed first).
</next_action>
