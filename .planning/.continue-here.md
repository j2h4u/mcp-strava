---
context: default
phase: null
task: 12
total_tasks: 4
status: in_progress
last_updated: 2026-05-28T17:36:42.711Z
---

<current_state>
Milestone v1.1 closed. Post-milestone cleanup queue from the 2026-05-28T14:32Z handoff is partially complete: tasks 10 (spike) and 11 (admin lock UX + wrapper) are done and committed (3b38f59). Tasks 12 (catchup merge) and 13 (compact) remain. Owner container runs on the latest image; admin commands work cleanly through `just admin <cmd>`.
</current_state>

<completed_work>

- Task 10: ATTACH READ_ONLY spike — Done. DuckDB takes an OS-level exclusive lock; READ_ONLY does NOT bypass it. Three attempts all failed with `Conflicting lock is held in PID 1`. No code change. Scope for tasks 11-13 does not shrink.
- Task 11: Friendly lock error + `just admin` wrapper — Done (commit 3b38f59).
  - `MirrorDbLocked` exception in `src/mcp_strava/adapters/duckdb/connection.py`; both `open_expected_mirror_db` and `open_fixture_db` translate DuckDB IOException "Conflicting lock" → `MirrorDbLocked`.
  - `src/mcp_strava/cli.py::cmd_admin` catches `MirrorDbLocked` and prints a one-line hint pointing at `just admin <cmd>`; exit 2.
  - `Justfile` recipe `admin *args`: shebang bash, `set -euo pipefail`, `trap '{{compose}} start mcp-strava >/dev/null' EXIT`, then `compose stop` → `compose run --rm --no-deps -T --entrypoint python mcp-strava -m mcp_strava admin {{args}}`.
  - 2 new tests in `tests/test_duckdb_concurrency_guards.py` (25 passed). Live verification: `just admin mirror-coverage` cycles owner cleanly.
</completed_work>

<remaining_work>

- Task 12 (not started): Merge `admin backfill` + `admin backfill-streams` → `admin catchup`. Rename internals: `run_backfill → run_catchup`, `run_backfill_stream_channels → _fetch_missing_stream_channels`. Delete `admin mirror-refresh` (`cmd_db_refresh`) — refresh worker already handles it. Stage enum strings in `refresh_state.checkpoint_stage` stay as-is (persisted).
- Task 13 (not started): New `src/mcp_strava/maintenance/compact.py`. New `admin compact` CLI. Auto-compact at end of `admin catchup`. Compact runs against stopped owner — `just admin` wrapper already provides that.
</remaining_work>

<decisions_made>

- ATTACH READ_ONLY does NOT work against a live writer — confirmed empirically.
- Observation: post Phase 08-08 (ReadConn + memoized `_read_model_enabled`), the owner does not hold the lock continuously. Read-only admin commands can sometimes slip through `docker exec` while the owner is idle. Wrapper is still right for write-side work; original blocker text is correct but less catastrophic than thought.
- `compose run` must override the image ENTRYPOINT via `--entrypoint python`. The image's `ENTRYPOINT ["python", "-m", "mcp_strava.deploy.entrypoint"]` otherwise swallows the override and spawns another owner process. Discovered live during task 11 verification.
- `just admin` uses shebang-form bash recipe so the EXIT trap survives across recipe lines.
</decisions_made>

<blockers>

- None. Original 14:32 handoff blocker is mitigated for write path via `just admin` and for read path via post-08-08 transient-lock behavior. Write-side admin commands (backfill, compact, sql with writes) still must go through `just admin`.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured companion (machine-readable).
2. `src/mcp_strava/cli.py` — `cmd_backfill`, `cmd_backfill_streams`, `cmd_db_refresh`, `ADMIN_COMMANDS`. Task 12 source.
3. `src/mcp_strava/refresh/runtime.py` — `run_backfill`, `run_backfill_stream_channels`. Task 12 rename targets.

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: `docker compose run` without `--entrypoint` → image ENTRYPOINT swallows the override and starts a long-running owner. Mitigation: always set `--entrypoint python` when overriding the command via `compose run` on this image.
- ANTI-PATTERN: Multi-line just recipes lose `trap` and `set -euo pipefail` across lines because just runs each line in fresh `bash -uc`. Mitigation: shebang form (`#!/usr/bin/env bash`) so the recipe runs as one script.
- ANTI-PATTERN: Renaming `Stage` enum values without a data migration breaks persisted `refresh_state.checkpoint_stage` strings. Mitigation in task 12: keep persisted enum values unchanged even if surrounding function names change.

## Infrastructure State

- Container `mcp-strava`: running on the post-3b38f59 image, healthy. Recreated during task 11 verification.
- Branch: `main`. Latest commit: `3b38f59`.
- `data/strava.duckdb`: ~764 MB at `/opt/docker/mcp-strava/data/strava.duckdb` (one-time Phase-8 migration aftermath, not an ongoing leak).
- No background processes started by this session.

<context>
Session continued from the 2026-05-28T14:32Z pause. User explicitly asked for plain-language Russian responses and short, simple questions (the first English wall of text was pushed back). Keep that style. User chose scope option 2 (10 → 11 → 12 → 13). Tasks 12 and 13 are small and self-contained but combined with verification may exceed one session — consider asking the user up front whether to do both or just 12.
</context>

<next_action>
Start task 12: read `src/mcp_strava/cli.py` (cmd_backfill, cmd_backfill_streams, cmd_db_refresh) and `src/mcp_strava/refresh/runtime.py` (run_backfill, run_backfill_stream_channels). Then propose the `admin catchup` shape to the user before editing (two internal phases: `_fetch_missing_activities` and `_fetch_missing_stream_channels`). Delete `admin mirror-refresh` from `ADMIN_COMMANDS` and remove `cmd_db_refresh`. Keep persisted Stage enum strings unchanged.
</next_action>
