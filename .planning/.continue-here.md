---
context: default
phase: none (v1.1 milestone closed + archived)
task: null
total_tasks: null
status: paused
last_updated: 2026-06-11T15:11:58.009Z
---

# Continue Here — post-v1.1, fully deployed

No active phase. Milestone **v1.1 "DuckDB Analytics"** is closed, archived, tagged
(`v1.1` pushed), and phase 16 is **deployed to the live container**. Nothing is pending.

<current_state>
Git clean, 0 unpushed, HEAD `e2f6add`. Live container `mcp-strava` runs that exact image
and is healthy. Live DuckDB is on the phase-16 native schema, read-model fully
rematerialized (metric_version 21, dirty=0). All 6 MCP tools green (`just mcp-smoke-full`).
Phase directories archived to `.planning/milestones/v1.1-phases/`.
</current_state>

<completed_work>
- Phase 16 (DuckDB-native storage cleanup) executed, reviewed, verified.
- Phases 07/08 backfilled with VERIFICATION.md → all 16 phases Complete.
- Milestone v1.1 closed + archived (`17d5270`), tag `v1.1` pushed.
- Live DuckDB migrated in place to native schema (source data preserved: 625 activities /
  2.79M streams); image rebuilt with phase-16 code + numpy; redeployed.
- Fixed latent phase-15 tz bug in `project_fitness_state` (`1dbf72e`).
- Read-model perf: drain-on-recompute (`ed528ec`) + numpy-vectorized cardiac_drift Jenks
  (`e2f6add`) → full 625-activity recompute 9.6min → 83s.
</completed_work>

<remaining_work>
All OPTIONAL (nothing blocking):
- Perf: re-profile materialize now that cardiac drift is fixed. Next likely cost is
  `_upsert_facts_batch` (DuckDB fact writes — IO, not numpy-able; lever is batching / fewer
  transactions). Only pursue if a fresh profile shows it hot.
- Start milestone v1.2 via `/gsd-new-milestone` when there is new scope.
</remaining_work>

<decisions_made>
- numpy is now a dependency (cardiac_drift Jenks vectorized, identical values). Do NOT
  cargo-cult numpy elsewhere — the rest of the numeric surface is DuckDB-SQL-delegated or
  sequential recurrences (see memory `numpy-dependency-and-compute-surface`).
- Live schema migration is manual/one-shot by policy (no auto-migration; source is
  precious). Validate any future schema migration on a backup copy first; drop dependent
  views + indexes before ALTER/DROP COLUMN (DuckDB blocks otherwise).
</decisions_made>

<blockers>
None.
</blockers>

## Infrastructure State
- Container `mcp-strava` (compose `deploy/docker-compose.yml`): healthy, image `e2f6add`, numpy 2.4.6 in image.
- Live DB `/srv/mcp-strava/data/strava.duckdb`: phase-16 native schema, metric_version 21, dirty=0. No backup file retained (deleted after verification).
- Healthcheck (`deploy/healthcheck.py`): owner-PID liveness + HTTP `/mcp` + refresh heartbeat (`/tmp/*.json`); does NOT open DuckDB by design. `children` check is vacuous (refresh is an in-process thread, no child processes).
- Verify deploy any time: `just mcp-smoke-full` (from repo root).

## Critical Anti-Patterns (do NOT repeat)
- NO backward-compat / fallbacks anywhere (active dev, single instance). One-shot migrations are dead once run — delete them.
- Don't deploy a schema-changing phase by just `up -d --build` — the live DB needs explicit migration first (no auto-migration). Backup → validate on copy → apply → rebuild → smoke.

<context>
Long session: executed + verified phase 16, closed the milestone, then discovered the
"value" wasn't actually deployed (container on old code, DB on old schema). Did the real
deploy + migration, found/fixed a tz regression via live smoke, then chased a "9.6 min
recompute feels slow" thread to a numpy rewrite of cardiac drift. Everything is now real,
deployed, green, and pushed.
</context>

<next_action>
Nothing required. If continuing: `/gsd-new-milestone` for v1.2, OR re-profile materialize
(cProfile tottime, ~30 activities on a stopped-container DB copy) to confirm whether
`_upsert_facts_batch` is the next real hot spot before optimizing it.
</next_action>
