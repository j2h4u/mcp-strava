---
phase: 15-self-invalidating-read-model-source-fingerprint-auto-recompu
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - src/mcp_strava/refresh/_sync_ops.py
  - src/mcp_strava/refresh/runtime.py
  - src/mcp_strava/refresh/worker.py
  - src/mcp_strava/adapters/duckdb/read_model_materializer.py
  - src/mcp_strava/adapters/duckdb/__init__.py
  - src/mcp_strava/application/metric_services.py
  - src/mcp_strava/application/aggregate_services.py
  - src/mcp_strava/adapters/duckdb/aggregate_queries.py
  - tests/test_refresh_runtime.py
  - tests/test_metric_services.py
autonomous: true
requirements: [REQ-ZEROKNOB]
must_haves:
  truths:
    - "At the materialize chokepoint, when the live fingerprint differs from the stored one, the system bumps metric_version, records the new fingerprint, and enqueues every activity for recompute — with no manual step"
    - "The fingerprint check runs even when the dirty queue is empty: the worker no longer early-returns on dirty_count==0 before the chokepoint, so a logic-only edit (no dirty source rows) is still detected and recomputes"
    - "The hand-maintained CURRENT_METRIC_VERSION constant is deleted everywhere — including the adapters/duckdb/__init__.py re-export and its __all__ entry; all read/materialize/enqueue paths source the int from repo.current_metric_version()"
    - "Aggregate (digest) AND status reads pin metric_version = current across ALL aggregate read paths — _where_clause, _effective_range_for_metric, and query_status_facts — so point reads, aggregate reads, and status facts return only-current-or-empty, never a blend of old+new (R11)"
    - "aggregate_services.py threads the current version: repo.read_model_status() and query_training_aggregates() are both called with the resolved current metric_version"
    - "On auto-recompute a structured log event carries stored_fingerprint, current_fingerprint, reason, activities_enqueued, queued_at; the refresh-run record is stamped trigger_reason=logic_fingerprint_changed"
  artifacts:
    - path: "src/mcp_strava/refresh/_sync_ops.py"
      provides: "fingerprint-compare + bump + enqueue trigger at materialize_read_model_stage"
      contains: "logic_fingerprint_changed"
    - path: "src/mcp_strava/adapters/duckdb/aggregate_queries.py"
      provides: "metric_version = current filter in _where_clause"
      contains: "metric_version"
  key_links:
    - from: "_sync_ops.materialize_read_model_stage"
      to: "repository.enqueue_metric_version_recompute"
      via: "fingerprint mismatch triggers mass enqueue (wires the orphan at repository.py:534)"
      pattern: "enqueue_metric_version_recompute"
    - from: "aggregate_queries._where_clause"
      to: "metric_version pin"
      via: "WHERE metric_version = current"
      pattern: "metric_version = \\?"
---

<objective>
Wire the detector to the recompute. At the single materialize chokepoint, compare stored vs live fingerprint and on mismatch bump the version + enqueue all activities (wiring the orphan `enqueue_metric_version_recompute`). Delete `CURRENT_METRIC_VERSION` and source the int from `repo.current_metric_version()` everywhere. Add the R11 aggregate version pin. Emit self-explanatory observability.

Purpose: This is the plan that makes "change a constant → it recomputes" actually happen, and closes the version-blend hole on the digest path.
Output: trigger block, constant deletion + version threading, R11 filter, log events.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/CONTEXT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-RESEARCH.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-PATTERNS.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete CURRENT_METRIC_VERSION; source the int from repo.current_metric_version()</name>
  <files>src/mcp_strava/adapters/duckdb/repository.py, src/mcp_strava/adapters/duckdb/__init__.py, src/mcp_strava/adapters/duckdb/read_model_materializer.py, src/mcp_strava/refresh/runtime.py, src/mcp_strava/refresh/worker.py, src/mcp_strava/application/metric_services.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/repository.py (line 31 def to delete; line 408 default arg `metric_version: int = CURRENT_METRIC_VERSION` on update_activity_source_state_and_enqueue_dirty at 403)
    - src/mcp_strava/adapters/duckdb/__init__.py (line 5 re-export `from ...repository import CURRENT_METRIC_VERSION, DuckDBRepository`; line 10 `"CURRENT_METRIC_VERSION"` in __all__ — BOTH must be removed)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (line 8 import, line 370 default arg)
    - src/mcp_strava/refresh/runtime.py (line 8 import; call sites 110, 181, 275)
    - src/mcp_strava/refresh/worker.py (line 16 import; uses at 62-63, 69-71)
    - src/mcp_strava/application/metric_services.py (line 11 import; uses at 187, 333, 340, 358, 360, 406, 468, 904, 936)
  </read_first>
  <action>
    Remove `CURRENT_METRIC_VERSION = 1` (repository.py:31) and every import/re-export of it. In adapters/duckdb/__init__.py delete it from the `from ...repository import` line (5) AND from `__all__` (10). Replace each reference with a value sourced from `repo.current_metric_version()` at the point of use: in service functions (metric_services.py) compute `version = repo.current_metric_version()` once per request after `repo = DuckDBRepository.from_connection(conn)` and pass it where CURRENT_METRIC_VERSION was passed (187, 333, 340, 358, 360, 406, 468, 904, 936). In runtime.py and worker.py, read `repo.current_metric_version()` and pass that into materialize_read_model_stage / read_model_status.

    Default-arg resolution (review HIGH — pick the required-param form): for `update_activity_source_state_and_enqueue_dirty` (repository.py:403/408) REMOVE the `= CURRENT_METRIC_VERSION` default and make `metric_version: int` a REQUIRED parameter; thread the explicit current version from the sync callers (each sync caller resolves `repo.current_metric_version()` and passes it), so the repo does not resolve its own current version mid-sync. Apply the same required-param treatment to `materialize_read_model` (read_model_materializer.py:370): make `metric_version: int` required and have callers pass `repo.current_metric_version()`. Update every caller of both functions accordingly. Confirm zero remaining references: `grep -rn CURRENT_METRIC_VERSION src/` returns nothing.
  </action>
  <verify>
    <automated>bash -c '! grep -rn CURRENT_METRIC_VERSION src/'</automated>
    <automated>uv run python -c "import mcp_strava.adapters.duckdb as d; assert 'CURRENT_METRIC_VERSION' not in d.__all__ and not hasattr(d, 'CURRENT_METRIC_VERSION')"</automated>
  </verify>
  <done>CURRENT_METRIC_VERSION no longer exists in src/; all read/materialize/enqueue paths source the int from repo.current_metric_version(); suite still imports and runs.</done>
</task>

<task type="auto">
  <name>Task 2: Fingerprint trigger + observability at the materialize chokepoint + worker empty-queue gate</name>
  <files>src/mcp_strava/refresh/_sync_ops.py, src/mcp_strava/refresh/worker.py</files>
  <read_first>
    - src/mcp_strava/refresh/_sync_ops.py (materialize_read_model_stage at 269 — the single chokepoint; imports at 11-17)
    - src/mcp_strava/adapters/duckdb/repository.py (enqueue_metric_version_recompute at 534 — the orphan to wire; current_logic_version/bump_logic_version from 15-02)
    - src/mcp_strava/metric_registry.py (compute_logic_fingerprint from 15-01)
    - src/mcp_strava/refresh/worker.py (_materialize_dirty_read_model at 60 — `if dirty_count == 0: return 0` at ~64 SHORT-CIRCUITS before the chokepoint; _emit structured-event style at ~62-79; read_model_materialize_started/_ok events)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (record_read_model_refresh_run trigger_reason field; materialize-ok return)
  </read_first>
  <action>
    At the top of materialize_read_model_stage (_sync_ops.py:269), before calling materialize_duckdb_read_model: stored = repo.current_logic_version(); live = compute_logic_fingerprint(); if stored is not None and stored["logic_fingerprint"] != live: new_version = int(stored["metric_version"]) + 1; repo.bump_logic_version(new_version, live, now_iso); enqueued = repo.enqueue_metric_version_recompute(new_version, reason="logic_fingerprint_changed", queued_at=now_iso); emit a structured event "read_model_logic_recompute" with fields stored_fingerprint, current_fingerprint=live, reason="logic_fingerprint_changed", activities_enqueued=enqueued, queued_at=now_iso. When stored is None (fresh/unseeded), do nothing (adopt-current handled by 15-02 seed). Then resolve current_version = repo.current_metric_version() and pass it as the metric_version to materialize_duckdb_read_model (replacing the parameter that came from the deleted constant). Stamp trigger_reason="logic_fingerprint_changed" on the refresh-run record for the recompute cycle (thread a trigger_reason through materialize_read_model's record_read_model_refresh_run, defaulting to "materialize_read_model"). Extend the materialize-ok structured event to include metric_version and duration_ms (compute duration around the materialize call). Match the existing _emit(event, **fields) convention; additive fields only.

    Worker empty-queue gate (review MEDIUM): in worker.py _materialize_dirty_read_model (60), the early `if dirty_count == 0: return 0` returns BEFORE materialize_read_model_stage runs, so a logic-only edit with no dirty source rows is never detected. Fix: do NOT short-circuit on dirty_count==0 when a logic change may be pending. Either (a) move the fingerprint compare-and-enqueue ahead of the dirty-count read so a mismatch enqueues rows (making dirty_count>0 on this same call), or (b) when dirty_count==0, still call materialize_read_model_stage once (the chokepoint's own fingerprint check will enqueue + recompute on mismatch, and is a cheap no-op when fingerprints match). Pick (b) for the smaller change: replace the bare `return 0` with a single materialize_read_model_stage call whose result reports whether a logic recompute fired; keep the dirty-count fast path for the normal already-dirty case. After this, a fingerprint mismatch is detected even with an empty dirty queue.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_refresh_runtime.py -k "fingerprint or recompute or logic or empty_queue"</automated>
  </verify>
  <done>A fingerprint mismatch at the chokepoint bumps the version, enqueues all activities via the orphan method, emits the recompute event, and stamps trigger_reason=logic_fingerprint_changed; a matching fingerprint is a no-op; fresh DB does not recompute; a logic-only edit with an EMPTY dirty queue is still detected and recomputes (worker no longer early-returns past the fingerprint check).</done>
</task>

<task type="auto">
  <name>Task 3: R11 — pin metric_version = current across ALL aggregate + status read paths + tests</name>
  <files>src/mcp_strava/adapters/duckdb/aggregate_queries.py, src/mcp_strava/application/aggregate_services.py, tests/test_refresh_runtime.py, tests/test_metric_services.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/aggregate_queries.py (_where_clause at 933 — shared by the 3 query builders 693/781/860; COUNT(DISTINCT metric_version) tripwire at 723/805/893; _effective_range_for_metric at 607 — derives all-time ranges from UNPINNED views, called at 147/158; query_status_facts at 163 — queries activity_metric_facts DIRECTLY at 265/307/336/366/392/432/501 with NO metric_version filter; query_training_aggregates at 152 — entry that has the conn)
    - src/mcp_strava/application/aggregate_services.py (line 78 `read_model = repo.read_model_status()` called UNPINNED; line 79 `query_training_aggregates(conn, query_request)` called without a version — this file is the caller that must resolve + thread the current version)
    - src/mcp_strava/application/metric_services.py (how query_training_aggregates is invoked / where the repo+current version are available)
    - tests/test_metric_services.py (no-blend test target; _repo_with_facts / _aggregate_fixture helpers at lines 21-22)
  </read_first>
  <action>
    Thread the current metric_version through the ENTIRE aggregate + status read path, not just _where_clause (this is the consensus HIGH — version pinning is incomplete). Concretely:

    1. Signature: give query_training_aggregates a new explicit `metric_version: int` parameter resolved by the CALLER (aggregate_services.py), NOT resolved internally — so the version is consistent with the rest of the request. Pass it down to _where_clause and _effective_range_for_metric.
    2. _where_clause (933): add a `metric_version: int` param and include `metric_version = ?` in the WHERE for the versioned sources (daily_load_fact, training_model_fact, rolling_period_fact, activity_metric_fact, activity_summary_fact, social_fact). Bind the current int.
    3. _effective_range_for_metric (607): add a `metric_version: int` param and pin the range-deriving query to `metric_version = ?` so all-time ranges are computed from current-version rows only (today it reads unpinned views/facts).
    4. query_status_facts (163): add a `metric_version: int` param (or resolve via the conn's repo) and add `metric_version = ?` to each direct activity_metric_facts SELECT (265/307/336/366/392/432/501) so status facts are current-only.
    5. aggregate_services.py: resolve `version = DuckDBRepository.from_connection(conn).current_metric_version()` once, then call `repo.read_model_status(metric_version=version)` (78) and `query_training_aggregates(conn, query_request, metric_version=version)` (79) with it. (read_model_status already takes metric_version — pass it explicitly instead of relying on a default.)

    Keep COUNT(DISTINCT metric_version) AS metric_version_count as a tripwire (should be <= 1 after the fix). Add a regression test: seed fact rows at version 1 AND version 2, then assert a point read (list_workouts / get_fitness_state), an aggregate read (get_training_aggregates weekly/monthly), AND a status read all return only the current version's rows — never a blend (metric_version_count <= 1; values match the current-version rows only). Update any existing mixed-version aggregate test to assert the new "pinned current" behavior, keeping metric_version_count as the tripwire.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py -k "no_blend or version_pin or status"</automated>
  </verify>
  <done>ALL aggregate/status read paths (_where_clause, _effective_range_for_metric, query_status_facts) and aggregate_services.py filter to the current metric_version; the no-blend test (seeded multi-version DB) passes for point, aggregate, AND status reads; metric_version_count tripwire stays <= 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refresh cycle → recompute fan-out | Trigger fires only on internal fingerprint mismatch; no external input crosses here |
| aggregate query → fact tables | Version pin is a parameterized `?`; identifiers are schema literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | _where_clause metric_version interpolation | mitigate | Pin added as `metric_version = ?` placeholder; never string-formatted from input |
| T-15-06 | Denial of Service | recompute fan-out enqueues all activities | accept | Recompute is async via the existing dirty-queue + drain loop; sub-second per CONTEXT; single chokepoint, fires only on real mismatch |
| T-15-07 | Information disclosure | structured log fields | accept | Logs carry fingerprints/counts/timestamps only — no secrets, no Strava PII |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 (full unit suite green after rewiring).
- `bash -c '! grep -rn CURRENT_METRIC_VERSION src/'` (constant fully removed).
- `uv run ruff check src tests` / `ruff format --check src tests` / `uv run pyright src` clean.
- A seeded multi-version DB: both point and aggregate reads return only-current rows; recompute event emitted on a forced fingerprint change.
</verification>

<success_criteria>
- The chokepoint auto-bumps + enqueues on fingerprint mismatch; constant deleted; aggregate path pins the current version (R11); observability events present and self-explanatory.
</success_criteria>

<output>
Create `.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- Trigger block in `_sync_ops.materialize_read_model_stage`
- Worker empty-queue gate fix (`_materialize_dirty_read_model` no longer early-returns past the fingerprint check on `dirty_count == 0`)
- `read_model_logic_recompute` structured log event; `metric_version`+`duration_ms` on materialize-ok event; `trigger_reason="logic_fingerprint_changed"` on the refresh-run record
- `metric_version = ?` pin in `aggregate_queries._where_clause`, `_effective_range_for_metric`, and `query_status_facts`
- New `metric_version: int` parameter on `query_training_aggregates` (resolved by `aggregate_services.py`)
- Removal of `CURRENT_METRIC_VERSION` (symbol deleted; `adapters/duckdb/__init__.py` re-export + `__all__` entry removed; `update_activity_source_state_and_enqueue_dirty` and `materialize_read_model` default args made required; all call sites re-sourced from `repo.current_metric_version()`)
