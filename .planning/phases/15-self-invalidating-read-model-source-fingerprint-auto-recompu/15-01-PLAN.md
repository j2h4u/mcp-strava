---
phase: 15-self-invalidating-read-model-source-fingerprint-auto-recompu
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/metric_registry.py
  - tests/test_logic_fingerprint.py
autonomous: true
requirements: [REQ-ZEROKNOB]
must_haves:
  truths:
    - "compute_logic_fingerprint() returns the same sha256 hex string across separate processes and PYTHONHASHSEED-varied subprocesses"
    - "Editing the source text of any module in COMPUTE_SOURCE_MODULES changes the fingerprint; editing an unlisted module does not"
    - "A completeness test fails (CI red) if a compute module reachable from the materializer path is missing from COMPUTE_SOURCE_MODULES"
  artifacts:
    - path: "src/mcp_strava/metric_registry.py"
      provides: "COMPUTE_SOURCE_MODULES tuple + compute_logic_fingerprint()"
      contains: "def compute_logic_fingerprint"
    - path: "tests/test_logic_fingerprint.py"
      provides: "determinism, coverage-by-construction, and completeness tests"
      contains: "def test_"
  key_links:
    - from: "metric_registry.compute_logic_fingerprint"
      to: "inspect.getsource / importlib.import_module"
      via: "sha256 over sorted module source text"
      pattern: "inspect\\.getsource"
---

<objective>
Create the source-text logic fingerprint: `COMPUTE_SOURCE_MODULES` (explicit module tuple) and `compute_logic_fingerprint()` (sha256 over sorted `inspect.getsource(import_module(m))`). This is the detector that makes the read-model self-invalidating — every later plan rides on it.

Purpose: Replace the hand-maintained version int's "did the logic change?" judgment with an automatic, source-derived fingerprint (dbt `state:modified` analog). Coverage is automatic-by-construction because the hashed unit is the whole module.
Output: `compute_logic_fingerprint()` + `COMPUTE_SOURCE_MODULES` in `metric_registry.py`; a new `tests/test_logic_fingerprint.py` proving determinism, sensitivity, and completeness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/CONTEXT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-RESEARCH.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-PATTERNS.md
</context>

<feature>
  <name>Source-text logic fingerprint</name>
  <files>src/mcp_strava/metric_registry.py, tests/test_logic_fingerprint.py</files>
  <read_first>
    - src/mcp_strava/metric_registry.py (registry owns the compute-surface inventory; add the tuple + fn here; mind the validate-block style at ~2085)
    - src/mcp_strava/adapters/duckdb/repository.py (lines 3, 135 — `_semantic_json_hash`: the sha256().hexdigest() idiom to mirror; also why repository must be imported at runtime, not top-level, to avoid the repository→schema→metric_registry cycle)
    - tests/test_metric_services.py (lines 1-22 — fixture import conventions: `from __future__ import annotations`, pytest, src on pythonpath)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (lines 1-16 — the imports define the compute path the completeness test must cover: metrics, cardiac_drift?, training, hr_zones, constants, repository)
  </read_first>
  <behavior>
    - Determinism: compute_logic_fingerprint() == compute_logic_fingerprint() within a process; and equal when run in a fresh subprocess (e.g. `subprocess.run([sys.executable, "-c", ...])`) and under PYTHONHASHSEED=0 vs PYTHONHASHSEED=random. (Proves it is NOT builtin hash().)
    - Coverage-by-construction (sensitivity): the fingerprint is a function of the concatenated source text of exactly the listed modules. Test by monkeypatching `inspect.getsource` for one listed module to return altered text → fingerprint changes; altering an unlisted module's text → fingerprint unchanged.
    - Completeness (poka-yoke): every module transitively imported by `read_model_materializer`'s compute path (walk `read_model_materializer` + the metrics/training/hr_zones/constants/repository modules it imports, restricted to the `mcp_strava` package) is present in COMPUTE_SOURCE_MODULES. Test fails with a clear message naming any missing module.
    - Format-sensitivity is ACCEPTED (CONTEXT): a comment/whitespace edit to a hashed module changes the fingerprint. Do not normalize/strip — assert nothing that forbids this.
  </behavior>
  <implementation>
    RED: write tests/test_logic_fingerprint.py first — determinism (in-process + subprocess + PYTHONHASHSEED), sensitivity (monkeypatch getsource), completeness (compare a transitively-walked module set against COMPUTE_SOURCE_MODULES). Run; they fail (no symbol yet).
    GREEN: in metric_registry.py add COMPUTE_SOURCE_MODULES = ("mcp_strava.metrics", "mcp_strava.cardiac_drift", "mcp_strava.training", "mcp_strava.hr_zones", "mcp_strava.constants", "mcp_strava.adapters.duckdb.read_model_materializer", "mcp_strava.adapters.duckdb.repository") and compute_logic_fingerprint() using hashlib.sha256 over sorted(inspect.getsource(import_module(m)) for m in COMPUTE_SOURCE_MODULES), each chunk delimited by b"\x00", encoded utf-8, returning .hexdigest(). Do the import_module calls INSIDE the function (runtime import) to avoid the import cycle. Verify the exact module list against the materializer's actual imports while implementing the completeness test — adjust the tuple (e.g. add/remove cardiac_drift) so the completeness test passes by listing reality, not by weakening the test.
    REFACTOR: ensure ruff (E4/E7/E9,F,I,B,UP) + pyright clean; sha256 idiom matches `_semantic_json_hash`.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer source → fingerprint input | `import_module` targets come from a hard-coded in-repo tuple, never from runtime input or Strava data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering / Elevation | `import_module(m)` in compute_logic_fingerprint | mitigate | `COMPUTE_SOURCE_MODULES` is a literal tuple of in-repo module names; never derived from user/Strava input. No dynamic module names. |
| T-15-03 | Denial of Service | `inspect.getsource` raising OSError | accept (guarded later) | Source ships with `pip install /app` (Dockerfile:19); packaged-install smoke in 15-05 / `just test` guards regression. Refresh records failed runs rather than crashing. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_logic_fingerprint.py` exits 0 (determinism, sensitivity, completeness all green).
- `uv run ruff check src tests` and `uv run ruff format --check src tests` clean.
- `uv run pyright src` clean.
- `python -c "from mcp_strava.metric_registry import compute_logic_fingerprint; print(compute_logic_fingerprint())"` prints a 64-char hex string with no import error (proves no top-level cycle).
</verification>

<success_criteria>
- `compute_logic_fingerprint()` and `COMPUTE_SOURCE_MODULES` exist in `metric_registry.py`.
- Determinism proven across processes and PYTHONHASHSEED values (not builtin hash()).
- Completeness test fails loudly if a materializer-path compute module is omitted from the tuple.
- No import cycle introduced (runtime import_module inside the function).
</success_criteria>

<output>
Create `.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- `COMPUTE_SOURCE_MODULES` (module-level tuple, `metric_registry.py`)
- `compute_logic_fingerprint()` (function, `metric_registry.py`)
- `tests/test_logic_fingerprint.py` (new test file)
