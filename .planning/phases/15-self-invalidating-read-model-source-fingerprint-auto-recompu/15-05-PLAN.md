---
phase: 15-self-invalidating-read-model-source-fingerprint-auto-recompu
plan: 05
type: execute
wave: 4
depends_on: [15-03]
files_modified:
  - src/mcp_strava/metrics.py
  - src/mcp_strava/metric_registry.py
  - src/mcp_strava/adapters/duckdb/schema.py
  - src/mcp_strava/adapters/duckdb/read_model_materializer.py
  - src/mcp_strava/application/metric_services.py
  - tests/test_metric_services.py
  - tests/test_logic_fingerprint.py
  - tests/test_docker_runtime.py
autonomous: true
requirements: [REQ-TIME, REQ-ZEROKNOB]
must_haves:
  truths:
    - "start_time_local (HH:MM) is a materialized fact column derived by PARSING start_date_local with datetime.fromisoformat() and formatting strftime('%H:%M') — NOT a [11:16] string slice; mixed-offset and trailing-Z values are normalized; an unparseable/missing value yields None"
    - "relative_time is computed at read time by parsing the activity's start_date_local (a full datetime, not the date-only activity_date) to a datetime and diffing against the service's now; < 24h renders 'Hh Mm', >= 1 day renders 'Nd Hh' (minutes dropped), boundary at exactly 24h renders '1d 0h'; None when start_date_local is missing/unparseable"
    - "Adding start_time_local migrates additively on a live DB (NULL on old rows until re-materialized) and computing the fingerprint still works under the pip install /app Docker image (no OSError)"
    - "An UNCONDITIONAL inspect.getsource smoke in test_logic_fingerprint.py (editable install) plus a real docker compose exec check both prove getsource succeeds on every COMPUTE_SOURCE_MODULES module"
  artifacts:
    - path: "src/mcp_strava/adapters/duckdb/read_model_materializer.py"
      provides: "start_time_local populated in the activity fact dict"
      contains: "start_time_local"
    - path: "src/mcp_strava/application/metric_services.py"
      provides: "relative_time read-time field on the workout payload"
      contains: "relative_time"
  key_links:
    - from: "read_model_materializer._activity_fact"
      to: "activity_metric_facts.start_time_local"
      via: "HH:MM from start_date_local"
      pattern: "start_time_local"
    - from: "metric_services._activity_payload"
      to: "relative_time"
      via: "now - activity_dt formatted"
      pattern: "relative_time"
---

<objective>
Add finer workout time granularity: materialize `start_time_local` (HH:MM) as a fact column derived from `start_date_local`, and add a read-time `relative_time` field to the workout payload. Also fold in the packaged-install fingerprint smoke (getsource under the Docker image) since this plan touches the materialized-column registry.

Purpose: Expose stable time-of-day on workout rows and a human "Nd Hh / Hh Mm ago" recency string without storing a value that depends on the current time.
Output: new materialized column + payload field, read-time relative_time, packaged-install smoke.

**Dependency note (cycle-2 MEDIUM — relaxed from [15-03, 15-04] to [15-03]):** This plan only needs 15-03's recompute/version plumbing; the Walk discount (15-04) and these time fields do not interact, so 15-05 no longer serializes behind 15-04 — both run in wave 4. The ONLY file shared with 15-04 is `read_model_materializer.py`, and the edits are in DIFFERENT functions (15-04 touches `_materialize_daily_facts`/`_materialize_model_facts`/observed_trimp at :280; 15-05 touches `_activity_fact` at :83). 15-05 does NOT touch `repository.py` (15-04 does). If 15-04 and 15-05 execute concurrently and both stage edits to `read_model_materializer.py`, coordinate at the function level — apply each plan's hunks to its own function; do not clobber the other's region. No logical/data dependency exists between them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/CONTEXT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-RESEARCH.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-PATTERNS.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize start_time_local as an additive fact column</name>
  <files>src/mcp_strava/metrics.py, src/mcp_strava/metric_registry.py, src/mcp_strava/adapters/duckdb/schema.py, src/mcp_strava/adapters/duckdb/read_model_materializer.py</files>
  <read_first>
    - src/mcp_strava/metrics.py (pure-domain module, storage-free per docstring; ALREADY in COMPUTE_SOURCE_MODULES — this is the layering-neutral home for parse_local_hhmm; existing pure-fn shape e.g. calc_hrr_pct)
    - src/mcp_strava/metric_registry.py (MATERIALIZED_FACT_COLUMN_REGISTRY at 1730; _MATERIALIZED_FACT_COLUMN_SQL_METADATA at 1574; FactColumnDefinition; activity_metric_fact_add_column_sql / activity_metric_facts_table_sql / materialized_fact_column_definition used by schema; existing start_time metric at 418 + calculation text at 158)
    - src/mcp_strava/adapters/duckdb/schema.py (ACTIVITY_METRIC_FACT_LATE_COLUMNS at 281 — the additive-late-column list; ensure_provenance_columns at 518; calories_kcal is the analog late column)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (_activity_fact at 83 builds the fact dict; activity row has summary_json; existing read-time extraction str(summary.get("start_date_local",""))[11:16] in metric_services._activity_payload:309 shows the HH:MM slice)
  </read_first>
  <action>
    Add a nullable VARCHAR column start_time_local to activity_metric_facts via the additive-late-column path: register it in MATERIALIZED_FACT_COLUMN_REGISTRY["activity_metric_facts"] and _MATERIALIZED_FACT_COLUMN_SQL_METADATA (type VARCHAR, nullable True, default None), and add "start_time_local" to ACTIVITY_METRIC_FACT_LATE_COLUMNS (schema.py:281) so ensure_provenance_columns adds it to live DBs (NULL on old rows until re-materialized). In _activity_fact (read_model_materializer.py), populate "start_time_local" by PARSING the activity's start_date_local (from activity.summary_json) with datetime.fromisoformat() and formatting strftime("%H:%M") — do NOT use the fragile [11:16] string slice. Normalize the input first: strip a trailing "Z" (replace with "+00:00") so fromisoformat accepts it on 3.14, and accept both offset-aware and naive strings. Wrap the parse in try/except (ValueError/TypeError) returning None when start_date_local is missing/empty/unparseable. Factor this into a small module-level helper `parse_local_hhmm(start_date_local: str | None) -> str | None` so the same logic is reused by the payload layer in Task 2.

    HELPER HOME (cycle-2 LOW — layering-neutral placement, REQUIRED): put `parse_local_hhmm` in `src/mcp_strava/metrics.py`, NOT in the adapter (`read_model_materializer.py`) and NOT in the application module (`metric_services.py`). Rationale: `metrics.py` is the pure-domain module (storage-free per its docstring), is ALREADY in COMPUTE_SOURCE_MODULES (the fingerprint tuple), and is already imported by the adapter (`read_model_materializer.py`) — and the application layer may import it too — so both consumers can use it without an import-direction violation. Importing it FROM the adapter INTO the application (or vice versa) would be a layering violation; sourcing it from the shared pure-domain `metrics.py` avoids that. (Name it without a leading underscore since it is a cross-module shared helper, not a module-private function.) Because metrics.py, read_model_materializer.py, and metric_registry.py are all in COMPUTE_SOURCE_MODULES, this edit flips the fingerprint and triggers a one-time recompute that backfills the column — that is intended.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py -k "start_time"</automated>
  </verify>
  <done>start_time_local exists as a nullable activity_metric_facts column, is populated by the materializer from start_date_local, and migrates additively on an existing DB.</done>
</task>

<task type="auto">
  <name>Task 2: Surface start_time_local + read-time relative_time on the workout payload</name>
  <files>src/mcp_strava/application/metric_services.py, tests/test_metric_services.py</files>
  <read_first>
    - src/mcp_strava/application/metric_services.py (_activity_payload at 273 — payload dict incl. existing start_time at 309; list_workouts_service compact_rows at 425; get_workout_detail_service at 454; services already have checked_at/now)
    - .planning/phases/15-.../15-RESEARCH.md (Code Examples: _relative_time rule — <24h -> "Hh Mm"; >=1 day -> "Nd Hh")
    - tests/test_metric_services.py (payload assertions; fixture helpers)
  </read_first>
  <action>
    Surface the materialized start_time_local on the payload (prefer the fact column over a read-time computation; choose the fact column as the source of truth, falling back to `metrics.parse_local_hhmm(summary.start_date_local)` from Task 1 only if the column is NULL on an un-rematerialized row — reuse the SAME helper from metrics.py, do not reintroduce the [11:16] slice).

    Add a read-time relative_time field computed in the service layer (NOT materialized). SOURCE FIELD (review MEDIUM): parse the activity's `start_date_local` (the full datetime from summary_json) to a datetime — NOT `activity_date`, which is date-only. Normalize like Task 1 (strip trailing "Z" -> "+00:00", accept aware/naive). To diff against the service `now`, make both sides comparable: if the parsed activity datetime is timezone-aware, convert `now` to the same awareness (or drop tzinfo from both consistently) so the subtraction never raises a naive/aware TypeError. Then: total_minutes = int((now - activity_dt).total_seconds() // 60); hours, minutes = divmod(total_minutes, 60); if hours < 24 -> f"{hours}h {minutes}m"; else days, rem = divmod(hours, 24) -> f"{days}d {rem}h". When start_date_local is missing/unparseable, relative_time is None. Thread the service's `now`/checked_at into _activity_payload (add a `now: datetime` param) so the field is read-time and deterministic in tests; update both calling service signatures (list_workouts_service compact_rows path and get_workout_detail_service). Add relative_time to both the full payload and the list_workouts compact_rows key set (CONTEXT surfaces it on workout payloads).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py -k "relative_time or start_time"</automated>
  </verify>
  <done>Workout payloads carry start_time_local (parsed via fromisoformat/strftime, not slicing) and a read-time relative_time; the 24h boundary and the "Hh Mm" / "Nd Hh" formats are asserted, including the boundary case at exactly 24h (renders "1d 0h"); a trailing-Z and an offset-bearing start_date_local both parse correctly; a missing/garbage start_date_local yields None for both fields without raising.</done>
</task>

<task type="auto">
  <name>Task 3: Packaged-install fingerprint smoke — unconditional (local) + real docker compose exec</name>
  <files>tests/test_logic_fingerprint.py, tests/test_docker_runtime.py</files>
  <read_first>
    - tests/test_logic_fingerprint.py (15-01's fingerprint tests — add the unconditional getsource smoke here so `uv run pytest -q` catches it on the editable install too)
    - tests/test_docker_runtime.py (existing docker/runtime test style; how it shells into the container)
    - Justfile (line 37 `test:` recipe runs `{{compose}} build` + `{{compose}} up -d --wait` + `{{compose}} exec -T mcp-strava {{smoke}}`; `compose := "docker compose -f deploy/docker-compose.yml"` at line 3 — the real packaged-install execution surface)
    - deploy/Dockerfile (line 19 pip install /app — source ships into site-packages)
    - src/mcp_strava/metric_registry.py (compute_logic_fingerprint, COMPUTE_SOURCE_MODULES)
  </read_first>
  <action>
    Two complementary smokes (review LOW/MEDIUM — make the local one unconditional, keep the Docker one as the real packaged-install proof):

    1. UNCONDITIONAL local smoke in tests/test_logic_fingerprint.py: a test that iterates COMPUTE_SOURCE_MODULES and calls inspect.getsource(import_module(m)) for EACH, asserting none raise OSError, AND asserts compute_logic_fingerprint() returns a 64-char hex string. This runs under `uv run pytest -q` on the editable install — so the regression is caught locally, not only in Docker. No skipif/conditional guard.

    2. REAL docker compose exec smoke: add a test (or wire into the existing docker-runtime path) that runs `docker compose -f deploy/docker-compose.yml exec -T mcp-strava python -c "from mcp_strava.metric_registry import compute_logic_fingerprint; print(compute_logic_fingerprint())"` against the running container (the pip-install /app, non-editable layout) and asserts a 64-char hex string with exit 0 (no OSError). This proves getsource works on INSTALLED source — the assumption the local editable test cannot cover. Gate it behind the same availability check the existing docker-runtime tests use (skip cleanly if the container/compose is unavailable so `uv run pytest -q` stays green locally), but ensure the `just test` phase gate exercises it.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_logic_fingerprint.py -k "getsource or packaged or smoke"</automated>
  </verify>
  <done>compute_logic_fingerprint() / inspect.getsource is proven to work BOTH on the editable install (unconditional local test in test_logic_fingerprint.py) AND under the packaged pip install /app layout via a real docker compose exec; no OSError on either path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary_json start_date_local → fact column | Strava-sourced timestamp string; sliced, not executed |
| now → relative_time | read-time formatting of a server clock value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | start_date_local parsing | mitigate | Parsed with datetime.fromisoformat() + strftime('%H:%M') (NOT a [11:16] string slice); trailing-Z normalized to +00:00; parse wrapped in try/except (ValueError/TypeError) -> None on missing/garbage input; no eval/format injection |
| T-15-11 | Denial of Service | inspect.getsource OSError under packaged install | mitigate | Task 3 smoke proves getsource works on installed source (A1); refresh records failed runs rather than crashing |
</threat_model>

<verification>
- `uv run pytest -q tests/test_metric_services.py tests/test_docker_runtime.py` exits 0.
- `uv run pytest -q` full unit suite green.
- `uv run ruff check src tests` / `ruff format --check src tests` / `uv run pyright src` clean.
- `just test` green (unit + Docker build + live MCP smoke) — phase gate.
</verification>

<success_criteria>
- start_time_local materialized + surfaced; relative_time read-time field with correct 24h-boundary formatting; packaged-install getsource smoke green.
</success_criteria>

<output>
Create `.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- `activity_metric_facts.start_time_local` column + registry/metadata/late-column entries
- `parse_local_hhmm(start_date_local)` datetime-parsing helper in `metrics.py` (pure-domain, layering-neutral; fromisoformat + strftime, Z/offset-normalizing, None-safe) reused by materializer (adapter) + payload (application)
- `relative_time` read-time payload field + `now: datetime` param on `_activity_payload` (+ updated service signatures)
- unconditional local `inspect.getsource` smoke in `test_logic_fingerprint.py` + real `docker compose exec` packaged-install smoke
