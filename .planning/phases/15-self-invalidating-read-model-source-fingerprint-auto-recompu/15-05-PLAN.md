---
phase: 15-self-invalidating-read-model-source-fingerprint-auto-recompu
plan: 05
type: execute
wave: 4
depends_on: [15-03, 15-04]
files_modified:
  - src/mcp_strava/metric_registry.py
  - src/mcp_strava/adapters/duckdb/schema.py
  - src/mcp_strava/adapters/duckdb/read_model_materializer.py
  - src/mcp_strava/application/metric_services.py
  - tests/test_metric_services.py
  - tests/test_docker_runtime.py
autonomous: true
requirements: [REQ-TIME, REQ-ZEROKNOB]
must_haves:
  truths:
    - "start_time_local (HH:MM) is a materialized fact column derived from the activity start_date_local and is surfaced in the workout payload"
    - "relative_time is computed at read time from now: < 24h renders 'Hh Mm', >= 1 day renders 'Nd Hh' (minutes dropped), with the boundary at exactly 24h"
    - "Adding start_time_local migrates additively on a live DB (NULL on old rows until re-materialized) and computing the fingerprint still works under the pip install /app Docker image (no OSError)"
  artifacts:
    - path: "src/mcp_strava/adapters/duckdb/read_model_materializer.py"
      provides: "start_time_local populated in the activity fact dict"
      contains: "start_time_local"
    - path: "src/mcp_strava/application/metric_services.py"
      provides: "relative_time read-time field on the workout payload"
      contains: "relative_time"
  key_links:
    - from: "read_model_materializer._activity_fact"
      to: "activity_metric_facts.start_time_local"
      via: "HH:MM from start_date_local"
      pattern: "start_time_local"
    - from: "metric_services._activity_payload"
      to: "relative_time"
      via: "now - activity_dt formatted"
      pattern: "relative_time"
---

<objective>
Add finer workout time granularity: materialize `start_time_local` (HH:MM) as a fact column derived from `start_date_local`, and add a read-time `relative_time` field to the workout payload. Also fold in the packaged-install fingerprint smoke (getsource under the Docker image) since this plan touches the materialized-column registry.

Purpose: Expose stable time-of-day on workout rows and a human "Nd Hh / Hh Mm ago" recency string without storing a value that depends on the current time.
Output: new materialized column + payload field, read-time relative_time, packaged-install smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/CONTEXT.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-RESEARCH.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-PATTERNS.md
@.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize start_time_local as an additive fact column</name>
  <files>src/mcp_strava/metric_registry.py, src/mcp_strava/adapters/duckdb/schema.py, src/mcp_strava/adapters/duckdb/read_model_materializer.py</files>
  <read_first>
    - src/mcp_strava/metric_registry.py (MATERIALIZED_FACT_COLUMN_REGISTRY at 1730; _MATERIALIZED_FACT_COLUMN_SQL_METADATA at 1574; FactColumnDefinition; activity_metric_fact_add_column_sql / activity_metric_facts_table_sql / materialized_fact_column_definition used by schema; existing start_time metric at 418 + calculation text at 158)
    - src/mcp_strava/adapters/duckdb/schema.py (ACTIVITY_METRIC_FACT_LATE_COLUMNS at 281 — the additive-late-column list; ensure_provenance_columns at 518; calories_kcal is the analog late column)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (_activity_fact at 83 builds the fact dict; activity row has summary_json; existing read-time extraction str(summary.get("start_date_local",""))[11:16] in metric_services._activity_payload:309 shows the HH:MM slice)
  </read_first>
  <action>
    Add a nullable VARCHAR column start_time_local to activity_metric_facts via the additive-late-column path: register it in MATERIALIZED_FACT_COLUMN_REGISTRY["activity_metric_facts"] and _MATERIALIZED_FACT_COLUMN_SQL_METADATA (type VARCHAR, nullable True, default None), and add "start_time_local" to ACTIVITY_METRIC_FACT_LATE_COLUMNS (schema.py:281) so ensure_provenance_columns adds it to live DBs (NULL on old rows until re-materialized). In _activity_fact (read_model_materializer.py), populate "start_time_local": the HH:MM substring of the activity's start_date_local (parse from activity.summary_json's start_date_local, mirroring the [11:16] slice; None when absent). Because read_model_materializer.py and metric_registry.py are both in COMPUTE_SOURCE_MODULES, this edit will flip the fingerprint and trigger a one-time recompute that backfills the column — that is intended.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py -k "start_time"</automated>
  </verify>
  <done>start_time_local exists as a nullable activity_metric_facts column, is populated by the materializer from start_date_local, and migrates additively on an existing DB.</done>
</task>

<task type="auto">
  <name>Task 2: Surface start_time_local + read-time relative_time on the workout payload</name>
  <files>src/mcp_strava/application/metric_services.py, tests/test_metric_services.py</files>
  <read_first>
    - src/mcp_strava/application/metric_services.py (_activity_payload at 273 — payload dict incl. existing start_time at 309; list_workouts_service compact_rows at 425; get_workout_detail_service at 454; services already have checked_at/now)
    - .planning/phases/15-.../15-RESEARCH.md (Code Examples: _relative_time rule — <24h -> "Hh Mm"; >=1 day -> "Nd Hh")
    - tests/test_metric_services.py (payload assertions; fixture helpers)
  </read_first>
  <action>
    Surface the materialized start_time_local on the payload (prefer the fact column over the read-time slice; keep the existing "start_time" behavior or replace it with the fact column value per CONTEXT — choose the fact column as the source of truth, falling back to the summary slice only if the column is NULL on an un-rematerialized row). Add a read-time relative_time field computed in the service layer (NOT materialized) from the activity datetime and the service's now: total_minutes = (now - activity_dt) in minutes; hours, minutes = divmod(total_minutes, 60); if hours < 24 -> f"{hours}h {minutes}m"; else days, rem = divmod(hours, 24) -> f"{days}d {rem}h". Thread the service's `now`/checked_at into _activity_payload (add a `now` param) so the field is read-time and deterministic in tests. Add relative_time to both the full payload and the list_workouts compact_rows key set if a relative recency is wanted there (CONTEXT surfaces it on workout payloads).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py -k "relative_time or start_time"</automated>
  </verify>
  <done>Workout payloads carry start_time_local and a read-time relative_time; the 24h boundary and the "Hh Mm" / "Nd Hh" formats are asserted, including the boundary case at exactly 24h (renders "1d 0h").</done>
</task>

<task type="auto">
  <name>Task 3: Packaged-install fingerprint smoke (getsource under Docker)</name>
  <files>tests/test_docker_runtime.py</files>
  <read_first>
    - tests/test_docker_runtime.py (existing docker/runtime test style)
    - deploy/Dockerfile (line 19 pip install /app — source ships into site-packages)
    - src/mcp_strava/metric_registry.py (compute_logic_fingerprint)
  </read_first>
  <action>
    Add a test asserting compute_logic_fingerprint() returns a 64-char hex string without raising OSError when run against the installed (non-editable) package layout. If the suite already exercises an in-container path, assert it there; otherwise add a focused test that imports compute_logic_fingerprint and calls it, guarding the A1 assumption (getsource works on installed source). If the Docker path is only exercised by `just test`, add a marker/assertion that fails loudly if getsource raises, so the packaged-install regression is caught by the phase gate.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_docker_runtime.py -k "fingerprint or getsource or logic"</automated>
  </verify>
  <done>compute_logic_fingerprint() is proven to work under the packaged (pip install /app) layout; no OSError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary_json start_date_local → fact column | Strava-sourced timestamp string; sliced, not executed |
| now → relative_time | read-time formatting of a server clock value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | start_date_local parsing | mitigate | Treat as an opaque string slice ([11:16]); guard None/short strings; no eval/format injection |
| T-15-11 | Denial of Service | inspect.getsource OSError under packaged install | mitigate | Task 3 smoke proves getsource works on installed source (A1); refresh records failed runs rather than crashing |
</threat_model>

<verification>
- `uv run pytest -q tests/test_metric_services.py tests/test_docker_runtime.py` exits 0.
- `uv run pytest -q` full unit suite green.
- `uv run ruff check src tests` / `ruff format --check src tests` / `uv run pyright src` clean.
- `just test` green (unit + Docker build + live MCP smoke) — phase gate.
</verification>

<success_criteria>
- start_time_local materialized + surfaced; relative_time read-time field with correct 24h-boundary formatting; packaged-install getsource smoke green.
</success_criteria>

<output>
Create `.planning/phases/15-self-invalidating-read-model-source-fingerprint-auto-recompu/15-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- `activity_metric_facts.start_time_local` column + registry/metadata/late-column entries
- `relative_time` read-time payload field + `now` param on `_activity_payload`
- packaged-install fingerprint smoke test
