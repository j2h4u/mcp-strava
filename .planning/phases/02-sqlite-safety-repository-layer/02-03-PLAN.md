---
phase: 02-sqlite-safety-repository-layer
plan: 02-03
type: tdd
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/mcp_strava/adapters/sqlite/repository.py
  - src/mcp_strava/types.py
  - src/mcp_strava/db.py
  - src/mcp_strava/report.py
  - src/mcp_strava/analytics.py
  - src/mcp_strava/trends.py
  - src/mcp_strava/metrics.py
  - tests/test_load_status.py
  - tests/test_repository_boundary.py
autonomous: true
requirements:
  - SAFE-03
  - REPO-01
  - REPO-03
  - TEST-01
must_haves:
  truths:
    - "Missing-HR and missing-stream activities are represented as UNKNOWN or PARTIAL rather than rest days."
    - "Observed HR stream data still produces the same numeric TRIMP/load inputs used by existing Banister and weekly math."
    - "Report, weekly, trend, and metric read paths can consume repository methods instead of raw application-level SQL."
    - "D-06: Read-path parity protects lower-level numeric/load signals instead of exact user-facing report wording."
    - "D-07: Observed TRIMP history, Banister/form/load, and key aggregates remain deterministic where source data is observed."
    - "D-08: Tests avoid exact-match daily/weekly report wording snapshots."
    - "D-13: Missing HR/stream data is explicit and never treated as rest."
    - "D-14: Repository-level daily load contract exposes REST, UNKNOWN, PARTIAL, and OBSERVED statuses."
    - "D-15: Daily load contract separates observed load from effective numeric input used by existing training math."
    - "D-16: Missing-data tests use hermetic SQLite fixtures only."
    - "D-17: Missing-data tests prove no live network and explicit missing-data semantics."
  artifacts:
    - path: "tests/test_load_status.py"
      provides: "Hermetic missing-data semantics and observed-load parity tests"
    - path: "src/mcp_strava/adapters/sqlite/repository.py"
      provides: "Daily load status contract and read methods for analytics"
    - path: "src/mcp_strava/report.py"
      provides: "Daily report read path using repository load data"
    - path: "src/mcp_strava/analytics.py"
      provides: "Weekly digest read path using repository load data"
    - path: "src/mcp_strava/trends.py"
      provides: "Trend read path using repository load data"
  key_links:
    - from: "src/mcp_strava/report.py"
      to: "src/mcp_strava/adapters/sqlite/repository.py"
      via: "daily_report obtains training load through repository daily load methods"
    - from: "src/mcp_strava/analytics.py"
      to: "src/mcp_strava/adapters/sqlite/repository.py"
      via: "weekly_digest obtains daily TRIMP history through repository methods"
    - from: "src/mcp_strava/types.py"
      to: "src/mcp_strava/training.py"
      via: "DailyLoadPoint exposes observed numeric input separately from completeness status"
---

<objective>
Adopt repository reads in analytics and make missing data explicit.

Purpose: D-13 through D-15 require missing HR/stream sessions to be visible as uncertainty, while D-06/D-07 require current numeric/load signals to stay stable where data is observed.
Output: Daily load status contract, repository-backed read paths, and hermetic parity tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sqlite-safety-repository-layer/02-CONTEXT.md
@.planning/phases/02-sqlite-safety-repository-layer/02-RESEARCH.md
@.planning/phases/02-sqlite-safety-repository-layer/02-PATTERNS.md
@src/mcp_strava/report.py
@src/mcp_strava/analytics.py
@src/mcp_strava/trends.py
@src/mcp_strava/metrics.py
@src/mcp_strava/training.py
@src/mcp_strava/types.py
</context>

<decision_traceability>
- D-06: Implemented by preserving lower-level numeric/load signal parity rather than exact report wording.
- D-07: Implemented by observed TRIMP history and Banister/form/load parity tests.
- D-08: Implemented by tests that compare numeric invariants and status semantics, not full daily/weekly report exact matches.
- D-13: Implemented by explicit missing HR/stream status handling that never treats missing data as rest.
- D-14: Implemented by repository-level daily load statuses `REST`, `UNKNOWN`, `PARTIAL`, and `OBSERVED`.
- D-15: Implemented by separating observed load from effective numeric input for existing training math.
- D-16: Implemented by temp SQLite fixture tests only.
- D-17: Implemented by missing-data semantics and no-live-network tests.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing missing-data and observed-load parity tests</name>
  <files>tests/test_load_status.py, tests/test_repository_boundary.py</files>
  <read_first>
    - src/mcp_strava/db.py
    - src/mcp_strava/report.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/training.py
    - tests/test_smoke.py
  </read_first>
  <behavior>
    - Day with no activities is `REST` and effective numeric load is 0.
    - Day with an activity but no streams is `UNKNOWN`, not `REST`.
    - Day with streams but no HR is `PARTIAL` or `UNKNOWN`, not `REST`.
    - Day with HR-bearing streams is `OBSERVED` and its effective TRIMP matches the pre-Phase-2 `get_daily_trimp_history()` result for observed data.
    - Banister/form/load calls continue to receive numeric observed load input while the status contract preserves uncertainty for later layers.
  </behavior>
  <action>Create `tests/test_load_status.py` with temp DB fixtures that include one rest day, one activity without streams, one activity with streams but no HR, and one activity with HR streams. Extend `tests/test_repository_boundary.py` if needed to assert report/analytics/trends modules no longer introduce new raw SQLite connection acquisition outside the adapter bridge. Tests must encode D-13/D-14/D-15 and SAFE-03 parity.</action>
  <verify>
    <automated>python3 -m pytest tests/test_load_status.py tests/test_repository_boundary.py -q</automated>
  </verify>
  <done>Focused tests fail before load-status implementation and do not call live Strava APIs or mutate the real DB.</done>
  <acceptance_criteria>
    - `tests/test_load_status.py` asserts all four statuses: `REST`, `UNKNOWN`, `PARTIAL`, `OBSERVED`.
    - Test fixtures use `tmp_path` or in-memory/temp DBs only.
    - Observed TRIMP parity is compared numerically, not through full report exact-match text.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement daily load status contract and migrate read paths</name>
  <files>src/mcp_strava/adapters/sqlite/repository.py, src/mcp_strava/types.py, src/mcp_strava/db.py, src/mcp_strava/report.py, src/mcp_strava/analytics.py, src/mcp_strava/trends.py, src/mcp_strava/metrics.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/types.py
    - src/mcp_strava/report.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/trends.py
    - src/mcp_strava/metrics.py
    - src/mcp_strava/training.py
  </read_first>
  <action>Add a `DailyLoadPoint` contract with `date`, `status`, `observed_trimp`, `effective_trimp`, and activity counts/details sufficient to distinguish REST, UNKNOWN, PARTIAL, and OBSERVED per D-13/D-14/D-15. Implement repository methods to build daily load status over a date window and to return the numeric TRIMP history used by existing training math only from OBSERVED HR stream data. Update `db.py::get_daily_trimp_history` to delegate to the repository for compatibility. Update `report.py`, `analytics.py`, and `trends.py` to acquire load data through repository methods while preserving current numeric behavior for observed data. For `metrics.py`, keep per-activity stream metric queries behind repository helpers where practical without changing the cardiac drift algorithm.</action>
  <verify>
    <automated>python3 -m pytest tests/test_load_status.py tests/test_repository_boundary.py -q</automated>
  </verify>
  <done>Focused tests pass; missing data is explicit; observed numeric load parity remains stable; read paths use repository methods where Phase 2 requires them.</done>
  <acceptance_criteria>
    - `src/mcp_strava/types.py` defines a daily load status contract containing `REST`, `UNKNOWN`, `PARTIAL`, and `OBSERVED` values.
    - `get_daily_trimp_history` remains available but delegates to repository load history.
    - `report.py`, `analytics.py`, and `trends.py` no longer construct daily load history with direct SQL.
    - `python3 -m pytest tests/test_load_status.py tests/test_repository_boundary.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sensor data -> training math | Missing HR/stream data can be mistaken for true rest if status is collapsed to numeric zero. |
| Repository -> analytics modules | Repository must preserve observed numeric behavior while surfacing uncertainty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | daily load status | mitigate | Separate `observed_trimp` and `effective_trimp`; never classify activity-present missing data as REST. |
| T-02-07 | Repudiation | parity checks | mitigate | Numeric parity tests compare observed TRIMP/load invariants before/after repository delegation. |
| T-02-SC | Tampering | package installs | accept | No package-manager installs are planned for this plan. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_load_status.py tests/test_repository_boundary.py -q`
- `just test`
</verification>

<success_criteria>
- REPO-03 is implemented with explicit status semantics.
- SAFE-03 lower-level numeric parity is protected for observed data.
- Existing report/weekly/trend behavior remains verifiable without exact text snapshots.
</success_criteria>

<output>
Create `.planning/phases/02-sqlite-safety-repository-layer/02-03-SUMMARY.md` when done.
</output>
