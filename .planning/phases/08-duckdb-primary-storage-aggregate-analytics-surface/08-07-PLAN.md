---
phase: 08-duckdb-primary-storage-aggregate-analytics-surface
plan: 08-07
type: tdd
wave: 5
depends_on:
  - 08-06
files_modified:
  - src/mcp_strava/interfaces/mcp_http.py
  - src/mcp_strava/application/metric_services.py
  - src/mcp_strava/devtools/mcp_client/client.py
  - tests/test_mcp_surface.py
  - tests/test_metric_services.py
  - tests/test_metric_registry.py
  - tests/test_mcp_test_client.py
autonomous: true
requirements:
  - P8-SC-04
  - P8-SC-03
  - P8-D-ALL
requirements_addressed:
  - P8-SC-04
  - P8-SC-03
  - P8-D-ALL
user_setup: []
must_haves:
  truths:
    - "D-15/D-16: MCP exposes exactly six product tools, adding only `get_training_aggregates` to the existing five."
    - "D-17/D-18/D-19/D-20/D-21/D-22: `get_training_aggregates` is scenario-oriented, product-parameter based, bundle-aware, gear-free, factual, and free of raw/admin/sync/debug inputs."
    - "D-23/D-24/D-25: `compare_periods` calls the aggregate layer twice with bounded `bucket=all_time` requests and has no separate row-scanning or recompute path."
    - "D-42/D-43/D-44: aggregate and comparison responses include provenance, coverage, missing reasons, sample sizes, and metric-version status."
  artifacts:
    - path: "src/mcp_strava/interfaces/mcp_http.py"
      provides: "six-tool MCP allowlist and get_training_aggregates registration"
    - path: "src/mcp_strava/application/metric_services.py"
      provides: "compare_periods rewrite over aggregate layer"
    - path: "src/mcp_strava/devtools/mcp_client/client.py"
      provides: "six-tool smoke/default call set"
  key_links:
    - from: "src/mcp_strava/interfaces/mcp_http.py"
      to: "src/mcp_strava/application/aggregate_services.py"
      via: "get_training_aggregates_service"
      pattern: "get_training_aggregates_service"
    - from: "src/mcp_strava/application/metric_services.py"
      to: "src/mcp_strava/application/aggregate_services.py"
      via: "bucket=all_time calls"
      pattern: "all_time"
---

<objective>
Expose the aggregate MCP tool and rewrite period comparison over the shared aggregate layer.

Purpose: add the only new Phase 8 MCP aggregate surface while preserving the product-only allowlist and eliminating duplicated comparison math. This implements P8-SC-03/P8-SC-04 and D-15 through D-25 plus D-42 through D-44.
Output: `get_training_aggregates` MCP tool, six-tool surface tests, `compare_periods` aggregate-layer rewrite, and MCP client smoke coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-CONTEXT.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-PATTERNS.md
@.planning/phases/07-materialized-metrics-read-model/07-05-SUMMARY.md
@.planning/phases/07-materialized-metrics-read-model/07-06-SUMMARY.md
@src/mcp_strava/interfaces/mcp_http.py
@src/mcp_strava/application/aggregate_services.py
@src/mcp_strava/application/metric_services.py
@src/mcp_strava/application/metric_registry.py
@src/mcp_strava/devtools/mcp_client/client.py
@tests/test_mcp_surface.py
@tests/test_metric_services.py
@tests/test_mcp_test_client.py
</context>

<decision_traceability>
- D-15/D-16 define the exact tool set change.
- D-17 through D-22 define `get_training_aggregates` parameters and factual/no-admin contract.
- D-23 through D-25 define the `compare_periods` rewrite over the aggregate layer.
- D-42 through D-44 define response metadata for aggregate rows and comparisons.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing MCP aggregate surface and compare delegation tests</name>
  <files>tests/test_mcp_surface.py, tests/test_metric_services.py, tests/test_mcp_test_client.py</files>
  <read_first>
    - src/mcp_strava/interfaces/mcp_http.py
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/application/aggregate_services.py
    - src/mcp_strava/devtools/mcp_client/client.py
    - tests/test_mcp_surface.py
    - tests/test_metric_services.py
    - tests/test_mcp_test_client.py
  </read_first>
  <behavior>
    - MCP tool list is exactly `get_fitness_state`, `list_workouts`, `get_workout_detail`, `compare_periods`, `project_fitness_state`, and `get_training_aggregates`.
    - `get_training_aggregates` schema has product parameters only and excludes SQL, table, query plan, migration, sync, backfill, recompute, dirty queue, token, raw stream, raw Strava, debug, admin, and gear parameters.
    - `compare_periods_service` delegates to the aggregate layer twice with `bucket=all_time`, preserving each period's explicit start and exclusive end bounds.
    - Default MCP client smoke calls include the sixth tool.
  </behavior>
  <action>Update tests before implementation. In `tests/test_mcp_surface.py`, assert exact six-tool ordering/names, read-only annotations, structured output, and forbidden parameter names. In `tests/test_metric_services.py`, add a spy/fake aggregate service proving `compare_periods_service` calls the aggregate layer for period A and period B with `bucket=all_time`, with each call carrying the requested period start and exclusive end so bounded all-time comparisons cannot include later activities. Preserve sample size, coverage, missing reasons, delta, delta percent, trend direction, and metric-version status. In `tests/test_mcp_test_client.py`, require default smoke/script calls for `get_training_aggregates` using a registry bundle and bucketed date range.</action>
  <verify>
    <automated>uv run pytest -q tests/test_mcp_surface.py tests/test_metric_services.py tests/test_mcp_test_client.py</automated>
  </verify>
  <done>Tests fail until the MCP tool and compare rewrite are implemented.</done>
  <acceptance_criteria>
    - Tests assert exactly one new MCP tool.
    - Tests reject gear, raw SQL, table, query plan, admin, sync, backfill, token, recompute, and raw stream parameters.
    - Tests prove compare-periods aggregate-layer delegation by observable call arguments or injected fake.
    - Tests prove compare-periods all-time aggregate calls preserve bounded period end dates.
    - Tests require factual response keys and no coaching language.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register get_training_aggregates in MCP and client smoke paths</name>
  <files>src/mcp_strava/interfaces/mcp_http.py, src/mcp_strava/devtools/mcp_client/client.py, tests/test_mcp_surface.py, tests/test_mcp_test_client.py</files>
  <read_first>
    - tests/test_mcp_surface.py
    - tests/test_mcp_test_client.py
    - src/mcp_strava/interfaces/mcp_http.py
    - src/mcp_strava/application/aggregate_services.py
    - src/mcp_strava/devtools/mcp_client/client.py
  </read_first>
  <behavior>
    - MCP handler delegates to `get_training_aggregates_service` and returns the standard ServiceEnvelope payload.
    - Tool annotations remain read-only, non-destructive, idempotent, and closed-world.
    - MCP instructions continue to forbid sync/admin/debug/raw SQL/token/raw Strava capabilities.
  </behavior>
  <action>Add `get_training_aggregates` to `MCP_TOOL_NAMES` as the only new tool. Register it in `build_mcp_server()` with structured output and read-only annotations. Parameters must be product-level only: `start_date`, `end_date`, `bucket`, `metric_ids`, `metric_bundle`, `scope`, `sports`, `include_empty_buckets`, `as_of_day`, and `window_days` if the service supports rolling requests. Delegate all validation to `aggregate_services.get_training_aggregates_service`; do not build SQL in the MCP handler. Update `devtools/mcp_client/client.py` expected tool names and default warm calls to include an aggregate request such as a weekly digest bundle over a recent date range.</action>
  <verify>
    <automated>uv run pytest -q tests/test_mcp_surface.py tests/test_mcp_test_client.py tests/test_training_aggregates.py</automated>
  </verify>
  <done>MCP exposes exactly six product tools and the aggregate tool returns a ServiceEnvelope-shaped payload.</done>
  <acceptance_criteria>
    - `MCP_TOOL_NAMES` contains exactly six product tools.
    - Tool schema contains no SQL/table/query/admin/sync/raw/token/gear parameter.
    - MCP handler contains no DuckDB SQL strings.
    - Client default calls include `get_training_aggregates`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite compare_periods over aggregate service</name>
  <files>src/mcp_strava/application/metric_services.py, tests/test_metric_services.py, tests/test_metric_registry.py</files>
  <read_first>
    - tests/test_metric_services.py
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/application/aggregate_services.py
    - src/mcp_strava/application/metric_registry.py
  </read_first>
  <behavior>
    - `compare_periods_service` uses the aggregate layer for both periods with `bucket=all_time`.
    - Comparison output includes period values, delta, delta percent where meaningful, trend direction, sample sizes, coverage, missing reasons, and metric-version status.
    - Separate Python row scanning and per-metric recompute maps for period comparison are removed when the aggregate registry covers the metrics.
  </behavior>
  <action>Refactor `compare_periods_service` so it validates its existing product parameters, builds two aggregate-layer requests with `bucket='all_time'`, explicit period starts, and explicit exclusive period ends, calls the aggregate service/query layer for period A and period B, and formats deltas from aggregate rows. Remove or shrink `COMPARE_PERIODS_HANDLERS`, `ACTIVITY_SCALAR_FACTS`, `MODEL_FACTS`, and `ROLLING_FACTS` when they duplicate registry aggregate semantics. Update `tests/test_metric_registry.py` by replacing handler-map assertions such as `COMPARE_PERIODS_HANDLERS` coverage with an equivalent assertion that every registry metric exposed to `compare_periods` resolves through aggregate-layer metadata and query specs. Preserve public response fields that existing tests require, add response-shape parity assertions before/after the rewrite where practical, and source values from aggregate rows. Keep product-neutral wording and do not add coaching/advice claims.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py tests/test_metric_registry.py tests/test_training_aggregates.py tests/test_security_guards.py</automated>
  </verify>
  <done>`compare_periods` and `get_training_aggregates` share the same aggregate semantics and comparison tests pass.</done>
  <acceptance_criteria>
    - `compare_periods_service` makes two aggregate calls with `bucket=all_time`.
    - Each aggregate call uses the requested period's start and exclusive end as the bounded all-time bucket range.
    - Separate row-scanning comparison logic is absent or limited to compatibility formatting over aggregate rows.
    - Registry tests no longer depend on separate compare-periods handler maps.
    - Mixed metric versions are degraded/reported, not silently compared.
    - Security guards still block request-time raw stream scans and recompute helpers.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool schema | Untrusted agent inputs enter product tool handlers. |
| MCP handler -> application service | Handler must not expose SQL/storage/admin controls. |
| Comparison formatter -> aggregate rows | Formatter must preserve provenance and version status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Elevation of Privilege | `mcp_http.py` | mitigate | Exact six-tool allowlist and forbidden parameter/schema tests. |
| T-08-07-02 | Tampering | `get_training_aggregates` inputs | mitigate | Product parameters only; service validates enums/bundles before queries. |
| T-08-07-03 | Information Disclosure | MCP response | mitigate | Tests reject raw SQL, table names, query plans, raw streams, tokens, and kudos names. |
| T-08-07-04 | Repudiation | `compare_periods` | mitigate | Tests require sample sizes, coverage, missing reasons, and metric-version status. |
| T-08-07-05 | Denial of Service | MCP aggregate call | mitigate | Handler delegates to prepared aggregate layer; 100 ms p95 gate follows in 08-08. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_mcp_surface.py tests/test_metric_services.py tests/test_mcp_test_client.py tests/test_training_aggregates.py tests/test_security_guards.py`
- `uv run pytest -q`
</verification>

<success_criteria>
- MCP exposes exactly six product tools with only one new aggregate tool.
- `get_training_aggregates` accepts product parameters and returns factual aggregate envelopes.
- `compare_periods` uses the aggregate layer for both periods.
- No MCP raw SQL, storage, migration, admin, sync, token, raw stream, recompute, debug, or gear surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-07-SUMMARY.md` when done.
</output>
