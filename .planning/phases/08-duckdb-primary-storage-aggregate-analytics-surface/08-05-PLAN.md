---
phase: 08-duckdb-primary-storage-aggregate-analytics-surface
plan: 08-05
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/application/metric_registry.py
  - tests/test_metric_registry.py
  - docs/metrics.md
autonomous: true
requirements:
  - P8-SC-03
  - P8-SC-04
  - P8-D-ALL
requirements_addressed:
  - P8-SC-03
  - P8-SC-04
  - P8-D-ALL
user_setup: []
must_haves:
  truths:
    - "D-19: explicit metric ids and predefined metric bundles come from the metric registry."
    - "D-29/D-30: supported scopes and rolling windows are registry-controlled and whitelisted."
    - "D-32: aggregation semantics live in the metric registry; duplicate pseudo metrics are not created for alternate math."
    - "D-33/D-34/D-35/D-36/D-37/D-38/D-39/D-40/D-41: registry metadata defines sum, calendar average, weighted average, ratio-of-sums, quantile, last-state, distribution, and kudos aggregate behavior."
    - "D-42/D-43/D-44: registry metadata provides fields needed for aggregate provenance, coverage, and metric-version status."
  artifacts:
    - path: "src/mcp_strava/types.py"
      provides: "MetricDefinition aggregate metadata fields"
    - path: "src/mcp_strava/application/metric_registry.py"
      provides: "registry-owned aggregate modes, denominators, bundles, supported scopes, and rolling windows"
    - path: "tests/test_metric_registry.py"
      provides: "semantic coverage for aggregate registry contract"
  key_links:
    - from: "src/mcp_strava/application/metric_registry.py"
      to: "src/mcp_strava/application/aggregate_services.py"
      via: "metric and bundle definitions"
      pattern: "aggregate_.*|bundle"
---

<objective>
Extend the metric registry with aggregate semantics and bundles.

Purpose: make the registry the single source for aggregation behavior before building DuckDB query builders or MCP handlers. This implements P8-SC-03/P8-SC-04 prerequisites and D-19/D-29/D-30/D-32 through D-44.
Output: typed registry fields, bundle definitions, semantic tests, and metric documentation updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-CONTEXT.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-RESEARCH.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-PATTERNS.md
@.planning/phases/07-materialized-metrics-read-model/07-05-SUMMARY.md
@src/mcp_strava/types.py
@src/mcp_strava/application/metric_registry.py
@tests/test_metric_registry.py
@docs/metrics.md
</context>

<decision_traceability>
- D-32 is implemented by adding aggregation fields to `MetricDefinition`, not by minting duplicate metric ids.
- D-33 through D-41 are implemented as registry metadata for concrete existing metrics.
- D-19/D-29/D-30 are implemented through registry-defined bundles, scopes, and rolling windows.
- D-42 through D-44 are enabled by registry metadata needed by aggregate rows and comparisons.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing aggregate registry contract tests</name>
  <files>tests/test_metric_registry.py</files>
  <read_first>
    - src/mcp_strava/types.py
    - src/mcp_strava/application/metric_registry.py
    - tests/test_metric_registry.py
    - .planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-CONTEXT.md
  </read_first>
  <behavior>
    - Every metric exposed to `get_training_aggregates` has aggregate mode, unit, denominator metadata, supported scopes, supported buckets, metric-version policy, and calculation text.
    - Registry bundles such as daily brief, weekly digest, monthly digest, period comparison, sport efficiency, and historical facts resolve only to known metric ids.
    - Weighted HR/power metrics require denominator/weight metadata and cannot silently fall back to naive averages.
    - Distribution/skewed metrics require quantiles p25, median, p75 and sample-size visibility.
  </behavior>
  <action>Extend `tests/test_metric_registry.py` with RED tests for `MetricDefinition` aggregate metadata. Require fields such as `aggregate_mode`, `aggregate_source`, `denominator`, `weight_column`, `numerator_column`, `supported_buckets`, `supported_scopes`, `bundle_ids`, `quantiles`, and `metric_version_policy` where applicable. Add tests mapping D-33 through D-41: volume/load metrics sum; named daily averages use calendar-day denominator; HR/power averages require weights; pace/speed use ratio of sums; skewed metrics use quantiles; model-state metrics use last state; categorical facts use distributions; kudos aggregate counts without kudos names. Add tests proving no metric id is duplicated solely for alternate aggregate math.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py</automated>
  </verify>
  <done>Tests fail until registry aggregate fields and bundle definitions exist.</done>
  <acceptance_criteria>
    - Tests enumerate all supported aggregate modes by exact string.
    - Tests reject unknown bundle metric ids.
    - Tests reject missing calculation descriptions for aggregate-exposed metrics.
    - Tests reject pseudo metric ids created only for alternate aggregation math.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement aggregate metadata in registry types and definitions</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/application/metric_registry.py</files>
  <read_first>
    - tests/test_metric_registry.py
    - src/mcp_strava/types.py
    - src/mcp_strava/application/metric_registry.py
    - .planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-CONTEXT.md
  </read_first>
  <behavior>
    - Registry metadata can drive aggregate query construction without handler-specific math.
    - Supported buckets are `day`, `week`, `month`, `year`, and `all_time`; rolling windows are `7`, `14`, `28`, `42`, and `90`.
    - Supported scopes are `global`, `per_sport`, or `both`, with sport-sensitive metrics defaulting to per-sport unless marked otherwise.
  </behavior>
  <action>Extend `MetricDefinition` in `types.py` with aggregate metadata fields while preserving existing service consumers. Add registry helper arguments in `metric_registry.py` for aggregate mode, source fact grain, denominator/weight/numerator columns, quantile defaults, supported buckets, supported scopes, bundle ids, and metric-version policy. Define metric bundles from agent scenarios in D-17 and D-19: `daily_brief`, `weekly_digest`, `monthly_digest`, `period_comparison`, `sport_efficiency`, and `historical_facts`. Mark gear/equipment as unsupported by omitting any gear scope/filter metadata per D-20/D-31. Keep categorical statuses as distribution/count metrics, not numeric averages.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py tests/test_metric_services.py</automated>
  </verify>
  <done>Registry tests pass and existing MCP metric services still resolve their metric definitions.</done>
  <acceptance_criteria>
    - Existing metric ids remain stable unless a current runtime need proves removal is required.
    - Aggregate-exposed metrics include units, calculation text, aggregate mode, denominator policy, and metric-version policy.
    - Bundles are defined in the registry and contain only known metric ids.
    - Gear/equipment is not present as an aggregate filter, scope, or bundle dimension.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document aggregate semantics from registry metadata</name>
  <files>docs/metrics.md, tests/test_metric_registry.py</files>
  <read_first>
    - docs/metrics.md
    - src/mcp_strava/application/metric_registry.py
    - tests/test_metric_registry.py
  </read_first>
  <action>Update `docs/metrics.md` so aggregate modes, denominators, quantiles, scopes, bundle names, and factual/provenance requirements are generated from or explicitly checked against registry metadata. Preserve product-neutral wording: describe calculation and aggregation mechanics, not coaching advice. Extend tests so docs include every aggregate mode and bundle id exposed by the registry.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py</automated>
  </verify>
  <done>Metric docs reflect the registry aggregate contract and drift tests pass.</done>
  <acceptance_criteria>
    - Docs mention each bundle id and aggregate mode from the registry.
    - Docs state weekly buckets use Monday start and date ranges are half-open.
    - Docs do not add coaching/readiness language.
    - Tests fail if registry aggregate metadata and docs drift.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry metadata -> SQL query builder | Metric metadata controls whitelisted aggregate SQL construction. |
| Registry metadata -> MCP response | Units, calculations, and provenance shape agent-facing facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | `metric_registry.py` | mitigate | Tests require every aggregate-exposed metric to declare mode, denominator, scope, buckets, and version policy. |
| T-08-05-02 | Information Disclosure | aggregate metadata | mitigate | Registry excludes raw table names, raw SQL, raw streams, token state, and kudos names from aggregate definitions. |
| T-08-05-03 | Repudiation | metric calculations | mitigate | Calculation descriptions are mandatory and docs are drift-tested. |
| T-08-05-04 | Tampering | metric versions | mitigate | Registry marks mixed-version behavior for aggregate/comparison rows. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_metric_registry.py tests/test_metric_services.py`
- `uv run pytest -q`
</verification>

<success_criteria>
- Aggregation semantics live in the metric registry.
- Bundles are registry-defined and scenario-oriented.
- Weighted, ratio, quantile, state, distribution, and kudos semantics are explicit.
- No gear/equipment aggregate dimension is added.
</success_criteria>

<output>
Create `.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-05-SUMMARY.md` when done.
</output>
