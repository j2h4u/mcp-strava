---
phase: 08-duckdb-primary-storage-aggregate-analytics-surface
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_docker_runtime.py
autonomous: false
requirements:
  - P8-SC-05
  - P8-D-ALL
requirements_addressed:
  - P8-SC-05
  - P8-D-ALL
user_setup: []
must_haves:
  truths:
    - "P8-SC-05: Python runtime remains Python 3.14 and DuckDB imports successfully in the project environment."
    - "D-45/D-46: Phase 8 follows the researched DuckDB defaults without reopening locked technical decisions."
    - "Package legitimacy: `duckdb` is not added until the human verification gate clears the [ASSUMED] research status."
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned DuckDB dependency range after package legitimacy approval"
    - path: "uv.lock"
      provides: "Resolved dependency lock including DuckDB"
    - path: "tests/test_docker_runtime.py"
      provides: "Python 3.14 and DuckDB dependency expectation"
  key_links:
    - from: "pyproject.toml"
      to: "deploy/Dockerfile"
      via: "container package install"
      pattern: "duckdb>=1\\.5\\.3,<1\\.6"
---

<objective>
Establish the approved DuckDB dependency baseline for Phase 8.

Purpose: satisfy the required human package-legitimacy checkpoint before adding `duckdb`, then pin the dependency so migration, repository, aggregate, and Docker plans can build on a single package version. This plan implements P8-SC-05 and cites D-45/D-46 by using the researched technical default rather than asking new business questions.
Output: approved dependency gate evidence, `duckdb>=1.5.3,<1.6` in project metadata, updated lockfile, and a dependency/runtime test.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-CONTEXT.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-RESEARCH.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-VALIDATION.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-PATTERNS.md
@pyproject.toml
@uv.lock
@tests/test_docker_runtime.py
</context>

<decision_traceability>
- D-45/D-46 are covered by applying `08-RESEARCH.md` and `08-PATTERNS.md` as the technical decision record; no new product/business question is required.
- P8-SC-05 is covered by the DuckDB dependency pin and Python 3.14 verification.
- The Package Legitimacy Audit in `08-RESEARCH.md` marks `duckdb` as [ASSUMED] only because `slopcheck` was unavailable, so this plan inserts a blocking human verification before install.
</decision_traceability>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify DuckDB package legitimacy before install</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - .planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-RESEARCH.md
    - pyproject.toml
    - uv.lock
  </read_first>
  <action>Before editing package metadata, verify the PyPI package named exactly `duckdb` against the Phase 8 Package Legitimacy Audit. Confirm the registry is PyPI, the project URLs point to DuckDB-maintained sources, release `1.5.3` has CPython 3.14 compatible wheels, and the Docker Python 3.14 dry-run install/import succeeds. Do not add any similarly named package. This checkpoint exists because `slopcheck` was unavailable during research.</action>
  <verify>
    <automated>docker run --rm python:3.14-slim python -m pip install --dry-run 'duckdb==1.5.3'</automated>
    <automated>docker run --rm python:3.14-slim sh -c "python -m pip install 'duckdb==1.5.3' >/dev/null && python -c 'import sys, duckdb; assert sys.version_info[:2] == (3, 14); print(duckdb.__version__)'"</automated>
    <human-check>Approve only if the package name, source/project metadata, release files, and Python 3.14 import smoke match `08-RESEARCH.md`.</human-check>
  </verify>
  <done>Human approval is recorded in the plan summary before any `duckdb` dependency is added.</done>
  <acceptance_criteria>
    - No package metadata is edited before the checkpoint is approved.
    - Verification evidence names the exact package `duckdb` and version `1.5.3`.
    - Verification evidence includes a Python 3.14 Docker import smoke.
    - The summary records the approval result and command outputs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add pinned DuckDB dependency and runtime expectation</name>
  <files>pyproject.toml, uv.lock, tests/test_docker_runtime.py</files>
  <read_first>
    - pyproject.toml
    - uv.lock
    - tests/test_docker_runtime.py
    - deploy/Dockerfile
  </read_first>
  <action>After Task 1 approval, run `uv add 'duckdb>=1.5.3,<1.6'` so `pyproject.toml` and `uv.lock` carry the dependency. Add or extend `tests/test_docker_runtime.py` so the runtime contract asserts Python 3.14 remains required and project metadata includes the pinned DuckDB range. Keep the existing `mcp` and `PyYAML` constraints unchanged. Do not alter Docker bind policy, DB path, or runtime topology in this plan.</action>
  <verify>
    <automated>uv run pytest -q tests/test_docker_runtime.py</automated>
    <automated>uv run python -c "import duckdb, sys; assert sys.version_info[:2] == (3, 14); print(duckdb.__version__)"</automated>
  </verify>
  <done>`duckdb>=1.5.3,<1.6` is pinned, the lockfile is updated, and local Python 3.14 imports DuckDB successfully.</done>
  <acceptance_criteria>
    - `pyproject.toml` contains `duckdb>=1.5.3,<1.6`.
    - `uv.lock` resolves DuckDB.
    - Existing dependencies are not loosened or removed.
    - `tests/test_docker_runtime.py` fails if Python 3.14 or the DuckDB dependency is removed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package registry -> runtime dependency | External package metadata enters the local runtime. |
| Docker base image -> project runtime | Python patch-level and wheel compatibility affect runtime startup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-SC | Tampering | package install | mitigate | Blocking human package-legitimacy checkpoint before adding `duckdb`; verify exact package and Python 3.14 wheel/import smoke. |
| T-08-01-01 | Denial of Service | Docker runtime | mitigate | Test asserts Python 3.14 remains the runtime baseline and DuckDB imports before downstream plans rely on it. |
| T-08-01-02 | Information Disclosure | dependency review logs | accept | Package verification commands do not read `.env`, token files, or live DB paths. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_docker_runtime.py`
- `uv run python -c "import duckdb, sys; assert sys.version_info[:2] == (3, 14); print(duckdb.__version__)"`
</verification>

<success_criteria>
- The `duckdb` dependency is human-approved and pinned.
- Python 3.14 remains the declared and tested runtime baseline.
- Downstream Phase 8 plans can import DuckDB without changing dependency policy.
</success_criteria>

<output>
Create `.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-01-SUMMARY.md` when done.
</output>
