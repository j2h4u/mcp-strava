---
phase: "13"
plan: "03"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - pyproject.toml
  - src/mcp_strava/refresh/worker.py
  - src/mcp_strava/refresh/health.py
autonomous: true
requirements:
  - "Code quality / tooling hygiene (ruff + pyright introduced in Phase 11 side-work)"

must_haves:
  truths:
    - "pyproject.toml [tool.pyright] contains typeCheckingMode = standard explicitly"
    - "pyproject.toml [tool.pyright] include = [src] remains unchanged (src-only scope per D-06)"
    - "No noqa: BLE001 comments remain in src/"
    - "Live noqa comments in mcp_http.py:26 and constants.py:118 are untouched"
    - "just check exits 0 (ruff lint + ruff format-check pass)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Explicit typeCheckingMode = standard pin (D-05)"
      contains: "typeCheckingMode = \"standard\""
    - path: "src/mcp_strava/refresh/worker.py"
      provides: "BLE001 noqa removal (D-07)"
    - path: "src/mcp_strava/refresh/health.py"
      provides: "BLE001 noqa removal (D-07)"
  key_links:
    - from: "pyproject.toml [tool.pyright]"
      to: "uv run pyright src invocation"
      via: "typeCheckingMode pin makes green state version-drift-proof"
      pattern: "typeCheckingMode = \"standard\""
---

<objective>
Apply pyright config pin and noqa hygiene — changes that are independent of the error sweep and can run in parallel with Plan 02.

D-05: Add `typeCheckingMode = "standard"` to `[tool.pyright]` in pyproject.toml. The current config relies on the implicit default; pinning makes the zero-error state reproducible across pyright version upgrades.

D-06: Confirm (do NOT change) that `include = ["src"]` remains in `[tool.pyright]`. The type-check scope stays src-only.

D-07: Remove the 3 vestigial `# noqa: BLE001` comments — `BLE001` (blind-except) is not in the ruff select set (`E4,E7,E9,F,I,B,UP`), so these comments suppress nothing. Locations:
- `src/mcp_strava/refresh/worker.py:108` — `except Exception as exc:  # noqa: BLE001`
- `src/mcp_strava/refresh/worker.py:248` — `except Exception as exc:  # noqa: BLE001`
- `src/mcp_strava/refresh/health.py:62` — `except Exception:  # noqa: BLE001 — health recording must never break the worker loop`

KEEP untouched:
- `src/mcp_strava/interfaces/mcp_http.py:26` — `# noqa: F401` (live re-export suppression)
- `src/mcp_strava/constants.py:118` — `# noqa: E402, F401, I001` (live namespace re-export block)

Purpose: Lock the pyright mode so the green state is durable; remove dead suppressions that obscure the real noqa signal.

Output: pyproject.toml with typeCheckingMode pin; worker.py and health.py with BLE001 comments removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin typeCheckingMode in pyproject.toml — D-05/D-06</name>
  <files>pyproject.toml</files>

  <read_first>
  Read pyproject.toml [tool.pyright] section (lines 45–48 approx) to see the current state before editing.
  </read_first>

  <action>
  In the `[tool.pyright]` section of pyproject.toml, add `typeCheckingMode = "standard"` on its own line. The section currently reads:

    [tool.pyright]
    include = ["src"]
    pythonVersion = "3.14"

  After the edit it must read:

    [tool.pyright]
    include = ["src"]
    pythonVersion = "3.14"
    typeCheckingMode = "standard"

  Do NOT change `include`, `pythonVersion`, or any other key. Do NOT add `tests/` to `include` (D-06 locks scope to src-only). Do NOT change ruff config or any other section.
  </action>

  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && grep -n 'typeCheckingMode' pyproject.toml</automated>
  </verify>

  <acceptance_criteria>
  - `grep -n 'typeCheckingMode = "standard"' pyproject.toml` returns exactly one match in the `[tool.pyright]` section
  - `grep -n 'include = \["src"\]' pyproject.toml` still returns a match (scope unchanged)
  - `grep -c 'tests' pyproject.toml` shows `tests` only under `[tool.pytest.ini_options]` testpaths, not under `[tool.pyright]`
  - `uv run pyright src 2>&1 | head -3` runs without config-parse errors
  </acceptance_criteria>

  <done>pyproject.toml [tool.pyright] contains typeCheckingMode = "standard"; scope and pythonVersion unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Remove vestigial noqa: BLE001 comments — D-07</name>
  <files>
    src/mcp_strava/refresh/worker.py
    src/mcp_strava/refresh/health.py
  </files>

  <read_first>
  Read the target lines before editing to confirm the exact text:
  - src/mcp_strava/refresh/worker.py lines 106–110 (the line 108 noqa)
  - src/mcp_strava/refresh/worker.py lines 246–252 (the line 248 noqa)
  - src/mcp_strava/refresh/health.py lines 60–65 (the line 62 noqa)

  Also confirm the live noqa comments are present and untouched before starting:
  - `grep -n "noqa: F401" src/mcp_strava/interfaces/mcp_http.py` should show line 26
  - `grep -n "noqa: E402" src/mcp_strava/constants.py` should show line 118
  </read_first>

  <action>
  Remove only the `# noqa: BLE001` suffix (and any trailing inline comment that is part of the BLE001 annotation) from the three lines. The `except Exception` / `except Exception as exc` clause itself is intentional catch-all behavior — do NOT change it to a narrower exception type, do NOT remove the except clause.

  Specific edits:
  - worker.py:108: remove `  # noqa: BLE001` from end of line; leave `except Exception as exc:` intact
  - worker.py:248: remove `  # noqa: BLE001` from end of line; leave `except Exception as exc:` intact
  - health.py:62: remove `  # noqa: BLE001 — health recording must never break the worker loop` from end of line; leave `except Exception:` intact

  Do NOT touch mcp_http.py or constants.py.
  </action>

  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && rg -n 'noqa: BLE001' src/ && echo "FOUND (bad)" || echo "not found (good)"</automated>
  </verify>

  <acceptance_criteria>
  - `rg -n 'noqa: BLE001' src/` returns no matches
  - `grep -n 'noqa: F401' src/mcp_strava/interfaces/mcp_http.py` still shows line 26 match (live noqa intact)
  - `grep -n 'noqa: E402' src/mcp_strava/constants.py` still shows line 118 match (live noqa intact)
  - `uv run ruff check src tests 2>&1 | tail -3` still reports "All checks passed!"
  - `uv run python -c "from mcp_strava.refresh import worker, health; print('ok')"` exits 0
  </acceptance_criteria>

  <done>No noqa: BLE001 in src/; live F401 and E402/F401/I001 noqa comments intact; ruff still passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config-only changes | No code logic modified; no new inputs, outputs, auth, or external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |

This plan makes two categories of change: adding one key to the pyright TOML config, and removing inline comment suffixes from three `except` lines. Neither introduces new runtime behavior, new inputs, new data paths, or new external surface. No threats are introduced.
</threat_model>

<verification>
After both tasks:

```
cd /home/j2h4u/repos/j2h4u/mcp-strava
grep 'typeCheckingMode' pyproject.toml
rg -c 'noqa: BLE001' src/ || echo "0 matches (good)"
uv run ruff check src tests 2>&1 | tail -3
```

All three checks must be clean.
</verification>

<success_criteria>
- `grep 'typeCheckingMode = "standard"' pyproject.toml` matches exactly once
- `rg 'noqa: BLE001' src/` returns no output
- `grep 'noqa: F401' src/mcp_strava/interfaces/mcp_http.py` still matches (line 26)
- `grep 'noqa: E402' src/mcp_strava/constants.py` still matches (line 118)
- `uv run ruff check src tests` reports "All checks passed!"
</success_criteria>

<output>
Create `.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-03-SUMMARY.md` when done.
</output>
