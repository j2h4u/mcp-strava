---
phase: "13"
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/mcp_strava/preflight.py
  - src/mcp_strava/interfaces/mcp_http.py
  - src/mcp_strava/adapters/duckdb/connection.py
  - src/mcp_strava/devtools/mcp_client/client.py
autonomous: true
requirements:
  - "Code quality / tooling hygiene (ruff + pyright introduced in Phase 11 side-work)"

must_haves:
  truths:
    - "uv run pyright src reports 0 errors after this plan"
    - "All None-guard sites identified post-D-01 are guarded with explicit is None / truthiness / assert"
    - "mcp_http.py:12 imports TransportSecuritySettings from mcp.server.transport_security (public path)"
    - "connection.py close() call is guarded by a local cast so pyright accepts it"
    - "client.py sorted() calls operate on sets with None filtered out"
    - "just test exits 0"
  artifacts:
    - path: "src/mcp_strava/preflight.py"
      provides: "None-guard for _fetchone result (D-02)"
    - path: "src/mcp_strava/interfaces/mcp_http.py"
      provides: "Public import path for TransportSecuritySettings (D-04.1)"
      contains: "from mcp.server.transport_security import TransportSecuritySettings"
    - path: "src/mcp_strava/adapters/duckdb/connection.py"
      provides: "Localized cast for .close() call (D-04.2)"
    - path: "src/mcp_strava/devtools/mcp_client/client.py"
      provides: "None-filtered sorted(set) (D-04.3)"
  key_links:
    - from: "interfaces/mcp_http.py"
      to: "mcp.server.transport_security.TransportSecuritySettings"
      via: "public module path replacing private fastmcp.server path"
      pattern: "from mcp\\.server\\.transport_security import TransportSecuritySettings"
---

<objective>
Sweep all pyright errors that survive after Plan 01's bulk widening (D-01 + D-03).

The executor MUST re-run `uv run pyright src` first to get the actual post-Plan-01 residual list. The decisions below describe the expected residual sites — but the exact remaining errors are authoritative; fix only what pyright still reports.

D-02: Sweep remaining `reportOptionalSubscript` / `reportOptionalMemberAccess` errors on `_fetchone()` results that `Any` widening did NOT fix (the None branch is still possible). Primary known site: `src/mcp_strava/preflight.py:118`. Guard with `if result is None: ...` / truthiness / `assert result is not None` as appropriate. Check all sites in the residual list; most call sites in repository.py already have guards (lines 396, 680–685, 986, 1298, 1504, 1568, 2048) — do not disturb those.

D-04 one-offs (fix each only if still present in the residual list):
1. `reportPrivateImportUsage` — `interfaces/mcp_http.py:12`: change `from mcp.server.fastmcp.server import TransportSecuritySettings` to `from mcp.server.transport_security import TransportSecuritySettings` (verified public path).
2. `connection.py:113` — `conn.close()` on `object`-typed variable: add a localized `cast` or `isinstance`-narrowing rather than importing duckdb types (preserves deliberate duckdb-type-avoidance pattern from repository.py:156).
3. `devtools/mcp_client/client.py:254,258` — `sorted(set(...))` where the set may contain `None` (from `tool.get("name")`): filter `None` values before constructing the set (e.g., filter in the set comprehension or with a conditional expression).
4. Iterable/operator one-offs (`aggregate_queries.py`, `repository.py:685`, `metric_services.py:551`) — expected to be cleared by D-01; confirm. Fix per-site with a 1-line guard or cast ONLY if any remain in the actual residual list.

Purpose: Reach 0 pyright errors. Plan 03 (config/hygiene) runs in parallel and does not affect error count.

**Frontmatter note (residual-driven scope):** `files_modified` lists the expected D-02/D-04 sites. D-04.4 is conditional — if the post-Plan-01 residual still shows iterable/operator errors, the executor may also patch `src/mcp_strava/adapters/duckdb/aggregate_queries.py` and/or `src/mcp_strava/services/metric_services.py` with 1-line guards/casts. These are NOT touched by the parallel Plan 03 (`pyproject.toml`, `refresh/worker.py`, `refresh/health.py`), so extending the file set at execution time is parallel-safe. Record any such files in the SUMMARY.

Output: All four files patched; `uv run pyright src` exits with 0 errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-CONTEXT.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture post-Plan-01 residual and fix None-guards — D-02</name>
  <files>src/mcp_strava/preflight.py</files>

  <read_first>
  First, run `uv run pyright src 2>&1` and save the full output — this is the authoritative residual list that drives all fixes in this plan.

  Then read src/mcp_strava/preflight.py around line 118 to understand the None-guard context before editing. Read any other file that appears in the residual list under `reportOptionalSubscript` or `reportOptionalMemberAccess`.
  </read_first>

  <action>
  For each `reportOptionalSubscript` or `reportOptionalMemberAccess` error in the residual list:
  - Read the call site to understand whether None is genuinely possible or a guaranteed-non-None invariant.
  - If the None case is a real possibility, add an explicit `if result is None:` guard with an appropriate return/raise, or use a truthiness guard (`if not result:`).
  - If the None case is structurally impossible (e.g., aggregate queries with no GROUP BY always return exactly one row), use `assert result is not None, "..."` to make the invariant explicit and machine-checkable.
  - Do NOT add `# type: ignore` suppressions.
  - Primary expected site: preflight.py line 118. Apply the same treatment to any other sites in the residual list.
  - Do NOT disturb the already-guarded sites in repository.py (lines 396, 680–685, 986, 1298, 1504, 1568, 2048) unless they still appear in the residual.
  </action>

  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pyright src 2>&1 | grep -c "reportOptionalSubscript\|reportOptionalMemberAccess" || true</automated>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pyright src 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
  - The first verify command returns 0 (no remaining reportOptionalSubscript or reportOptionalMemberAccess errors)
  - The second verify command's total error count is <= the pre-Task-1 residual count (the None-guard edits introduced NO net-new pyright errors of any other category before Task 2 begins)
  - `uv run python -c "from mcp_strava.preflight import run_preflight; print('import ok')"` exits 0
  </acceptance_criteria>

  <done>All None-guard pyright errors cleared; preflight.py (and any other affected files) import cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Fix D-04 one-offs (import path, cast, None-filter, conditional residuals)</name>
  <files>
    src/mcp_strava/interfaces/mcp_http.py
    src/mcp_strava/adapters/duckdb/connection.py
    src/mcp_strava/devtools/mcp_client/client.py
  </files>

  <read_first>
  Use the residual pyright output captured in Task 1 to determine which D-04 one-offs remain. Read each target file before editing:
  - interfaces/mcp_http.py lines 1–15 (import block)
  - adapters/duckdb/connection.py lines 105–120 (the __exit__ method with conn.close())
  - devtools/mcp_client/client.py lines 248–260 (verify_tool_surface function)

  Before touching D-04.2, verify that `from typing import cast` is already imported or can be added to connection.py.
  </read_first>

  <action>
  Apply each fix only if still present in the post-Plan-01 residual list:

  D-04.1 — mcp_http.py line 12: Replace `from mcp.server.fastmcp.server import TransportSecuritySettings` with `from mcp.server.transport_security import TransportSecuritySettings`. This is a public re-export path verified against the installed mcp package. No other changes in this file.

  D-04.2 — connection.py line 113 (the `conn.close()` call inside `__exit__`): The variable `conn` is typed as whatever the dict value type is; if pyright still flags it, add a localized `cast` from `typing`. Use `cast("Any", conn).close()` (or import `Any` and `cast` from `typing` if not already present). Do NOT import duckdb types — preserve the deliberate duckdb-type-avoidance pattern.

  D-04.3 — client.py line 254: `tool_names = {tool.get("name") for tool in tools if isinstance(tool, dict)}` can produce a `set[str | None]`. Change to filter None: `tool_names = {name for tool in tools if isinstance(tool, dict) and (name := tool.get("name")) is not None}`. Line 258 `sorted(tool_names - EXPECTED_TOOL_NAMES)` should then type-check correctly because both sets are `set[str]`.

  D-04.4 — Iterable/operator one-offs in aggregate_queries.py, repository.py:685, metric_services.py: Check the residual list. If any remain, apply a 1-line guard or `cast(list[...], value)` per site. If the residual shows 0 such errors, skip entirely.
  </action>

  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pyright src 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep -n "from mcp.server.transport_security import TransportSecuritySettings" src/mcp_strava/interfaces/mcp_http.py` returns a match (D-04.1)
  - `grep -n "from mcp.server.fastmcp.server import TransportSecuritySettings" src/mcp_strava/interfaces/mcp_http.py` returns no match (old private path removed)
  - `uv run python -c "from mcp_strava.interfaces.mcp_http import TransportSecuritySettings; print('ok')"` exits 0
  - `uv run pyright src 2>&1 | tail -3` shows "0 errors, 0 warnings, 0 informations"
  - `uv run pytest tests/ -x -q 2>&1 | tail -5` exits 0
  </acceptance_criteria>

  <done>pyright src reports 0 errors; all four D-04 one-offs addressed; test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type annotations only | No new runtime behavior, inputs, outputs, or external surface |
| import path change (D-04.1) | Switches from private to public mcp SDK symbol; same runtime object |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; existing locked dependencies unchanged |

D-04.1 changes only the import path for `TransportSecuritySettings` from a private submodule to its documented public re-export. The runtime object is identical. No security surface change. All other fixes are annotation-only (None guards, cast, set comprehension filter).
</threat_model>

<verification>
After both tasks:

```
cd /home/j2h4u/repos/j2h4u/mcp-strava
uv run pyright src 2>&1 | tail -5
uv run pytest tests/ -x -q 2>&1 | tail -5
```

Both must exit 0. pyright must report "0 errors, 0 warnings, 0 informations".
</verification>

<success_criteria>
- `uv run pyright src` → "0 errors, 0 warnings, 0 informations"
- `mcp_http.py` imports `TransportSecuritySettings` from `mcp.server.transport_security`
- `connection.py __exit__` has a cast/narrowing so `.close()` type-checks
- `client.py verify_tool_surface` filters None from `tool_names` set
- `uv run pytest tests/ -x -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-02-SUMMARY.md` when done.
</output>
