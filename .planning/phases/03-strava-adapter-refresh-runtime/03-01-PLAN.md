---
phase: 03-strava-adapter-refresh-runtime
plan: 03-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/adapters/strava/__init__.py
  - src/mcp_strava/adapters/strava/types.py
  - src/mcp_strava/adapters/strava/token_provider.py
  - src/mcp_strava/adapters/strava/rate_limit.py
  - src/mcp_strava/adapters/strava/transport.py
  - tests/test_strava_adapter.py
autonomous: true
requirements:
  - STRAVA-01
  - STRAVA-02
  - TEST-02
must_haves:
  truths:
    - "Strava OAuth refresh, HTTP execution, retry policy, and rate-limit policy live behind `src/mcp_strava/adapters/strava/` ports."
    - "Token persistence uses `fcntl.flock(LOCK_EX)` on a sidecar lock file and `tempfile` + `os.fsync` + `os.replace` for atomic write."
    - "Token file is chmodded 0o600 after every successful write."
    - "Adapter code never includes the access/refresh token value in raised exceptions, error messages, or stdout/stderr output."
    - "Rate-limit policy parses both `X-RateLimit-*` and `X-ReadRateLimit-*` header pairs (15-min and daily for each) and enforces the stricter remaining budget across all four windows."
    - "Transport accepts an injected `TokenProvider`, `RateLimitPolicy`, `Clock`, and `Sleeper`; tests run with fakes and require no internet access."
    - "Transport returns a typed `StravaResponse(data, rate_info, status)` or raises a typed `StravaUnavailable(reason)` after the retry budget is exhausted."
    - "D-01: read-runtime/refresh-runtime split is preserved by isolating all Strava transport in this package."
    - "D-10: token persistence is atomic and single-writer safe."
    - "D-14: rate-limit policy tracks both overall and read/non-upload windows and enforces the stricter remaining budget."
    - "TEST-02: adapter tests pass with no live Strava API calls."
  artifacts:
    - path: "src/mcp_strava/adapters/strava/token_provider.py"
      provides: "Single-writer token provider with atomic file write and exclusive flock"
    - path: "src/mcp_strava/adapters/strava/transport.py"
      provides: "Strava HTTPS transport with injected token provider, rate-limit policy, clock, and sleeper"
    - path: "src/mcp_strava/adapters/strava/rate_limit.py"
      provides: "Dual-window dual-scope Strava rate-limit policy with `decide_next_call()` API"
    - path: "src/mcp_strava/adapters/strava/types.py"
      provides: "StravaResponse, StravaRateInfo, StravaUnavailable, RefreshReason, and adapter-local dataclasses/enums"
    - path: "tests/test_strava_adapter.py"
      provides: "Hermetic adapter tests with fake transport, fake clock, fake sleeper, and concurrency/atomicity coverage"
  key_links:
    - from: "src/mcp_strava/adapters/strava/transport.py"
      to: "src/mcp_strava/adapters/strava/token_provider.py"
      via: "Transport reads access token through TokenProvider.access_token() and triggers refresh via TokenProvider.refresh()"
    - from: "src/mcp_strava/adapters/strava/transport.py"
      to: "src/mcp_strava/adapters/strava/rate_limit.py"
      via: "Transport asks RateLimitPolicy.decide_next_call() before each call and updates it from response headers"
    - from: "tests/test_strava_adapter.py"
      to: "src/mcp_strava/adapters/strava/transport.py"
      via: "Tests inject FakeTransport collaborators and assert retry/refresh/backoff behavior with no network IO"
---

<objective>
Create the Strava adapter package owning OAuth/HTTP/retry/rate-limit behavior with injectable collaborators.

Purpose: Per D-01, D-10, D-14, STRAVA-01, STRAVA-02, and TEST-02, every Strava API interaction must move behind a dedicated adapter so the read-runtime cannot reach Strava and concurrent refresh attempts are safe.
Output: Strava adapter package (`adapters/strava/`), atomic-write single-writer token provider, dual-window rate-limit policy, retry/backoff transport, and hermetic adapter pytest coverage with fakes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-CONTEXT.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-RESEARCH.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/sync.py
@src/mcp_strava/settings.py
</context>

<decision_traceability>
- D-01: Implemented by isolating Strava API calls inside `adapters/strava/`; nothing in this plan imports `refresh/` or reads SQLite directly.
- D-10: Implemented by `FileTokenProvider` using `fcntl.flock(LOCK_EX)` on a sidecar lock plus tempfile + `os.fsync` + `os.replace` atomic write and 0o600 mode.
- D-14: Implemented by `RateLimitPolicy` parsing both `X-RateLimit-*` and `X-ReadRateLimit-*` pairs and deriving the minimum remaining budget across `(overall_short, overall_long, read_short, read_long)`.
- D-13: Partially implemented by typed `StravaUnavailable(reason)` exceptions matching the product-safe reason codes consumed by Plan 03-02.
- STRAVA-01/STRAVA-02/TEST-02: Implemented by the adapter package plus hermetic tests with fake collaborators and AST/import boundary discipline.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Strava adapter tests with fake collaborators</name>
  <files>tests/test_strava_adapter.py</files>
  <read_first>
    - tests/test_sqlite_safety.py
    - tests/test_security_guards.py
    - src/mcp_strava/db.py
    - src/mcp_strava/sync.py
    - src/mcp_strava/settings.py
    - .planning/phases/03-strava-adapter-refresh-runtime/03-RESEARCH.md
    - .planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
  </read_first>
  <behavior>
    - TokenProvider.refresh() acquires an exclusive `fcntl.flock(LOCK_EX)` on a sidecar lock file before reading or writing the token file.
    - TokenProvider.refresh() never leaves the token file truncated; a crash injected between tempfile write and `os.replace` leaves the original file intact and the temp file removed.
    - TokenProvider write path produces a token file with mode 0o600 after every successful write.
    - Two concurrent TokenProvider.refresh_if_needed() callers in separate threads (or subprocesses) cause exactly one fake-transport refresh exchange; both observe the same final access token.
    - RateLimitPolicy.decide_next_call() returns `proceed` when all four windows have remaining quota; returns `wait_until(...)` when any window's remaining quota is zero; returns `exhausted(reason)` only when the daily windows are zeroed.
    - RateLimitPolicy.update_from_headers() parses both `X-RateLimit-Limit/Usage` and `X-ReadRateLimit-Limit/Usage` (each as `short,long`); missing headers leave existing counters untouched.
    - StravaTransport.fetch(path) returns a typed `StravaResponse(data, rate_info, status)` on 200; raises `StravaUnavailable(reason='token_unavailable')` after exhausting the retry budget on 401; raises `StravaUnavailable(reason='rate_limited')` after exhausting on 429; raises `StravaUnavailable(reason='network_unstable')` after exhausting on `URLError/OSError`.
    - StravaTransport delegates time and sleep to injected Clock/Sleeper; tests advance them without real wall-clock waits.
    - No adapter code path includes the access or refresh token value in raised exceptions, formatted error strings, stdout, or stderr (assert by capturing both streams during induced failures).
    - Adapter tests do not perform live network I/O (monkeypatch any `urllib.request.urlopen` to a fake; absence of a real socket is asserted by monkeypatching the network at test setup).
  </behavior>
  <action>Create `tests/test_strava_adapter.py` using `tmp_path`, `monkeypatch`, and `reset_settings_cache()` for isolated token paths. Build `FakeStravaHttp` returning canned `(status, headers, body)` triples and a configurable script of responses. Build `FakeClock` exposing `now()` returning a controlled float and `FakeSleeper` recording sleep durations without sleeping. Cover D-10 with a threaded race test that uses real `fcntl.flock` against a real `tmp_path/.env` and asserts exactly one fake refresh exchange occurs. Cover D-14 with a parametrized rate-limit-header table including the four-window minimum case. Cover the never-log-token contract by capturing `capsys` during induced 401/429/URLError paths and asserting the access token literal is absent. Add a module-level `monkeypatch` fixture that replaces `urllib.request.urlopen` with a no-op raising `RuntimeError("live network forbidden")` so any accidental real call fails loud. Name tests so that requirement IDs and decision IDs appear in test names or docstrings (e.g., `test_token_provider_is_single_writer_per_D10`). Tests must fail until the adapter package exists.</action>
  <verify>
    <automated>python3 -m pytest tests/test_strava_adapter.py -q</automated>
  </verify>
  <done>`tests/test_strava_adapter.py` exists with named tests for token provider concurrency/atomicity, rate-limit policy across both header pairs, transport retry/refresh/backoff, and never-log-token; running the file exits non-zero before implementation.</done>
  <acceptance_criteria>
    - `tests/test_strava_adapter.py` contains literal references to `FakeStravaHttp`, `FakeClock`, `FakeSleeper`, `X-ReadRateLimit-Limit`, and `StravaUnavailable`.
    - `python3 -m pytest tests/test_strava_adapter.py -q` exits non-zero before implementation lands.
    - Test names or docstrings reference STRAVA-01, STRAVA-02, D-10, D-14, and TEST-02.
    - A module-level monkeypatch replaces `urllib.request.urlopen` with a guard raising "live network forbidden"; no test bypasses it.
    - At least one test runs two concurrent refresh attempts via `threading.Thread` (or `subprocess.Popen` with `fcntl`) and asserts exactly one upstream refresh exchange.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Strava adapter package (token provider, rate-limit policy, transport, types)</name>
  <files>src/mcp_strava/adapters/strava/__init__.py, src/mcp_strava/adapters/strava/types.py, src/mcp_strava/adapters/strava/token_provider.py, src/mcp_strava/adapters/strava/rate_limit.py, src/mcp_strava/adapters/strava/transport.py</files>
  <read_first>
    - src/mcp_strava/db.py
    - src/mcp_strava/sync.py
    - src/mcp_strava/settings.py
    - src/mcp_strava/adapters/sqlite/backup.py
    - .planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
    - .planning/phases/03-strava-adapter-refresh-runtime/03-RESEARCH.md
    - tests/test_strava_adapter.py
  </read_first>
  <action>Create `src/mcp_strava/adapters/strava/__init__.py` re-exporting `FileTokenProvider`, `StravaTransport`, `RateLimitPolicy`, `StravaResponse`, `StravaUnavailable`, `RefreshReason`. In `types.py`, define frozen dataclasses `StravaResponse(data, rate_info, status)`, `StravaRateInfo(overall_short, overall_long, read_short, read_long)` where each window is `tuple[int | None, int | None]` representing `(used, limit)`, plus `class StravaUnavailable(Exception)` carrying a `reason: str` from the set `{token_unavailable, rate_limited, network_unstable, refresh_incomplete}` and a `RefreshReason` string-enum. In `token_provider.py`, implement `FileTokenProvider(path: Path)` with: `access_token()`, `refresh_if_needed(http: Callable, clock: Clock) -> str`, `refresh(http: Callable, clock: Clock) -> str`. Use `fcntl.flock(LOCK_EX)` on `path.with_suffix(path.suffix + '.lock')`; after lock acquisition, re-read the token file to absorb a sibling process refresh; on cache miss, POST to `https://www.strava.com/oauth/token` via the injected http callable; write through tempfile created in `path.parent` with `os.fdopen + flush + os.fsync`, then `os.replace`; `os.chmod(path, 0o600)`; clean up tempfile on any exception. Never include `STRAVA_ACCESS_TOKEN` or `STRAVA_REFRESH_TOKEN` values in raised messages or returned errors. In `rate_limit.py`, implement `RateLimitPolicy` with internal state `{overall_short, overall_long, read_short, read_long}` each `(used: int | None, limit: int | None)`. `update_from_headers(headers: Mapping[str, str])` parses both `X-RateLimit-Limit/Usage` and `X-ReadRateLimit-Limit/Usage`, splitting on comma. `decide_next_call(now: float) -> RateLimitDecision` returns `Decision.proceed()`, `Decision.wait_until(timestamp)`, or `Decision.exhausted(reason)` based on minimum remaining; daily windows produce a date-rollover timestamp, short windows produce a 15-min boundary. In `transport.py`, implement `StravaTransport(token_provider, policy, clock, sleeper, http=urlopen)` and `fetch(path: str) -> StravaResponse`. Use `urllib.request.Request` with the bearer header from `token_provider.access_token()`. Before each call, call `policy.decide_next_call(clock.now())` and sleep via `sleeper` when waiting; refuse and raise `StravaUnavailable('rate_limited')` if exhausted. On 200, parse headers via `policy.update_from_headers()` and return `StravaResponse`. On 401, call `token_provider.refresh()` and retry once. On 429, parse `Retry-After`, sleep, retry up to 3 attempts; mark policy exhausted between attempts. On `URLError`/`OSError`/`socket.timeout`, sleep `[1, 5, 30][attempt]` and retry up to 3 attempts. After the budget is exhausted, raise `StravaUnavailable` with the appropriate reason. Do not include the bearer token in any exception message.</action>
  <verify>
    <automated>python3 -m pytest tests/test_strava_adapter.py -q</automated>
  </verify>
  <done>All Strava adapter tests pass; token provider is single-writer and atomic; rate-limit policy tracks both header pairs; transport raises typed `StravaUnavailable` on exhausted budgets and never logs tokens.</done>
  <acceptance_criteria>
    - `src/mcp_strava/adapters/strava/token_provider.py` calls `fcntl.flock(` with `LOCK_EX` and `os.replace(` and `os.chmod(` with `0o600`.
    - `src/mcp_strava/adapters/strava/rate_limit.py` contains both `X-RateLimit-` and `X-ReadRateLimit-` literals.
    - `src/mcp_strava/adapters/strava/transport.py` raises `StravaUnavailable` with one of `token_unavailable`, `rate_limited`, `network_unstable` after retry exhaustion.
    - `src/mcp_strava/adapters/strava/transport.py` does not contain `print(` and does not include `'STRAVA_ACCESS_TOKEN'` value formatting in raise sites.
    - `src/mcp_strava/adapters/strava/__init__.py` re-exports `FileTokenProvider`, `StravaTransport`, `RateLimitPolicy`, `StravaResponse`, `StravaUnavailable`, `RefreshReason`.
    - `python3 -m pytest tests/test_strava_adapter.py -q` exits 0.
    - The package contains no `import sqlite3` and no imports from `mcp_strava.refresh` or `mcp_strava.adapters.sqlite`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> Token Provider | Token file may be partially written, world-readable, or held by another process. |
| Adapter -> Strava API | Network may fail, return 401/429, or be tampered with at the OS level. |
| Adapter -> Logs | Token values must never escape into stdout, stderr, or exception messages. |
| Adapter -> Rate-limit policy | Missing or malformed headers must not produce false `proceed` decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `token_provider.py` | mitigate | Exclusive `fcntl.flock` + tempfile + `os.fsync` + `os.replace` + `0o600` chmod; concurrency test asserts only one upstream refresh. |
| T-03-02 | Information Disclosure | `transport.py` / `token_provider.py` | mitigate | Adapter never logs or raises token values; tests capture stderr/stdout and assert absence. |
| T-03-03 | Denial of Service (self) | `rate_limit.py` / `transport.py` | mitigate | Dual-window dual-scope policy enforces the stricter remaining budget; typed `StravaUnavailable('rate_limited')` keeps callers honest. |
| T-03-SC | Tampering | package installs | accept | No package-manager installs are planned for this plan. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_strava_adapter.py -q`
- `just test`
- Grep guard: `grep -R 'import sqlite3' src/mcp_strava/adapters/strava/` returns no matches.
- Grep guard: `grep -R 'from mcp_strava.refresh' src/mcp_strava/adapters/strava/` returns no matches.
</verification>

<success_criteria>
- STRAVA-01, STRAVA-02, and TEST-02 have executable coverage through hermetic adapter tests with fake collaborators.
- Token provider survives crash injection and concurrent refresh attempts.
- Rate-limit policy enforces the stricter of overall and read/non-upload remaining budgets across both 15-min and daily windows.
- Transport raises typed `StravaUnavailable(reason=...)` instead of returning ad-hoc errors; consumers in Plan 03-02 can map reasons directly to `refresh_state.last_error_code`.
</success_criteria>

<output>
Create `.planning/phases/03-strava-adapter-refresh-runtime/03-01-SUMMARY.md` when done.
</output>
