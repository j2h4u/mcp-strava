---
phase: 06-full-fidelity-strava-mirror
plan: 06-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/adapters/sqlite/schema.py
  - src/mcp_strava/adapters/sqlite/migrations.py
  - src/mcp_strava/adapters/sqlite/repository.py
  - src/mcp_strava/application/mirror_coverage.py
  - src/mcp_strava/cli.py
  - tests/test_full_fidelity_mirror.py
  - tests/test_repository_boundary.py
  - tests/test_cli_surface.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - MIRROR-01
  - MIRROR-02
  - STREAM-02
  - STREAM-03
  - COVERAGE-01
  - TEST-05
must_haves:
  truths:
    - "D-01: Phase 6 implements a lossless normalized mirror, not a permanent raw payload store."
    - "D-02: MIRROR-01 and MIRROR-02 mean preserving full Strava information content in queryable normalized structures."
    - "D-03: existing `summary_json` and `detail_json` remain allowed, but streams become normalized structured storage."
    - "D-04: raw stream payload capture is optional/debug/transient only and not the source-of-truth contract."
    - "D-05: stream point storage uses a wide + extra JSON model."
    - "D-06: hot analytics columns remain scalar columns: `heartrate`, `velocity`, `altitude`, `cadence`, `grade`, `gap_speed`, `gap_distance`, `is_moving`, `lat`, and `lng`."
    - "D-07: per-point `values_json` retains extra stream channels not represented as scalar columns."
    - "D-08: stream channel metadata is stored per activity/channel with `channel_key`, `original_size`, `resolution`, `series_type`, fetch/batch identity, and availability/error status."
    - "D-09: pure long/EAV-only storage is not the hot analytics path."
    - "D-10: adding only fixed current Strava columns is insufficient."
    - "D-21: coverage controls are local admin CLI only."
    - "D-22: command shape may be `admin mirror-coverage` if the product/admin boundary remains clear."
    - "D-23: repo tests use temp/copy databases; live runtime checks belong under `/opt/docker/mcp-strava`."
    - "D-25: SQLite remains the primary mirror database."
    - "D-26: DuckDB is not the operational mirror in Phase 6."
    - "D-27: DuckDB remains a deferred analytics/read-model idea."
    - "D-28: official SQLite/DuckDB findings are rationale only; implementation scope stays SQLite."
  artifacts:
    - path: "src/mcp_strava/adapters/sqlite/schema.py"
      provides: "Phase 6 required inventory for stream points and channel metadata"
    - path: "src/mcp_strava/adapters/sqlite/migrations.py"
      provides: "Migration target that adds lossless stream fields/tables with backup and parity checks"
    - path: "src/mcp_strava/application/mirror_coverage.py"
      provides: "Aggregate coverage service used by admin CLI and tests"
---

<objective>
Create the lossless stream storage foundation and aggregate coverage inventory before changing live ingestion.

Output: SQLite schema/migration/repository support for `values_json`, stream channel metadata, and admin-only mirror coverage reporting, with temp-DB tests proving no existing rows are dropped.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
@.planning/phases/06-full-fidelity-strava-mirror/06-RESEARCH.md
@.planning/phases/06-full-fidelity-strava-mirror/06-PATTERNS.md
@src/mcp_strava/adapters/sqlite/schema.py
@src/mcp_strava/adapters/sqlite/migrations.py
@src/mcp_strava/adapters/sqlite/repository.py
@src/mcp_strava/cli.py
@tests/test_sqlite_safety.py
@tests/test_repository_boundary.py
</context>

<decision_traceability>
- D-01 through D-10 are implemented by normalized stream metadata plus wide stream points with `values_json`.
- D-21 through D-23 are implemented by admin-only coverage command and temp/copy DB tests.
- D-25 through D-28 are implemented by keeping all schema/repository work in SQLite.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing lossless stream schema and coverage tests</name>
  <files>tests/test_full_fidelity_mirror.py, tests/test_repository_boundary.py, tests/test_cli_surface.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/schema.py
    - src/mcp_strava/adapters/sqlite/migrations.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/cli.py
    - .planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
  </read_first>
  <action>Create `tests/test_full_fidelity_mirror.py` with a temp SQLite fixture matching current schema version 2. Add tests that fail until migration version 3 creates or preserves: `streams.values_json`, `stream_channels` with columns `activity_id`, `channel_key`, `original_size`, `resolution`, `series_type`, `fetched_at`, `batch_id`, `status`, and `error`, index `idx_stream_channels_activity`, and existing `streams` hot columns including `latlng`. Add schema tests that fail until required inventory is version-aware: version 3 inventory requires `streams.latlng`, `streams.values_json`, and `stream_channels`; version 4 inventory is reserved for Plan 06-03 and must not be conflated with version 3. Add repository tests that fail until `insert_stream_rows_chunked` stores `lat`, `lng`, and nullable `values_json` without dropping existing scalar values. Add transaction tests that simulate channel metadata failure during stream replacement and assert stream rows plus metadata are both rolled back. Add CLI tests that fail until `python -m mcp_strava admin mirror-coverage --db <fixture> --json` returns JSON with `status`, `activities_with_streams`, `stream_points`, `gps_points`, `channels`, and `backfill_needed` keys. Extend smoke imports for `mcp_strava.application.mirror_coverage`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_repository_boundary.py tests/test_cli_surface.py tests/test_smoke.py -q</automated>
  </verify>
  <done>Tests fail before schema, repository, and admin coverage code exist.</done>
  <acceptance_criteria>
    - `tests/test_full_fidelity_mirror.py` creates only temp databases under `tmp_path`.
    - Tests assert `stream_channels` metadata columns by exact name.
    - Tests assert `streams.values_json` exists and stores unknown channel values.
    - Tests assert version 3 schema inventory still requires `latlng` while version 4 handling is separate.
    - Tests assert stream replacement plus channel metadata writes are one atomic transaction.
    - CLI test invokes `admin mirror-coverage`, not a product command or MCP tool.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SQLite schema, migration, and repository support for lossless stream metadata</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/adapters/sqlite/schema.py, src/mcp_strava/adapters/sqlite/migrations.py, src/mcp_strava/adapters/sqlite/repository.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/schema.py
    - src/mcp_strava/adapters/sqlite/migrations.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - tests/test_full_fidelity_mirror.py
    - tests/test_repository_boundary.py
  </read_first>
  <action>Add a migration target from user_version 2 to 3. The migration must create `stream_channels` with primary key or unique identity over `activity_id` and `channel_key`, add nullable `values_json` to `streams`, keep existing scalar hot columns, and leave `latlng` in place for Plan 06-03. Replace the flat required-inventory contract with version-aware structures such as `REQUIRED_COLUMNS_BY_VERSION`, `REQUIRED_TABLES_BY_VERSION`, and `REQUIRED_INDEXES_BY_VERSION` plus a helper that validates against the current `PRAGMA user_version`: v3 requires `latlng` and `values_json`; v4 is added by Plan 06-03 and removes `latlng`. Add `idx_stream_channels_activity` during the v3 migration and include it in v3 required-index validation. Define `stream_channels.batch_id` as nullable text populated from the refresh/backfill run identity when available and left null for fixture/manual writes; do not invent a foreign key until a concrete sync-log id is passed through the runtime. Add repository methods `upsert_stream_channel_metadata(activity_id, channel_key, original_size, resolution, series_type, fetched_at, batch_id, status, error)`, `replace_stream_rows_and_channel_metadata(activity_id, rows, metadata, chunk_size)`, `merge_stream_channel_values(activity_id, rows, metadata, missing_channel_keys)`, and `stream_channel_coverage(...)`. `replace_stream_rows_and_channel_metadata` must run in one SQLite transaction: delete old stream rows for the activity, delete old channel metadata for the activity, insert replacement stream rows, upsert latest requested/returned channel metadata, and commit only after all steps pass. On any exception, roll back both stream rows and metadata. Update stream insert/replace binding to accept `lat`, `lng`, and nullable `values_json`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_repository_boundary.py tests/test_sqlite_safety.py -q</automated>
  </verify>
  <done>Migration and repository tests pass for lossless stream metadata without deleting existing rows.</done>
  <acceptance_criteria>
    - `MIGRATIONS` contains target version `3`.
    - Required inventory validation branches by `PRAGMA user_version`; v3 validates `latlng` plus `values_json`, and does not use the final v4 inventory.
    - `REQUIRED_INDEXES_BY_VERSION[3]` or equivalent includes `idx_stream_channels_activity`.
    - `run_migrations()` on a version 2 fixture leaves row counts for `activities`, `streams`, `athlete_zones`, `sync_log`, `kudos`, `refresh_state`, and `refresh_requests` unchanged.
    - `streams.values_json` is nullable TEXT or equivalent and survives insert/replace calls.
    - `stream_channels` can record `status='available'`, `status='unavailable'`, and `status='error'` without failing preflight.
    - `replace_stream_rows_and_channel_metadata` cannot commit deleted/replaced stream rows unless matching channel metadata changes commit in the same transaction.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add aggregate mirror coverage service and admin command</name>
  <files>src/mcp_strava/application/mirror_coverage.py, src/mcp_strava/cli.py, tests/test_full_fidelity_mirror.py, tests/test_cli_surface.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/cli.py
    - src/mcp_strava/application/freshness.py
    - tests/test_cli_surface.py
  </read_first>
  <action>Create `src/mcp_strava/application/mirror_coverage.py` with dataclass or dict-producing functions that read through `SQLiteRepository` and return aggregate coverage fields: `activities_total`, `activities_with_streams`, `stream_points`, `gps_points`, `channels`, `channel_status_counts`, `activities_missing_channel_metadata`, `activities_missing_extra_values`, and `backfill_needed`. Add `cmd_mirror_coverage` under `ADMIN_COMMANDS` as `mirror-coverage`; support `--json` and optional `--db <path>` for temp/copy DB inspection. Do not add a top-level product command. Do not import this service from `src/mcp_strava/interfaces/mcp_http.py`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_cli_surface.py tests/test_mcp_surface.py -q</automated>
  </verify>
  <done>Admin coverage output is aggregate, deterministic, and absent from MCP.</done>
  <acceptance_criteria>
    - `ADMIN_COMMANDS` contains `mirror-coverage`.
    - `COMMANDS` does not contain `mirror-coverage`.
    - `python -m mcp_strava admin mirror-coverage --db <fixture> --json` returns JSON with `status: ok`.
    - No coverage output includes `summary_json`, `detail_json`, raw stream payloads, OAuth tokens, or arbitrary SQL results.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strava API payload -> SQLite mirror | Returned stream channels must not be silently dropped. |
| SQLite migration -> existing mirror | Schema changes must not delete existing rows or replace the live DB. |
| Admin coverage -> operator output | Coverage output must remain aggregate and avoid raw personal payload exposure. |
| Admin CLI -> MCP | Operational coverage controls must not cross into agent-facing MCP tools. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | SQLite migration | mitigate | Migration tests assert row parity and required inventory before/after version 3. |
| T-06-02 | Information Disclosure | Coverage CLI | mitigate | Coverage output is aggregate and tests reject raw payload/secrets exposure. |
| T-06-03 | Information Integrity | Stream metadata | mitigate | Repository tests assert unknown channel values and metadata are retained. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_repository_boundary.py tests/test_cli_surface.py tests/test_smoke.py -q`
- `python3 -m pytest tests/test_mcp_surface.py tests/test_security_guards.py -q`
- `just test`
</verification>

<success_criteria>
- SQLite schema can retain stream channel metadata and extra per-point values.
- Existing stream analytics hot columns continue to work.
- Admin-only coverage command reports aggregate mirror coverage.
- No MCP tool or product command exposes coverage/backfill/admin/raw mirror internals.
</success_criteria>

<output>
Create `.planning/phases/06-full-fidelity-strava-mirror/06-01-SUMMARY.md` when done.
</output>
