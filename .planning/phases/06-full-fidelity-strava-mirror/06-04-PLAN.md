---
phase: 06-full-fidelity-strava-mirror
plan: 06-04
type: tdd
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/mcp_strava/refresh/checkpoints.py
  - src/mcp_strava/refresh/runtime.py
  - src/mcp_strava/refresh/_sync_ops.py
  - src/mcp_strava/adapters/sqlite/repository.py
  - src/mcp_strava/application/mirror_coverage.py
  - src/mcp_strava/cli.py
  - src/mcp_strava/interfaces/mcp_http.py
  - src/mcp_strava/deploy/preflight.py
  - tests/test_refresh_runtime.py
  - tests/test_cli_surface.py
  - tests/test_mcp_surface.py
  - tests/test_security_guards.py
  - tests/test_docker_runtime.py
  - tests/test_full_fidelity_mirror.py
autonomous: true
requirements:
  - MIRROR-01
  - STREAM-01
  - STREAM-02
  - COVERAGE-01
  - BACKFILL-01
  - TEST-05
must_haves:
  truths:
    - "D-18: historical backfill fetches only missing stream channels and metadata for activities that already have stream rows; it is not a full Strava resync."
    - "D-19: backfill is resumable, checkpointed, and rate-limit-aware; incomplete/delayed results preserve existing mirror data."
    - "D-20: backfill reports remaining activities, missing channels, metadata gaps, and likely API call count where possible."
    - "D-21: coverage and backfill controls are local admin CLI commands, not MCP."
    - "D-22: admin command shape may be `admin backfill-streams` and `admin mirror-coverage` if product/admin boundary remains clear."
    - "D-23: live verification is Docker-first because runtime data lives under `/opt/docker/mcp-strava`; repo tests use temp/copy databases."
    - "D-24: MCP exposes only read-only training metrics and never raw payloads, coverage reports, stream backfill status, admin backfill controls, SQL, or sync logs."
    - "D-25: SQLite remains the primary mirror."
    - "D-26: Phase 6 does not migrate the operational mirror to DuckDB."
    - "D-27: DuckDB is deferred for future analytics/read-model use."
    - "D-28: implementation stays on SQLite schema/repository/migration changes."
  artifacts:
    - path: "src/mcp_strava/refresh/runtime.py"
      provides: "Resumable stream-channel backfill runtime"
    - path: "src/mcp_strava/cli.py"
      provides: "Admin-only backfill and coverage controls"
    - path: "src/mcp_strava/interfaces/mcp_http.py"
      provides: "Unchanged read-only MCP allowlist plus forbidden-admin guards"
---

<objective>
Add resumable rate-limit-aware stream-channel backfill and Docker-safe verification while preserving the read-only MCP boundary.

Output: local admin CLI backfill/coverage workflow that fills only missing stream channel values/metadata, reports remaining work, handles rate limits through existing checkpoints/backoff, and proves MCP did not gain admin tools.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
@.planning/phases/06-full-fidelity-strava-mirror/06-RESEARCH.md
@.planning/phases/06-full-fidelity-strava-mirror/06-PATTERNS.md
@src/mcp_strava/refresh/runtime.py
@src/mcp_strava/refresh/checkpoints.py
@src/mcp_strava/refresh/_sync_ops.py
@src/mcp_strava/cli.py
@src/mcp_strava/interfaces/mcp_http.py
@tests/test_refresh_runtime.py
@tests/test_security_guards.py
</context>

<decision_traceability>
- D-18 through D-20 are implemented by resumable stream-channel backfill and estimate/reporting output.
- D-21 through D-24 are implemented by admin-only CLI controls and unchanged MCP read-only allowlist.
- D-25 through D-28 keep storage and implementation scope on SQLite.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing stream-channel backfill candidate and rate-limit tests</name>
  <files>tests/test_refresh_runtime.py, tests/test_full_fidelity_mirror.py, tests/test_cli_surface.py</files>
  <read_first>
    - src/mcp_strava/refresh/runtime.py
    - src/mcp_strava/refresh/checkpoints.py
    - src/mcp_strava/refresh/_sync_ops.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Add tests that create activities with existing stream rows but missing `stream_channels` metadata or missing requested channel values. Tests must fail until backfill candidate queries include those activities and exclude activities with no stream rows. Add repository merge tests proving stream-channel backfill uses update/merge-only writes: existing scalar columns, existing `values_json` keys, and existing stream rows survive even when the Strava streams endpoint returns all requested channels. Add fake Strava transport tests proving backfill fetches only `/activities/{id}/streams?...`, never `/athlete/activities`, details, or kudos. Add checkpoint routing tests for explicit stream-channel backfill stages: `run_once` rejects an active stream-channel backfill with a clear operator message, legacy `run_backfill` does not consume stream-channel checkpoints, and the new stream-channel backfill resumes from its own cursor. Add rate-limit tests where `StravaUnavailable("rate_limited")` preserves checkpoint stage and existing stream rows, records delayed/failed status, and allows resume after backoff. Add dry-run tests that report `status`, `mode`, `activities_considered`, `activities_to_backfill`, `missing_channels`, `metadata_missing`, `estimated_api_calls`, and `checkpoint_stage` without calling transport.</action>
  <verify>
    <automated>python3 -m pytest tests/test_refresh_runtime.py tests/test_full_fidelity_mirror.py tests/test_cli_surface.py -q</automated>
  </verify>
  <done>Tests fail before stream-channel backfill and dry-run reporting exist.</done>
  <acceptance_criteria>
    - Backfill candidates require existing stream rows.
    - Backfill writes call merge/update-only repository methods and never call `replace_stream_rows_chunked` or `replace_stream_rows_and_channel_metadata`.
    - Backfill does not call `/athlete/activities`, `/activities/{id}` detail, or kudos endpoints.
    - `run_once`, legacy `run_backfill`, and stream-channel backfill route active checkpoint stages explicitly and reject incompatible stages.
    - Rate-limit failure leaves existing rows untouched and checkpointed.
    - Dry run performs no transport calls and returns the exact aggregate JSON fields named in the task action.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resumable stream-channel backfill runtime</name>
  <files>src/mcp_strava/refresh/checkpoints.py, src/mcp_strava/refresh/runtime.py, src/mcp_strava/refresh/_sync_ops.py, src/mcp_strava/adapters/sqlite/repository.py, src/mcp_strava/application/mirror_coverage.py</files>
  <read_first>
    - src/mcp_strava/refresh/checkpoints.py
    - src/mcp_strava/refresh/runtime.py
    - src/mcp_strava/refresh/_sync_ops.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/application/mirror_coverage.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Add explicit stream-channel backfill stages named `STREAM_CHANNELS_BACKFILL` and `COMPLETE_STREAM_CHANNELS_BACKFILL` to `Stage` or equivalent. Extend `is_backfill_stage`, `is_active_backfill_stage`, and add `is_stream_channel_backfill_stage` so daily refresh, legacy full-stream/detail backfill, and stream-channel backfill route checkpoint states deterministically. Update `run_once` to reject active `STREAM_CHANNELS_BACKFILL` with an error naming `admin backfill-streams`; update legacy `run_backfill` to reject stream-channel checkpoints instead of consuming them. Implement repository candidate methods that find activities with stream rows and missing channel metadata/values. Implement a dry-run estimator returning `status`, `mode`, `activities_considered`, `activities_to_backfill`, `missing_channels` keyed by channel name, `metadata_missing`, `estimated_api_calls` where one candidate activity equals one streams endpoint call, and `checkpoint_stage`. Implement runtime backfill that uses existing lease/backoff/checkpoint discipline, fetches the streams endpoint with the configured key set, extracts only channels missing from each candidate, and writes through `merge_stream_channel_values` or equivalent. The merge method must update only existing `(activity_id, time_offset)` rows, JSON-merge new missing keys into `values_json` without replacing existing keys, preserve all scalar columns, upsert metadata for newly available/unavailable/error channels, and never downgrade an existing `status='available'` metadata row. Report incomplete/delayed status without deleting rows. Keep existing `run_backfill` semantics for older admin backfill if still needed, but ensure `admin backfill-streams` uses only the stream-channel-specific path.</action>
  <verify>
    <automated>python3 -m pytest tests/test_refresh_runtime.py tests/test_full_fidelity_mirror.py -q</automated>
  </verify>
  <done>Backfill is resumable, rate-limit-aware, and scoped to missing stream channels/metadata.</done>
  <acceptance_criteria>
    - Backfill checkpoint cursor stores the activity id currently being processed.
    - Existing rows are preserved if transport raises `StravaUnavailable`.
    - Backfill result includes `status`, `mode`, `checkpoint_stage`, `activities_considered`, `activities_to_backfill`, `missing_channels`, `metadata_missing`, and `estimated_api_calls`.
    - Stream-channel backfill performs update/merge-only writes and never deletes or replaces stream rows.
    - Existing `values_json` keys and `stream_channels.status='available'` rows are preserved unless the same channel is being newly filled from missing/unavailable/error to available.
    - `is_active_backfill_stage` and `is_backfill_stage` explicitly include or reject the new stages according to their runtime routing role.
    - No full summary resync is triggered by stream-channel backfill.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add admin backfill command and preserve MCP allowlist</name>
  <files>src/mcp_strava/cli.py, src/mcp_strava/interfaces/mcp_http.py, tests/test_cli_surface.py, tests/test_mcp_surface.py, tests/test_security_guards.py</files>
  <read_first>
    - src/mcp_strava/cli.py
    - src/mcp_strava/interfaces/mcp_http.py
    - tests/test_cli_surface.py
    - tests/test_mcp_surface.py
    - tests/test_security_guards.py
  </read_first>
  <action>Add `admin backfill-streams` with `--dry-run`, `--since YYYY-MM-DD`, `--limit N` if practical, `--json`, and optional `--db <path>` for temp/copy DB tests. The command must call the stream-channel backfill path, not full sync. Keep legacy `admin backfill` if it is still used, but update help text so `admin backfill` is described as legacy whole-stream/detail backfill and `admin backfill-streams` is described as Phase 6 missing-channel/metadata backfill. Extend MCP tests and security guards to assert forbidden names include `mirror-coverage`, `backfill-streams`, `stream_backfill`, `coverage`, `status`, `sync_log`, `raw`, `sql`, and `admin`. Do not add or rename the existing five MCP tools: `get_fitness_state`, `list_workouts`, `get_workout_detail`, `compare_periods`, and `project_fitness_state`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_cli_surface.py tests/test_mcp_surface.py tests/test_security_guards.py -q</automated>
  </verify>
  <done>Backfill is admin-only and MCP remains read-only metrics only.</done>
  <acceptance_criteria>
    - `ADMIN_COMMANDS` contains `backfill-streams`.
    - Admin help distinguishes legacy `backfill` from Phase 6 `backfill-streams`.
    - `COMMANDS` does not contain `backfill-streams` or `mirror-coverage`.
    - `MCP_TOOL_NAMES` remains exactly the five read-only metric tools.
    - Tests fail if MCP exposes sync/admin/debug/raw/SQL/backfill/status/coverage tools.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add Docker/runtime-safe verification hooks</name>
  <files>src/mcp_strava/deploy/preflight.py, src/mcp_strava/deploy/prepare_runtime.py, tests/test_docker_runtime.py, tests/test_full_fidelity_mirror.py</files>
  <read_first>
    - src/mcp_strava/deploy/preflight.py
    - src/mcp_strava/deploy/prepare_runtime.py
    - tests/test_docker_runtime.py
    - .planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
  </read_first>
  <action>Update runtime preflight so version-aware Phase 6 mirror inventory is required when the runtime DB has been migrated: v3 is accepted only as the intermediate stream-channel schema before GPS canonicalization, and final Phase 6 runtime validation expects v4 without `latlng`. Add source/tests for operator-safe commands that inspect `/opt/docker/mcp-strava/data/strava.db` only when explicitly invoked. Tests should verify defaults do not mutate `/opt/docker/mcp-strava` and that planned smoke instructions use admin CLI coverage/backfill dry-run against runtime paths rather than repo `data/strava.db`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_docker_runtime.py tests/test_full_fidelity_mirror.py -q</automated>
  </verify>
  <done>Runtime verification is Docker-first and does not mutate live data during tests.</done>
  <acceptance_criteria>
    - Tests assert `/opt/docker/mcp-strava` is referenced only in deploy/runtime-safe code or docs.
    - Repo-local tests use temp or copied databases.
    - Runtime preflight validates versioned v3/v4 Phase 6 required inventory and treats v4 as the final post-GPS schema.
    - `just test` does not require live Docker or live Strava API.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill CLI -> Strava API | Backfill must not become a full account resync. |
| Rate-limited Strava -> local mirror | Rate-limit failures must preserve existing rows and checkpoint. |
| Admin CLI -> MCP | Operational controls must stay below MCP. |
| Repo tests -> live runtime | Tests must not mutate `/opt/docker/mcp-strava` or live mirror data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service | Backfill runtime | mitigate | Checkpoint/backoff tests cover rate limits and resumability. |
| T-06-06 | Information Disclosure | MCP surface | mitigate | MCP allowlist/forbidden tests block admin, raw, SQL, coverage, status, and backfill tools. |
| T-06-07 | Tampering | Live runtime DB | mitigate | Tests use temp/copy DBs and live commands are explicit operator-only smoke steps. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_refresh_runtime.py tests/test_cli_surface.py tests/test_mcp_surface.py tests/test_security_guards.py tests/test_docker_runtime.py tests/test_full_fidelity_mirror.py -q`
- `just test`
- Manual when explicitly approved: `python3 -m mcp_strava admin mirror-coverage --db /opt/docker/mcp-strava/data/strava.db --json`
- Manual dry-run when explicitly approved: `python3 -m mcp_strava admin backfill-streams --db /opt/docker/mcp-strava/data/strava.db --dry-run --json`
</verification>

<success_criteria>
- Stream-channel backfill is resumable, rate-limit-aware, and scoped to existing activities with missing channel data/metadata.
- Operator can inspect remaining stream/GPS/channel coverage from local admin CLI.
- Backfill dry-run reports useful remaining work and likely API call count.
- MCP still exposes only read-only training metric tools and no admin/status/debug/raw/SQL/backfill/coverage surface.
- Runtime/live data handling stays under `/opt/docker/mcp-strava`.
</success_criteria>

<output>
Create `.planning/phases/06-full-fidelity-strava-mirror/06-04-SUMMARY.md` when done.
</output>
