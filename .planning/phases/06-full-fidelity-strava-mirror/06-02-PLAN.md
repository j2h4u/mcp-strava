---
phase: 06-full-fidelity-strava-mirror
plan: 06-02
type: tdd
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/refresh/_sync_ops.py
  - src/mcp_strava/adapters/sqlite/repository.py
  - tests/test_refresh_runtime.py
  - tests/test_repository_boundary.py
  - tests/test_full_fidelity_mirror.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - MIRROR-01
  - MIRROR-02
  - STREAM-01
  - STREAM-02
  - STREAM-03
  - TEST-05
must_haves:
  truths:
    - "D-01: stream ingest preserves normalized information content rather than making raw JSON the contract."
    - "D-02: MIRROR-01 and MIRROR-02 are satisfied by queryable normalized structures."
    - "D-03: `summary_json` and `detail_json` may remain while streams use structured storage."
    - "D-04: raw stream payload is not required for MCP or the primary mirror contract."
    - "D-05: stream point rows use wide scalar columns plus extra JSON."
    - "D-06: current analytics scalar columns stay populated from returned stream channels."
    - "D-07: unknown/future stream channels are retained in point-level `values_json`."
    - "D-08: every requested or returned channel gets metadata/status rows."
    - "D-09: no pure long/EAV-only model for analytics hot path."
    - "D-10: fixed known columns alone are rejected; parser must preserve extra keys."
    - "D-14: new writes should prepare for canonical `lat`/`lng` GPS reads."
    - "D-17: existing normalized stream rows are not deleted just to add the new model."
    - "D-25: ingest writes to SQLite as primary mirror."
  artifacts:
    - path: "src/mcp_strava/refresh/_sync_ops.py"
      provides: "Generalized stream payload builder and all-channel stream fetch handling"
    - path: "src/mcp_strava/types.py"
      provides: "Stream response structures that preserve extra channel keys"
    - path: "tests/test_refresh_runtime.py"
      provides: "Fake-transport tests for unknown keys, metadata, and projection parity"
---

<objective>
Replace fixed stream parsing with generalized all-channel ingestion while preserving the current analytics projection columns.

Output: refresh runtime stores every returned stream channel, records channel metadata/status, keeps known scalar projections, and retains unknown per-point values in `values_json`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
@.planning/phases/06-full-fidelity-strava-mirror/06-RESEARCH.md
@.planning/phases/06-full-fidelity-strava-mirror/06-PATTERNS.md
@src/mcp_strava/refresh/_sync_ops.py
@src/mcp_strava/types.py
@src/mcp_strava/adapters/sqlite/repository.py
@tests/test_refresh_runtime.py
</context>

<decision_traceability>
- D-01 through D-10 are translated into generalized stream parsing and repository writes.
- D-14 and D-17 are handled by writing `lat`/`lng` when available and preserving rows through repository boundaries.
- D-25 keeps the mirror in SQLite.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing all-channel ingestion tests</name>
  <files>tests/test_refresh_runtime.py, tests/test_repository_boundary.py, tests/test_full_fidelity_mirror.py</files>
  <read_first>
    - src/mcp_strava/refresh/_sync_ops.py
    - src/mcp_strava/types.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Extend fake Strava stream responses with `time`, `heartrate`, `velocity_smooth`, `altitude`, `cadence`, `latlng`, `grade_smooth`, `grade_adjusted_speed`, `grade_adjusted_distance`, `moving`, `watts`, `temp`, and `unknown_future_key`. Add tests that fail until `sync_streams` requests the complete configured key set, stores known scalar projections, writes `lat` and `lng` from `latlng`, writes `values_json` containing `watts`, `temp`, and `unknown_future_key`, and upserts `stream_channels` rows with `original_size`, `resolution`, `series_type`, `status`, and null `error` for available channels. Add a missing-channel test that records `status='unavailable'` or equivalent metadata for requested channels absent from the response without failing the whole activity.</action>
  <verify>
    <automated>python3 -m pytest tests/test_refresh_runtime.py tests/test_repository_boundary.py tests/test_full_fidelity_mirror.py -q</automated>
  </verify>
  <done>Tests fail until generalized all-channel ingestion is implemented.</done>
  <acceptance_criteria>
    - Fake transport sees a stream request containing `watts` and `temp` in addition to existing keys.
    - Repository rows contain scalar `heartrate`, `velocity`, `altitude`, `cadence`, `grade`, `gap_speed`, `gap_distance`, `is_moving`, `lat`, and `lng`.
    - `values_json` contains keys not mapped to scalar columns.
    - Missing requested channels do not raise and produce metadata/status evidence.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize stream response parsing and projection building</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/refresh/_sync_ops.py</files>
  <read_first>
    - src/mcp_strava/types.py
    - src/mcp_strava/refresh/_sync_ops.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Replace the fixed-only `StravaStreams` path for refresh ingestion with a helper that operates over the raw keyed stream response. Define a complete requested key tuple or comma string including existing keys plus `distance`, `watts`, and `temp`; keep configured extension support if existing settings provide it, otherwise make the tuple locally explicit. Use `time` as canonical point index. For each point, populate scalar projection names: `time_offset`, `heartrate`, `velocity`, `altitude`, `cadence`, `lat`, `lng`, `grade`, `gap_speed`, `gap_distance`, and `is_moving`. Put returned non-scalar or non-hot-path values into `values_json` keyed by original Strava channel name. Build channel metadata for returned channels and requested-but-absent channels. Do not require all channels to have equal length; omit out-of-range point values and reflect channel size in metadata.</action>
  <verify>
    <automated>python3 -m pytest tests/test_refresh_runtime.py tests/test_repository_boundary.py -q</automated>
  </verify>
  <done>Stream payload builder preserves returned channels and scalar analytics projections.</done>
  <acceptance_criteria>
    - `STREAM_KEYS` or replacement request builder contains `time`, `distance`, `latlng`, `altitude`, `velocity_smooth`, `heartrate`, `cadence`, `watts`, `temp`, `moving`, `grade_smooth`, `grade_adjusted_speed`, and `grade_adjusted_distance`.
    - Unknown returned keys are not dropped before repository write.
    - `latlng` values are split into numeric `lat` and `lng` fields for new writes.
    - Existing analytics functions using `heartrate` and `velocity` continue to pass tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write all-channel rows and metadata through repository methods</name>
  <files>src/mcp_strava/adapters/sqlite/repository.py, src/mcp_strava/refresh/_sync_ops.py, tests/test_refresh_runtime.py, tests/test_repository_boundary.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/refresh/_sync_ops.py
    - tests/test_repository_boundary.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Update `_insert_streams` and `_replace_streams` so they pass stream point rows and channel metadata to repository methods introduced in Plan 06-01. Ensure insert/backfill paths do not delete existing rows when only metadata is being enriched. Ensure replace remains appropriate for normal activity stream refresh but preserves metadata consistency in the same transaction or adjacent committed unit. Add tests that `activities_missing_streams()` still finds activities with no stream rows and that activities with stream rows but missing metadata are handled by Plan 06-04 backfill candidates, not by a full stream replacement here.</action>
  <verify>
    <automated>python3 -m pytest tests/test_refresh_runtime.py tests/test_repository_boundary.py tests/test_full_fidelity_mirror.py -q</automated>
  </verify>
  <done>All-channel stream writes are repository-backed and analytics projection tests pass.</done>
  <acceptance_criteria>
    - `_insert_streams` returns inserted point count and writes channel metadata.
    - `_replace_streams` rewrites point rows for the activity and leaves metadata aligned with the latest response.
    - Existing `run_once` checkpoint/resume tests still pass.
    - No live Strava API calls are used in tests.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strava response -> parser | Unknown keys and uneven channels must not corrupt point projection. |
| Parser -> repository | Extra values and metadata must survive typed conversion. |
| Refresh failure -> mirror | Partial/rate-limited responses must not erase existing mirror data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Integrity | Stream parser | mitigate | Fake transport tests assert unknown keys land in `values_json`. |
| T-06-04 | Tampering | Stream replacement | mitigate | Repository tests assert replacement does not lose metadata or existing rows outside the target activity. |
| T-06-05 | Denial of Service | Uneven stream channel lengths | mitigate | Parser tolerates absent/out-of-range channel values and records status. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_refresh_runtime.py tests/test_repository_boundary.py tests/test_full_fidelity_mirror.py -q`
- `python3 -m pytest tests/test_application_reports.py tests/test_application_workouts.py tests/test_load_status.py -q`
- `just test`
</verification>

<success_criteria>
- Stream ingestion preserves every returned channel in SQLite.
- Existing analytics hot columns remain populated and tests pass.
- Missing/unsupported channels become coverage metadata, not activity-level failures.
- No MCP, raw, SQL, or admin surface is added by this plan.
</success_criteria>

<output>
Create `.planning/phases/06-full-fidelity-strava-mirror/06-02-SUMMARY.md` when done.
</output>

