---
phase: 06-full-fidelity-strava-mirror
plan: 06-03
type: tdd
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/mcp_strava/adapters/sqlite/schema.py
  - src/mcp_strava/adapters/sqlite/migrations.py
  - src/mcp_strava/adapters/sqlite/repository.py
  - src/mcp_strava/refresh/_sync_ops.py
  - src/mcp_strava/metrics.py
  - tests/test_full_fidelity_mirror.py
  - tests/test_sqlite_safety.py
  - tests/test_repository_boundary.py
  - tests/test_load_status.py
autonomous: true
requirements:
  - GPS-01
  - GPS-02
  - STREAM-03
  - TEST-05
must_haves:
  truths:
    - "D-11: canonical GPS storage is `lat` and `lng` REAL columns on stream points."
    - "D-12: `latlng` JSON is removed in Phase 6 after migration and is not permanent compatibility."
    - "D-13: migration fills `lat` and `lng` locally from existing `latlng` JSON with no Strava API calls."
    - "D-14: post-migration writes and reads use only `lat` and `lng` for canonical GPS data."
    - "D-15: deleting `latlng` immediately is acceptable; no backward-compatibility obligation remains."
    - "D-16: migration creates backup and verifies integrity, stream row counts, GPS counts, channel coverage, and analytics parity."
    - "D-17: existing normalized stream rows are preserved and enriched; they are not deleted/replaced solely to add GPS structure."
    - "D-23: live verification is Docker-first under `/opt/docker/mcp-strava`; repo tests use temp/copy DBs."
    - "D-25: SQLite remains the primary mirror during the GPS migration."
  artifacts:
    - path: "src/mcp_strava/adapters/sqlite/migrations.py"
      provides: "Canonical GPS migration with backup/preflight/post-check/parity"
    - path: "src/mcp_strava/adapters/sqlite/schema.py"
      provides: "Required inventory after `latlng` removal"
    - path: "tests/test_full_fidelity_mirror.py"
      provides: "GPS migration parity and no-live-call tests"
---

<objective>
Canonicalize GPS storage by filling `lat`/`lng` from local `latlng` JSON, removing `latlng`, and proving migration parity.

Output: SQLite migration and repository/query updates that make `lat` and `lng` the only canonical GPS fields without forcing Strava API calls or losing analytics parity.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-full-fidelity-strava-mirror/06-CONTEXT.md
@.planning/phases/06-full-fidelity-strava-mirror/06-RESEARCH.md
@.planning/phases/06-full-fidelity-strava-mirror/06-PATTERNS.md
@src/mcp_strava/adapters/sqlite/schema.py
@src/mcp_strava/adapters/sqlite/migrations.py
@src/mcp_strava/adapters/sqlite/repository.py
@src/mcp_strava/refresh/_sync_ops.py
@tests/test_sqlite_safety.py
</context>

<decision_traceability>
- D-11 through D-17 are implemented by the canonical GPS migration and post-migration repository/query updates.
- D-23 and D-25 constrain verification and storage engine choice.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing canonical GPS migration tests</name>
  <files>tests/test_full_fidelity_mirror.py, tests/test_sqlite_safety.py, tests/test_repository_boundary.py, tests/test_load_status.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/migrations.py
    - src/mcp_strava/adapters/sqlite/schema.py
    - tests/test_sqlite_safety.py
    - tests/test_load_status.py
  </read_first>
  <action>Add temp-DB tests with mixed rows: rows with only `latlng`, rows with `lat`/`lng`, rows with both, and rows with no GPS. Tests must fail until migration fills missing `lat`/`lng` from valid `latlng` JSON, leaves non-GPS rows unchanged, preserves total stream row count, preserves GPS point count, removes `latlng` from final schema inventory, and keeps analytics parity snapshots within tolerance. Add a source or fake-transport assertion that no Strava transport/API call is used by GPS migration.</action>
  <verify>
    <automated>python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_sqlite_safety.py tests/test_repository_boundary.py tests/test_load_status.py -q</automated>
  </verify>
  <done>Tests fail before the canonical GPS migration exists.</done>
  <acceptance_criteria>
    - Tests assert `PRAGMA table_info(streams)` does not include `latlng` after migration.
    - Tests assert valid `latlng` JSON `[43.2, 76.9]` becomes `lat=43.2` and `lng=76.9`.
    - Tests assert malformed `latlng` does not abort the migration and is counted in a post-check warning or failure list.
    - Tests assert no fixture touches `data/strava.db` or `/opt/docker/mcp-strava`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backup-protected GPS canonicalization migration</name>
  <files>src/mcp_strava/adapters/sqlite/schema.py, src/mcp_strava/adapters/sqlite/migrations.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/schema.py
    - src/mcp_strava/adapters/sqlite/migrations.py
    - tests/test_full_fidelity_mirror.py
    - tests/test_sqlite_safety.py
  </read_first>
  <action>Add the migration step that transforms `streams` into the final Phase 6 shape without `latlng`. Before rebuilding/dropping columns, compute a GPS preflight snapshot with `stream_rows`, `gps_points_from_lat_lng`, `gps_points_from_latlng`, channel coverage counts, and analytics parity snapshot. During migration, fill `lat` and `lng` from valid local `latlng` JSON only when scalar coordinates are missing. Rebuild the table if SQLite column drop support is not sufficient in this codebase. After migration, run SQLite integrity check, required inventory validation, row-count parity, GPS coverage parity, channel coverage parity, and analytics parity. Keep the existing backup creation path in `run_migrations()` and make failure messages name the failed parity dimension.</action>
  <verify>
    <automated>python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_sqlite_safety.py -q</automated>
  </verify>
  <done>Migration removes `latlng` only after local fill and parity checks pass.</done>
  <acceptance_criteria>
    - Final `REQUIRED_COLUMNS["streams"]` excludes `latlng` and includes `lat`, `lng`, and `values_json`.
    - `run_migrations()` still creates a backup before schema mutation.
    - Stream row count before and after migration is identical.
    - GPS point count after migration is at least the count of rows that had valid local GPS before migration.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove post-migration `latlng` reads and writes</name>
  <files>src/mcp_strava/adapters/sqlite/repository.py, src/mcp_strava/refresh/_sync_ops.py, src/mcp_strava/metrics.py, tests/test_repository_boundary.py, tests/test_refresh_runtime.py</files>
  <read_first>
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/refresh/_sync_ops.py
    - src/mcp_strava/metrics.py
    - tests/test_repository_boundary.py
    - tests/test_refresh_runtime.py
  </read_first>
  <action>Update stream selects, inserts, replacements, and metric helpers to use `lat` and `lng` only. Remove `latlng` from bind lists, test fixtures that represent post-migration schema, and current-code assumptions. Keep tests that construct old-schema fixtures only in migration-specific setup. Add source guard tests that fail if non-migration runtime files reference `latlng` as a permanent stream column after migration.</action>
  <verify>
    <automated>python3 -m pytest tests/test_repository_boundary.py tests/test_refresh_runtime.py tests/test_full_fidelity_mirror.py tests/test_load_status.py -q</automated>
  </verify>
  <done>Runtime reads and writes use canonical `lat`/`lng` only.</done>
  <acceptance_criteria>
    - `src/mcp_strava/refresh/_sync_ops.py` writes `lat` and `lng`, not `latlng`.
    - `SQLiteRepository.activity_stream_rows()` returns rows based on `lat` and `lng`.
    - Non-migration runtime code does not require a `streams.latlng` column.
    - Existing analytics and load-status tests pass with post-migration fixtures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Old schema -> new schema | Mixed GPS data must be transformed without losing stream rows. |
| Local migration -> Strava API | GPS migration must use local data only and avoid API calls. |
| Migration -> analytics | Training metrics must remain equivalent after schema rewrite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | GPS migration | mitigate | Backup, integrity, row-count, GPS-count, and analytics parity checks block unsafe completion. |
| T-06-05 | Repudiation | Migration output | mitigate | Failure messages name the parity dimension and backup path is reported by existing migration command. |
| T-06-06 | Denial of Service | Malformed local `latlng` | mitigate | Tests cover malformed JSON handling without aborting unaffected rows. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_full_fidelity_mirror.py tests/test_sqlite_safety.py tests/test_repository_boundary.py tests/test_refresh_runtime.py tests/test_load_status.py -q`
- `python3 -m pytest tests/test_application_reports.py tests/test_application_workouts.py -q`
- `just test`
</verification>

<success_criteria>
- `streams.latlng` is removed from final schema and runtime code.
- Valid local `latlng` values are migrated into `lat`/`lng`.
- Stream row count, GPS coverage, channel coverage, and analytics parity are verified.
- No live Strava resync or Strava API call is required for GPS migration.
</success_criteria>

<output>
Create `.planning/phases/06-full-fidelity-strava-mirror/06-03-SUMMARY.md` when done.
</output>

