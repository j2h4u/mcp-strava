---
phase: 10-materialize-unwired-training-metrics-and-enforce-core-domain
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - tests/test_security_guards.py
autonomous: true
requirements:
  - "Core/domain separation (PROJECT.md Active)"
user_setup: []

must_haves:
  truths:
    - "A boundary test forbids domain modules (training, metrics, cardiac_drift, hr_zones, sports) from importing mcp_strava.db or mcp_strava.adapters.duckdb (in addition to the existing strava/refresh prohibition)"
    - "The boundary test passes only because plan 10-01 already removed the mcp_strava.db import from metrics.py"
  artifacts:
    - path: "tests/test_security_guards.py"
      provides: "Storage/adapter import-boundary guard for domain modules"
      contains: "mcp_strava.adapters.duckdb"
  key_links:
    - from: "tests/test_security_guards.py"
      to: "_import_violations"
      via: "reuse of existing AST helper"
      pattern: "_import_violations"
---

<objective>
Add the missing import-boundary guard (CONTEXT.md scope item 4): extend the existing
test_read_modules_do_not_import_strava_or_refresh family so domain modules are ALSO forbidden from
importing mcp_strava.db and mcp_strava.adapters.duckdb, and widen the guarded module set to include
hr_zones and sports. This is the blind spot that let the storage leak in metrics.py pass green.

Purpose: turn the architecture invariant into an enforced, regression-proof CI check.
Output: an updated/expanded boundary test in tests/test_security_guards.py reusing the existing
_import_violations AST helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/CONTEXT.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md
@tests/test_security_guards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the domain import-boundary guard to forbid storage/adapter imports</name>
  <files>tests/test_security_guards.py</files>
  <read_first>
    - tests/test_security_guards.py (the existing test_read_modules_do_not_import_strava_or_refresh at ~line 374 and the _import_violations helper at ~line 208 — note it already handles `import X`, `from X import`, and `from mcp_strava import X` forms)
    - .planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md (Pitfall 1 and 2: forbid BOTH prefixes AND widen the module list; do NOT forbid sibling domain imports or a broad `mcp_strava` prefix)
  </read_first>
  <action>
    Update the existing test_read_modules_do_not_import_strava_or_refresh so its read_modules list is
    src/mcp_strava/training.py, src/mcp_strava/metrics.py, src/mcp_strava/cardiac_drift.py,
    src/mcp_strava/hr_zones.py, src/mcp_strava/sports.py, and its disallowed-prefix tuple passed to
    _import_violations is ("mcp_strava.adapters.strava", "mcp_strava.refresh", "mcp_strava.db",
    "mcp_strava.adapters.duckdb"). Rename the test to test_read_modules_do_not_import_storage_strava_or_refresh
    (keep a one-line docstring stating the intent). Do NOT forbid the bare `mcp_strava` prefix or any sibling
    domain module (e.g. metrics.py importing mcp_strava.cardiac_drift must remain allowed). assert violations == [].
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_security_guards.py -k storage_strava_or_refresh -x -q</automated>
  </verify>
  <done>The extended boundary test passes (because 10-01 removed the metrics.py db import) and forbids mcp_strava.db / mcp_strava.adapters.duckdb across the five domain modules. Sibling domain imports remain allowed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain module ↔ storage/adapter tier | The test itself is the enforcement seam: CI fails if a domain module imports storage or an adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | architectural erosion (domain re-couples to storage) | mitigate | AST import scan over the five domain modules forbids mcp_strava.db and mcp_strava.adapters.duckdb prefixes; reuses the proven _import_violations helper. |
| T-10-SC | Tampering | package installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `PYTHONPATH=src python -m pytest tests/test_security_guards.py -k storage_strava_or_refresh -x -q` passes.
- The disallowed prefixes include both `mcp_strava.db` and `mcp_strava.adapters.duckdb`.
- The read_modules list includes training, metrics, cardiac_drift, hr_zones, sports.
</verification>

<success_criteria>
- Domain modules are guard-enforced against storage/adapter imports.
- Sibling domain imports remain permitted (no false positive).
</success_criteria>

<output>
Create `.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-02-SUMMARY.md` when done.
</output>
