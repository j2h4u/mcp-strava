---
phase: 10-materialize-unwired-training-metrics-and-enforce-core-domain
plan: 03
type: tdd
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/mcp_strava/adapters/duckdb/read_model_materializer.py
  - tests/test_read_model_materialization.py
autonomous: true
requirements:
  - "fix unmaterialized registered metrics (260525-jpo preserve-and-fix)"
  - "Core/domain separation (PROJECT.md Active)"
user_setup: []

must_haves:
  truths:
    - "_activity_fact fetches stream rows via the repo and calls the pure metrics functions to populate hr_recovery_*, vertical_speed_*, cardiac_drift_*, and hrr_pct instead of writing hardcoded defaults"
    - "hrr_pct reuses the hr_max_observed already computed in _activity_fact (max-to-date), not metrics.py max_heartrate()"
    - "Rolling medians (rolling_median_hr_recovery, rolling_median_cardiac_drift_pct) populate automatically because _materialize_rolling_facts already SELECTs hr_recovery_median_rate and cardiac_drift_pct"
    - "TRIMP, HR zones, and cardiac_cost values are unchanged (no regression)"
  artifacts:
    - path: "src/mcp_strava/adapters/duckdb/read_model_materializer.py"
      provides: "Materializer that computes the previously-empty metric columns"
      contains: "calc_hr_recovery"
    - path: "tests/test_read_model_materialization.py"
      provides: "Assertion that the 13 columns are non-default after materialization"
      contains: "hr_recovery_pause_count"
  key_links:
    - from: "src/mcp_strava/adapters/duckdb/read_model_materializer.py"
      to: "mcp_strava.metrics"
      via: "import of pure calc functions"
      pattern: "from mcp_strava.metrics import"
    - from: "_activity_fact"
      to: "repo.stream_hr_velocity_time_rows / stream_altitude_rows / stream_hr_velocity_simple_rows / activity_median_heartrate"
      via: "row fetch feeding the pure functions"
      pattern: "stream_hr_velocity_time_rows"
---

<objective>
Wire the pure metric functions from plan 10-01 into read_model_materializer._activity_fact (CONTEXT.md
scope item 3): fetch stream rows via the repository, call calc_hr_recovery / calc_vertical_speed /
calc_cardiac_drift / calc_hrr_pct, and populate the ~13 columns that currently hold hardcoded defaults.
hrr_pct reuses the hr_max_observed already computed in _activity_fact (line 141). This is the bug fix —
the registered, exposed metrics stop returning null/0.

Purpose: closes the latent product bug; the rolling medians populate for free.
Output: updated _activity_fact; an extended materialization test proving the columns are non-default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/CONTEXT.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md
@src/mcp_strava/adapters/duckdb/read_model_materializer.py
@src/mcp_strava/metrics.py
@tests/test_read_model_materialization.py
</context>

<feature>
  <name>Materialize the previously-empty metric columns</name>
  <files>src/mcp_strava/adapters/duckdb/read_model_materializer.py, tests/test_read_model_materialization.py</files>
  <behavior>
    After materialize_read_model runs on an activity with HR/velocity/altitude streams:
      - hr_recovery_* columns reflect calc_hr_recovery output (pause_count >= 0; when a pause exists,
        pause_count >= 1 and median_rate not None) instead of fixed 0/None.
      - vertical_speed_* columns reflect calc_vertical_speed output (vmh > 0 for ascending streams).
      - cardiac_drift_* columns reflect calc_cardiac_drift output (pct/severity/quality set; significant
        stored as 1/0).
      - hrr_pct reflects calc_hrr_pct(median_hr, hr_rest, hr_max_observed); not None when median HR and
        hr_max_observed exist.
      - When a pure function returns None (insufficient data), the corresponding column keeps its default
        (None or 0) — no crash.
      - trimp, zone1_seconds..zone5_seconds, cardiac_cost remain byte-identical to before (regression guard).
  </behavior>
  <implementation>
    RED: extend tests/test_read_model_materialization.py with a test asserting the populated columns on the
    existing seeded activity. GREEN: edit _activity_fact to fetch + compute + populate. Verify the existing
    materializer tests still pass (no regression).
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — assert the 13 columns are populated after materialization</name>
  <files>tests/test_read_model_materialization.py</files>
  <read_first>
    - tests/test_read_model_materialization.py (the _seed_dirty_activity_with_streams helper seeds 180 rows incl. altitude; the existing test_duckdb_materializer_writes_fact_tiers_and_clears_dirty_rows shows how to fetch a fact via repo.fetch_activity_metric_fact)
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (_activity_fact return dict — the 13 default columns at lines 190-205)
  </read_first>
  <action>
    Add test_duckdb_materializer_populates_metric_columns_not_defaults that seeds the dirty activity (reuse
    _seed_dirty_activity_with_streams), runs materialize_read_model, fetches the activity_metric_fact for id
    920, and asserts the metric columns are populated rather than fixed defaults: at minimum
    vertical_speed_vmh is not None and > 0 (the seeded altitude ascends), cardiac_drift_quality is not None,
    and hrr_pct is not None. Also assert trimp > 0 still holds (regression). Use plain dict-key access like
    the existing test.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_read_model_materialization.py -k populates_metric_columns -x -q</automated>
  </verify>
  <done>The new test exists and FAILS (columns still at defaults: vertical_speed_vmh is None / hrr_pct is None) — RED confirmed before wiring.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — fetch rows and call the pure functions in _activity_fact</name>
  <files>src/mcp_strava/adapters/duckdb/read_model_materializer.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/read_model_materializer.py (_activity_fact body — note hr_max_observed at line 141 and the default columns at lines 190-205)
    - src/mcp_strava/metrics.py (post-10-01 pure signatures: calc_hr_recovery(rows), calc_vertical_speed(rows), calc_cardiac_drift(rows, sport_type), calc_hrr_pct(median_hr, hr_rest, hr_max))
    - .planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md (the column→pure-fn-field mapping table and the verified repo method signatures)
  </read_first>
  <action>
    Add `from mcp_strava.metrics import calc_hr_recovery, calc_vertical_speed, calc_cardiac_drift, calc_hrr_pct`
    to read_model_materializer.py. In _activity_fact, before building the return dict: fetch
    hr_rows = repo.stream_hr_velocity_time_rows(activity_id); alt_rows = repo.stream_altitude_rows(activity_id);
    drift_rows = repo.stream_hr_velocity_simple_rows(activity_id, Config.Thresholds.VEL_MOVING);
    median_hr = repo.activity_median_heartrate(activity_id). Compute hr = calc_hr_recovery(hr_rows),
    vspeed = calc_vertical_speed(alt_rows), drift = calc_cardiac_drift(drift_rows, activity.sport_type),
    hrr = calc_hrr_pct(median_hr, athlete.hr_rest, hr_max_observed). Replace the 13 hardcoded defaults with
    None-safe access from these results per the RESEARCH.md mapping table: hr_recovery_pause_count =
    hr.pauses_found if hr else 0; hr_recovery_total_rest_sec = hr.total_rest_sec if hr else 0;
    hr_recovery_median_rate/best_rate/worst_rate/avg_rate from hr or None; vertical_speed_vmh/total_ascent_m/
    duration_hours from vspeed or None; cardiac_drift_pct/severity/quality from drift or None;
    cardiac_drift_significant = 1 if (drift and drift.is_significant) else 0; hrr_pct = hrr. Use the
    hr_max_observed already in scope for hrr_pct — do NOT call repo.max_heartrate(). Leave trimp, zone seconds,
    and cardiac_cost computation untouched.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_read_model_materialization.py tests/test_metric_registry.py -x -q</automated>
  </verify>
  <done>The populates-metric-columns test passes, the existing materializer tests pass (no regression in trimp/zones/cardiac_cost), and _activity_fact imports and calls the pure functions with hr_max_observed feeding hrr_pct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter ↔ storage | _activity_fact fetches stream rows via parameterized repo methods (no raw SQL added here). |
| adapter ↔ domain | _activity_fact calls pure metrics functions; data flows one way (rows in, dataclasses out). |
| materializer ↔ live DuckDB | Re-materialization rewrites stored fact values (null → computed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-DI | Tampering | live DuckDB fact rows during re-materialize | mitigate | Re-materialize is operator-run (plan-level, not this code task); operator confirms read-only backup ~/backups/mcp-strava-safe/ intact and respects DuckDB single-writer (owner-driven) before the live run. Code change itself is idempotent per-activity. |
| T-10-IV | Tampering | stream-row fetch | accept | Repo methods use parameterized queries (verified); no new SQL string built from input here. |
| T-10-SC | Tampering | package installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `PYTHONPATH=src python -m pytest tests/test_read_model_materialization.py -k populates_metric_columns -x -q` passes.
- `PYTHONPATH=src python -m pytest tests/test_read_model_materialization.py tests/test_metric_registry.py -x -q` passes (no regression).
- `_activity_fact` imports calc_hr_recovery/calc_vertical_speed/calc_cardiac_drift/calc_hrr_pct and uses hr_max_observed for hrr_pct.
</verification>

<success_criteria>
- The 13 previously-default columns are computed from the pure functions.
- hrr_pct uses max-to-date hr_max_observed.
- Rolling medians populate automatically; TRIMP/zones/cardiac_cost unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-03-SUMMARY.md` when done.
</output>