---
phase: 09-product-factual-bundles-and-cli-read-model-consolidation
plan: 09-04
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - Justfile
  - src/mcp_strava/devtools/mcp_client/client.py
  - tests/test_mcp_surface.py
  - tests/test_mcp_test_client.py
  - tests/test_security_guards.py
  - docs/deployment.md
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, PERF-01, TEST-03, TEST-04, TEST-06]
requirements_addressed: [MCP-01, MCP-02, MCP-03, PERF-01, TEST-03, TEST-04, TEST-06]
user_setup: []
must_haves:
  truths:
    - "D-03: MCP remains exactly the current six product tools; no new tool is added for product bundles."
    - "D-14: compare_periods remains aggregate-layer backed."
    - "D-16/D-19: CLI and MCP both consume product services while admin/sync/raw/debug controls stay below MCP."
    - "D-08/D-09/D-10: safety/status facts are factual threshold rows, not advice."
    - "D-11/D-12/D-13/D-15: registry and guard tests prevent unregistered facts, dead calculations, synthetic metric loss, and request-time recompute."
    - "Review feedback: MCP bundle smoke verifies `bundle_completeness` reason codes and documents which daily-brief facts are returned by `get_training_aggregates` versus composed from `list_workouts`/`get_workout_detail`."
  artifacts:
    - path: "src/mcp_strava/devtools/mcp_client/client.py"
      provides: "MCP smoke bundle calls through existing six-tool surface"
    - path: "tests/test_mcp_surface.py"
      provides: "exact MCP allowlist and bundle response assertions"
    - path: "tests/test_security_guards.py"
      provides: "no admin/raw/recompute/dead-handler leakage guards"
    - path: "docs/deployment.md"
      provides: "Phase 9 verification notes for product bundle and CLI consolidation"
  key_links:
    - from: "src/mcp_strava/devtools/mcp_client/client.py"
      to: "src/mcp_strava/interfaces/mcp_http.py"
      via: "scripted get_training_aggregates bundle calls"
      pattern: "daily_brief|weekly_digest|historical_facts"
    - from: "tests/test_security_guards.py"
      to: "src/mcp_strava/application"
      via: "AST import and request-path guard checks"
      pattern: "request_time_recompute|admin"
---

<objective>
Finalize Phase 9 boundary, smoke, and documentation gates.

Purpose: prove the product bundle and CLI consolidation did not expand MCP, leak admin/debug controls, revive request-time recompute, or lose performance/read-model guard coverage. This closes D-03, D-08 through D-16, and D-19.
Output: MCP smoke coverage for product bundles, final boundary guards, deployment verification notes, and full gate commands.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-CONTEXT.md
@.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-02-SUMMARY.md
@.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-03-SUMMARY.md
@src/mcp_strava/interfaces/mcp_http.py
@src/mcp_strava/devtools/mcp_client/client.py
@tests/test_mcp_surface.py
@tests/test_mcp_test_client.py
@tests/test_security_guards.py
@docs/deployment.md
@Justfile
</context>

<decision_traceability>
- D-03 requires no new MCP tool.
- D-08/D-09/D-10 require factual status facts only.
- D-11/D-12/D-13/D-14/D-15 require registry/read-model/aggregate-backed semantics.
- D-16/D-19 require shared product services with admin controls below MCP.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final MCP bundle smoke and allowlist tests</name>
  <files>src/mcp_strava/devtools/mcp_client/client.py, tests/test_mcp_surface.py, tests/test_mcp_test_client.py</files>
  <read_first>
    - src/mcp_strava/devtools/mcp_client/client.py
    - src/mcp_strava/interfaces/mcp_http.py
    - tests/test_mcp_surface.py
    - tests/test_mcp_test_client.py
  </read_first>
  <behavior>
    - Test 1: MCP tool allowlist remains exactly `get_fitness_state`, `list_workouts`, `get_workout_detail`, `compare_periods`, `project_fitness_state`, and `get_training_aggregates` per D-03.
    - Test 2: MCP client smoke calls include `get_training_aggregates` requests for `daily_brief`, `weekly_digest`, and `historical_facts` bundles through the existing tool per D-02/D-03.
    - Test 3: bundle MCP payloads contain rows plus bundle sections, `bundle_completeness`, freshness, completeness, calculation metadata, read-model metadata, and caller-visible reason codes for unavailable/skipped/scope-incompatible metrics per D-02/D-04/D-05/D-06/D-07.
    - Test 4: MCP smoke documents and asserts the daily-brief composition contract: `get_training_aggregates(bundle_id="daily_brief")` returns aggregate/status/model/load sections and `list_workouts`/`get_workout_detail` provide recent workout/detail facts; no new MCP tool or workout-object payload is added to aggregate rows per D-03/D-05.
    - Test 5: MCP payloads contain no sync/admin/raw/sql/token/log/debug fields and no coaching/advice phrases such as `you should`, `need to`, `recommended`, `hydrate`, `worry`, or `change behavior` per D-08/D-19.
  </behavior>
  <action>Add tests and client default/script coverage for Phase 9 bundles through `get_training_aggregates`; do not register new MCP tools or prompts. Assert `bundle_completeness` is present for each bundle section and that every unavailable/skipped/scope-incompatible metric has a reason code. Keep the existing response cache behavior for expensive aggregate calls and add a cache-key assertion so `bundle_id`, scope, dates, sport filter, as-of day, and window days are part of cache identity. Ensure scripted smoke arguments use registered bundle ids and bounded date/window values. Preserve exact six-tool assertions per D-03 and forbidden surface assertions per D-19.</action>
  <verify>
    <automated>uv run pytest -q tests/test_mcp_surface.py tests/test_mcp_test_client.py</automated>
  </verify>
  <done>MCP smoke and tests exercise Phase 9 product bundles through the existing six-tool surface only.</done>
</task>

<task type="auto">
  <name>Task 2: Add final boundary guards and verification docs</name>
  <files>Justfile, tests/test_security_guards.py, docs/deployment.md</files>
  <read_first>
    - Justfile
    - tests/test_security_guards.py
    - docs/deployment.md
    - src/mcp_strava/application/product_facts.py
    - src/mcp_strava/application/aggregate_services.py
    - src/mcp_strava/cli.py
  </read_first>
  <action>Add final guard coverage proving product bundle services and CLI read paths do not import Strava adapters, sync runtime, token refresh, raw SQL handlers, old report recompute helpers, old analytics row scanners, legacy `application.reports`/`application.workouts`, or dead CLI handlers per D-11 through D-16 and D-18/D-19. Extend `docs/deployment.md` with the Phase 9 verification sequence: targeted bundle tests, CLI surface tests, MCP smoke/full test, Docker p95 gate, and non-Docker fallback by timing `get_training_aggregates_service` bundle fixture calls under the 500 ms warm p95 target. Add a dedicated `Justfile` recipe named `phase9-bundle-smoke` that runs the targeted MCP/client bundle smoke commands; document it in `docs/deployment.md`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_security_guards.py tests/test_mcp_surface.py tests/test_mcp_test_client.py && just test</automated>
  </verify>
  <done>Boundary guards and deployment verification docs prove Phase 9 remains factual, read-model-backed, product-only, and smoke-testable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> existing tool handlers | Agent-provided tool arguments enter six tool handlers. |
| CLI/product services -> runtime DB | Product facts read prepared local facts through repository/service contracts. |
| Documentation -> operator commands | Runbook commands may be copied into live runtime verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-S | Spoofing | MCP tool ids | mitigate | Exact allowlist tests assert no new or renamed MCP tool ids. |
| T-09-04-T | Tampering | smoke scripts | mitigate | Scripted calls use product parameters only and no raw SQL/table/column inputs. |
| T-09-04-R | Repudiation | verification docs | mitigate | Deployment docs list exact targeted tests, `just test`, and optional p95 commands. |
| T-09-04-I | Information Disclosure | MCP/CLI payloads | mitigate | Security guards block raw/admin/token/log/debug fields and old raw payload helpers. |
| T-09-04-D | Denial of Service | aggregate bundle smoke | mitigate | Smoke calls use bounded requests and existing cache for expensive aggregate calls. |
| T-09-04-E | Elevation of Privilege | admin controls | mitigate | Guards prove sync/backfill/raw/sql/token/migration/log controls remain below MCP/product surfaces. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan set; keep package files unchanged. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_mcp_surface.py tests/test_mcp_test_client.py tests/test_security_guards.py` and `just test`.
</verification>

<success_criteria>
- Existing six MCP tools remain exact.
- Bundle smoke covers daily, weekly, and historical factual bundles through `get_training_aggregates`.
- Bundle smoke verifies `bundle_completeness` reason codes and the MCP daily-brief composition contract.
- Security guards block admin/raw/recompute/dead-handler leakage.
- Verification docs tell executors how to prove Phase 9 end to end.
</success_criteria>

<output>
Create `.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-04-SUMMARY.md` when done.
</output>
