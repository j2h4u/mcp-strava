---
phase: 09-product-factual-bundles-and-cli-read-model-consolidation
plan: 09-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/application/metric_registry.py
  - src/mcp_strava/adapters/duckdb/schema.py
  - src/mcp_strava/adapters/duckdb/aggregate_queries.py
  - tests/test_metric_registry.py
  - tests/test_training_aggregates.py
  - docs/metrics.md
autonomous: true
requirements: [APP-01, APP-02, MCP-01, MCP-03, READMODEL-01, READMODEL-04, TEST-03, TEST-06]
requirements_addressed: [APP-01, APP-02, MCP-01, MCP-03, READMODEL-01, READMODEL-04, TEST-03, TEST-06]
user_setup: []
must_haves:
  truths:
    - "D-01: planning proceeds from existing Phase 9 CONTEXT.md and current codebase evidence; no new external research or package selection is required."
    - "D-02/D-04: `daily_brief`, `weekly_digest`, and `historical_facts` aggregate bundles validate and query without mixed-scope or unsupported-column failures."
    - "D-07/D-08/D-09/D-10: historical facts and safety/status facts are factual registered outputs with machine-readable codes, thresholds, windows, evidence, completeness, and calculation metadata."
    - "D-07: `activity_streak_days`, `rest_streak_days`, and `last_hike_days_ago` are derived from explicit DuckDB windowed views or query CTEs, not mapped to absent `training_model_daily` columns."
    - "D-09: `consecutive_high_load_hikes` and `running_volume_jump` have registered thresholds and populated fixture evidence from cross-activity windows."
    - "D-11/D-12/D-13/D-14/D-15: registry metadata remains the source of truth for materialized columns, synthetic metrics, aggregate bundles, and compare-period aggregate semantics."
    - "D-20/D-21/D-22: kudos and supported gear/shoe facts are factual product context without aggregate gear filtering or grouping."
  artifacts:
    - path: "src/mcp_strava/types.py"
      provides: "typed status fact registry/output contracts"
    - path: "src/mcp_strava/application/metric_registry.py"
      provides: "registered product bundle, historical, status, kudos, and gear fact contracts"
    - path: "src/mcp_strava/adapters/duckdb/schema.py"
      provides: "DuckDB views exposing registered read-model context columns without new physical period tables"
    - path: "src/mcp_strava/adapters/duckdb/aggregate_queries.py"
      provides: "mixed-scope bundle execution over registry metadata"
    - path: "tests/test_training_aggregates.py"
      provides: "bundle query behavior tests"
    - path: "tests/test_metric_registry.py"
      provides: "registry and docs drift tests"
  key_links:
    - from: "src/mcp_strava/application/metric_registry.py"
      to: "src/mcp_strava/adapters/duckdb/aggregate_queries.py"
      via: "AGGREGATE_METRIC_BUNDLES and MetricDefinition aggregate metadata"
      pattern: "metrics_for_aggregate_bundle"
    - from: "src/mcp_strava/application/metric_registry.py"
      to: "src/mcp_strava/adapters/duckdb/schema.py"
      via: "MATERIALIZED_FACT_COLUMN_REGISTRY and aggregate view projections"
      pattern: "MATERIALIZED_FACT_COLUMN_REGISTRY"
    - from: "src/mcp_strava/application/metric_registry.py"
      to: "src/mcp_strava/adapters/duckdb/aggregate_queries.py"
      via: "STATUS_FACT_REGISTRY threshold/evidence definitions consumed by status fact queries"
      pattern: "STATUS_FACT_REGISTRY"
---

<objective>
Make the registry and DuckDB aggregate layer safe for Phase 9 product factual bundles.

Purpose: unblock daily, weekly, historical, and status fact bundles by keeping all metric, dependency, dimension, provenance, kudos, gear, and synthetic metric semantics registry-backed per D-02 through D-15 and D-20 through D-22.
Output: bundle-safe registry metadata, aggregate view/query support, docs drift coverage, and behavior tests.
</objective>

<execution_context>
@/home/j2h4u/.codex/get-shit-done/workflows/execute-plan.md
@/home/j2h4u/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-CONTEXT.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-05-SUMMARY.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-06-SUMMARY.md
@.planning/phases/08-duckdb-primary-storage-aggregate-analytics-surface/08-07-SUMMARY.md
@src/mcp_strava/application/metric_registry.py
@src/mcp_strava/adapters/duckdb/schema.py
@src/mcp_strava/adapters/duckdb/aggregate_queries.py
@tests/test_metric_registry.py
@tests/test_training_aggregates.py
@docs/metrics.md
</context>

<source_audit>
## Phase 9 Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Product factual bundles and CLI read-model consolidation | Covered by plans 09-01 through 09-04. |
| REQ | APP-01, APP-02, APP-03, APP-04, CLI-01, CLI-02, CLI-03, MCP-01, MCP-02, MCP-03, READMODEL-01, READMODEL-04, PERF-01, TEST-03, TEST-04, TEST-06 | Distributed across plan frontmatter; every listed requirement appears in at least one Phase 9 plan. |
| RESEARCH | No Phase 9 RESEARCH.md; D-01 locks that no additional product discovery is required | Covered as Level 0 discovery in 09-01; no package install or external API selection is planned. |
| CONTEXT | D-01 | Covered by 09-01 objective and no-research task scope. |
| CONTEXT | D-02, D-04, D-05, D-06, D-07 | Covered by 09-01 bundle contracts and 09-02 product fact services. |
| CONTEXT | D-08, D-09, D-10 | Covered by 09-01 status fact registry/query contracts and 09-02 factual status sections. |
| CONTEXT | D-11, D-12, D-13, D-14, D-15 | Covered by 09-01 registry/query work and 09-04 drift/boundary guards. |
| CONTEXT | D-16, D-17, D-18, D-19 | Covered by 09-03 CLI consolidation and admin/product boundary tests. |
| CONTEXT | D-20, D-21, D-22 | Covered by 09-01 registry/query contracts, 09-02 product facts, and 09-03 CLI behavior. |
| CONTEXT Deferred Ideas | Training model redesign, coaching interpretation, medical advice, aggregate gear filtering/grouping unless required, raw/admin MCP | Excluded from all plans. |
</source_audit>

<decision_traceability>
- D-01 is implemented by using existing CONTEXT.md, codebase maps, summaries, and local source reads only.
- D-02/D-04 define the bundle safety target for `daily_brief`, `weekly_digest`, and `historical_facts`.
- D-08/D-09/D-10 define status facts as threshold/category facts, not prescriptive warnings.
- D-11/D-12/D-13/D-14/D-15 define registry-backed metrics, synthetic metric retention, and aggregate-backed comparison.
- D-20/D-21/D-22 define kudos and supported gear facts without adding aggregate gear filtering/grouping.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing bundle and registry contract tests</name>
  <files>tests/test_metric_registry.py, tests/test_training_aggregates.py</files>
  <read_first>
    - tests/test_metric_registry.py
    - tests/test_training_aggregates.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/adapters/duckdb/aggregate_queries.py
  </read_first>
  <behavior>
    - Test 1: `metrics_for_aggregate_bundle("daily_brief")`, `"weekly_digest"`, and `"historical_facts"` return only known metrics with aggregate metadata per D-02/D-04/D-11.
    - Test 2: DuckDB aggregate queries for those bundles return rows for global and per-sport capable metrics without `ValueError` for mixed scope, rolling-window, or unsupported column combinations per D-04/D-13.
    - Test 3: `historical_facts` queries return non-`unavailable` fixture rows for `activity_streak_days`, `rest_streak_days`, and `last_hike_days_ago`; they do not select absent `training_model_daily.activity_streak_days`, `training_model_daily.rest_streak_days`, or `training_model_daily.last_hike_days_ago` columns per D-07.
    - Test 4: status facts for stale mirror/read-model, missing HR/streams, excessive high-zone exposure, HR anomaly bursts, cardiac drift significance/quality, consecutive high-load hikes, and running volume jump have registered code, threshold, window, evidence keys, completeness reasons, metric ids, and calculation metadata per D-08/D-09/D-10.
    - Test 5: populated DuckDB fixtures produce non-`unavailable` status fact evidence for excessive Z5 exposure, HR anomaly bursts, cardiac drift significance with quality, consecutive high-load hikes, and running volume jump; unavailable status is allowed only with an explicit missing reason.
    - Test 6: kudos count is available in list/detail/aggregate contexts and kudos names remain excluded from aggregate bundles per D-20.
    - Test 7: supported gear/shoe facts are registered as factual context and aggregate request dataclasses still reject gear/equipment filters per D-21/D-22.
  </behavior>
  <action>Write RED tests first. Keep tests fixture-backed with DuckDB temp databases and no Strava network/auth access. Assert the bundle failure points directly: mixed global/per-sport metrics, historical window facts, rolling-window metrics, status facts, synthetic metrics, kudos behavior, and no aggregate gear filters. Addresses review concerns Claude H-1 and Claude H-2 by requiring executable historical/status fact derivation instead of metadata-only registration. Cite D-02/D-04/D-07 through D-14 and D-20 through D-22 in test names or comments where useful.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py tests/test_training_aggregates.py</automated>
  </verify>
  <done>RED tests fail only because Phase 9 bundle/status/gear registry and aggregate query behavior is not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registry-backed bundle-safe aggregate facts</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/application/metric_registry.py, src/mcp_strava/adapters/duckdb/schema.py, src/mcp_strava/adapters/duckdb/aggregate_queries.py</files>
  <read_first>
    - src/mcp_strava/types.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/adapters/duckdb/schema.py
    - src/mcp_strava/adapters/duckdb/aggregate_queries.py
    - src/mcp_strava/constants.py
  </read_first>
  <behavior>
    - Test 1: all RED tests from Task 1 pass.
    - Test 2: `compare_periods` continues to use aggregate metadata and no Python row-scanning comparison path is introduced per D-14.
    - Test 3: `MATERIALIZED_FACT_COLUMN_REGISTRY` remains the only allowed source for fact/dependency/dimension/provenance columns per D-11/D-12.
  </behavior>
  <action>Implement the smallest registry-backed query support needed for the failing tests. Add typed status fact contracts in `src/mcp_strava/types.py`, for example `StatusFactDefinition` and `StatusFact`, with code, metric_id, threshold, window, evidence, completeness, calculation, and materialized/read-model provenance fields. Add `STATUS_FACT_REGISTRY` in `metric_registry.py`; thresholds and evidence keys must live in that registry, not as private literals inside query code. Include definitions for `excessive_z5_exposure` (`zone5_seconds > 300` and Z5 lower bound 177), `hr_anomaly_burst` (`hr_anomaly_count >= 3` and jump threshold 30 bpm), `cardiac_drift_significant_quality` (`cardiac_drift_significant == 1` with quality in `good|fair`), `consecutive_high_load_hikes` (two consecutive Hike days with combined TRIMP > 800), `running_volume_jump` (current Monday-start running week distance vs previous week with 10 percent and 15 percent thresholds), `missing_hr`, `missing_streams`, `stale_mirror_data`, and `stale_read_model_facts` per D-08/D-09/D-10. For Claude H-1, stop mapping `activity_streak_days`, `rest_streak_days`, and `last_hike_days_ago` to absent `training_model_daily` columns: add an explicit DuckDB windowed view or CTE-backed source, such as `v_historical_context_facts`, where streaks are derived from consecutive `daily_load_facts.activity_count` days ending at the requested/as-of day and last-hike age is derived from the latest `activity_metric_facts` row with `sport_type = 'Hike'`. Register only those derived view/projection columns needed by the aggregate query allowlist per D-11/D-12. For Claude H-2, add the cross-activity status fact query path that computes high-load-hike and running-volume-jump evidence from local read-model/activity facts; return unavailable only with explicit missing reasons like `insufficient_hike_history`, `no_previous_running_week`, or `read_model_unavailable`. Normalize mixed bundle semantics inside `aggregate_queries.py` by splitting unsupported scope/window combinations per metric and merging factual rows; do not add public request parameters. Keep `compare_periods` on the existing aggregate service path per D-14. Preserve kudos count aggregation while keeping `kudos_names` detail-only per D-20.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py tests/test_training_aggregates.py</automated>
  </verify>
  <done>`daily_brief`, `weekly_digest`, and `historical_facts` bundles are queryable, registry-backed, and free of scope/column failures.</done>
</task>

<task type="auto">
  <name>Task 3: Update registry-derived metrics documentation</name>
  <files>docs/metrics.md</files>
  <read_first>
    - docs/metrics.md
    - src/mcp_strava/application/metric_registry.py
  </read_first>
  <action>Update `docs/metrics.md` so the documented product bundles, status facts, kudos behavior, and supported gear context match the registry state implemented in Task 2 per D-08 through D-15 and D-20 through D-22. Keep the document descriptive and factual; do not introduce user-facing coaching language, aggregate gear filters, or new MCP tool names.</action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_registry.py tests/test_training_aggregates.py</automated>
  </verify>
  <done>Metrics documentation matches registry-backed bundle/status/gear behavior and the docs drift tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP/CLI request -> aggregate query builder | Untrusted product parameters become query requests. |
| Registry metadata -> DuckDB SQL templates | Registered column names and aggregate modes select SQL fragments. |
| Read model -> product payload | Local activity and social facts become MCP/CLI-visible data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-S | Spoofing | `AggregateRequest.sport_filter` | mitigate | Validate against `ALL_SPORTS`; do not accept gear/equipment filters per D-22. |
| T-09-01-T | Tampering | `aggregate_queries.py` SQL templates | mitigate | Use registry-whitelisted columns and parameterized date/sport inputs; no caller-supplied table/column SQL. |
| T-09-01-R | Repudiation | status facts | mitigate | Include code, threshold, window, evidence, completeness, calculation, and materialized/read-model metadata per D-08/D-10. |
| T-09-01-I | Information Disclosure | kudos and gear facts | mitigate | Expose kudos names only on per-workout detail and gear facts only when mirrored product data supports them; no raw Strava payloads. |
| T-09-01-D | Denial of Service | mixed bundle expansion | mitigate | Split bundle query work by metric support and keep supported bucket/window allowlists bounded. |
| T-09-01-E | Elevation of Privilege | aggregate request surface | mitigate | Do not add raw/admin/sync/debug parameters or MCP tools per D-03/D-19. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this plan set; keep package files unchanged. |
</threat_model>

<verification>
Run `uv run pytest -q tests/test_metric_registry.py tests/test_training_aggregates.py` and confirm there are no Strava network calls, raw SQL request parameters, or new MCP/admin tools.
</verification>

<success_criteria>
- `daily_brief`, `weekly_digest`, and `historical_facts` aggregate bundles validate and query successfully.
- Status facts are factual and machine-readable, with registered formulas and thresholds.
- Historical streak/last-hike facts and D-09 windowed status facts produce fixture-backed non-`unavailable` values when supporting rows exist.
- Synthetic metrics remain in aggregate/comparison bundles where meaningful.
- Gear facts are factual context only; aggregate gear filtering/grouping remains absent.
</success_criteria>

<output>
Create `.planning/phases/09-product-factual-bundles-and-cli-read-model-consolidation/09-01-SUMMARY.md` when done.
</output>
