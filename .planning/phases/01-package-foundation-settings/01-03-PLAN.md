---
phase: 01-package-foundation-settings
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/test_smoke.py
  - Justfile
  - scripts/run_tests.py
  - pyproject.toml
autonomous: true
requirements: ["FOUND-03", "FOUND-01", "FOUND-02"]
user_setup: []
must_haves:
  truths:
    - "FOUND-03 is satisfied: just test runs pytest and validates the baseline smoke behavior after the package refactor."
    - "D-08 and D-09 are satisfied: the primary test workflow is pytest through pyproject.toml, and just test no longer calls scripts/run_tests.py."
    - "Existing smoke coverage still exercises imports, daily report, training helpers, metrics helpers, analytics helpers, and sport registry behavior from mcp_strava."
    - "D-01 through D-09 remain represented across the finalized plans and no Phase 1 plan commits .planning/config.json, .env, or data files."
  artifacts:
    - path: "tests/test_smoke.py"
      provides: "Pytest-discovered smoke behavior over package imports"
      contains: "from mcp_strava.training import ewma"
    - path: "Justfile"
      provides: "Developer-facing test command"
      contains: "python3 -m pytest"
    - path: "pyproject.toml"
      provides: "Pytest discovery configuration"
      contains: "testpaths"
  key_links:
    - from: "Justfile"
      to: "pytest"
      via: "test recipe"
      pattern: "python3 -m pytest"
    - from: "tests/test_smoke.py"
      to: "src/mcp_strava"
      via: "package imports"
      pattern: "from mcp_strava\\."
---

<objective>
Convert the smoke suite and developer test command to pytest over the packaged code.

Purpose: prove Phase 1's package and settings foundation still supports the trusted baseline behavior through the required `just test` command.

Output: pytest-compatible smoke tests, `just test` backed by `python3 -m pytest`, and no primary reliance on the custom `scripts/run_tests.py` runner.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation-settings/01-CONTEXT.md
@.planning/phases/01-package-foundation-settings/01-RESEARCH.md
@.planning/phases/01-package-foundation-settings/01-PATTERNS.md
@Justfile
@pyproject.toml
@tests/test_smoke.py
@scripts/run_tests.py
</context>

<threat_model>
Trust boundaries:
- Test code is trusted developer code and may read the local database through the existing report smoke path.
- The Strava network, token refresh, sync, and backfill paths remain out of scope for Phase 1 verification.
- Ignored local files remain outside the commit boundary.

Threat register:
- T01 | Tampering | local mirror database | Mitigated by keeping tests read-only and forbidding sync/backfill/migration commands in this plan.
- T02 | Information disclosure | local `.env` values | Mitigated by tests importing modules and reading local report data without printing token values.
- T03 | Repudiation | test workflow ambiguity | Mitigated by making `just test` the single visible command and removing `scripts/run_tests.py` from the primary path.
- T04 | Boundary regression | top-level `types.py` import shadowing | Mitigated by pytest importing `mcp_strava.types` from `src/` instead of relying on `scripts/` as cwd.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Convert smoke imports to package imports</name>
  <files>tests/test_smoke.py</files>
  <read_first>tests/test_smoke.py, src/mcp_strava/types.py, src/mcp_strava/training.py, src/mcp_strava/report.py, src/mcp_strava/settings.py</read_first>
  <action>Update `tests/test_smoke.py` so all imports use `mcp_strava.*` instead of `strava_lib.*`. Remove the old docstring guidance that says tests run without pytest due to a `types.py` naming conflict. Keep the existing test function names and assertions for imports, daily report, forward simulation, EWMA, form zone, trend, one-day simulation, decoupling, median/pct_change, and sports registry. Add one lightweight assertion that `mcp_strava.settings.load_settings(environ={}, project_root=tmp_path)` returns a settings object whose default database path ends with `data/strava.db`.</action>
  <verify>python3 -m pytest tests/test_smoke.py</verify>
  <acceptance_criteria>
    - `tests/test_smoke.py` contains `from mcp_strava.training import ewma`.
    - `tests/test_smoke.py` contains no `strava_lib`.
    - `tests/test_smoke.py` contains `load_settings`.
    - `python3 -m pytest tests/test_smoke.py` exits 0.
  </acceptance_criteria>
  <done>Smoke tests import the packaged code and remain pytest-discoverable.</done>
</task>

<task type="auto">
  <name>Task 2: Route just test to pytest</name>
  <files>Justfile, pyproject.toml</files>
  <read_first>Justfile, pyproject.toml, .planning/phases/01-package-foundation-settings/01-CONTEXT.md</read_first>
  <action>Change the `test` recipe in `Justfile` to run `python3 -m pytest`. Preserve `alias tests := test` and `alias smoke := test`. Confirm `pyproject.toml` has pytest configuration for `tests` and `src` so `just test` works from the repo root without manual `PYTHONPATH` exports.</action>
  <verify>just test</verify>
  <acceptance_criteria>
    - `Justfile` contains `python3 -m pytest`.
    - `Justfile` does not contain `scripts/run_tests.py`.
    - `alias tests := test` remains present.
    - `alias smoke := test` remains present.
    - `just test` exits 0.
  </acceptance_criteria>
  <done>`just test` is the developer-facing pytest command.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the custom runner from the primary workflow</name>
  <files>scripts/run_tests.py, scripts/</files>
  <read_first>scripts/run_tests.py, Justfile, pyproject.toml, tests/test_smoke.py</read_first>
  <action>Delete `scripts/run_tests.py` if no remaining primary workflow references it. If the `scripts/` directory becomes empty after Plan 01 and this task, remove the empty directory from version control. Do not add a compatibility shim unless a verification command proves it is still required. The accepted primary runner is pytest through `just test`.</action>
  <verify>just test</verify>
  <acceptance_criteria>
    - `rg "scripts/run_tests.py" Justfile pyproject.toml tests src` returns no matches.
    - `test ! -f scripts/run_tests.py` exits 0, unless a written note in the summary explains why a non-primary shim was retained.
    - `just test` exits 0 after runner retirement.
    - `test -f data/strava.db` exits 0 after the test workflow migration.
  </acceptance_criteria>
  <done>The old custom runner is not the primary test path and baseline smoke behavior passes through pytest.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest` exits 0.
- [ ] `just test` exits 0.
- [ ] `rg "strava_lib|scripts/run_tests.py" tests Justfile pyproject.toml src` returns no primary-path references, except historical text in planning artifacts.
- [ ] `python3 -m mcp_strava` still reaches usage output.
- [ ] `test -f data/strava.db` exits 0.
</verification>

<success_criteria>
- FOUND-03 is covered by pytest and `just test`.
- The smoke suite still validates the package imports and trusted baseline analytics behavior.
- Phase 1 has a reproducible test workflow from the repo root.
- No data, token, sync, Strava API, migration, MCP, or Docker behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation-settings/01-03-SUMMARY.md`.
</output>
