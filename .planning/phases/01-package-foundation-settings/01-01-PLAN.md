---
phase: 01-package-foundation-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/mcp_strava/__init__.py
  - src/mcp_strava/__main__.py
  - src/mcp_strava/cli.py
  - src/mcp_strava/analytics.py
  - src/mcp_strava/api_schema.py
  - src/mcp_strava/cardiac_drift.py
  - src/mcp_strava/constants.py
  - src/mcp_strava/db.py
  - src/mcp_strava/metrics.py
  - src/mcp_strava/report.py
  - src/mcp_strava/sports.py
  - src/mcp_strava/strava_api_reference.py
  - src/mcp_strava/sync.py
  - src/mcp_strava/training.py
  - src/mcp_strava/trends.py
  - src/mcp_strava/types.py
  - scripts/cli.py
  - scripts/strava_lib/
autonomous: true
requirements: ["FOUND-01"]
user_setup: []
must_haves:
  truths:
    - "FOUND-01 is satisfied: the project imports from the installable mcp_strava package instead of relying on scripts/ path insertion."
    - "D-01 and D-02 are satisfied: existing runtime modules are moved into the package layout immediately, and old scripts/ execution is not preserved as the primary runtime contract."
    - "D-06 and D-07 are satisfied: python -m mcp_strava reaches the CLI dispatcher, and pyproject.toml does not define a mcp-strava console executable."
    - "data/strava.db remains in data/strava.db and this plan performs no sync, backfill, token refresh, schema migration, or database reset."
  artifacts:
    - path: "pyproject.toml"
      provides: "Installable package metadata and src package discovery"
      contains: "name = \"mcp-strava\""
    - path: "src/mcp_strava/__main__.py"
      provides: "Module entrypoint for python -m mcp_strava"
      contains: "from mcp_strava.cli import main"
    - path: "src/mcp_strava/cli.py"
      provides: "Moved CLI dispatcher"
      contains: "COMMANDS ="
    - path: "src/mcp_strava/training.py"
      provides: "Moved training analytics module"
      contains: "def ewma("
  key_links:
    - from: "src/mcp_strava/__main__.py"
      to: "src/mcp_strava/cli.py"
      via: "main import"
      pattern: "from mcp_strava\\.cli import main"
    - from: "src/mcp_strava/cli.py"
      to: "src/mcp_strava/report.py"
      via: "package import"
      pattern: "from mcp_strava\\.report import daily_report"
---

<objective>
Move the current runtime into an installable Python package and add the module entrypoint required by Phase 1.

Purpose: remove the ad hoc `scripts/` import path as the primary runtime boundary while keeping the current analytics code mechanically intact for later service-boundary phases.

Output: `pyproject.toml`, `src/mcp_strava/` package modules, and `python -m mcp_strava` as the Phase 1 entrypoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-foundation-settings/01-CONTEXT.md
@.planning/phases/01-package-foundation-settings/01-RESEARCH.md
@.planning/phases/01-package-foundation-settings/01-PATTERNS.md
@Justfile
@scripts/cli.py
@scripts/strava_lib/constants.py
@scripts/strava_lib/db.py
@scripts/strava_lib/training.py
@tests/test_smoke.py
</context>

<threat_model>
Trust boundaries:
- Local source tree and package metadata are trusted developer-controlled files.
- `.env`, `.planning/config.json`, and `data/strava.db` are local runtime state and must not be committed or modified by packaging work.
- Strava API/network calls are outside this phase.

Threat register:
- T01 | Tampering | `data/strava.db` | Mitigated by excluding `data/` from `files_modified`, avoiding migration/sync commands, and verifying the DB file still exists after the move.
- T02 | Information disclosure | `.env` and `.planning/config.json` | Mitigated by not reading secret values into generated files and not staging ignored config files.
- T03 | Spoofing/import confusion | Python import path | Mitigated by using `src/mcp_strava` imports and removing `strava_lib.*` imports from package source.
- T04 | Boundary regression | CLI executable contract | Mitigated by requiring `python -m mcp_strava` and forbidding `[project.scripts]` for `mcp-strava` in this phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add package metadata without a console executable</name>
  <files>pyproject.toml</files>
  <read_first>.planning/phases/01-package-foundation-settings/01-CONTEXT.md, .planning/phases/01-package-foundation-settings/01-RESEARCH.md, .gitignore, pyproject.toml</read_first>
  <action>Create `pyproject.toml` for package `mcp-strava` using setuptools with `package-dir = {\"\" = \"src\"}` and package discovery under `src`. Set `requires-python = \">=3.13\"`. Keep runtime dependencies empty for Phase 1. Add pytest as a test/development dependency target, and add pytest config with `testpaths = [\"tests\"]` and `pythonpath = [\"src\"]`. Do not add `[project.scripts]` or a `mcp-strava` console executable because D-07 defers it.</action>
  <verify>python3 -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); assert data['project']['name']=='mcp-strava'; assert 'scripts' not in data.get('project', {})"</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "mcp-strava"`.
    - `pyproject.toml` contains `requires-python = ">=3.13"`.
    - `pyproject.toml` contains `where = ["src"]`.
    - `pyproject.toml` does not contain `[project.scripts]`.
    - `pyproject.toml` configures pytest to include `tests` and `src`.
  </acceptance_criteria>
  <done>Package metadata exists and does not create the deferred console executable.</done>
</task>

<task type="auto">
  <name>Task 2: Move runtime modules into src/mcp_strava</name>
  <files>src/mcp_strava/*.py, scripts/strava_lib/</files>
  <read_first>scripts/strava_lib/__init__.py, scripts/strava_lib/analytics.py, scripts/strava_lib/api_schema.py, scripts/strava_lib/cardiac_drift.py, scripts/strava_lib/constants.py, scripts/strava_lib/db.py, scripts/strava_lib/metrics.py, scripts/strava_lib/report.py, scripts/strava_lib/sports.py, scripts/strava_lib/strava_api_reference.py, scripts/strava_lib/sync.py, scripts/strava_lib/training.py, scripts/strava_lib/trends.py, scripts/strava_lib/types.py</read_first>
  <action>Create `src/mcp_strava/` and move the current `scripts/strava_lib` modules into it with the same module filenames. Update imports in moved source from `strava_lib.*` to `mcp_strava.*`. Keep module-level functions, dataclasses, constants, and error behavior mechanically equivalent. Do not restructure into core/repository/adapter directories yet. Remove `scripts/strava_lib/` only after the package imports are updated.</action>
  <verify>python3 -c "from mcp_strava.training import ewma; from mcp_strava.constants import Config; assert ewma({}, tau=7)=={}; assert Config.Model.Banister.TAU_FITNESS == 42"</verify>
  <acceptance_criteria>
    - `src/mcp_strava/training.py` exists and contains `def ewma(`.
    - `src/mcp_strava/report.py` exists and contains `def daily_report(`.
    - `src/mcp_strava/db.py` exists and contains `class DbConn`.
    - `rg "strava_lib" src/mcp_strava` returns no matches.
    - `python3 -c "from mcp_strava.training import ewma; assert ewma({}, tau=7)=={}"` exits 0.
  </acceptance_criteria>
  <done>Runtime modules import from the package namespace without the old scripts path.</done>
</task>

<task type="auto">
  <name>Task 3: Provide python -m mcp_strava entrypoint</name>
  <files>src/mcp_strava/cli.py, src/mcp_strava/__main__.py, scripts/cli.py</files>
  <read_first>scripts/cli.py, src/mcp_strava/cli.py, src/mcp_strava/__main__.py</read_first>
  <action>Move the current CLI dispatcher into `src/mcp_strava/cli.py`, update its package imports to `mcp_strava.*`, and change the usage text from `cli.py <command> [args]` to `python -m mcp_strava <command> [args]`. Create `src/mcp_strava/__main__.py` that imports `main` from `mcp_strava.cli` and calls it under the `if __name__ == "__main__"` guard. Remove `scripts/cli.py` as a primary executable after the package entrypoint works.</action>
  <verify>python3 -m mcp_strava >/tmp/mcp-strava-entry.out 2>/tmp/mcp-strava-entry.err; test "$?" -eq 1; rg "Usage: python -m mcp_strava" /tmp/mcp-strava-entry.err</verify>
  <acceptance_criteria>
    - `src/mcp_strava/__main__.py` contains `from mcp_strava.cli import main`.
    - `src/mcp_strava/__main__.py` contains `main()`.
    - `src/mcp_strava/cli.py` contains `COMMANDS =`.
    - `python3 -m mcp_strava` exits non-zero with usage text containing `python -m mcp_strava <command> [args]`.
    - `test -f data/strava.db` exits 0 after the package move.
  </acceptance_criteria>
  <done>Module entrypoint reaches the package CLI and the Strava database remains untouched.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -c "from mcp_strava.training import ewma; assert ewma({}, tau=7)=={}"` exits 0.
- [ ] `python3 -m mcp_strava` prints usage for the package entrypoint and exits 1 when no command is provided.
- [ ] `rg "from strava_lib|import strava_lib" src/mcp_strava` returns no matches.
- [ ] `test -f data/strava.db` exits 0.
- [ ] `git status --short --ignored .env .planning/config.json data/strava.db` does not show those files staged.
</verification>

<success_criteria>
- FOUND-01 is covered by an installable `src/mcp_strava` package.
- The primary runtime path is no longer `scripts/strava_lib` on `sys.path`.
- `python -m mcp_strava` is available and no `mcp-strava` console script is introduced.
- No data, token, sync, Strava API, migration, or MCP behavior is changed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation-settings/01-01-SUMMARY.md`.
</output>
