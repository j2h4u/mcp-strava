---
phase: "14"
plan: "03"
type: execute
wave: 3
depends_on:
  - "14-02"
files_modified: []
autonomous: true
requirements:
  - "Metric platform maintainability / registry source-of-truth"
must_haves:
  truths:
    - "just check exits 0 after registry-generated activity_metric_facts schema cutover"
    - "just test exits 0 after registry-generated activity_metric_facts schema cutover"
    - "No Phase 14 work expands into source grammar, payload generation, computed metric DAGs, or rematerialize-all"
    - "No live DB or Strava API call is required for Phase 14 verification"
  artifacts: []
  key_links:
    - from: "Phase 14 scope fence"
      to: "14-CONTEXT.md incremental path"
      via: "verification confirms only slice 1 landed"
      pattern: "slice 1 only"
---

<objective>
Run the terminal Phase 14 gates and verify the registry-owned fact schema slice is
complete.

This is verification-only unless a gate reveals a narrow missed item from Plans
14-01 or 14-02. It exists so the phase has an explicit final checkpoint before
execution moves to security/validation/UAT workflows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-CONTEXT.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-RESEARCH.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-01-SUMMARY.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification gates</name>
  <files></files>
  <read_first>
    - Justfile targets `check`, `lint`, `fmt-check`, `typecheck`, and `test`
    - `14-01-SUMMARY.md` and `14-02-SUMMARY.md`
  </read_first>
  <action>
    Run the full project gates:

    1. `just check`
    2. `just test`

    If either fails, diagnose whether the failure is a direct missed item from
    Plans 14-01/14-02. Apply only minimal fixes inside the existing Phase 14 file
    set. Do not broaden scope into later metric-platform slices.

    Also run a scope-fence scan and record results in the SUMMARY:

    - `rg -n "source=.*detail_json|rematerialize|computed:|ACTIVITY_SCALAR_FACTS" src/mcp_strava tests`

    Existing matches are allowed, but the executor must confirm Phase 14 did not
    introduce source grammar, rematerialization command, computed-metric DAG, or
    per-tool payload-generation changes.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && just check && just test</automated>
  </verify>
  <acceptance_criteria>
    - `just check` exits 0
    - `just test` exits 0
    - Scope scan confirms no Phase 14 edits introduced later-slice capabilities
    - SUMMARY records the last relevant output lines for both gates
  </acceptance_criteria>
  <done>Full quality gates pass and Phase 14 scope fence is preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | Runs local quality/test commands against existing code; no live DB or API mutation required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-LIVE | Tampering | live DuckDB mirror | mitigate | Verification commands must not run live admin migration/cutover commands or call Strava APIs |
| T-14-SC | Tampering | dependency supply chain | accept | No package installs in this plan |
</threat_model>

<verification>
Run:

```
cd /home/j2h4u/repos/j2h4u/mcp-strava
just check
just test
```
</verification>

<success_criteria>
- `just check` exits 0
- `just test` exits 0
- Phase 14 remains limited to registry-owned `activity_metric_facts` schema generation
- No live database or Strava API operation is needed for verification
</success_criteria>

<output>
Create `.planning/phases/14-metric-platform-registry-owned-fact-schema/14-03-SUMMARY.md` when done.
</output>
