---
phase: "14"
plan: "02"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - src/mcp_strava/adapters/duckdb/schema.py
  - tests/test_metric_registry.py
  - tests/test_duckdb_repository.py
autonomous: true
requirements:
  - "Metric platform maintainability / registry source-of-truth"
must_haves:
  truths:
    - "schema.py uses registry-generated SQL for activity_metric_facts table creation"
    - "ensure_provenance_columns() uses registry-generated ADD COLUMN SQL for the current additive activity fact columns"
    - "The activity_metric_facts schema created in a temp DuckDB DB matches registry metadata for names, types, nullability, defaults, and primary key"
    - "The additive migration path works against an older temp activity_metric_facts fixture missing the late columns"
    - "No live database under /opt/docker/mcp-strava/data is opened or mutated"
  artifacts:
    - path: "src/mcp_strava/adapters/duckdb/schema.py"
      provides: "Runtime schema cutover to registry-generated activity_metric_facts DDL and ALTER statements"
      contains: "activity_metric_facts_table_sql"
    - path: "tests/test_metric_registry.py"
      provides: "Stronger registry-vs-DuckDB schema tests"
    - path: "tests/test_duckdb_repository.py"
      provides: "Repository/init compatibility coverage for generated additive migrations"
  key_links:
    - from: "metric_registry.activity_metric_facts_table_sql"
      to: "schema.create_schema"
      via: "schema.py imports generated DDL instead of duplicating activity_metric_facts column definitions"
      pattern: "activity_metric_facts_table_sql"
---

<objective>
Cut `schema.py` over to registry-generated `activity_metric_facts` DDL and
registry-generated additive migration SQL.

This plan removes the main Phase 14 duplication: the hand-written
`CREATE TABLE activity_metric_facts` column list and the hand-written ADD COLUMN
types in `ensure_provenance_columns()`. It must preserve the exact current
runtime schema and keep all verification on temp DuckDB databases.

Out of scope: generating the other materialized fact tables, changing aggregate
view projection semantics, source grammar, computed-metric dispatch, and
rematerialization/backfill commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-CONTEXT.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-RESEARCH.md
@.planning/phases/14-metric-platform-registry-owned-fact-schema/14-01-SUMMARY.md
@src/mcp_strava/metric_registry.py
@src/mcp_strava/adapters/duckdb/schema.py
@tests/test_metric_registry.py
@tests/test_duckdb_repository.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen schema parity tests before runtime cutover</name>
  <files>tests/test_metric_registry.py</files>
  <read_first>
    - tests/test_metric_registry.py existing schema registry tests
    - src/mcp_strava/metric_registry.py helpers from Plan 14-01
    - src/mcp_strava/adapters/duckdb/schema.py current create_schema behavior
  </read_first>
  <action>
    Replace or extend the old names-only drift test with stronger assertions over a
    temp DuckDB schema created by `create_schema(conn)`:

    - Every `activity_metric_facts` column exists in registry order.
    - DuckDB `information_schema.columns` type and nullability match the registry
      metadata for representative columns.
    - Defaults match for representative defaulted columns:
      `missing_reasons_json`, `zone1_seconds`, `cardiac_drift_significant`,
      `anomaly_count`, `heartrate_sample_count`.
    - Primary key columns are `activity_id` and `metric_version`.

    Add a test for `ensure_provenance_columns()` that creates an old-style temp
    `activity_metric_facts` table missing the late additive columns, calls the
    function, and verifies all currently allowed late columns were added with the
    registry-owned types.

    Also add a broad schema smoke assertion after `create_schema(conn)`:

    - every table listed in `DUCKDB_TABLES` exists;
    - the expected aggregate/read-model views still exist;
    - the existing activity metric index still exists.

    This smoke check protects the string-assembly cutover from accidentally
    dropping non-activity tables, indexes, or views while the activity table
    parity assertions still pass.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pytest tests/test_metric_registry.py tests/test_duckdb_repository.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - The strengthened tests fail before schema.py is cut over if schema.py still carries divergent hand-written definitions
    - Broad temp-DB smoke coverage proves all expected tables, views, and the existing activity metric index survive the cutover
    - Tests use `duckdb.connect(database=":memory:")` or temp files only
    - Tests do not read or mutate the live DuckDB file
  </acceptance_criteria>
  <done>Schema parity tests verify type/null/default/primary-key behavior, not names only</done>
</task>

<task type="auto">
  <name>Task 2: Generate activity_metric_facts table DDL from the registry</name>
  <files>src/mcp_strava/adapters/duckdb/schema.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/schema.py `DUCKDB_SCHEMA_SQL`, `create_schema`
    - src/mcp_strava/metric_registry.py generated DDL helpers
  </read_first>
  <action>
    Import the generated activity fact DDL helper from `metric_registry.py` and use
    it in `schema.py` so `create_schema(conn)` no longer owns a second hard-coded
    `activity_metric_facts` column list.

    A safe shape is:

    - Split the static schema string around the activity fact table section, or
      construct `DUCKDB_SCHEMA_SQL` from smaller string constants.
    - Insert `activity_metric_facts_table_sql()` exactly where the old table lived.
    - Leave non-activity fact tables (`daily_load_facts`, `training_model_daily`,
      `rolling_period_facts`) unchanged in this phase.
    - Leave indexes and views in `schema.py`; the view projection still explicitly
      controls derived view columns such as sample counts and summary_json fields.

    Preserve comments only where they still describe runtime behavior; do not keep
    duplicate column comments that now belong in registry descriptions.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run python -c "import duckdb; from mcp_strava.adapters.duckdb.schema import create_schema; conn=duckdb.connect(database=':memory:'); create_schema(conn); print(conn.execute('select count(*) from information_schema.columns where table_name=?', ['activity_metric_facts']).fetchone()[0])"</automated>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run python -c "import duckdb; from mcp_strava.adapters.duckdb.schema import DUCKDB_TABLES, create_schema; conn=duckdb.connect(database=':memory:'); create_schema(conn); tables={r[0] for r in conn.execute('show tables').fetchall()}; assert set(DUCKDB_TABLES).issubset(tables); print('schema smoke ok')"</automated>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pytest tests/test_metric_registry.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `schema.py` references `activity_metric_facts_table_sql`
    - The old inline `CREATE TABLE activity_metric_facts (` block no longer carries the full duplicate column list
    - `create_schema(conn)` succeeds on a memory DuckDB database
    - Existing views still create successfully
  </acceptance_criteria>
  <done>activity_metric_facts table creation is registry-generated while schema.py still owns indexes and views</done>
</task>

<task type="auto">
  <name>Task 3: Generate additive activity fact migrations from registry metadata</name>
  <files>src/mcp_strava/adapters/duckdb/schema.py tests/test_duckdb_repository.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/schema.py `ensure_provenance_columns`
    - src/mcp_strava/adapters/duckdb/repository.py `DuckDBRepository.__init__` handling around `ensure_provenance_columns`
    - tests/test_duckdb_repository.py tests that monkeypatch `ensure_provenance_columns`
  </read_first>
  <action>
    Replace the hand-written ALTER statement list with registry-generated SQL.

    Keep the additive policy explicit and narrow:

    - Define the current late column allowlist in `schema.py` as migration policy:
      `observed_min_hr`, `observed_max_hr`, `hr_zone_model`, `hr_max_used`,
      `hr_rest_used`, `calories_kcal`.
    - For each name, call the registry helper that renders
      `ALTER TABLE activity_metric_facts ADD COLUMN IF NOT EXISTS ...`.
    - If a name is missing from the registry, fail fast at import or call time.

    Do not attempt to ALTER every registry column. Some non-null historical columns
    are not safe to add to existing populated tables without defaults, and this
    phase is not a generic migration engine.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pytest tests/test_metric_registry.py tests/test_duckdb_repository.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `ensure_provenance_columns()` no longer embeds SQL types by hand for the late activity fact columns
    - The additive allowlist remains explicit and contains only the current late columns
    - Targeted tests prove generated ALTER SQL works against an old temp fixture
    - Repository initialization behavior around missing fresh tables remains unchanged
  </acceptance_criteria>
  <done>Activity fact additive migration SQL is generated from registry metadata and covered by temp-DB tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema metadata -> DuckDB DDL | Registry strings become runtime DuckDB DDL for one materialized fact table |
| existing DuckDB files -> additive migration | Repository init may call generated ADD COLUMN SQL on existing local DBs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-DRIFT | Tampering | generated activity_metric_facts DDL | mitigate | Strong temp-DB tests compare generated schema against registry metadata and representative defaults/nullability/PK |
| T-14-02-MIGRATION | Tampering | existing local DuckDB files | mitigate | Only the existing late-column allowlist is altered; generated SQL supplies types but does not expand migration scope |
| T-14-02-DATA | Information Disclosure | live Strava mirror | mitigate | Tests and verification use memory/temp DuckDB only; no live admin command or `/opt/docker/mcp-strava/data` mutation |
</threat_model>

<verification>
Run:

```
cd /home/j2h4u/repos/j2h4u/mcp-strava
uv run pytest tests/test_metric_registry.py tests/test_duckdb_repository.py -q -x
uv run pytest tests/test_read_model_materialization.py tests/test_training_aggregates.py -q -x
```
</verification>

<success_criteria>
- `schema.py` uses `activity_metric_facts_table_sql()` for table creation
- `ensure_provenance_columns()` uses registry-generated ADD COLUMN SQL for the current late activity fact columns
- Temp DuckDB schema tests verify names, representative types, nullability, defaults, and primary key
- Repository initialization compatibility tests still pass
- No live DB mutation occurs
</success_criteria>

<output>
Create `.planning/phases/14-metric-platform-registry-owned-fact-schema/14-02-SUMMARY.md` when done.
</output>

<review_feedback_addressed>
- Cycle 1 OpenCode/Codex MEDIUM: Plan 14-02 now requires broad temp-DB smoke
  coverage so `create_schema(conn)` cannot silently lose unrelated tables,
  views, or the activity metric index during the registry-generated DDL cutover.
</review_feedback_addressed>
