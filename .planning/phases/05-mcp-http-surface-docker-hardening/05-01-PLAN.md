---
phase: 05-mcp-http-surface-docker-hardening
plan: 05-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/application/metric_registry.py
  - src/mcp_strava/application/__init__.py
  - docs/metrics.md
  - tests/test_metric_registry.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - MCP-01
  - MCP-03
  - TEST-03
must_haves:
  truths:
    - "D-01: MCP metric contract is designed for intelligent agents and does not inherit CLI command names."
    - "D-02: registry classifies service outputs as prepared metrics/model facts, not coaching recommendations."
    - "D-03: exposed_in values use final tool ids `get_fitness_state`, `list_workouts`, `get_workout_detail`, `compare_periods`, and `project_fitness_state`."
    - "D-04: freshness/completeness are response metadata, not a standalone primary tool."
    - "D-07/D-08: every intentional synthetic metric is registered, mapped, or explicitly marked `excluded_interpretation` with numeric inputs preserved."
    - "D-09/D-10: registry metadata includes unit/source/scope/sport_scope/comparison_mode/directionality/requirements/missing_reasons/exposed_in and enables automatic global/per-sport comparison behavior."
    - "D-11: tests fail if current synthetic metrics disappear from registry coverage."
    - "D-18: recommendation/weekly-plan labels are excluded as interpretation while numeric/model facts remain available."
  artifacts:
    - path: "src/mcp_strava/application/metric_registry.py"
      provides: "MetricDefinition dataclass, complete METRIC_REGISTRY, helper functions, and excluded interpretation mapping"
    - path: "docs/metrics.md"
      provides: "Human-readable metric catalog for agent/tool documentation"
    - path: "tests/test_metric_registry.py"
      provides: "Registry completeness, metadata, and no-silent-loss tests"
---

<objective>
Create the Phase 5 metric registry that prevents accidental loss of synthetic metrics and becomes the source of truth for MCP metric exposure.

Output: metric definition types, complete registry with comparison metadata, explicit excluded interpretation list, docs, and tests proving all existing computed metrics are accounted for.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-RESEARCH.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-PATTERNS.md
@src/mcp_strava/types.py
@src/mcp_strava/metrics.py
@src/mcp_strava/training.py
@src/mcp_strava/analytics.py
@src/mcp_strava/report.py
@tests/test_application_reports.py
@tests/test_application_workouts.py
</context>

<decision_traceability>
- D-01 through D-05 are reflected in final tool ids and no standalone data-status exposure.
- D-07 through D-11 are implemented directly by registry definitions and tests.
- D-12 through D-14 are prepared by comparison metadata used in later plans.
- D-18 is implemented by explicit interpretation exclusions and numeric/model facts.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing metric registry coverage tests</name>
  <files>tests/test_metric_registry.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/types.py
    - src/mcp_strava/metrics.py
    - src/mcp_strava/training.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/report.py
    - .planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
    - .planning/phases/05-mcp-http-surface-docker-hardening/05-RESEARCH.md
  </read_first>
  <action>Create `tests/test_metric_registry.py` with source-level tests that import `mcp_strava.application.metric_registry`. Assert `METRIC_REGISTRY` contains registry ids for: `activity_id`, `activity_date`, `sport_type`, `activity_name`, `distance_km`, `moving_time_min`, `elapsed_time_min`, `elevation_m`, `start_time`, `trimp`, `avg_hr`, `max_hr`, `time_in_hr_zones_min`, `hr_recovery_pauses`, `hr_recovery_total_rest_sec`, `hr_recovery_median_bpm_per_min`, `hr_recovery_best_bpm_per_min`, `hr_recovery_worst_bpm_per_min`, `hr_recovery_avg_bpm_per_min`, `vertical_speed_m_per_h`, `vertical_ascent_m`, `vertical_duration_h`, `cardiac_cost`, `cardiac_cost_adjusted`, `cardiac_drift_pct`, `cardiac_drift_severity`, `cardiac_drift_quality`, `cardiac_drift_significant`, `hrr_pct`, `z5_seconds`, `hr_anomaly_count`, `daily_trimp`, `total_trimp_14d`, `avg_trimp_per_day`, `active_days`, `rest_days`, `weekly_trimp`, `by_sport_trimp`, `by_sport_distance_km`, `by_sport_time_min`, `by_sport_elevation_m`, `fitness`, `fatigue`, `form`, `form_zone`, `banister_history`, `acwr`, `acwr_zone`, `atl`, `ctl`, `acwr_history`, `daily_avg_trimp_7d`, `daily_avg_trimp_28d`, `daily_avg_trimp_90d`, `rolling_median_cc`, `rolling_median_cc_adj`, `rolling_median_epkm`, `volume_7d`, `volume_28d`, `load_trend_pct`, `efficiency_trend_pct`, `run_90d_median_cc_trend_pct`, `activity_streak_days`, `rest_streak_days`, `last_hike_days_ago`, `progressive_load_bonus`, `progressive_cc_trends`, `projected_daily_trimp`, `projected_fitness`, `projected_fatigue`, `projected_form`, `target_date_form`, `post_weekend_monday_form`, and `activity_template_trimp`. Assert every registry entry has non-empty `metric_id`, `unit`, `source`, `scope`, `sport_scope`, `comparison_mode`, `directionality`, `requirements`, `missing_reasons`, and `exposed_in`. Assert `sport_scope` is one of `global`, `per_sport`, or `both`; `comparison_mode` is one of `sum`, `avg`, `median`, `last`, `min`, `max`, `trend`, `distribution`, or `none`; and `exposed_in` contains only final MCP tool ids from D-03. Add tests asserting `EXCLUDED_INTERPRETATIONS` includes `recommendation.action`, `recommendation.intensity`, `recommendation.confidence`, `weekly_plan.on_track`, `weekly_plan.plan_days.activity`, and natural-language safety warning text, each with `preserved_metric_ids` pointing to numeric/model facts. Extend `tests/test_smoke.py` to import the metric registry module.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_registry.py tests/test_smoke.py -q</automated>
  </verify>
  <done>Tests fail before `metric_registry.py` exists and before registry coverage is complete.</done>
  <acceptance_criteria>
    - `tests/test_metric_registry.py` names all required metric ids listed in this task action.
    - Tests reject unknown `sport_scope`, `comparison_mode`, and `exposed_in` values.
    - Tests assert interpretation exclusions preserve at least one numeric/model input id.
    - `tests/test_smoke.py` imports `mcp_strava.application.metric_registry`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registry dataclasses and complete metric catalog</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/application/metric_registry.py, src/mcp_strava/application/__init__.py</files>
  <read_first>
    - src/mcp_strava/types.py
    - src/mcp_strava/metrics.py
    - src/mcp_strava/training.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/report.py
    - tests/test_metric_registry.py
  </read_first>
  <action>Add dataclasses to `src/mcp_strava/types.py`: `MetricDefinition(metric_id: str, label: str, unit: str, source: str, scope: str, sport_scope: str, comparison_mode: str, directionality: str, requirements: list[str], missing_reasons: list[str], exposed_in: list[str], description: str = "")` and `ExcludedInterpretation(field: str, reason: str, preserved_metric_ids: list[str])`. Create `src/mcp_strava/application/metric_registry.py` with constants `MCP_TOOL_IDS = ("get_fitness_state", "list_workouts", "get_workout_detail", "compare_periods", "project_fitness_state")`, `METRIC_REGISTRY: dict[str, MetricDefinition]`, `EXCLUDED_INTERPRETATIONS: dict[str, ExcludedInterpretation]`, `metric_definition(metric_id: str) -> MetricDefinition`, `metrics_for_tool(tool_id: str) -> list[MetricDefinition]`, `comparable_metrics(scope: str | None = None, sport_scope: str | None = None) -> list[MetricDefinition]`, and `metric_catalog_payload() -> dict[str, object]`. Populate registry entries for all ids asserted by Task 1. Mark body/load/model metrics such as `trimp`, `fitness`, `fatigue`, `form`, `acwr`, `atl`, and `ctl` as `sport_scope="global"`; mark cardiac cost, cardiac drift, HR recovery, HRR percentage, vertical speed, elevation density, and pace/efficiency metrics as `per_sport`; mark volume metrics such as distance, duration, elevation, activity counts, and zone distributions as `both` where global and per-sport aggregation are valid. Use `comparison_mode="none"` for identity fields and `distribution` for zone maps. Keep units explicit, for example `km`, `minutes`, `seconds`, `m`, `m_per_hour`, `bpm`, `bpm_per_min`, `percent`, `trimp`, `model_units`, `ratio`, and `count`. Do not include coaching labels such as `should`, `ready`, `recommended`, or `best_scenario` as metrics.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_registry.py tests/test_smoke.py -q</automated>
  </verify>
  <done>Metric registry tests pass and all synthetic metrics are accounted for.</done>
  <acceptance_criteria>
    - `src/mcp_strava/application/metric_registry.py` defines `METRIC_REGISTRY`, `EXCLUDED_INTERPRETATIONS`, and `MCP_TOOL_IDS`.
    - `len(METRIC_REGISTRY)` is greater than 50.
    - `EXCLUDED_INTERPRETATIONS["recommendation.action"].preserved_metric_ids` contains `form`, `weekly_trimp`, and `active_days`.
    - `metrics_for_tool("get_data_status")` is not valid because `get_data_status` is not in `MCP_TOOL_IDS`.
    - `python3 -m pytest tests/test_metric_registry.py tests/test_smoke.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document the metric registry contract</name>
  <files>docs/metrics.md, tests/test_metric_registry.py</files>
  <read_first>
    - src/mcp_strava/application/metric_registry.py
    - .planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
    - tests/test_metric_registry.py
  </read_first>
  <action>Create `docs/metrics.md` with a compact table generated or manually synchronized from `METRIC_REGISTRY`. The table must include columns `metric_id`, `unit`, `scope`, `sport_scope`, `comparison_mode`, `directionality`, and `exposed_in`. Add a section `Excluded interpretation fields` listing every `EXCLUDED_INTERPRETATIONS` key and the preserved metric ids. Add a short contract note that MCP tools expose prepared metrics and model facts only; downstream intelligent agents perform human-facing interpretation. Extend `tests/test_metric_registry.py` with a source assertion that `docs/metrics.md` contains every registry `metric_id` and every excluded interpretation key.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_registry.py -q</automated>
  </verify>
  <done>Metric docs are synchronized with the registry.</done>
  <acceptance_criteria>
    - `docs/metrics.md` contains the table headers `metric_id`, `sport_scope`, `comparison_mode`, and `exposed_in`.
    - Every key in `METRIC_REGISTRY` appears in `docs/metrics.md`.
    - Every key in `EXCLUDED_INTERPRETATIONS` appears in `docs/metrics.md`.
    - `python3 -m pytest tests/test_metric_registry.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code metrics -> registry | Existing synthetic metrics must not be silently dropped when designing MCP. |
| Registry -> MCP services | MCP services must expose only registered metrics or explicit metadata. |
| Model facts -> interpretation | Coaching labels must not be represented as metric facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Metric registry | mitigate | Registry coverage tests assert all current synthetic metrics and exclusions. |
| T-05-02 | Information Integrity | Interpretation exclusions | mitigate | `EXCLUDED_INTERPRETATIONS` preserves numeric/model inputs while rejecting advice labels. |
| T-05-03 | Repudiation | Metric docs | mitigate | `docs/metrics.md` is tested against the registry so contract drift is visible. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_metric_registry.py tests/test_smoke.py -q`
- `just test`
</verification>

<success_criteria>
- Complete metric registry exists and imports cleanly.
- All current synthetic metrics are registered, mapped, or explicitly excluded as interpretation.
- Registry metadata supports automatic period comparison and MCP tool exposure.
- Metric docs are synchronized with registry ids.
</success_criteria>

<output>
Create `.planning/phases/05-mcp-http-surface-docker-hardening/05-01-SUMMARY.md` when done.
</output>
