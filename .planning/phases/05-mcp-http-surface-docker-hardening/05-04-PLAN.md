---
phase: 05-mcp-http-surface-docker-hardening
plan: 05-04
type: tdd
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - pyproject.toml
  - src/mcp_strava/interfaces/__init__.py
  - src/mcp_strava/interfaces/mcp_http.py
  - src/mcp_strava/application/metric_services.py
  - tests/test_mcp_surface.py
  - tests/test_security_guards.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - TEST-03
must_haves:
  truths:
    - "D-01: MCP tool names are final agent-facing names, not CLI service names."
    - "D-02/D-14: MCP tools expose prepared metrics and model facts only."
    - "D-03: MCP tool allowlist is exactly `get_fitness_state`, `list_workouts`, `get_workout_detail`, `compare_periods`, and `project_fitness_state`."
    - "D-04/D-05: no `get_data_status`, planned-activity, trip-readiness, or route-readiness tool is exposed in Phase 5."
    - "D-06: MCP registry excludes sync, backfill, token refresh, raw Strava API, arbitrary SQL, sync logs, admin, and debug operations."
    - "D-19/D-21/D-22: HTTP MCP backend is designed for local/shared Docker gateway use and is not a public standalone exposure."
    - "MCP-04: local profile rejects unsafe bind/origin combinations; container/gateway profile must be explicit."
  artifacts:
    - path: "src/mcp_strava/interfaces/mcp_http.py"
      provides: "FastMCP Streamable HTTP server, tool registration, bind/origin guard, and direct main entrypoint"
    - path: "tests/test_mcp_surface.py"
      provides: "Tool allowlist, annotations, structuredContent, metadata, and unsafe-bind tests"
---

<objective>
Expose the five metric services through a read-only HTTP MCP server with a strict tool allowlist, structured responses, and local/container-safe HTTP configuration.

Output: MCP dependency, server module, tool registry tests, forbidden-tool tests, and startup guards for unsafe bind/origin settings.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-RESEARCH.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-PATTERNS.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-02-PLAN.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-03-PLAN.md
@pyproject.toml
@src/mcp_strava/settings.py
@src/mcp_strava/application/metric_services.py
@src/mcp_strava/application/metric_registry.py
@src/mcp_strava/types.py
@tests/test_security_guards.py
</context>

<decision_traceability>
- D-01 through D-06 are enforced by the MCP tool allowlist and forbidden-name tests.
- D-19 through D-22 are enforced by HTTP bind/default behavior.
- MCP-01 through MCP-04 and TEST-03 are covered by this plan.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing MCP surface contract tests</name>
  <files>tests/test_mcp_surface.py, tests/test_security_guards.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/settings.py
    - tests/test_security_guards.py
    - .planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
  </read_first>
  <action>Create `tests/test_mcp_surface.py` with tests importing `mcp_strava.interfaces.mcp_http`. Assert `MCP_TOOL_NAMES` or equivalent exported allowlist is exactly `("get_fitness_state", "list_workouts", "get_workout_detail", "compare_periods", "project_fitness_state")`. Assert forbidden names are absent: `get_data_status`, `daily_report`, `weekly_summary`, `recent_workouts`, `workout_analytics`, `freshness`, `sync`, `backfill`, `sql`, `raw`, `token`, `token_refresh`, `admin`, `log`, `sync_log`, `db_preflight`, `db_check`, `db_migrate`, and `mirror_refresh`. Test each tool descriptor has annotations equivalent to `readOnlyHint=True`, `destructiveHint=False`, `idempotentHint=True`, and `openWorldHint=False`, plus a JSON input schema and structured output path. Add a fixture that monkeypatches metric service functions to return a tiny `ServiceEnvelope`; call each registered tool through the server's in-process registry or official MCP Python SDK test client and assert the returned `structuredContent` has `data`, `freshness`, `completeness`, `warnings`, and `rationale`. Add startup-guard tests for `validate_http_settings(settings)`: local profile with host `0.0.0.0` raises `ValueError`; local profile with host `127.0.0.1` passes; container profile with host `0.0.0.0` passes only when an explicit allow flag/env such as `MCP_STRAVA_ALLOW_CONTAINER_BIND=1` is present. Extend `tests/test_security_guards.py` with an AST guard rejecting imports of Strava adapter, sync runtime, token refresh, raw SQL helper, or CLI command registry from `src/mcp_strava/interfaces/mcp_http.py`. Extend `tests/test_smoke.py` to import the MCP module.</action>
  <verify>
    <automated>python3 -m pytest tests/test_mcp_surface.py tests/test_security_guards.py tests/test_smoke.py -q</automated>
  </verify>
  <done>Tests fail before MCP dependency and server module exist.</done>
  <acceptance_criteria>
    - `tests/test_mcp_surface.py` asserts the exact five-tool allowlist.
    - Tests prove old CLI/product names are not MCP tool names.
    - Tests prove forbidden sync/admin/debug/raw names are absent.
    - Tests exercise structured content for every tool.
    - Tests cover unsafe bind rejection.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add MCP SDK dependency and HTTP server module</name>
  <files>pyproject.toml, src/mcp_strava/interfaces/__init__.py, src/mcp_strava/interfaces/mcp_http.py, tests/test_mcp_surface.py</files>
  <read_first>
    - pyproject.toml
    - src/mcp_strava/settings.py
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/types.py
    - tests/test_mcp_surface.py
  </read_first>
  <action>Add runtime dependency `mcp>=1.21,<2` to `pyproject.toml`. Create `src/mcp_strava/interfaces/__init__.py` and `src/mcp_strava/interfaces/mcp_http.py`. In `mcp_http.py`, implement `MCP_TOOL_NAMES` as the exact tuple from Task 1, `FORBIDDEN_TOOL_NAMES` for tests, `validate_http_settings(settings, environ=None) -> None`, `build_mcp_server()`, and `main(argv: list[str] | None = None) -> int`. Use the official MCP Python SDK FastMCP server with Streamable HTTP at `/mcp`. Register exactly five tools that call `get_fitness_state_service`, `list_workouts_service`, `get_workout_detail_service`, `compare_periods_service`, and `project_fitness_state_service`. Each tool must convert `ServiceEnvelope` to JSON-safe structured content through `dc_to_dict()` and must not duplicate large JSON into text content. Tool descriptions must use factual verbs such as `returns`, `lists`, `compares`, and `projects`; avoid words `coach`, `recommend`, `should`, `ready`, and `sync`. Add tool annotations through the SDK's public annotation API so list-tools metadata has read-only/idempotent/non-destructive hints. `validate_http_settings` must reject `0.0.0.0`, `::`, and public bind hosts under `runtime_profile="local"`; allow `0.0.0.0` only under `runtime_profile="container"` and explicit `MCP_STRAVA_ALLOW_CONTAINER_BIND=1`. If SDK exposes Origin middleware/settings, configure allowed origins for localhost and gateway/container usage; otherwise keep the bind guard and document the SDK limitation in module comments. Do not import `mcp_strava.cli`, `mcp_strava.sync`, `mcp_strava.adapters.strava`, or `mcp_strava.refresh.runtime`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_mcp_surface.py tests/test_security_guards.py tests/test_smoke.py -q</automated>
  </verify>
  <done>MCP server module imports, tools list, and bind guards pass tests.</done>
  <acceptance_criteria>
    - `pyproject.toml` includes `mcp>=1.21,<2` in `[project].dependencies`.
    - `src/mcp_strava/interfaces/mcp_http.py` defines `build_mcp_server`, `main`, `MCP_TOOL_NAMES`, and `validate_http_settings`.
    - Tool registration count is exactly five.
    - `validate_http_settings` rejects local `0.0.0.0` and allows explicit container bind.
    - `python3 -m pytest tests/test_mcp_surface.py tests/test_security_guards.py tests/test_smoke.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify MCP structured responses against fixture services</name>
  <files>tests/test_mcp_surface.py, src/mcp_strava/interfaces/mcp_http.py</files>
  <read_first>
    - tests/test_mcp_surface.py
    - src/mcp_strava/interfaces/mcp_http.py
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/types.py
  </read_first>
  <action>Complete tests so they call each MCP tool through the official SDK in-process test client when available, or a narrow adapter around `build_mcp_server()` if the SDK test helper is unavailable. Assertions must prove each call returns `structuredContent` with exactly the shared envelope keys, `isError` is false for fixture success cases, and text content is only a short factual summary such as the tool title or omitted entirely. Add one failure-path test for missing workout id that returns `isError` false with `completeness.status == "unavailable"` and `warnings` containing `workout_not_found`, preserving the product contract that missing data is factual metadata rather than transport failure.</action>
  <verify>
    <automated>python3 -m pytest tests/test_mcp_surface.py tests/test_metric_services.py -q</automated>
  </verify>
  <done>MCP structured response tests pass for success and unavailable-workout cases.</done>
  <acceptance_criteria>
    - Every MCP tool test asserts `structuredContent["freshness"]` and `structuredContent["completeness"]`.
    - Missing workout id is represented as unavailable data, not a sync/admin action and not an exception stack.
    - `python3 -m pytest tests/test_mcp_surface.py tests/test_metric_services.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> local backend | Unauthenticated backend must stay local/container-network scoped. |
| MCP tools -> application services | Tools may call metric services only, not CLI/admin/sync code. |
| MCP response -> agent | Response must be structured metric facts with trust metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of Privilege | MCP tool registry | mitigate | Exact allowlist tests and forbidden-name tests. |
| T-05-11 | Information Disclosure | MCP HTTP backend | mitigate | Bind guard rejects public local exposure and Docker plan avoids host ports. |
| T-05-12 | Tampering | MCP tool implementation | mitigate | Import guard rejects CLI/sync/Strava/token/runtime imports. |
| T-05-13 | Information Integrity | MCP response content | mitigate | StructuredContent tests require envelope metadata and no coaching/admin fields. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_mcp_surface.py tests/test_metric_services.py tests/test_security_guards.py tests/test_smoke.py -q`
- `just test`
</verification>

<success_criteria>
- MCP HTTP server registers exactly five read-only metric tools.
- MCP tools return structured envelope content with freshness/completeness metadata.
- Forbidden sync/admin/debug/raw surfaces are absent.
- Unsafe local bind settings fail before serving.
</success_criteria>

<output>
Create `.planning/phases/05-mcp-http-surface-docker-hardening/05-04-SUMMARY.md` when done.
</output>
