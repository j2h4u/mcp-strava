---
phase: 12-decouple-db-py-into-focused-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/adapters/duckdb/connection.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "MirrorConn and ReadConn context managers exist in adapters/duckdb/connection.py with identical behavior to the former db.py DbConn/ReadConn"
    - "The thread-local read-connection pool (reuse + evict-on-error + reset) behaves exactly as before"
    - "reset_thread_connections() is importable from adapters/duckdb/connection and clears the same thread-local the read path uses"
  artifacts:
    - path: "src/mcp_strava/adapters/duckdb/connection.py"
      provides: "MirrorConn, ReadConn, _thread_state, _thread_read_connections, reset_thread_connections, _db_path"
      contains: "class MirrorConn"
  key_links:
    - from: "src/mcp_strava/adapters/duckdb/connection.py"
      to: "open_expected_mirror_db"
      via: "MirrorConn/ReadConn __enter__ open the mirror via the existing in-module helper"
      pattern: "open_expected_mirror_db"
---

<objective>
Land the DuckDB connection-lifetime helpers in their adapter home (D-01, D-02, D-03). Add `MirrorConn` (renamed from `DbConn`), `ReadConn`, the thread-local read pool, `reset_thread_connections`, and `_db_path` into `adapters/duckdb/connection.py` — beside `open_expected_mirror_db`, which they already depend on.

This is an ADDITIVE step in the D-10 hard-cut order: the new home is created now; callers still import from `db.py` until their own migration plans (12-03, 12-04). `db.py` is NOT edited or deleted here. Dead `init_db` is NOT relocated — it is deleted later (12-02 records the rationale; the function leaves the tree when db.py is deleted in 12-05).

Purpose: DuckDB connection-lifetime logic belongs in the DuckDB adapter, not a legacy glue module (GP-04 dependency rule).
Output: `MirrorConn`/`ReadConn`/`reset_thread_connections`/`_db_path` defined in `connection.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/adapters/duckdb/connection.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move MirrorConn + ReadConn + thread-local pool into connection.py</name>
  <files>src/mcp_strava/adapters/duckdb/connection.py</files>
  <read_first>
    - src/mcp_strava/db.py (the source definitions: DbConn L35-44, _thread_state L56, _thread_read_connections L59-64, ReadConn L67-94, reset_thread_connections L97-107, _db_path L20-21, _open_storage_connection L28-29)
    - src/mcp_strava/adapters/duckdb/connection.py (target; already has open_expected_mirror_db)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md (verbatim-move unit + Pitfall 1)
  </read_first>
  <action>
    Add to adapters/duckdb/connection.py: `import threading` and `from mcp_strava.settings import get_settings`. Define `_db_path() -> str` returning `str(get_settings().database_path)`. Define `class MirrorConn` (renamed from DbConn per D-03) as a context manager whose `__enter__` opens via `open_expected_mirror_db(_db_path())` and `__exit__` closes — the former DbConn used `_open_storage_connection` which is just `open_expected_mirror_db`; collapse to a direct call. Move `_thread_state = threading.local()`, `_thread_read_connections`, `class ReadConn`, and `reset_thread_connections` VERBATIM as a single unit (D-02) — preserve ReadConn's evict-on-error `__exit__` exactly so the thread-local and its reset hook share one object identity (PATTERNS Pitfall 1). Do NOT relocate `init_db` (dead code per RESEARCH RI-03; deleted with db.py in 12-05). Do NOT edit db.py in this plan. Per D-09: the athlete-zones orchestration (`get_zones`) is dead and is deleted with db.py, not built as a service.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_strava.adapters.duckdb.connection import MirrorConn, ReadConn, reset_thread_connections, _db_path; print('ok')"</automated>
  </verify>
  <done>MirrorConn, ReadConn, reset_thread_connections, _db_path importable from adapters/duckdb/connection; ReadConn retains evict-on-error semantics; init_db NOT present in connection.py.</done>
</task>

<task type="auto">
  <name>Task 2: Lint/typecheck the new connection home and confirm suite still green</name>
  <files>src/mcp_strava/adapters/duckdb/connection.py</files>
  <read_first>
    - src/mcp_strava/adapters/duckdb/connection.py (the edited file)
    - Justfile (lint/typecheck/test recipes)
  </read_first>
  <action>
    Run `uv run ruff check src/mcp_strava/adapters/duckdb/connection.py` and `uv run ruff format --check src/mcp_strava/adapters/duckdb/connection.py`; fix any findings (line-length 120, import order I). Run `uv run pyright src` to confirm no new type errors from the additions. Confirm the full local suite is still green — callers still import from db.py at this point, so nothing should break (additive step).
  </action>
  <verify>
    <automated>uv run ruff check src/mcp_strava/adapters/duckdb/connection.py && uv run pytest -q -x</automated>
  </verify>
  <done>ruff clean on connection.py; pyright reports no new errors; `uv run pytest -q` green (323 tests) — additive change broke nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → DuckDB file | connection open/close + lock translation (unchanged — relocated only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | thread-local read pool relocation | mitigate | Move `_thread_state`+`ReadConn`+`reset_thread_connections` as one unit so conftest reset clears the read path's pool; verified by test_metric_services reuse test in 12-04 and full suite in Task 2 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `uv run python -c "from mcp_strava.adapters.duckdb.connection import MirrorConn, ReadConn, reset_thread_connections"` succeeds.
- `uv run pytest -q` green (additive; no callers migrated yet).
- `uv run ruff check src` and `uv run pyright src` clean.
</verification>

<success_criteria>
Connection helpers live in `adapters/duckdb/connection.py` with `DbConn`→`MirrorConn` rename and verbatim thread-pool semantics; db.py untouched; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-01-SUMMARY.md` when done.
</output>
