---
phase: 12-decouple-db-py-into-focused-modules
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/mcp_strava/application/aggregate_services.py
  - src/mcp_strava/application/metric_services.py
  - src/mcp_strava/application/product_facts.py
  - src/mcp_strava/application/freshness.py
  - src/mcp_strava/application/mirror_coverage.py
  - src/mcp_strava/sync.py
  - src/mcp_strava/refresh/bootstrap.py
  - src/mcp_strava/refresh/worker.py
  - tests/conftest.py
  - tests/test_metric_services.py
  - tests/test_refresh_runtime.py
  - tests/test_application_services.py
  - tests/test_security_guards.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "All application/* + refresh/* + sync.py callers import connection helpers and the StravaClient from the new homes, not from mcp_strava.db"
    - "repository_from_connection / repository_from_path indirection is gone (D-04); callers use DuckDBRepository.from_connection / from_path directly"
    - "bootstrap.py uses SystemClock/SystemSleeper, builds its transport via the shared _build_strava_transport helper (no parallel chain), and sources creds via the settings token-file accessor; its duplicate _read_token_values/_required_strava_client is removed (D-05/D-06)"
    - "bootstrap keeps RealClock=SystemClock / RealSleeper=SystemSleeper aliases (re-exported by sync) so cli.py imports stay resolvable through wave 3; importing mcp_strava.cli succeeds after this plan"
    - "every test monkeypatch site that exercises the application/refresh/sync code migrated in THIS wave is retargeted in THIS wave (review HIGH — wave placement): conftest.py reset pool, test_metric_services.py (_db_path/open_expected_mirror_db/reset_thread_connections + metric_services.repository_from_connection), test_refresh_runtime.py (worker.DbConn x6 + worker.repository_from_connection), test_application_services.py (freshness.repository_from_connection), and the sync.DbConn->sync.MirrorConn sites in test_security_guards.py — so this wave lands green when its own Task 3 runs the FULL suite"
    - "no monkeypatch.setattr(..., \"DbConn\"/\"repository_from_connection\", ...) in the retargeted-here files points at a renamed/removed attribute (plain setattr would error) or silently no-ops via raising=False (would pass vacuously)"
  artifacts:
    - path: "src/mcp_strava/sync.py"
      provides: "refresh entrypoints using MirrorConn + direct repository factory"
      contains: "MirrorConn"
  key_links:
    - from: "src/mcp_strava/application/metric_services.py"
      to: "src/mcp_strava/adapters/duckdb/connection.py"
      via: "imports ReadConn from the adapter home"
      pattern: "from mcp_strava.adapters.duckdb.connection import"
---

<objective>
Migrate the application, refresh, and sync callers off `db.py` onto the homes landed in 12-01/12-02 (D-10 steps 2-3). Concretely:
- `application/{aggregate_services,metric_services,product_facts,freshness,mirror_coverage}.py`: import `ReadConn`/`MirrorConn` from `adapters/duckdb/connection`; replace `repository_from_connection(conn)` with `DuckDBRepository.from_connection(conn)` (D-04).
- `sync.py`, `refresh/worker.py`: import `MirrorConn` from the adapter home; use direct repository factory.
- `refresh/bootstrap.py`: import `MirrorConn` + direct factory; switch `RealClock`/`RealSleeper` to `SystemClock`/`SystemSleeper`; source `STRAVA_CLIENT_ID`/`SECRET` from settings (D-06) and remove its duplicate `_read_token_values`/`_required_strava_client` env parser (D-05).

This is a behavior-preserving mechanical migration. `db.py` is still present (deleted in 12-05) — but after this plan no production module under application/refresh/sync imports from it.

**Wave-placement fix (review HIGH — Codex; replan 2026-05-30):** because this plan's own verification runs `test_refresh_runtime.py`, `test_security_guards.py`, and the FULL `uv run pytest -q`, the test monkeypatch retargets that correspond to the production code migrated HERE must also land HERE — they cannot be deferred to 12-04 or this wave will not be green mid-migration. Task 3 (new) retargets `conftest.py`, `test_metric_services.py`, `test_refresh_runtime.py`, `test_application_services.py`, and the `sync.DbConn`->`sync.MirrorConn` sites in `test_security_guards.py`. CLI-specific test retargets (`test_cli_surface.py`, cli guards, `DbConn`->`MirrorConn` in `test_smoke.py`/`test_phase01_validation.py`, and the `test_repository_boundary.py` retargets) stay in 12-04 alongside the cli.py migration.

Purpose: honor the dependency rule (GP-04) and remove pure-indirection factories (D-04, GP-01).
Output: all listed src callers routed through the new homes; the tests that exercise them retargeted in the same wave; suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/refresh/bootstrap.py
@src/mcp_strava/sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint application/* imports to new homes + direct repository factory (D-04)</name>
  <files>src/mcp_strava/application/aggregate_services.py, src/mcp_strava/application/metric_services.py, src/mcp_strava/application/product_facts.py, src/mcp_strava/application/freshness.py, src/mcp_strava/application/mirror_coverage.py</files>
  <read_first>
    - src/mcp_strava/db.py (current symbol names: ReadConn, DbConn→MirrorConn, repository_from_connection)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Blast Radius table — exact per-file imports)
    - src/mcp_strava/adapters/duckdb/connection.py (new home, from 12-01)
    - each of the five application files (current usage of the imported symbols)
  </read_first>
  <action>
    In each of the five application files, change `from mcp_strava.db import ...` to import `ReadConn` and/or `MirrorConn` from `mcp_strava.adapters.duckdb.connection`. Replace every `repository_from_connection(conn)` call with `DuckDBRepository.from_connection(conn)` (import `DuckDBRepository` from `mcp_strava.adapters.duckdb.repository` if not already imported) per D-04. `aggregate_services`/`metric_services` import `ReadConn, repository_from_connection`; `product_facts` imports `ReadConn`; `freshness`/`mirror_coverage` import `DbConn, repository_from_connection` (→ `MirrorConn` + direct factory). No behavior change — same connection objects, same repository.
  </action>
  <verify>
    <automated>uv run python -c "import mcp_strava.application.aggregate_services, mcp_strava.application.metric_services, mcp_strava.application.product_facts, mcp_strava.application.freshness, mcp_strava.application.mirror_coverage; print('app-imports-ok')" && ! rg -q "from mcp_strava\.db import|import mcp_strava\.db" src/mcp_strava/application</automated>
  </verify>
  <done>No application/* file imports from mcp_strava.db (grep fail-closed); all use adapters/duckdb/connection + DuckDBRepository.from_connection; all five modules import cleanly. NOTE: the test suites that monkeypatch these modules (test_metric_services.py, test_application_services.py) are retargeted in Task 3 of THIS plan — do not run them here, they would fail against the still-old monkeypatch targets.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sync.py + refresh/worker.py + refresh/bootstrap.py</name>
  <files>src/mcp_strava/sync.py, src/mcp_strava/refresh/worker.py, src/mcp_strava/refresh/bootstrap.py</files>
  <read_first>
    - src/mcp_strava/refresh/bootstrap.py (RealClock/RealSleeper L20-27, _read_token_values L64-73, _required_strava_client L76-83, build_refresh_collaborators L86-100, DbConn import L14)
    - src/mcp_strava/sync.py (DbConn import L7, RealClock/RealSleeper re-exports L9-18)
    - src/mcp_strava/refresh/worker.py (DbConn + repository_from_connection import L16)
    - src/mcp_strava/settings.py (cred resolution from 12-02 Task 1)
    - src/mcp_strava/adapters/strava/clock.py (SystemClock/SystemSleeper from 12-02 Task 2)
  </read_first>
  <action>
    In `worker.py` and `sync.py`: import `MirrorConn` from `adapters/duckdb/connection` (replacing `DbConn`) and use `DuckDBRepository.from_connection` directly (D-04). In `bootstrap.py`: import `MirrorConn` + direct factory; build its transport by calling the SHARED `_build_strava_transport(settings, clock, sleeper)` helper added to `adapters/strava/client.py` in 12-02 (do NOT keep a second parallel wiring chain — review suggestion); use `SystemClock()`/`SystemSleeper()` for the clock/sleeper; remove the duplicate `_read_token_values`/`_required_strava_client` env parser and source creds via the settings token-file accessor from 12-02 Task 1 (D-05/D-06). Keep `build_refresh_collaborators`'s return tuple and behavior identical.

    **CLI import-ordering safety (review HIGH — Codex divergent view, verified: cli.py L19-23 AND sync.py L9-18 both import `RealClock`/`RealSleeper` from `mcp_strava.refresh.bootstrap`).** Do NOT delete the `RealClock`/`RealSleeper` names from bootstrap in this wave. Instead, after defining the migration to `SystemClock`/`SystemSleeper`, keep backward-name aliases in `bootstrap.py` through wave 3: `RealClock = SystemClock` and `RealSleeper = SystemSleeper` (module-level assignments, replacing the old class bodies). This keeps cli.py's `from mcp_strava.refresh.bootstrap import RealClock, RealSleeper` (and sync.py's re-export at L9-18) resolvable until cli.py migrates in 12-04. sync.py keeps re-exporting `RealClock`/`RealSleeper` in its `__all__` for the same reason. These two aliases are deleted in 12-04 (Task 1) once cli.py no longer imports them. Net effect: no import break mid-wave; the suite stays importable after 12-03.
  </action>
  <verify>
    <automated>uv run python -c "import mcp_strava.sync, mcp_strava.refresh.worker, mcp_strava.refresh.bootstrap, mcp_strava.cli; from mcp_strava.refresh.bootstrap import RealClock, RealSleeper; print('ok')" && ! rg -q "from mcp_strava\.db import|import mcp_strava\.db" src/mcp_strava/sync.py src/mcp_strava/refresh/worker.py src/mcp_strava/refresh/bootstrap.py</automated>
  </verify>
  <done>sync/worker/bootstrap import from new homes (db.py import grep fail-closed); bootstrap builds transport via the shared 12-02 helper, uses SystemClock/SystemSleeper + settings token-file creds, duplicate env parser removed; RealClock/RealSleeper aliases retained on bootstrap (and re-exported by sync) so cli.py still imports cleanly; importing mcp_strava.cli succeeds. NOTE: test_refresh_runtime.py + the sync.DbConn sites in test_security_guards.py are retargeted in Task 3 of THIS plan — do not run them here, the un-retargeted worker.DbConn plain-setattr would error.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget the application/refresh/sync test monkeypatch sites migrated in this wave (review HIGH — wave placement)</name>
  <files>tests/conftest.py, tests/test_metric_services.py, tests/test_refresh_runtime.py, tests/test_application_services.py, tests/test_security_guards.py</files>
  <read_first>
    - tests/conftest.py (L58 `from mcp_strava.db import reset_thread_connections` + isolation fixture — RI-01: must keep calling it)
    - tests/test_metric_services.py (L368 `import mcp_strava.db as db`; L379 monkeypatch db._db_path; L388 db.open_expected_mirror_db; db.reset_thread_connections; L653 `metric_services.repository_from_connection`)
    - tests/test_refresh_runtime.py (L581/582, 621/622, 723/724, 785/786, 838/839, 866: `monkeypatch.setattr(worker, "DbConn", ...)` and `monkeypatch.setattr(worker, "repository_from_connection", ...)`)
    - tests/test_application_services.py (L203: `monkeypatch.setattr(freshness, "repository_from_connection", ...)`)
    - tests/test_security_guards.py (L637 + L700 `monkeypatch.setattr(sync, "DbConn", FakeDbConn)` — ONLY these two sync.DbConn sites belong to this wave; the init_db DDL guards and negative-import literals stay in 12-04 Task 3)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Test couplings table + Pitfall 1/2)
  </read_first>
  <action>
    Patch at the LOOKUP SITE (the module object that actually resolves the name at call time) so interception still works. These are exactly the test sites that exercise the production code migrated in Tasks 1-2; retargeting them here keeps THIS wave green when Task 4 runs the full suite. Retarget:
    - conftest.py L58: import `reset_thread_connections` from `mcp_strava.adapters.duckdb.connection`; keep the fixture invoking it (RI-01).
    - test_metric_services.py: change `import mcp_strava.db as db` to `import mcp_strava.adapters.duckdb.connection as db` (or equivalent); retarget `_db_path`, `open_expected_mirror_db`, `reset_thread_connections` (L379/L388/...) onto that module. For L653 `metric_services.repository_from_connection`: that function is GONE after Task 1 (D-04). Repoint to patch `metric_services.DuckDBRepository.from_connection` (the symbol metric_services now imports) so the fake repo still intercepts — do NOT leave `setattr(metric_services, "repository_from_connection", ...)` which would create a dead attribute.
    - test_refresh_runtime.py (6 sites): `worker.DbConn` -> `worker.MirrorConn`; `worker.repository_from_connection` -> patch `worker.DuckDBRepository.from_connection` (the factory worker now calls per D-04). Plain `setattr` (no raising=False) on the old names would ERROR once worker no longer has them — must be retargeted.
    - test_application_services.py L203: `freshness.repository_from_connection` -> `freshness.DuckDBRepository.from_connection` (freshness migrated in Task 1).
    - test_security_guards.py L637/L700: sync.py imports `MirrorConn` (not `DbConn`) after Task 2, so `monkeypatch.setattr(sync, "DbConn", FakeDbConn)` would silently create a DEAD attribute while the real `MirrorConn` bypasses the fake. Change both to `monkeypatch.setattr(sync, "MirrorConn", FakeDbConn)`. Touch ONLY these two sites in this plan — the init_db guards and negative-import literals are retargeted in 12-04 Task 3.
    Note: `get_settings` patches in test_security_guards come from `settings`, not `db`, so are unaffected — leave them alone.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py tests/test_application_services.py tests/test_refresh_runtime.py -x && rg -q 'setattr\(sync,\s*"DbConn"' tests/test_security_guards.py && exit 1 || echo "sync-DbConn-retargeted"</automated>
  </verify>
  <done>conftest resets the relocated pool; the application/refresh/sync monkeypatch sites resolve to live module objects (worker.MirrorConn, worker/freshness/metric_services.DuckDBRepository.from_connection, sync.MirrorConn) with no dead-attribute or vacuous-no-op sites; the listed suites green; no `setattr(sync, "DbConn"` literal remains (fail-closed grep).</done>
</task>

<task type="auto">
  <name>Task 4: Full local suite + static checks after src caller + same-wave test retargets</name>
  <files>src/mcp_strava/sync.py</files>
  <read_first>
    - Justfile (test/lint/typecheck recipes)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Pitfall 3 — uv pytest vs just test)
  </read_first>
  <action>
    Run the full local suite and static checks to confirm the application/refresh/sync migration plus the same-wave test retargets (Task 3) preserved behavior. cli.py and the CLI-specific tests (test_cli_surface.py, test_smoke.py, test_phase01_validation.py, test_repository_boundary.py) still reference db.py at this point — they are migrated in 12-04; db.py is not deleted until 12-05. The full suite must be green NOW because Task 3 retargeted every test that exercises code migrated in this wave; tests still pointing at db.py exercise only cli.py-path code that is untouched here. Fix any ruff/pyright findings introduced by the import changes (unused imports, import order).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check src tests && uv run pyright src</automated>
  </verify>
  <done>`uv run pytest -q` green (323 tests) — the wave lands green because same-wave test retargets are complete; ruff + pyright clean; only cli.py + CLI-specific tests still import db.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → adapters | read/write paths now depend directly on adapter homes (dependency rule, GP-04) |
| settings → bootstrap | creds sourced from settings instead of a bootstrap-local .env parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | caller behavior parity during import swap | mitigate | Mechanical import/factory swap with no logic change; full suite green in Task 3 |
| T-12-04 | Tampering | bootstrap cred parser consolidation | mitigate | Reuse settings resolution (already validated) instead of a second env parser; security-guard + smoke suites green in Task 2 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- No `from mcp_strava.db import` or `import mcp_strava.db` remains under `src/mcp_strava/application`, `src/mcp_strava/refresh`, or `sync.py` (cli.py migrated in 12-04). Use a fail-closed grep: `! rg -q "from mcp_strava\.db import|import mcp_strava\.db" src/mcp_strava/application src/mcp_strava/refresh src/mcp_strava/sync.py`.
- No `setattr(sync, "DbConn"`, `setattr(worker, "DbConn"`, or `setattr(..., "repository_from_connection"` site remains in the files retargeted here (conftest, test_metric_services, test_refresh_runtime, test_application_services, the two sync sites in test_security_guards). CLI-path test sites are out of scope (12-04).
- `uv run pytest -q` green — the wave lands green because same-wave test retargets are complete; ruff + pyright clean.
</verification>

<success_criteria>
All non-cli production callers routed through new homes; D-04 indirection removed; bootstrap consolidated onto settings + SystemClock/SystemSleeper; the application/refresh/sync test monkeypatch sites retargeted in this same wave; full suite green mid-migration.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-03-SUMMARY.md` when done.
</output>
