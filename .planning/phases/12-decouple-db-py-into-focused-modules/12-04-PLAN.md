---
phase: 12-decouple-db-py-into-focused-modules
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/mcp_strava/cli.py
  - tests/conftest.py
  - tests/test_metric_services.py
  - tests/test_repository_boundary.py
  - tests/test_smoke.py
  - tests/test_phase01_validation.py
  - tests/test_security_guards.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "cli.py imports MirrorConn + StravaClient from the new homes and uses direct repository factories; no db.py import remains in cli.py"
    - "conftest isolation fixture imports reset_thread_connections from adapters/duckdb/connection and still clears the read pool (RI-01)"
    - "test monkeypatch-by-path targets (_db_path, open_expected_mirror_db, api_request, refresh_token) point at the new module objects, not mcp_strava.db"
    - "security-guard negative-import literals reference the new StravaClient home; the now-dead init_db DDL guards are removed/retargeted"
  artifacts:
    - path: "tests/conftest.py"
      provides: "isolation fixture resetting the relocated thread pool"
      contains: "reset_thread_connections"
  key_links:
    - from: "tests/test_metric_services.py"
      to: "src/mcp_strava/adapters/duckdb/connection.py"
      via: "monkeypatch targets the relocated _db_path / open_expected_mirror_db"
      pattern: "adapters.duckdb.connection"
---

<objective>
Migrate the last consumers of `db.py` (D-10 steps 4-5): `cli.py` and the test suite. After this plan, NOTHING in the repo imports `mcp_strava.db` — clearing the way for deletion in 12-05.

- `cli.py`: import `MirrorConn` + `StravaClient` from new homes; `refresh_token()` and the `cmd_strava_raw` `api_request` go through `StravaClient` (D-08); use direct repository factories (D-04); pick up `SystemClock`/`SystemSleeper` (D-07) via the sync re-exports updated in 12-03.
- Tests: repoint imports AND monkeypatch-by-path targets to the relocated module objects; update `test_security_guards.py` literal guards to the new home; remove/retarget the `init_db` DDL guards (init_db is dead per RESEARCH RI-03/Open Question 1).

Purpose: complete the hard-cut migration with the regression net intact (GP-02 — no shim left behind).
Output: cli + tests on new homes; `db.py` referenced by nothing; suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/cli.py
@tests/conftest.py
@tests/test_security_guards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate cli.py to new homes (D-04, D-07, D-08)</name>
  <files>src/mcp_strava/cli.py</files>
  <read_first>
    - src/mcp_strava/cli.py (imports L15-21: DbConn, refresh_token, repository_from_connection, repository_from_path, RealClock, RealSleeper; cmd_refresh L63-65; cmd_strava_raw L120-126 with local `from mcp_strava.db import api_request`; clock/sleeper use L281-282)
    - src/mcp_strava/adapters/strava/client.py (StravaClient.api_request/refresh_token from 12-02)
    - src/mcp_strava/sync.py (updated SystemClock/SystemSleeper re-exports from 12-03)
    - src/mcp_strava/adapters/duckdb/connection.py (MirrorConn)
  </read_first>
  <action>
    Replace cli.py's `from mcp_strava.db import DbConn, refresh_token, repository_from_connection, repository_from_path` with: `MirrorConn` from `adapters/duckdb/connection`; `DuckDBRepository.from_connection`/`from_path` directly (D-04); and route `refresh_token()` (cmd_refresh) + the `cmd_strava_raw` `api_request` (currently a local import L122) through a `StravaClient` instance (D-08) — preserve the existing call shapes (`token = refresh_token()`; `data, _rate = api_request(path)`). Replace `RealClock`/`RealSleeper` imports/usages (L20-21, L281-282) with `SystemClock`/`SystemSleeper` (D-07) sourced from the updated sync re-exports or directly from `adapters/strava/clock`. No db.py import — local or top-level — may remain.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_security_guards.py -x && uv run python -c "import ast,sys; t=ast.parse(open('src/mcp_strava/cli.py').read()); bad=[n for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom)) and 'mcp_strava.db'==getattr(n,'module',None) or (isinstance(n,ast.ImportFrom) and (n.module or '')=='mcp_strava.db')]; sys.exit(1 if bad else 0)"</automated>
  </verify>
  <done>cli.py imports MirrorConn + StravaClient + SystemClock/SystemSleeper from new homes, uses direct repository factories, and contains no `mcp_strava.db` import (top-level or local).</done>
</task>

<task type="auto">
  <name>Task 2: Retarget test imports and monkeypatch-by-path targets</name>
  <files>tests/conftest.py, tests/test_metric_services.py, tests/test_repository_boundary.py, tests/test_smoke.py, tests/test_phase01_validation.py</files>
  <read_first>
    - tests/conftest.py (L58-62 reset_thread_connections import + fixture — RI-01: must keep calling it)
    - tests/test_metric_services.py (L368-409: `import mcp_strava.db as db`; monkeypatch db._db_path, db.open_expected_mirror_db; db.reset_thread_connections)
    - tests/test_repository_boundary.py (L348 `import mcp_strava.db as legacy_db`; L356-357 monkeypatch api_request/refresh_token; L369 import DbConn, repository_from_connection)
    - tests/test_smoke.py (L21), tests/test_phase01_validation.py (L9): `from mcp_strava.db import DbConn`
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Test couplings table + Pitfall 1/2)
  </read_first>
  <action>
    Repoint conftest's `reset_thread_connections` import to `mcp_strava.adapters.duckdb.connection`; keep the fixture invoking it (RI-01). In test_metric_services: change `import mcp_strava.db as db` to `import mcp_strava.adapters.duckdb.connection as conn` (or alias) and retarget the three monkeypatch targets (`_db_path`, `open_expected_mirror_db`, `reset_thread_connections`) to that module object — these are the relocated symbols. In test_repository_boundary: retarget the `api_request`/`refresh_token` monkeypatches to the `StravaClient` home (the symbols the migrated cli/code now actually call — patch where they are looked up), and change `DbConn, repository_from_connection` to `MirrorConn` + `DuckDBRepository.from_connection`. In test_smoke + test_phase01_validation: `DbConn` → `MirrorConn` from `adapters/duckdb/connection`. Patch at the lookup site so the monkeypatch still intercepts the call path.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py tests/test_repository_boundary.py tests/test_smoke.py tests/test_phase01_validation.py -x</automated>
  </verify>
  <done>conftest resets the relocated pool; all monkeypatch targets resolve to the new module objects; the four test files import MirrorConn from the adapter home; targeted suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Update security-guard literals + retire dead init_db guards</name>
  <files>tests/test_security_guards.py</files>
  <read_first>
    - tests/test_security_guards.py (L32-38 _assert_no_schema_ddl_in_init_db; L200-205 test_sync_never_calls_init_db_and_db_init_db_has_no_ddl; L468-485 negative-import literals mcp_strava.db.api_request/refresh_token)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Open Question 1 + Pitfall 2)
    - src/mcp_strava/adapters/strava/client.py (the new home for api_request/refresh_token)
  </read_first>
  <action>
    Update the negative-import guard literals `mcp_strava.db.api_request` / `mcp_strava.db.refresh_token` (L468-485) to the new StravaClient home so the "metric/read modules must not import network code" protection keeps biting (PATTERNS Pitfall 2). For the init_db guards (L200-205 + helper L32-38): init_db is dead and is being deleted (RESEARCH Open Question 1). Default per GP-02: remove the now-vacuous `_assert_no_schema_ddl_in_init_db` helper and `test_sync_never_calls_init_db_and_db_init_db_has_no_ddl`, since they assert on a function that no longer exists. If the operator wants to preserve the "runtime runs no schema DDL" invariant, retarget it to assert against sync.py/runtime modules instead of the deleted init_db — but do not leave a test referencing a symbol that 12-05 deletes.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_security_guards.py -x</automated>
  </verify>
  <done>security-guard negative-import literals point at the StravaClient home; no test references the to-be-deleted init_db; test_security_guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → code under test | monkeypatch interception must target the real lookup site after relocation |
| read-path modules → network code | security guard enforces no network import in metric/read paths (literal updated to new home) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | stale security-guard literal passes vacuously | mitigate | Repoint literals to StravaClient home in the same plan that finishes the facade migration (Task 3) |
| T-12-06 | Tampering | monkeypatch retarget misses real lookup site | mitigate | Patch where the symbol is looked up; targeted suites in Tasks 2-3 prove interception still works |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- No file under `tests/` or `src/` imports `mcp_strava.db` (grep clean).
- conftest still resets the (relocated) thread pool; monkeypatches intercept the migrated call paths.
- `uv run pytest -q` green; ruff + pyright clean.
</verification>

<success_criteria>
cli + all tests migrated to new homes; security guards updated; dead init_db guards retired; nothing references db.py; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-04-SUMMARY.md` when done.
</output>
