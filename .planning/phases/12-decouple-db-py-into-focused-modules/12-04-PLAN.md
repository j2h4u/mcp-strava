---
phase: 12-decouple-db-py-into-focused-modules
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/mcp_strava/cli.py
  - src/mcp_strava/refresh/bootstrap.py
  - src/mcp_strava/sync.py
  - tests/conftest.py
  - tests/test_metric_services.py
  - tests/test_repository_boundary.py
  - tests/test_smoke.py
  - tests/test_phase01_validation.py
  - tests/test_security_guards.py
  - tests/test_refresh_runtime.py
  - tests/test_application_services.py
  - tests/test_cli_surface.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "cli.py imports MirrorConn + StravaClient + SystemClock/SystemSleeper from the new homes and uses direct repository factories; no db.py import (top-level or local) remains in cli.py"
    - "the RealClock/RealSleeper aliases kept on bootstrap (and re-exported by sync) through wave 3 are removed once cli.py no longer imports them (alias cleanup)"
    - "conftest isolation fixture imports reset_thread_connections from adapters/duckdb/connection and still clears the read pool (RI-01)"
    - "EVERY db.py-coupled monkeypatch site is retargeted: worker.DbConn/repository_from_connection (test_refresh_runtime.py x6), freshness.repository_from_connection (test_application_services.py), metric_services.repository_from_connection (test_metric_services.py:653), cli.api_request/cli.DbConn (test_cli_surface.py L182/185), sync.DbConn (test_security_guards.py L637/700), legacy_db.api_request/refresh_token (test_repository_boundary.py L353/354) — none left pointing at removed symbols"
    - "no monkeypatch.setattr(..., \"DbConn\"/\"repository_from_connection\", ...) targets a renamed/removed attribute (would fail at setattr) or silently no-ops via raising=False (would pass vacuously)"
    - "security-guard negative-import literals reference the new StravaClient home; the now-dead init_db DDL guards are removed/retargeted"
  artifacts:
    - path: "tests/conftest.py"
      provides: "isolation fixture resetting the relocated thread pool"
      contains: "reset_thread_connections"
  key_links:
    - from: "tests/test_metric_services.py"
      to: "src/mcp_strava/adapters/duckdb/connection.py"
      via: "monkeypatch targets the relocated _db_path / open_expected_mirror_db"
      pattern: "adapters.duckdb.connection"
---

<objective>
Migrate the last consumers of `db.py` (D-10 steps 4-5): `cli.py` and the test suite. After this plan, NOTHING in the repo imports `mcp_strava.db` — clearing the way for deletion in 12-05.

- `cli.py`: import `MirrorConn` + `StravaClient` from new homes; `refresh_token()` and the `cmd_strava_raw` `api_request` go through `StravaClient` (D-08); use direct repository factories (D-04); pick up `SystemClock`/`SystemSleeper` (D-07) directly from `adapters/strava/clock` (or the sync re-exports). Then remove the temporary `RealClock`/`RealSleeper` aliases left on `bootstrap.py` (and their `sync.py` re-export) in 12-03 — nothing imports them anymore.
- Tests: repoint imports AND ALL monkeypatch-by-path targets to the relocated module objects across the FULL blast radius verified during cross-AI review — not just the 5 files originally scoped. This is the dominant HIGH consensus concern: `tests/test_refresh_runtime.py`, `tests/test_application_services.py`, and `tests/test_cli_surface.py` were missing from scope, and two `sync.DbConn` sites in `test_security_guards.py` plus `metric_services.repository_from_connection` (test_metric_services.py:653) and `legacy_db.api_request/refresh_token` (test_repository_boundary.py) were un-enumerated. Update `test_security_guards.py` literal guards to the new home; remove/retarget the `init_db` DDL guards (init_db is dead per RESEARCH RI-03/Open Question 1).

Purpose: complete the hard-cut migration with the regression net intact (GP-02 — no shim left behind). A missed monkeypatch retarget would either FAIL at setattr (renamed attribute, no raising=False) or PASS VACUOUSLY (raising=False no-op bypassing the real object) — both verified as live risks against the checkout.
Output: cli + bootstrap-alias-cleanup + ALL coupled tests on new homes; `db.py` referenced by nothing; suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/cli.py
@tests/conftest.py
@tests/test_security_guards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate cli.py to new homes + clean up bootstrap/sync RealClock aliases (D-04, D-07, D-08)</name>
  <files>src/mcp_strava/cli.py, src/mcp_strava/refresh/bootstrap.py, src/mcp_strava/sync.py</files>
  <read_first>
    - src/mcp_strava/cli.py (imports L15-23: DbConn, refresh_token, repository_from_connection, repository_from_path; RealClock/RealSleeper from refresh.bootstrap L19-23; cmd_strava_raw local `from mcp_strava.db import api_request` L122; clock/sleeper use L281-282; cmd_refresh)
    - src/mcp_strava/adapters/strava/client.py (StravaClient.api_request(path) — no token param — / refresh_token from 12-02)
    - src/mcp_strava/adapters/strava/clock.py (SystemClock/SystemSleeper)
    - src/mcp_strava/refresh/bootstrap.py (the RealClock=SystemClock / RealSleeper=SystemSleeper aliases added in 12-03)
    - src/mcp_strava/sync.py (the RealClock/RealSleeper re-export + __all__ entries L9-18)
    - src/mcp_strava/adapters/duckdb/connection.py (MirrorConn)
  </read_first>
  <action>
    Replace cli.py's `from mcp_strava.db import DbConn, refresh_token, repository_from_connection, repository_from_path` with: `MirrorConn` from `adapters/duckdb/connection`; `DuckDBRepository.from_connection`/`from_path` directly (D-04); and route `refresh_token()` (cmd_refresh) + the `cmd_strava_raw` `api_request` (local import L122) through a `StravaClient` instance (D-08) — preserve the call shapes (`token = refresh_token()` becomes a `StravaClient().refresh_token()`; `data, _rate = api_request(path)` becomes `StravaClient().api_request(path)` with NO token arg). Replace cli's `RealClock`/`RealSleeper` import (L19-23) and usage (L281-282) with `SystemClock`/`SystemSleeper` imported directly from `adapters/strava/clock`. Then, since nothing imports them anymore, DELETE the temporary `RealClock = SystemClock` / `RealSleeper = SystemSleeper` aliases from `bootstrap.py` and remove `RealClock`/`RealSleeper` from `sync.py`'s imports, re-exports, and `__all__` (L9-18). No db.py import — local or top-level — may remain in cli.py.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_cli_surface.py tests/test_security_guards.py -x && rg -q "mcp_strava\.db" src/mcp_strava/cli.py && echo FAIL || echo "cli-clean"; rg -q "RealClock|RealSleeper" src/mcp_strava && echo "FAIL: aliases remain" || echo "aliases-removed"</automated>
  </verify>
  <done>cli.py imports MirrorConn + StravaClient + SystemClock/SystemSleeper from new homes, uses direct repository factories, calls StravaClient.api_request(path) with no token arg, and contains no `mcp_strava.db` import; RealClock/RealSleeper aliases removed from bootstrap.py and sync.py (no occurrences remain in src).</done>
</task>

<task type="auto">
  <name>Task 2: Retarget the FULL test blast radius — imports + every monkeypatch-by-path site (review HIGH consensus)</name>
  <files>tests/conftest.py, tests/test_metric_services.py, tests/test_repository_boundary.py, tests/test_smoke.py, tests/test_phase01_validation.py, tests/test_refresh_runtime.py, tests/test_application_services.py, tests/test_cli_surface.py</files>
  <read_first>
    - tests/conftest.py (L58 `from mcp_strava.db import reset_thread_connections` + fixture — RI-01: must keep calling it)
    - tests/test_metric_services.py (L368 `import mcp_strava.db as db`; L379 monkeypatch db._db_path; L388 db.open_expected_mirror_db; db.reset_thread_connections; L653 `metric_services.repository_from_connection`)
    - tests/test_repository_boundary.py (L348 `import mcp_strava.db as legacy_db`; L353-354 monkeypatch legacy_db.api_request/refresh_token; L369 `from mcp_strava.db import DbConn, repository_from_connection`)
    - tests/test_refresh_runtime.py (L581/582, 621/622, 723/724, 785/786, 838/839, 866: `monkeypatch.setattr(worker, "DbConn", ...)` and `monkeypatch.setattr(worker, "repository_from_connection", ...)`)
    - tests/test_application_services.py (L203: `monkeypatch.setattr(freshness, "repository_from_connection", ...)`)
    - tests/test_cli_surface.py (L182: `monkeypatch.setattr(cli, "api_request", forbidden, raising=False)`; L185: `monkeypatch.setattr(cli, "DbConn", forbidden, raising=False)`)
    - tests/test_smoke.py (L21), tests/test_phase01_validation.py (L9): `from mcp_strava.db import DbConn`
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Test couplings table + Pitfall 1/2)
  </read_first>
  <action>
    Patch at the LOOKUP SITE (the module object that actually resolves the name at call time) so interception still works. Retarget EVERY site:
    - conftest.py L58: import `reset_thread_connections` from `mcp_strava.adapters.duckdb.connection`; keep the fixture invoking it (RI-01).
    - test_metric_services.py: change `import mcp_strava.db as db` to the `adapters.duckdb.connection` module; retarget `_db_path`, `open_expected_mirror_db`, `reset_thread_connections` (L379/L388/...) to that module. For L653 `metric_services.repository_from_connection`: that function is GONE after 12-03 (D-04). Repoint to patch `metric_services.DuckDBRepository.from_connection` (or the `DuckDBRepository` symbol metric_services now imports) so the fake repo still intercepts — do NOT leave `setattr(metric_services, "repository_from_connection", ...)` which would create a dead attribute.
    - test_refresh_runtime.py (6 sites): `worker.DbConn` -> `worker.MirrorConn`; `worker.repository_from_connection` -> patch `worker.DuckDBRepository.from_connection` (the factory worker now calls per D-04). Plain `setattr` (no raising=False) on the old names would ERROR once worker no longer has them — must be retargeted.
    - test_application_services.py L203: `freshness.repository_from_connection` -> `freshness.DuckDBRepository.from_connection` (freshness migrated in 12-03).
    - test_cli_surface.py L182/L185: `cli.api_request` and `cli.DbConn` (both `raising=False`). After cli migration these names no longer exist on `cli`, so the guards no-op vacuously. Retarget to the symbols cli actually uses now: guard that cli does not call the network path via `StravaClient` and that it uses `MirrorConn` — patch `cli.MirrorConn` and the `StravaClient.api_request` lookup the test intends to forbid, dropping `raising=False` where the attribute should exist so a miss fails loudly.
    - test_repository_boundary.py: retarget L353-354 `legacy_db.api_request`/`refresh_token` to the `StravaClient` home (patch where the migrated code looks them up); change L369 `DbConn, repository_from_connection` -> `MirrorConn` + `DuckDBRepository.from_connection`.
    - test_smoke.py + test_phase01_validation.py: `DbConn` -> `MirrorConn` from `adapters/duckdb/connection`.
    Note (review): `get_settings` patches in test_security_guards come from `settings`, not `db`, so are unaffected and must be left alone.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_metric_services.py tests/test_repository_boundary.py tests/test_smoke.py tests/test_phase01_validation.py tests/test_refresh_runtime.py tests/test_application_services.py tests/test_cli_surface.py -x</automated>
  </verify>
  <done>conftest resets the relocated pool; ALL monkeypatch targets (worker/freshness/metric_services repository factory, worker.MirrorConn, cli network guards, legacy api_request/refresh_token, DbConn->MirrorConn) resolve to live module objects with no dead-attribute or vacuous-no-op sites; all eight listed suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Update security-guard literals + retire dead init_db guards</name>
  <files>tests/test_security_guards.py</files>
  <read_first>
    - tests/test_security_guards.py (L32-38 _assert_no_schema_ddl_in_init_db; L200-205 test_sync_never_calls_init_db_and_db_init_db_has_no_ddl; L468-485 negative-import literals mcp_strava.db.api_request/refresh_token; L637 + L700 `monkeypatch.setattr(sync, "DbConn", FakeDbConn)`)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Open Question 1 + Pitfall 2)
    - src/mcp_strava/adapters/strava/client.py (the new home for api_request/refresh_token)
    - src/mcp_strava/sync.py (now imports MirrorConn, not DbConn — from 12-03)
  </read_first>
  <action>
    Three retargets in this file (semantic, not just string-swap — review MEDIUM Codex):
    1. **sync.DbConn monkeypatches (L637, L700):** sync.py imports `MirrorConn` (not `DbConn`) after 12-03, so `monkeypatch.setattr(sync, "DbConn", FakeDbConn)` would silently create a DEAD attribute while the real `MirrorConn` bypasses the fake — both tests would break/pass-vacuously (review HIGH, OpenCode). Change both to `monkeypatch.setattr(sync, "MirrorConn", FakeDbConn)`.
    2. **Negative-import literals (L468-485):** retarget `mcp_strava.db.api_request` / `mcp_strava.db.refresh_token` to the new `StravaClient` home so the "metric/read modules must not import network code" guard keeps biting (PATTERNS Pitfall 2). This is a SEMANTIC retarget: the guard must forbid the read/product modules from importing the actual `StravaClient` network lookup path, and where product-handler guards previously forbade `DbConn`, they should now forbid `MirrorConn` — not merely swap the literal and pass vacuously.
    3. **init_db guards (L32-38 helper + L200-205 test):** init_db is dead and deleted in 12-05 (RESEARCH Open Question 1). Default per GP-02: remove `_assert_no_schema_ddl_in_init_db` and `test_sync_never_calls_init_db_and_db_init_db_has_no_ddl` (they assert on a function that ceases to exist). If the "runtime runs no schema DDL" invariant still matters, RETARGET it to assert against the live sync/runtime/bootstrap modules instead — but leave NO test referencing the symbol 12-05 deletes.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_security_guards.py -x && rg -q 'setattr\(sync,\s*"DbConn"' tests/test_security_guards.py && echo "FAIL: stale sync.DbConn patch" || echo "sync-patch-retargeted"</automated>
  </verify>
  <done>sync.DbConn monkeypatches (L637/L700) retargeted to sync.MirrorConn; negative-import literals semantically forbid the StravaClient network path and MirrorConn in read/product modules; no test references the to-be-deleted init_db; test_security_guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → code under test | monkeypatch interception must target the real lookup site after relocation |
| read-path modules → network code | security guard enforces no network import in metric/read paths (literal updated to new home) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | stale security-guard literal passes vacuously | mitigate | Repoint literals to StravaClient home in the same plan that finishes the facade migration (Task 3) |
| T-12-06 | Tampering | monkeypatch retarget misses real lookup site | mitigate | Patch where the symbol is looked up; targeted suites in Tasks 2-3 prove interception still works |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `rg "from mcp_strava\.db import|import mcp_strava\.db|mcp_strava\.db\." src tests` returns no matches (the canonical gate from 12-05 — replaces the brittle AST walk; review LOW Codex).
- No `setattr(sync, "DbConn"` / `setattr(worker, "DbConn"` / `setattr(cli, "DbConn"` / `repository_from_connection"` monkeypatch site remains anywhere under tests/ (every one retargeted).
- conftest still resets the (relocated) thread pool; monkeypatches intercept the migrated call paths.
- `uv run pytest -q` green; ruff + pyright clean.
</verification>

<success_criteria>
cli + all tests migrated to new homes; security guards updated; dead init_db guards retired; nothing references db.py; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-04-SUMMARY.md` when done.
</output>
