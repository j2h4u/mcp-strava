---
phase: 12-decouple-db-py-into-focused-modules
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/settings.py
  - src/mcp_strava/adapters/strava/clock.py
  - src/mcp_strava/adapters/strava/client.py
  - src/mcp_strava/adapters/strava/__init__.py
  - tests/test_strava_client.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "Strava client creds (STRAVA_CLIENT_ID/SECRET) resolve from the SAME source as today (the token file at settings.token_path, via the relocated _read_token_values logic), NOT from a new env/.env path — behavior parity with bootstrap._required_strava_client"
    - "Credential resolution is required-when-used: it fails fast (RuntimeError naming the missing keys + token_path) ONLY at StravaClient/refresh-collaborator construction, never inside load_settings() — the read-only mirror path that needs no Strava creds must keep loading settings cleanly (addresses review MEDIUM: settings fail-fast scope)"
    - "SystemClock and SystemSleeper (D-07) implement the Clock/Sleeper ports in a dedicated module"
    - "StravaClient facade (D-08) builds its transport from settings and exposes api_request(path) + refresh_token with identical (data, rate_headers) contract and StravaUnavailable->RuntimeError mapping; the unused token= parameter is DROPPED (GP-02, zero live callers)"
    - "Transport construction (FileTokenProvider->TokenRefreshTransport->StravaTransport) is factored into ONE helper shared by StravaClient and build_refresh_collaborators — no third parallel wiring path (addresses review suggestion)"
  artifacts:
    - path: "src/mcp_strava/adapters/strava/clock.py"
      provides: "SystemClock, SystemSleeper"
      contains: "class SystemClock"
    - path: "src/mcp_strava/adapters/strava/client.py"
      provides: "StravaClient facade + shared _build_strava_transport helper"
      contains: "class StravaClient"
    - path: "tests/test_strava_client.py"
      provides: "focused StravaClient facade test (parity + no-secret-leak)"
      contains: "StravaClient"
    - path: "src/mcp_strava/settings.py"
      provides: "Strava client cred accessor (token-file sourced, fail-fast-when-used)"
      contains: "STRAVA_CLIENT_ID"
  key_links:
    - from: "src/mcp_strava/adapters/strava/client.py"
      to: "StravaTransport"
      via: "shared _build_strava_transport composes FileTokenProvider->TokenRefreshTransport->StravaTransport from settings"
      pattern: "StravaTransport"
---

<objective>
Land the Strava auth + HTTP homes so callers can later route through them (D-05, D-06, D-07, D-08, D-09). Three additive pieces:

1. `settings.py` exposes a Strava client-cred accessor (D-06) that resolves creds from the SAME source they come from today — the token file at `settings.token_path` (the relocated `bootstrap._read_token_values` logic), NOT a new env/.env lookup.
2. `adapters/strava/clock.py` defines `SystemClock`/`SystemSleeper` (D-07, the clean rename of `_RealClock`/`_RealSleeper` and convergence target for `bootstrap.RealClock`/`RealSleeper`).
3. `adapters/strava/client.py` defines the `StravaClient` facade (D-08) owning transport construction, the `(data, rate_headers)` contract, `StravaUnavailable -> RuntimeError` / rate-limited-sentinel mapping, and `refresh_token()`.

Scope corrections from RESEARCH + cross-AI review (HONOR THESE):
- **D-05:** `_CompatTokenProvider` is deleted with db.py in 12-05; `_read_token_values` is NOT deleted blindly — its token-file-parsing behavior is the canonical cred source and is relocated (into settings or a small settings helper) so `StravaClient`/refresh-construction read creds from `settings.token_path`, preserving today's behavior. Do not introduce a new env/.env cred path.
- **D-06 fail-fast scope (review MEDIUM — Codex + OpenCode):** Credential resolution is REQUIRED-WHEN-USED. `load_settings()` MUST NOT raise on missing Strava creds — the read-only mirror path (CLI read commands, product handlers) loads settings without any Strava creds. The fail-fast (RuntimeError naming missing keys + token_path, exactly as `_required_strava_client` does today) fires ONLY when `StravaClient`/`build_refresh_collaborators` actually need the creds.
- **Key-prefix (review LOW — OpenCode):** The token-file keys are `STRAVA_CLIENT_ID`/`STRAVA_CLIENT_SECRET` (Strava's own naming, parsed from the token file — NOT the `MCP_STRAVA_*` settings env convention). Do NOT add bare `STRAVA_CLIENT_ID`/`STRAVA_CLIENT_SECRET` to the `MCP_STRAVA_*` `_KEYS` env set; they are token-file values, not settings env vars. This keeps `_KEYS` convention intact and behavior identical.
- **token= param (review MEDIUM — Codex + OpenCode):** `db.api_request(path, token=None)` has ZERO live callers passing a token (only the dead `get_zones` and cli's `api_request(path)`). Per GP-02 (no compat surface), `StravaClient.api_request` is defined as `api_request(self, path)` — the `token=` parameter is DROPPED, not carried as a dead/misleading kwarg.
- **Transport helper (review suggestion — Codex):** Factor `FileTokenProvider -> TokenRefreshTransport -> StravaTransport` construction into ONE helper (e.g. `_build_strava_transport(settings, clock, sleeper)`) used by BOTH `StravaClient` and `build_refresh_collaborators` (12-03), so there is no third parallel wiring path.
- **D-09:** `get_zones()` is DEAD CODE (zero live callers — RESEARCH RI-02/RI-03). Per GP-02 it is DELETED with db.py (12-05), NOT rebuilt as an application service. No `application/athlete_zones.py` is created.

This is ADDITIVE: db.py is not edited here; existing callers keep working until 12-03/12-04.

Purpose: config resolution belongs to settings; real clock/sleeper + HTTP facade belong to the Strava adapter (GP-01, GP-03, GP-04).
Output: settings creds, `clock.py`, `client.py`, and adapter `__init__` exports for the new public surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/settings.py
@src/mcp_strava/refresh/bootstrap.py
@src/mcp_strava/adapters/strava/__init__.py
@src/mcp_strava/adapters/strava/transport.py
@src/mcp_strava/adapters/strava/token_provider.py
@src/mcp_strava/adapters/strava/token_refresh.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve STRAVA_CLIENT_ID/SECRET in settings.py (D-06)</name>
  <files>src/mcp_strava/settings.py</files>
  <read_first>
    - src/mcp_strava/settings.py (the resolve() idiom, _KEYS set, frozen dataclasses, AthleteSettings fail-fast convention)
    - src/mcp_strava/refresh/bootstrap.py (_required_strava_client fail-fast pattern, lines 76-83)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md (settings.py section)
  </read_first>
  <behavior>
    - A settings-level accessor resolves STRAVA_CLIENT_ID/STRAVA_CLIENT_SECRET from the TOKEN FILE at settings.token_path (relocating bootstrap._read_token_values parsing) — the same source as today. NOT from env or .env.
    - load_settings() does NOT change: it must still succeed with no Strava creds present (read-only mirror path). Do NOT add STRAVA_CLIENT_* to the MCP_STRAVA_* _KEYS env set.
    - The accessor is required-when-used: it raises RuntimeError naming the missing keys and the token_path (verbatim parity with bootstrap._required_strava_client) only when called — never inside load_settings(). Missing creds must not break settings loading for read-only consumers.
  </behavior>
  <action>
    Per D-06, give settings ownership of Strava cred resolution WITHOUT changing load_settings() startup behavior. Relocate the token-file parsing from `bootstrap._read_token_values` (db-coupled today) into settings (e.g. a method `Settings.strava_client_credentials() -> tuple[str, str]` or a module helper that reads `settings.token_path`). It must reproduce `_required_strava_client` EXACTLY: read `STRAVA_CLIENT_ID`/`STRAVA_CLIENT_SECRET` from the token file, raise `RuntimeError(f"Missing Strava client settings: {', '.join(missing)}. Check {settings.token_path}")` when missing. Do NOT add these keys to `_KEYS` (they are token-file values, not `MCP_STRAVA_*` env settings — review LOW). Do NOT make `load_settings()` raise (review MEDIUM: fail-fast scope — read-only mirror path must keep loading). Do not log or echo secret values.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_phase01_validation.py -x && uv run python -c "from mcp_strava.settings import load_settings; load_settings(); print('load-ok-without-creds')"</automated>
  </verify>
  <done>settings exposes a token-file-sourced Strava cred accessor with verbatim _required_strava_client fail-fast semantics; load_settings() still succeeds with no creds (read-only path intact); STRAVA_CLIENT_* are NOT added to _KEYS; existing settings tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Add SystemClock/SystemSleeper in adapters/strava/clock.py (D-07)</name>
  <files>src/mcp_strava/adapters/strava/clock.py, src/mcp_strava/adapters/strava/__init__.py</files>
  <read_first>
    - src/mcp_strava/refresh/bootstrap.py (RealClock/RealSleeper, lines 20-27 — the convergence target using time.time())
    - src/mcp_strava/adapters/strava/types.py (Clock/Sleeper Protocols, lines 60-65)
    - src/mcp_strava/adapters/strava/__init__.py (export style + __all__)
  </read_first>
  <action>
    Create `adapters/strava/clock.py` with `class SystemClock` (`now()` returns `time.time()` — the clean idiom, matching bootstrap.RealClock and equivalent to db.py's `datetime.now().timestamp()`) and `class SystemSleeper` (`sleep(seconds)` calls `time.sleep`). These implement the Clock/Sleeper ports from types.py (D-07). Export `SystemClock`, `SystemSleeper` from `adapters/strava/__init__.py` (add to imports and `__all__`, preserving alphabetical ordering). Do not yet delete bootstrap.RealClock/RealSleeper — that rename ripples through bootstrap/sync/cli in 12-03/12-04.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_strava.adapters.strava import SystemClock, SystemSleeper; c=SystemClock(); assert isinstance(c.now(), float); print('ok')"</automated>
  </verify>
  <done>SystemClock/SystemSleeper importable from adapters/strava; now() returns float epoch seconds; sleep delegates to time.sleep; exported in __all__.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add StravaClient facade + shared transport helper + focused facade test (D-08)</name>
  <files>src/mcp_strava/adapters/strava/client.py, src/mcp_strava/adapters/strava/__init__.py, tests/test_strava_client.py</files>
  <read_first>
    - src/mcp_strava/db.py (_build_transport L194-198, api_request L208-217, refresh_token L201-205 — the exact contract + error mapping to preserve)
    - src/mcp_strava/refresh/bootstrap.py (build_refresh_collaborators L86-100 — the canonical construction chain that this helper will be shared with in 12-03)
    - src/mcp_strava/adapters/strava/transport.py (StravaTransport.fetch return shape, StravaResponse)
    - src/mcp_strava/settings.py (the token-file cred accessor from Task 1)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md (client.py section, replication rules)
  </read_first>
  <behavior>
    - StravaClient.api_request(path) — signature has NO token parameter (dropped per GP-02; zero live callers) — returns a (data, rate_headers) tuple; on StravaUnavailable with reason "rate_limited" returns ({"_rate_limited": True, "_retry_after": None}, {}); on any other StravaUnavailable raises RuntimeError(f"Strava API request failed: {reason}").
    - StravaClient.refresh_token() returns the refreshed token; on StravaUnavailable raises RuntimeError(f"Strava OAuth token refresh failed: {reason}").
    - Transport is built via the SHARED _build_strava_transport(settings, clock, sleeper) helper (creds via the Task 1 settings accessor, token_path, RateLimitPolicy) composing FileTokenProvider -> TokenRefreshTransport -> StravaTransport — the SAME helper build_refresh_collaborators will call in 12-03 (no parallel wiring). No client_secret appears in any exception message.
    - Focused test (tests/test_strava_client.py) covers: (1) normal api_request returns (data, rate_headers); (2) rate-limited StravaUnavailable -> sentinel dict; (3) non-rate StravaUnavailable -> RuntimeError "Strava API request failed:"; (4) refresh_token error -> RuntimeError "Strava OAuth token refresh failed:"; (5) no client_secret string leaks into any raised exception message.
  </behavior>
  <action>
    Create `adapters/strava/client.py` with a module-level `_build_strava_transport(settings, clock, sleeper)` helper that composes `FileTokenProvider -> TokenRefreshTransport -> StravaTransport` (sourcing creds from the Task 1 settings accessor, using `RateLimitPolicy()`), then `class StravaClient` that builds its transport via that helper with `SystemClock`/`SystemSleeper` (D-07). This helper is the single wiring path — 12-03 repoints `bootstrap.build_refresh_collaborators` to call it too. Define `api_request(self, path)` (NO `token` kwarg — review MEDIUM, GP-02) and `refresh_token(self)` preserving VERBATIM the return contract and error mapping from db.py (api_request L208-217, refresh_token L201-205), including the rate-limited sentinel dict. Do NOT include client_secret in error strings (only `exc.reason`, as today). Export `StravaClient` from `adapters/strava/__init__.py` (`__all__`, alphabetical). Write `tests/test_strava_client.py` covering the five behavior cases above (use fakes/monkeypatch for the transport so no real network). Do not edit db.py.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_strava_client.py tests/test_security_guards.py -x && uv run python -c "import inspect; from mcp_strava.adapters.strava import StravaClient; assert 'token' not in inspect.signature(StravaClient.api_request).parameters, 'token param must be dropped'; print('ok')"</automated>
  </verify>
  <done>StravaClient importable; api_request has no token parameter; api_request/refresh_token preserve db.py's contract + error mapping; transport built via shared _build_strava_transport; no secret in error messages; tests/test_strava_client.py (5 cases) + security-guard suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings/.env → process | Strava client creds resolution (relocated into settings, D-06) |
| process → Strava API | outbound HTTP via StravaClient facade (relocated from db.py, D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information Disclosure | StravaClient error mapping / settings creds | mitigate | Error messages surface only `exc.reason` (never client_secret), matching current db.py behavior; settings resolution reuses existing env handling — verified by test_security_guards in Task 3 |
| T-12-03 | Tampering | facade behavior parity with db.api_request | mitigate | Preserve (data, rate_headers) contract + rate-limited sentinel verbatim; full caller migration regression in 12-03/12-04 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `uv run python -c "from mcp_strava.adapters.strava import SystemClock, SystemSleeper, StravaClient"` succeeds.
- settings resolves the two creds and fails fast when required+missing.
- `uv run pytest -q -x` green (additive; db.py still present and used).
- `uv run ruff check src` and `uv run pyright src` clean.
</verification>

<success_criteria>
settings owns Strava creds (D-06); clock.py + client.py exist with parity-preserving behavior (D-07/D-08); D-05/D-09 dead-code deletions are scheduled (not relocated/rebuilt); suite green.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-02-SUMMARY.md` when done.
</output>
