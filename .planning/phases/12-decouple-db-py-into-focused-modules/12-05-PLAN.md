---
phase: 12-decouple-db-py-into-focused-modules
plan: 05
type: execute
wave: 4
depends_on: ["12-04"]
files_modified:
  - src/mcp_strava/db.py
  - src/mcp_strava/strava_api_reference.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "src/mcp_strava/db.py no longer exists in the tree"
    - "get_zones and init_db (dead code) are gone with db.py — never relocated or rebuilt (D-09, RI-03) — and no stale `get_zones()` reference comment remains (e.g. strava_api_reference.py L54)"
    - "no live mcp_strava.db / db.py reference remains in ops config (Justfile, Dockerfile, compose) either, not just src/tests"
    - "Full local pytest suite (323 tests) is green and the just test Docker smoke gate passes"
  artifacts:
    - path: "src/mcp_strava/db.py"
      provides: "DELETED — must not exist"
      contains: "DELETED"
  key_links:
    - from: "repository"
      to: "no module imports mcp_strava.db"
      via: "hard-cut deletion after all callers migrated"
      pattern: "mcp_strava.db"
---

<objective>
Delete `src/mcp_strava/db.py` and prove behavior parity (D-10 step 6, GP-02 hard-cut). By this point (after 12-01..12-04) no module references `db.py`; this plan removes the file and runs the authoritative phase gate.

Deleting db.py also removes the dead `get_zones` and `init_db` along with `_CompatTokenProvider`/`_read_token_values`/`_RealClock`/`_RealSleeper`/`_build_transport`/factory wrappers — every symbol either relocated (12-01/12-02) or confirmed dead (RESEARCH RI-02/RI-03).

Purpose: eliminate the last legacy coupling hotspot; the module that started the phase ceases to exist.
Output: db.py gone; suite + smoke green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@src/mcp_strava/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero references, then delete db.py</name>
  <files>src/mcp_strava/db.py</files>
  <read_first>
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Blast Radius — confirm every site migrated in 12-03/12-04)
    - src/mcp_strava/db.py (final confirmation of what is being deleted)
    - src/mcp_strava/strava_api_reference.py (L54 stale `get_zones()` comment referencing dead code)
  </read_first>
  <action>
    First assert nothing imports the module across src, tests, AND ops config: `rg "from mcp_strava\.db import|import mcp_strava\.db|mcp_strava\.db\." src tests` must return no matches, and `rg "mcp_strava\.db|db\.py" Justfile Dockerfile deploy compose.yaml docker-compose.yml 2>/dev/null` must surface no live reference (review suggestion OpenCode — not just src/tests). If any match remains, STOP — the corresponding caller migration (12-03/12-04) was incomplete; fix it before deleting. Once clean, delete `src/mcp_strava/db.py` (e.g., `git rm src/mcp_strava/db.py`). This removes the relocated-or-dead symbols in one cut (GP-02 — no shim, no re-export layer left behind). Also fix the stale `get_zones()` reference comment in `strava_api_reference.py` L54 (the function is deleted with db.py — RESEARCH RI-02; the repo's stated preference is no stale legacy references, review LOW Codex): rewrite the `GET /athlete/zones` note to record it as no-longer-used rather than naming the deleted `get_zones()`.
  </action>
  <verify>
    <automated>test ! -f src/mcp_strava/db.py && ! rg -q "mcp_strava\.db" src tests && ! rg -q "get_zones" src tests && echo "deleted-and-clean"</automated>
  </verify>
  <done>src/mcp_strava/db.py does not exist; no `mcp_strava.db` reference remains in src/tests or ops config; no `get_zones` reference (including the strava_api_reference.py comment) remains anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — local suite + static checks + Docker smoke</name>
  <files>src/mcp_strava/db.py</files>
  <read_first>
    - Justfile (test recipe = uv run pytest -q + compose build + container smoke)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Validation Architecture — sampling/phase gate)
  </read_first>
  <action>
    Run `uv run pytest -q` (323 tests must be green), `uv run ruff check src tests`, `uv run ruff format --check src tests`, and `uv run pyright src` — all clean. Then run the authoritative project gate `just test` (uv pytest + `docker compose build` + container up + MCP smoke) to confirm the live MCP surface still works after the refactor. Per the project's Docker rule, the smoke step recreates the container with the rebuilt image. If any check fails, the deletion exposed a missed coupling — diagnose against the Blast Radius table and fix in the appropriate prior plan's scope.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check src tests && uv run pyright src && just test</automated>
  </verify>
  <done>`uv run pytest -q` green (323); ruff + format-check + pyright clean; `just test` (Docker build + container smoke) passes — full behavior parity confirmed with db.py gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → runtime | deletion must not strand any import path; live MCP surface must still answer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Denial of Service | premature deletion strands an import | mitigate | Task 1 gates deletion on a zero-reference grep; Task 2 runs the Docker smoke gate to catch runtime-only breakage |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `src/mcp_strava/db.py` absent; `rg "mcp_strava\.db" src tests` empty.
- `uv run pytest -q` green; ruff/format/pyright clean; `just test` smoke passes.
</verification>

<success_criteria>
db.py deleted; dead get_zones/init_db gone with it; full local suite + Docker smoke green — the phase goal (dissolve db.py, behavior unchanged) achieved.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-05-SUMMARY.md` when done.
</output>
