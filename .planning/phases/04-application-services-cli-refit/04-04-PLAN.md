---
phase: 04-application-services-cli-refit
plan: 04-04
type: tdd
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/mcp_strava/cli.py
  - src/mcp_strava/application/registry.py
  - tests/test_cli_surface.py
  - tests/test_security_guards.py
  - tests/test_smoke.py
  - docs/cli.md
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - CLI-01
  - CLI-02
  - CLI-03
  - TEST-04
must_haves:
  truths:
    - "D-14: CLI uses a Product/Admin split."
    - "D-15: future MCP uses the product service registry only and must not discover admin/debug commands."
    - "D-16: no legacy compatibility aliases are required; replacement mapping documents retained capabilities."
    - "D-17: product commands provide human-readable default output and `--json` full envelope output."
    - "D-01/D-02: product CLI handlers call application services instead of owning analytics, SQL, or Strava logic."
    - "CLI-01: retained operator capabilities include report, weekly, workouts, freshness, refresh, backfill, SQL, raw/debug workflows."
    - "CLI-02: calculations live in services/adapters, not CLI handlers."
    - "CLI-03: replacement mapping exists in `docs/cli.md` and CLI help."
  artifacts:
    - path: "src/mcp_strava/cli.py"
      provides: "Product/admin command dispatcher with product service calls and admin/debug isolation"
    - path: "docs/cli.md"
      provides: "Replacement mapping for retained CLI capabilities"
    - path: "tests/test_cli_surface.py"
      provides: "CLI command surface, rendering, and service-call tests"
---

<objective>
Refit the CLI into product and admin surfaces over the new application services, with human-readable defaults and `--json` full-envelope output for product commands.

Output: thin CLI dispatcher, product/admin command split, replacement mapping docs, and tests/guards proving product paths use services and admin paths stay out of the product registry.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-application-services-cli-refit/04-CONTEXT.md
@.planning/phases/04-application-services-cli-refit/04-RESEARCH.md
@.planning/phases/04-application-services-cli-refit/04-PATTERNS.md
@src/mcp_strava/cli.py
@src/mcp_strava/application/registry.py
@src/mcp_strava/types.py
@tests/test_security_guards.py
@tests/test_smoke.py
</context>

<decision_traceability>
- D-14 through D-17 are implemented by CLI surface and docs.
- D-01 through D-03 are verified again by CLI product handlers calling only application services.
- CLI-01 through CLI-03 are the primary requirements for this plan.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI surface and rendering tests</name>
  <files>tests/test_cli_surface.py, tests/test_security_guards.py</files>
  <read_first>
    - src/mcp_strava/cli.py
    - src/mcp_strava/application/registry.py
    - src/mcp_strava/types.py
    - tests/test_security_guards.py
    - .planning/phases/04-application-services-cli-refit/04-CONTEXT.md
  </read_first>
  <action>Create `tests/test_cli_surface.py` with tests that monkeypatch application service functions to return fake `ServiceEnvelope` values. Assert product commands dispatch to services and do not call `daily_report()`, `weekly_digest()`, `DbConn.execute`, `api_request`, `sync_activities`, or `backfill_activities`. Cover product commands: `report daily`, `weekly`, `workouts recent`, `workout analyze <id>`, and `freshness`. For each product command, test default human-readable output contains key labels and does not print raw Python dataclass repr; test `--json` output parses as JSON and contains `data`, `freshness`, `completeness`, `warnings`, and `rationale`. Test admin commands exist for refresh/backfill/sql/raw/log (exact spelling chosen by implementation) and are absent from `PRODUCT_SERVICES`. Extend `tests/test_security_guards.py` with `test_product_service_registry_excludes_admin_debug_commands` and `test_cli_product_handlers_do_not_call_sync_or_raw_api_directly` using AST checks or monkeypatch spies.</action>
  <verify>
    <automated>python3 -m pytest tests/test_cli_surface.py tests/test_security_guards.py -q</automated>
  </verify>
  <done>Tests fail before CLI refit exists.</done>
  <acceptance_criteria>
    - `tests/test_cli_surface.py` contains tests for human-readable and `--json` product command modes.
    - Tests assert admin/debug commands are absent from `PRODUCT_SERVICES`.
    - Tests assert product handlers call application services, not existing calculation/sync/raw functions directly.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refit CLI to product/admin command split</name>
  <files>src/mcp_strava/cli.py, src/mcp_strava/application/registry.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/cli.py
    - src/mcp_strava/application/reports.py
    - src/mcp_strava/application/workouts.py
    - src/mcp_strava/application/freshness.py
    - src/mcp_strava/application/registry.py
    - src/mcp_strava/types.py
    - tests/test_cli_surface.py
  </read_first>
  <action>Refactor `src/mcp_strava/cli.py` so product commands parse args, call application services, and render either human-readable output or full JSON envelope via `dc_to_dict()`. Implement product command families for daily report, weekly summary, recent workouts, per-workout analytics, and freshness. Exact spelling is planner/executor discretion, but tests and docs must agree. Keep admin/debug commands separate under an `admin` namespace or an explicit admin registry: refresh, backfill, SQL, raw Strava path, sync log, db-preflight/check/migrate as retained local operator workflows. Remove old top-level product aliases unless the implementation intentionally leaves a clear error/replacement message; do not keep legacy aliases as hidden second paths. Keep module entrypoint behavior (`python -m mcp_strava`) showing usage and commands.

Human-readable product output should include freshness facts and factual warnings. `--json` must emit the full `ServiceEnvelope` JSON. Admin/debug output can remain current pragmatic text/JSON.</action>
  <verify>
    <automated>python3 -m pytest tests/test_cli_surface.py tests/test_security_guards.py tests/test_smoke.py -q</automated>
  </verify>
  <done>CLI product/admin split tests pass and smoke entrypoint remains operational.</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_cli_surface.py tests/test_security_guards.py tests/test_smoke.py -q` exits 0.
    - `PYTHONPATH=src python3 -m mcp_strava report daily --json` prints JSON with top-level `data`, `freshness`, `completeness`, `warnings`, `rationale` when the live DB exists.
    - `PYTHONPATH=src python3 -m mcp_strava freshness --json` prints freshness metadata without calling Strava.
    - `src/mcp_strava/cli.py` product handlers do not contain SQL query strings for analytics/listing behavior.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document replacement mapping and final Phase 4 verification</name>
  <files>docs/cli.md, tests/test_cli_surface.py, tests/test_security_guards.py</files>
  <read_first>
    - src/mcp_strava/cli.py
    - .planning/REQUIREMENTS.md
    - .planning/phases/04-application-services-cli-refit/04-CONTEXT.md
    - tests/test_cli_surface.py
  </read_first>
  <action>Create `docs/cli.md` with a compact replacement mapping table for retained capabilities: old activities/report/weekly/trend/sync/backfill/raw/sql/log/kudos/db-* workflows to the new product or admin command. Mark removed or deferred workflows explicitly if not retained. Add a short section stating product commands support human-readable default and `--json`, while admin/debug commands are local operator workflows and are not part of the future MCP surface. Add tests or assertions in `tests/test_cli_surface.py` that `docs/cli.md` contains the strings `report daily`, `weekly`, `workouts recent`, `workout analyze`, `freshness`, `admin refresh`, `admin backfill`, `admin sql`, `admin raw`, and `admin log` or the exact command spellings chosen in Task 2.</action>
  <verify>
    <automated>just test</automated>
  </verify>
  <done>CLI replacement mapping exists and the full test suite passes.</done>
  <acceptance_criteria>
    - `docs/cli.md` exists and contains a replacement mapping table.
    - `docs/cli.md` states admin/debug commands are not part of the MCP surface.
    - `just test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI product -> application services | Product CLI must not own analytics, SQL, sync, or Strava behavior. |
| CLI admin -> future MCP | Admin/debug commands must not enter product registry or future MCP tool surface. |
| Human CLI -> JSON automation | Human output must not hide metadata; JSON output must preserve full envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | Product/admin command split | mitigate | Product registry allowlist and tests excluding admin/debug command names. |
| T-04-09 | Information Disclosure | Raw/sql/admin commands | mitigate | Keep local admin namespace only; document as non-MCP surface. |
| T-04-10 | Information Integrity | Human-readable rendering | mitigate | Product renderers include freshness facts and factual warnings; `--json` full envelope tested. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_cli_surface.py tests/test_security_guards.py tests/test_smoke.py -q`
- `PYTHONPATH=src python3 -m mcp_strava report daily --json`
- `PYTHONPATH=src python3 -m mcp_strava freshness --json`
- `just test`
</verification>

<success_criteria>
- CLI exposes product analytics through application services with human/default and `--json` modes.
- CLI exposes retained admin/debug workflows separately.
- Replacement mapping is documented.
- Product registry remains safe for future MCP use.
</success_criteria>

<output>
Create `.planning/phases/04-application-services-cli-refit/04-04-SUMMARY.md` when done.
</output>

