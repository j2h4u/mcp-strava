---
phase: 04-application-services-cli-refit
plan: 04-02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/mcp_strava/report.py
  - src/mcp_strava/analytics.py
  - src/mcp_strava/application/reports.py
  - src/mcp_strava/application/__init__.py
  - tests/test_application_reports.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-04
  - TEST-04
must_haves:
  truths:
    - "D-01: daily and weekly product use cases are exposed by application services."
    - "D-02: report/weekly services compute from local SQLite mirror data without Strava, sync, raw API, or sync-log calls."
    - "D-07/D-10: service warnings remain factual and do not contain stale-data coaching policy."
    - "D-11: missing HR/streams/details produce partial/unavailable metadata, not invented zeros."
    - "D-12/D-13: daily and weekly service outputs use the shared envelope while preserving typed `DailyReport` and `WeeklyDigest` data."
    - "D-18/D-19: existing `DailyReport.activities_14d` per-activity metrics remain available through the daily report data payload."
  artifacts:
    - path: "src/mcp_strava/application/reports.py"
      provides: "Daily report and weekly summary application services returning ServiceEnvelope"
    - path: "tests/test_application_reports.py"
      provides: "Fixture-backed parity and metadata tests for daily/weekly services"
---

<objective>
Route daily report and weekly summary through application services that preserve current analytics while adding the shared factual envelope.

Output: report/weekly service functions, optional pure wrappers that accept a connection/repository for tests, and parity tests proving key existing outputs survive the refactor.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-application-services-cli-refit/04-CONTEXT.md
@.planning/phases/04-application-services-cli-refit/04-RESEARCH.md
@.planning/phases/04-application-services-cli-refit/04-PATTERNS.md
@src/mcp_strava/report.py
@src/mcp_strava/analytics.py
@src/mcp_strava/application/freshness.py
@src/mcp_strava/types.py
@tests/test_smoke.py
</context>

<decision_traceability>
- D-01, D-02, D-07, D-10, D-11, D-12, D-13, D-18, and D-19 are covered by the report/weekly services and tests.
- APP-01 and APP-02 are the primary requirements for this plan.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add report and weekly service parity tests</name>
  <files>tests/test_application_reports.py</files>
  <read_first>
    - tests/test_smoke.py
    - tests/test_application_services.py
    - src/mcp_strava/report.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/types.py
    - .planning/phases/04-application-services-cli-refit/04-CONTEXT.md
  </read_first>
  <action>Create `tests/test_application_reports.py` with fixture helpers that use a temporary SQLite database and inject it into services, either by passing a `connection=`/`repository=` keyword or by setting `MCP_STRAVA_DATABASE_PATH` plus resetting the settings cache. Do not rely only on monkeypatching `daily_report()` / `weekly_digest()` because those tests would miss service-to-DB wiring. Add tests asserting `get_daily_report_service(now=..., signal_first_use=False, connection=fixture_conn)` returns a `ServiceEnvelope` with `data.today`, `data.activities_14d`, `freshness.last_successful_refresh_at`, `completeness.status`, `warnings`, and `rationale`; `get_weekly_summary_service(...)` returns a `ServiceEnvelope` with `data.period`, `data.current_state`, `data.trends`, and metadata; `dc_to_dict(envelope)` produces JSON-safe nested dicts; when weekly digest has no data, the envelope uses `completeness.status='insufficient'` and `data is None` rather than raising. Replace brittle phrase-blocking checks with a structured invariant: every warning code must be in the factual allowlist (`mirror_stale`, `last_activity_old`, `missing_hr`, `missing_streams`, `missing_details`, `metric_unavailable`, `empty_weekly_history`, `insufficient_history`).</action>
  <verify>
    <automated>python3 -m pytest tests/test_application_reports.py -q</automated>
  </verify>
  <done>Tests fail before application report services exist.</done>
  <acceptance_criteria>
    - `tests/test_application_reports.py` contains tests for `get_daily_report_service`, `get_weekly_summary_service`, `dc_to_dict`, and insufficient weekly data.
    - Tests exercise real service wiring against a fixture DB through explicit injection or environment-driven settings reset.
    - Tests assert `freshness`, `completeness`, `warnings`, and `rationale` are present in both daily and weekly envelopes.
    - Tests assert warnings use factual allowlisted codes rather than coaching directives tied to stale data.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement daily and weekly application services</name>
  <files>src/mcp_strava/report.py, src/mcp_strava/analytics.py, src/mcp_strava/application/reports.py, src/mcp_strava/application/__init__.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/report.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/application/freshness.py
    - src/mcp_strava/types.py
    - tests/test_application_reports.py
    - tests/test_smoke.py
  </read_first>
  <action>Create `src/mcp_strava/application/reports.py` with `get_daily_report_service(*, now: datetime | None = None, signal_first_use: bool = True, connection=None) -> ServiceEnvelope` and `get_weekly_summary_service(*, now: datetime | None = None, signal_first_use: bool = True, connection=None) -> ServiceEnvelope`. If `connection` is not provided, open the expected mirror through existing `DbConn`; otherwise use the injected connection and do not open the live DB. Create `SQLiteRepository.from_connection(conn)`, build freshness metadata through `build_freshness_metadata()`, then call the existing calculations. Refactor `daily_report()` into an internal `daily_report_from_connection(conn, now_local=None)` while preserving `daily_report()` as a compatibility wrapper. Keep formula changes out of scope. For weekly, either refactor `weekly_digest()` to accept an optional repository or document the intentional raw-connection call; map `None` to `ServiceEnvelope(data=None, completeness.status='insufficient', missing=['insufficient_history'])`.

Add factual completeness aggregation for daily/weekly using repository load status as the source of truth where available: use `repo.daily_load_points_between(window_start, today)` to count `PARTIAL` and `UNKNOWN` days rather than inventing a separate `trimp == 0` rule. Inspect `DailyReport.activities_14d` via `EnrichedActivity` nullable fields for per-activity gaps. Inspect `WeeklyDigest.this_week` as serialized dict entries, not dataclass instances, checking keys such as `avg_hr`, `trimp`, and metric fields. Add factual warnings with codes such as `missing_hr`, `missing_stream_metrics`, `empty_weekly_history`, and `mirror_stale`; do not include coaching directives based on freshness.

Update `application/__init__.py` exports. Update `tests/test_smoke.py::test_imports` to import the new report services.</action>
  <verify>
    <automated>python3 -m pytest tests/test_application_reports.py tests/test_smoke.py -q</automated>
  </verify>
  <done>Daily/weekly service tests pass and existing smoke behavior remains intact.</done>
  <acceptance_criteria>
    - `src/mcp_strava/application/reports.py` defines `get_daily_report_service` and `get_weekly_summary_service`.
    - `src/mcp_strava/application/reports.py` imports neither `mcp_strava.adapters.strava` nor `mcp_strava.sync` nor `mcp_strava.refresh.runtime`.
    - `daily_report()` still exists for compatibility and smoke tests.
    - `weekly_digest(conn)` still exists for compatibility and smoke tests.
    - Service tests can run against a fixture DB without touching `data/strava.db`.
    - Weekly completeness inspection handles `WeeklyDigest.this_week` dict entries without attribute access.
    - `python3 -m pytest tests/test_application_reports.py tests/test_smoke.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reports service -> mirror DB | Reads local mirror and must not create empty DBs or call Strava. |
| Metadata -> recommendation consumer | Metadata is factual and must not hide stale/incomplete data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Integrity | Daily/weekly service parity | mitigate | Tests compare key fields from current `DailyReport` and `WeeklyDigest` behavior. |
| T-04-05 | Information Integrity | Missing metrics | mitigate | Null/unavailable metrics are surfaced through completeness and warnings. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_application_reports.py tests/test_smoke.py -q`
- `just test`
</verification>

<success_criteria>
- Daily and weekly application services return the shared envelope.
- Current daily/weekly analytics remain available with key-field parity.
- Services expose factual freshness/completeness warnings without coaching based on stale data.
</success_criteria>

<output>
Create `.planning/phases/04-application-services-cli-refit/04-02-SUMMARY.md` when done.
</output>
