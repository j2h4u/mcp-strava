---
phase: 04-application-services-cli-refit
plan: 04-03
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/mcp_strava/application/workouts.py
  - src/mcp_strava/application/__init__.py
  - src/mcp_strava/adapters/sqlite/repository.py
  - tests/test_application_workouts.py
autonomous: true
requirements:
  - APP-03
  - APP-04
  - TEST-04
must_haves:
  truths:
    - "D-01/D-02: workout services are product read services over the local mirror and do not call Strava/sync/admin paths."
    - "D-08/D-09: workout services expose refresh and last-activity facts separately."
    - "D-11: unavailable per-workout metrics remain null/unavailable with missing reasons."
    - "D-12/D-13: recent workouts and workout analytics return the shared envelope with typed service-specific data."
    - "D-18/D-19/D-20: workout detail/analytics preserve the existing `EnrichedActivity` metric surface and do not add similar-workout comparison."
  artifacts:
    - path: "src/mcp_strava/application/workouts.py"
      provides: "Recent workout and per-workout analytics services"
    - path: "tests/test_application_workouts.py"
      provides: "Workout list/detail parity, not-found, missing-HR/stream, and no-network tests"
---

<objective>
Expose recent workouts and per-workout analytics as product application services, preserving the current useful metric surface from `EnrichedActivity`.

Output: local-mirror workout services returning service envelopes and tests covering recent list, detail by id/latest, missing data, and no live Strava calls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-application-services-cli-refit/04-CONTEXT.md
@.planning/phases/04-application-services-cli-refit/04-RESEARCH.md
@.planning/phases/04-application-services-cli-refit/04-PATTERNS.md
@src/mcp_strava/metrics.py
@src/mcp_strava/types.py
@src/mcp_strava/adapters/sqlite/repository.py
@src/mcp_strava/application/freshness.py
</context>

<decision_traceability>
- D-18 through D-20 are the core decisions for this plan.
- APP-03 is implemented by recent-workout and workout-analytics services.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workout service contract and parity tests</name>
  <files>tests/test_application_workouts.py</files>
  <read_first>
    - src/mcp_strava/metrics.py
    - src/mcp_strava/types.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - tests/test_load_status.py
    - tests/test_repository_boundary.py
    - .planning/phases/04-application-services-cli-refit/04-CONTEXT.md
  </read_first>
  <action>Create `tests/test_application_workouts.py` with fixture SQLite activities and streams. Add tests asserting `get_recent_workouts_service(limit=2, signal_first_use=False)` returns a `ServiceEnvelope` with compact workouts sorted by date descending, local freshness metadata, and no Strava/network calls. Add tests asserting `get_workout_analytics_service(activity_id=...)` returns the existing enriched fields: `id`, `date`, `name`, `sport_type`, `distance_km`, `moving_time_min`, `elapsed_time_min`, `elevation_m`, `trimp`, `avg_hr`, `max_hr`, `hr_recovery`, `vertical_speed`, `cc`, `cardiac_drift`, `hrr_pct`, `start_time`. Add `latest` behavior test using newest activity id. Add not-found test returning `data=None`, `completeness.status='unavailable'`, and warning code `workout_not_found`. Add missing-HR/streams tests expecting nullable metric fields and missing reason codes such as `missing_hr`, `missing_streams`, and `metric_unavailable`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_application_workouts.py -q</automated>
  </verify>
  <done>Tests fail before workout services exist.</done>
  <acceptance_criteria>
    - `tests/test_application_workouts.py` contains tests for recent workouts, workout analytics by id, latest workout, not found, and missing HR/streams.
    - Tests assert no live network by monkeypatching `urllib.request.urlopen` to raise "live network forbidden".
    - Tests include expected fields from `EnrichedActivity` listed in D-19.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recent workout and workout analytics services</name>
  <files>src/mcp_strava/application/workouts.py, src/mcp_strava/application/__init__.py, src/mcp_strava/adapters/sqlite/repository.py</files>
  <read_first>
    - src/mcp_strava/metrics.py
    - src/mcp_strava/types.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/application/freshness.py
    - tests/test_application_workouts.py
  </read_first>
  <action>Create `src/mcp_strava/application/workouts.py` with `get_recent_workouts_service(limit: int = 15, *, now: datetime | None = None, signal_first_use: bool = True) -> ServiceEnvelope` and `get_workout_analytics_service(activity_id: int | str, *, now: datetime | None = None, signal_first_use: bool = True) -> ServiceEnvelope`. Use `DbConn` + `SQLiteRepository` for reads. `recent` should use `repo.recent_activities(limit)` and compute compact fields compatible with current `cmd_activities`: id, date, name, sport_type, distance_km, moving_time_min, trimp, avg_hr, max_hr. `analytics` should call `repo.activity_by_id()` (or latest id when `activity_id == 'latest'`) and then `enrich_activity(conn, row)` to preserve D-19. Add completeness helper functions that inspect activity rows and stream rows for missing `summary_json`, `detail_json`, streams, HR, and metric-specific unavailable fields. The services must use `build_freshness_metadata()` and must not import Strava adapter, sync, raw API, or refresh runtime execution functions.

If repository lacks efficient helpers, add narrowly scoped methods such as `latest_activity_id()` and `activity_stream_summary(activity_id)` returning counts for total stream rows and HR stream rows.</action>
  <verify>
    <automated>python3 -m pytest tests/test_application_workouts.py tests/test_application_services.py tests/test_security_guards.py -q</automated>
  </verify>
  <done>Workout services pass all contract tests and preserve current per-workout analytics fields.</done>
  <acceptance_criteria>
    - `src/mcp_strava/application/workouts.py` defines `get_recent_workouts_service` and `get_workout_analytics_service`.
    - `get_workout_analytics_service('latest')` resolves to the newest local activity by date.
    - `src/mcp_strava/application/workouts.py` contains neither `api_request` nor `refresh_token` nor `run_once` nor `StravaTransport`.
    - `python3 -m pytest tests/test_application_workouts.py tests/test_application_services.py tests/test_security_guards.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workout service -> local mirror | Reads must never create a new empty DB or call Strava. |
| Workout metric availability -> consumer | Missing HR/streams must be explicit to avoid false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Integrity | Per-workout analytics | mitigate | Preserve `EnrichedActivity` fields and add missing-data codes. |
| T-04-07 | Denial of Service | Recent-workout requests | mitigate | Use repository limit and no live network/API calls. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_application_workouts.py tests/test_application_services.py tests/test_security_guards.py -q`
- `just test`
</verification>

<success_criteria>
- Recent workouts and workout analytics are available through application services.
- Per-workout analytics preserve existing useful metrics.
- Missing HR/streams/details are factual metadata, not silent zeros.
</success_criteria>

<output>
Create `.planning/phases/04-application-services-cli-refit/04-03-SUMMARY.md` when done.
</output>

