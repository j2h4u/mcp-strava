---
phase: 10-materialize-unwired-training-metrics-and-enforce-core-domain
plan: 04
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-03"
files_modified:
  - src/mcp_strava/db.py
  - tests/test_smoke.py
  - tests/test_metric_services.py
  - tests/test_security_guards.py
autonomous: true
requirements:
  - "Core/domain separation (PROJECT.md Active)"
user_setup: []

must_haves:
  truths:
    - "db.py no longer defines get_daily_trimp_history (it has zero src importers)"
    - "test_smoke.py no longer imports get_daily_trimp_history (line 11) — the import is dropped in THIS plan, the same wave that deletes the function, so the suite stays collectable. (The line-12 metrics imports enrich_activity/calc_decoupling_with_gate/calc_efficiency_factor and the test_decoupling_invalid/test_calc_decoupling functions were already removed in plan 10-01 wave 1; this plan does NOT re-handle them.)"
    - "Stale enrich_activity references removed: test_metric_services.py _block_legacy_recompute list (line ~251) and test_security_guards.py negative-assert lists (lines ~292, ~490, ~495) no longer name the now-deleted enrich_activity (CONTEXT.md scope item 5)"
    - "The full test suite is green with no regression in TRIMP / HR zones / cardiac_cost"
  artifacts:
    - path: "src/mcp_strava/db.py"
      provides: "db module without the dead get_daily_trimp_history"
      contains: "def init_db"
    - path: "tests/test_smoke.py"
      provides: "Smoke tests referencing only live symbols"
      contains: "def test_imports"
  key_links:
    - from: "tests/test_smoke.py"
      to: "mcp_strava.metrics / mcp_strava.db"
      via: "imports of only surviving symbols"
      pattern: "from mcp_strava"
---

<objective>
Finish the cleanup (CONTEXT.md scope items 2, 5, 6): delete the dead db.py::get_daily_trimp_history and
repair the stale tests that referenced the symbols removed in plan 10-01, so the full suite is green.

Purpose: remove abandoned code and keep the test suite honest after the extraction/deletions.
Output: db.py without get_daily_trimp_history; test_smoke.py with its line-11 get_daily_trimp_history import
dropped in the same wave the function is deleted (the line-12 metrics imports and the
test_decoupling_invalid/test_calc_decoupling functions were already repaired in plan 10-01 wave 1 — this
plan does NOT re-handle them); and the stale enrich_activity references purged from test_metric_services.py
and test_security_guards.py (the CONTEXT.md scope item 5 cleanup that no prior plan covered).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/CONTEXT.md
@.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md
@src/mcp_strava/db.py
@tests/test_smoke.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead db.py::get_daily_trimp_history</name>
  <files>src/mcp_strava/db.py</files>
  <read_first>
    - src/mcp_strava/db.py (the get_daily_trimp_history function at ~line 241 and any imports it alone used: datetime/timedelta, get_settings, get_zone_model)
    - .planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md (dead-code table — zero src importers; test_security_guards.py keeps it in the cli.py forbidden-import set, which is correct and must NOT be changed)
  </read_first>
  <action>
    Remove the get_daily_trimp_history function from src/mcp_strava/db.py. Remove any module-level imports
    that become unused solely because of this deletion (verify by grep that the symbol is not referenced
    elsewhere in db.py before removing an import). Do NOT touch the legacy_db_imports set in
    test_security_guards.py — it correctly continues to forbid re-importing get_daily_trimp_history into cli.py.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/test_security_guards.py -k legacy -x -q && test "$(grep -c 'def get_daily_trimp_history' src/mcp_strava/db.py)" = "0" && PYTHONPATH=src python -m pytest tests/test_smoke.py --collect-only -q</automated>
  </verify>
  <done>get_daily_trimp_history is gone from db.py (grep count 0), db.py imports cleanly, and the cli.py legacy-import guard still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repair test_smoke.py — drop ONLY the get_daily_trimp_history import + DecouplingResult</name>
  <files>tests/test_smoke.py</files>
  <read_first>
    - tests/test_smoke.py (line 11 imports get_daily_trimp_history — the symbol THIS plan's Task 1 deletes; line 10 imports DecouplingResult from mcp_strava.types. NOTE: the line-12 metrics imports and the test_decoupling_invalid/test_calc_decoupling functions were ALREADY removed by plan 10-01 wave 1 — they will not be present; do not look for them and do not re-edit them.)
    - .planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-RESEARCH.md (dead-code table + Open Question on DecouplingResult — recommend leaving the dataclass defined in mcp_strava.types and dropping it from the smoke import)
  </read_first>
  <action>
    Scope this edit to ONLY the symbol this plan deletes (Task 1's get_daily_trimp_history) plus the
    DecouplingResult import-tidy. In test_imports: remove get_daily_trimp_history from the
    `from mcp_strava.db import DbConn, get_daily_trimp_history` line (keep DbConn), and remove DecouplingResult
    from the `from mcp_strava.types import BanisterResult, DecouplingResult` line (keep BanisterResult).
    Do NOT touch the line-12 metrics import or the test_decoupling_invalid/test_calc_decoupling functions —
    plan 10-01 already removed them in wave 1; they are not part of this plan. Do not add new assertions.
    Leave all unrelated smoke assertions intact.
  </action>
  <verify>
    <automated>! grep -q "get_daily_trimp_history" tests/test_smoke.py && ! grep -q "DecouplingResult" tests/test_smoke.py && PYTHONPATH=src python -m pytest tests/test_smoke.py -x -q</automated>
  </verify>
  <done>test_smoke.py no longer imports get_daily_trimp_history or DecouplingResult; tests/test_smoke.py passes. The line-12 metrics import and decoupling tests were already handled by plan 10-01 (not re-edited here).</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale enrich_activity references from test_metric_services.py and test_security_guards.py</name>
  <files>tests/test_metric_services.py, tests/test_security_guards.py</files>
  <read_first>
    - tests/test_metric_services.py (the _block_legacy_recompute helper at ~line 247-258: a `for name in (...)` tuple that includes "enrich_activity" and is guarded by `if hasattr(metric_services, name)`)
    - tests/test_security_guards.py (the forbidden_calls set at ~line 287-292 containing "enrich_activity"; the forbidden_imports set at ~line 488-491 containing "mcp_strava.metrics.enrich_activity"; the forbidden_calls set at ~line 492-495 containing "enrich_activity")
    - .planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/CONTEXT.md (scope item 5 — these stale refs were in scope but no prior task covered them)
  </read_first>
  <action>
    These are dead references to the enrich_activity function deleted in plan 10-01 — they are silently
    inert (the metric_services entry is hasattr-guarded so the monkeypatch is skipped; the security-guard
    entries are negative assertions that stay true post-deletion), so removing them is a no-functional-change
    cleanup that keeps the suite honest. (1) In tests/test_metric_services.py, remove the "enrich_activity"
    element from the `for name in (...)` tuple in _block_legacy_recompute (keep the other names:
    daily_report_from_connection, weekly_digest, check_z5_minutes, check_hr_anomalies, calc_banister).
    (2) In tests/test_security_guards.py, remove "enrich_activity" from the forbidden_calls set at ~line 292,
    remove "mcp_strava.metrics.enrich_activity" from the forbidden_imports set at ~line 490, and remove
    "enrich_activity" from the forbidden_calls set at ~line 495. Do NOT touch the legacy_db_imports set
    (~line 88) that still correctly forbids get_daily_trimp_history / api_request in cli.py. Leave every
    other entry in these sets intact.
  </action>
  <verify>
    <automated>! grep -n "enrich_activity" tests/test_metric_services.py && ! grep -n "enrich_activity" tests/test_security_guards.py && PYTHONPATH=src python -m pytest tests/test_metric_services.py tests/test_security_guards.py -x -q && echo NO_STALE_ENRICH_REFS</automated>
  </verify>
  <done>grep finds no remaining `enrich_activity` reference in tests/test_metric_services.py or tests/test_security_guards.py; both test files still pass; the legacy_db_imports cli.py guard is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dead-code removal ↔ test suite | Deleting source symbols must not leave dangling imports that break test collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | accidental deletion of a still-used symbol | mitigate | RESEARCH.md verified zero src importers for each deleted symbol; verify steps re-run grep + the full smoke/guard tests after removal. |
| T-10-SC | Tampering | package installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `grep -c 'def get_daily_trimp_history' src/mcp_strava/db.py` returns 0.
- `grep -c get_daily_trimp_history tests/test_smoke.py` returns 0 (import dropped in the same wave as the function deletion).
- `PYTHONPATH=src python -m pytest tests/test_smoke.py -x -q` passes.
- `PYTHONPATH=src python -m pytest tests/test_security_guards.py -k legacy -x -q` passes (cli.py legacy-import guard intact).
- `grep -c enrich_activity tests/test_metric_services.py tests/test_security_guards.py` returns 0 for both (stale refs removed).
- `PYTHONPATH=src python -m pytest tests/test_metric_services.py tests/test_security_guards.py -x -q` passes.
- Full suite green (run `just test` at wave merge) with no TRIMP / zones / cardiac_cost regression.
</verification>

<success_criteria>
- Dead db.py function removed; the matching test_smoke.py line-11 get_daily_trimp_history import is dropped in this same wave (no dangling test imports; the metrics-symbol imports were already handled in 10-01).
- Stale enrich_activity references removed from test_metric_services.py and test_security_guards.py (CONTEXT.md scope item 5); legacy_db_imports cli.py guard left intact.
- Full test suite green after the phase's deletions.
</success_criteria>

<output>
Create `.planning/phases/10-materialize-unwired-training-metrics-and-enforce-core-domain/10-04-SUMMARY.md` when done.
</output>