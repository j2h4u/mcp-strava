---
phase: "13"
plan: "04"
type: execute
wave: 3
depends_on:
  - "13-02"
  - "13-03"
files_modified: []
autonomous: true
requirements:
  - "Code quality / tooling hygiene (ruff + pyright introduced in Phase 11 side-work)"

must_haves:
  truths:
    - "just check exits 0 (lint + fmt-check + typecheck all pass)"
    - "just test exits 0 (full test suite green)"
    - "uv run pyright src reports 0 errors, 0 warnings, 0 informations"
    - "uv run ruff check src tests reports All checks passed!"
    - "uv run ruff format --check src tests reports no reformatting needed"
  artifacts: []
  key_links:
    - from: "Justfile check target"
      to: "lint + fmt-check + typecheck sub-targets"
      via: "just check runs all three gates sequentially"
      pattern: "just check"
---

<objective>
Run the D-08 definition-of-done gates and confirm Phase 13 is complete.

This plan is verification-only — no code changes. It exists as a named wave-3 checkpoint so the executor has a clean terminal state to record in the SUMMARY, and so the phase gate is explicit rather than buried in Plan 02's acceptance criteria.

D-08: Both gates must exit green:
1. `just check` — runs `lint` (ruff check) + `fmt-check` (ruff format --check) + `typecheck` (pyright src)
2. `just test` — runs the full pytest suite

If either gate fails, the executor diagnoses and patches in this plan's context rather than opening a new gap-closure phase. The fix scope is narrow: any remaining pyright error or ruff violation is a missed item from Plans 01–03 and can be resolved here without replanning.

Purpose: Provide an unambiguous terminal green state for Phase 13 and a clean SUMMARY artifact.

Output: No file changes. A SUMMARY.md recording gate outputs and confirming D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-CONTEXT.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-01-SUMMARY.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-02-SUMMARY.md
@.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run D-08 gates — just check and just test</name>
  <files></files>

  <read_first>
  Read the Justfile `check`, `lint`, `fmt-check`, `typecheck`, and `test` target definitions to understand exactly what each gate invokes before running them.
  </read_first>

  <action>
  Run the two D-08 gates in sequence and capture their full output:

  1. `just check` — must exit 0. If it fails, diagnose which sub-target failed (lint/fmt-check/typecheck) and apply a minimal fix to the offending file(s). Do NOT suppress errors with `# type: ignore` or `# noqa` unless the suppression target is explicitly in the ruff select set and the noise is a known false positive.

  2. `just test` — must exit 0. If any test fails, read the failure output carefully. Annotation-only changes (Plans 01–03) should not break tests; a failure here likely means a signature change was broader than intended (e.g., a method body was accidentally modified). Diagnose and revert/repair the minimal diff needed.

  Record the exact terminal output (last 20 lines) of both commands in the SUMMARY.
  </action>

  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && just check && just test</automated>
  </verify>

  <acceptance_criteria>
  - `just check` exits 0 with no error lines
  - `just test` exits 0; all tests passed, 0 failures, 0 errors
  - `uv run pyright src 2>&1 | grep -E "^Found [0-9]+ error"` shows "Found 0 errors"
  - `uv run ruff check src tests 2>&1` shows "All checks passed!"
  - `uv run ruff format --check src tests 2>&1` shows no files that would be reformatted
  </acceptance_criteria>

  <done>both just check and just test exit 0; Phase 13 definition-of-done (D-08) satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification-only | No code changes; runs read-only tool invocations against existing code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; gate commands are read-only tool invocations |

This plan makes zero code changes. It runs `just check` and `just test` — both are pre-existing Justfile targets that have been the authoritative gates throughout the project. No new threats.
</threat_model>

<verification>
```
cd /home/j2h4u/repos/j2h4u/mcp-strava
just check
just test
```

Both commands must exit 0. Record full output in SUMMARY.md.
</verification>

<success_criteria>
- `just check` exits 0 (all three sub-gates: lint + fmt-check + typecheck pass)
- `just test` exits 0 (full pytest suite green)
- pyright reports "0 errors, 0 warnings, 0 informations"
- Phase 13 SUMMARY documents the gate outputs confirming D-08
</success_criteria>

<output>
Create `.planning/phases/13-lint-and-type-check-cleanup-ruff-residual-pyright/13-04-SUMMARY.md` when done.
</output>
