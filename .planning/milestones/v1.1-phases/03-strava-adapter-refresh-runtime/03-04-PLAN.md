---
phase: 03-strava-adapter-refresh-runtime
plan: 03-04
type: auto
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/mcp_strava/sync.py
  - src/mcp_strava/db.py
  - src/mcp_strava/cli.py
  - tests/test_security_guards.py
  - tests/test_smoke.py
autonomous: true
requirements:
  - STRAVA-01
  - STRAVA-03
  - REFRESH-01
  - REFRESH-02
  - TEST-02
must_haves:
  truths:
    - "`src/mcp_strava/sync.py::sync_activities(quick: bool = False)` is a thin wrapper that builds production adapter collaborators and calls `refresh.runtime.run_once(repo, transport, policy, clock, sleeper, force=quick, mode='quick' if quick else 'daily')` (D-15)."
    - "`src/mcp_strava/sync.py::backfill_activities(since: str | None = None)` is a thin wrapper that builds production collaborators and calls `refresh.runtime.run_backfill(repo, transport, policy, clock, sleeper, since=since, owner='refresh-backfill')` (D-16). It does NOT call `run_once`."
    - "`src/mcp_strava/sync.py` no longer defines `_sync_kudos`, `_insert_streams`, `_replace_streams`, `_stream_payload`, `STREAM_KEYS`, `_is_iso_day`, `_safe_quick_sync_start_day` — those names moved to `src/mcp_strava/refresh/_sync_ops.py` per D-17 (deleted, not duplicated)."
    - "`src/mcp_strava/sync.py` no longer imports `urllib`, no longer defines an inline `RateLimiter` class, and no longer defines `_fetch_with_retry`; those concerns live in `mcp_strava.adapters.strava`."
    - "D-17: `src/mcp_strava/sync.py` imports from `mcp_strava.refresh` (public API: `run_once`, `run_backfill`, `RefreshPolicy`, etc.) but NEVER imports `mcp_strava.refresh._sync_ops` directly (private)."
    - "D-17: no module under `src/mcp_strava/refresh/` imports `mcp_strava.sync` (one-way dependency, asserted by AST guard `test_refresh_does_not_import_sync` in `tests/test_security_guards.py`)."
    - "`src/mcp_strava/db.py` no longer contains inline `load_env`, `save_env`, `refresh_token`, `api_request`, or `_parse_rate_headers` implementations; any compat shims delegate to the Strava adapter."
    - "`src/mcp_strava/cli.py::COMMANDS` includes `db-refresh`, backed by `refresh.runtime.run_once(...)` over production collaborators; the command accepts `--force` (D-15) and maps it to `run_once(force=True)`."
    - "AST/import boundary tests in `tests/test_security_guards.py` enforce that `report.py`, `analytics.py`, `trends.py`, `training.py`, `metrics.py`, and `cardiac_drift.py` never import `mcp_strava.adapters.strava` or `mcp_strava.refresh`."
    - "AST/import boundary tests in `tests/test_security_guards.py` enforce that nothing outside `src/mcp_strava/adapters/strava/` imports `urllib.request`."
    - "`tests/test_security_guards.py::test_cli_has_operator_only_sql_and_explicit_db_safety_commands` (or an extension) asserts `db-refresh` is registered in `COMMANDS`."
    - "Existing Phase 2 guarantees remain green: no direct sqlite3 outside adapters/sqlite, db.py, and narrow tests; no schema-changing DDL on runtime read/report/sync/backfill paths."
    - "`just test` passes end-to-end with no live network calls."
    - "D-01: read-runtime/refresh-runtime split is enforced at the source-text level."
    - "D-02: read-runtime modules do not import Strava-facing modules; refresh-runtime is the only caller of the adapter."
    - "STRAVA-01: OAuth/HTTP/retry/rate-limit live in `adapters/strava/` end-to-end after integration."
  artifacts:
    - path: "src/mcp_strava/sync.py"
      provides: "Thin compatibility wrapper: sync_activities -> refresh.runtime.run_once(force=quick), backfill_activities -> refresh.runtime.run_backfill"
    - path: "src/mcp_strava/db.py"
      provides: "Legacy compatibility shims delegating to `mcp_strava.adapters.strava`"
    - path: "src/mcp_strava/cli.py"
      provides: "`db-refresh [--force]` operator command wired to refresh-runtime"
    - path: "tests/test_security_guards.py"
      provides: "Extended AST/import boundary guards for `adapters/strava`, `refresh`, `urllib`, and the one-way refresh->sync dependency"
  key_links:
    - from: "src/mcp_strava/sync.py"
      to: "src/mcp_strava/refresh/__init__.py"
      via: "sync_activities calls refresh.runtime.run_once(force=quick); backfill_activities calls refresh.runtime.run_backfill"
    - from: "src/mcp_strava/cli.py"
      to: "src/mcp_strava/refresh/__init__.py"
      via: "db-refresh [--force] command builds production collaborators and calls refresh.runtime.run_once(force=...)"
    - from: "tests/test_security_guards.py"
      to: "src/mcp_strava/refresh/"
      via: "AST walker rejects any `from mcp_strava.sync` import inside src/mcp_strava/refresh/**.py (D-17)"
    - from: "tests/test_security_guards.py"
      to: "src/mcp_strava/adapters/strava/"
      via: "AST walker enumerates forbidden imports from read modules"
---

<objective>
Rewire production entrypoints (CLI, sync, db.py compat) to use the Phase 3 adapter and refresh-runtime, and lock the new boundaries with AST/import guards so future changes cannot reintroduce read-runtime -> Strava coupling.

Purpose: Per D-01, D-02, STRAVA-01, STRAVA-03, REFRESH-01, REFRESH-02, and TEST-02, the refactor only delivers value once production callsites stop talking to legacy `db.py`/`sync.py` internals and the new boundaries are enforced by tests.
Output: refactored `sync.py`/`db.py`/`cli.py` plus extended `tests/test_security_guards.py` boundary tests and `tests/test_smoke.py` adjustments where necessary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-CONTEXT.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-RESEARCH.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-01-PLAN.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-02-PLAN.md
@.planning/phases/03-strava-adapter-refresh-runtime/03-03-PLAN.md
@src/mcp_strava/sync.py
@src/mcp_strava/db.py
@src/mcp_strava/cli.py
@tests/test_security_guards.py
@tests/test_smoke.py
</context>

<decision_traceability>
- D-01: Enforced by AST boundary tests preventing read modules from importing Strava-facing modules.
- D-02: Enforced by AST tests proving sync/MCP/read paths do not import the Strava adapter or refresh runtime, even though `sync.py` itself does (as the integration shim).
- D-13: Preserved end-to-end because the refresh-runtime persists only product-safe reason codes through Plan 03-03 repository methods.
- D-15: Implemented at the wiring layer by `sync_activities(quick=True)` calling `run_once(force=True, mode='quick')` and the CLI exposing `db-refresh --force`. Daily `sync_activities()` (no quick) calls `run_once(force=False, mode='quick')` — same idempotency as the scheduled refresh.
- D-16: Implemented at the wiring layer by `backfill_activities()` calling `run_backfill(...)` (NOT `run_once`).
- D-17: Implemented by deleting the seven helper symbols from `sync.py` (they live in `refresh/_sync_ops.py` after 03-02) and by the AST guard `test_refresh_does_not_import_sync` walking `src/mcp_strava/refresh/**.py`.
- STRAVA-01/STRAVA-03/REFRESH-01/REFRESH-02/TEST-02: Closed by integrating refresh-runtime into operator entrypoints and asserting end-to-end behavior through `just test` plus boundary guards.
</decision_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Refactor sync.py and db.py to thin adapter-backed compatibility layers</name>
  <files>src/mcp_strava/sync.py, src/mcp_strava/db.py</files>
  <read_first>
    - src/mcp_strava/sync.py
    - src/mcp_strava/db.py
    - src/mcp_strava/adapters/strava/__init__.py
    - src/mcp_strava/refresh/__init__.py
    - src/mcp_strava/adapters/sqlite/repository.py
    - src/mcp_strava/settings.py
    - .planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
  </read_first>
  <action>Refactor `src/mcp_strava/sync.py` so that `sync_activities(quick: bool = False)` and `backfill_activities(since: str | None = None)` become thin wrappers per D-15 and D-16.

`sync_activities(quick=False)`:
  1. `run_preflight(get_settings().database_path)` (unchanged).
  2. Open a `DbConn`; build `SQLiteRepository(conn)`.
  3. Construct production collaborators:
     - `refresh_transport = TokenRefreshTransport(client_id=settings.strava_client_id, client_secret=settings.strava_client_secret, clock=real_clock, sleeper=real_sleeper)` (D-18).
     - `token_provider = FileTokenProvider(settings.token_path, refresh_transport=refresh_transport, clock=real_clock)`.
     - `policy = RateLimitPolicy()`.
     - `transport = StravaTransport(token_provider, policy, clock=real_clock, sleeper=real_sleeper)`.
  4. Call `refresh.runtime.run_once(repo, transport, RefreshPolicy.from_settings(get_settings()), real_clock, real_sleeper, force=quick, mode='quick' if quick else 'daily')` (D-15).
  5. Return the `RefreshResult`/`RefreshSkipped` to the caller (existing callers may discard it, but downstream consumers — including the CLI in Task 2 — surface it).

`backfill_activities(since=None)`:
  1. Same preflight/repo/collaborator construction as above.
  2. Call `refresh.runtime.run_backfill(repo, transport, RefreshPolicy.from_settings(get_settings()), real_clock, real_sleeper, since=since, owner='refresh-backfill')` (D-16). Do NOT call `run_once`.

Delete from `sync.py` (D-17 — these names live in `refresh/_sync_ops.py` after 03-02):
  - `_sync_kudos`, `_insert_streams`, `_replace_streams`, `_stream_payload`, `STREAM_KEYS`, `_is_iso_day`, `_safe_quick_sync_start_day`.

Also delete from `sync.py`: local `RateLimiter` class, `_fetch_with_retry`, `_parse_rate_headers`, and any `from mcp_strava.db import load_env, api_request` imports. `sync.py` MUST NOT contain `import urllib` (D-01 enforced by AST guard).

Refactor `src/mcp_strava/db.py`: delete inline `load_env`, `save_env`, `refresh_token`, `api_request`, `_parse_rate_headers`. Provide minimal compatibility shims:
  - `refresh_token()` shim constructs a default `TokenRefreshTransport`, `FileTokenProvider` (D-18) and calls `token_provider.refresh()`.
  - `api_request(path, token=None)` shim builds default `FileTokenProvider`/`RateLimitPolicy`/`StravaTransport` and delegates, returning the same tuple shape `(data, rate_info)` legacy callers expect.

`get_zones()`: keep calling `api_request(...)` via the shim for Phase 3 (acceptable per CONTEXT.md; the small direct-SQLite write inside `db.py::get_zones` is grandfathered — Phase 4 will route it through a `ZonesService` over the repository). Add an explicit comment noting the Phase 4 follow-up so the next reviewer does not flag it.

`DbConn`, `init_db`, and `get_daily_trimp_history` remain unchanged (`init_db` already an assertion-only no-op per Phase 2).

Before deleting `load_env` from `db.py`, run a call-site audit (`grep -RE 'load_env|api_request|refresh_token' src/ tests/`) and update every caller to either (a) use the new `FileTokenProvider`/`TokenRefreshTransport` constructors directly (preferred for new code in `sync.py`/`cli.py`), or (b) keep using the shim (acceptable for read-time `get_zones`/legacy code paths). The audit results must produce zero unhandled `load_env` references outside `db.py` and `settings.py`.</action>
  <verify>
    <automated>just test</automated>
  </verify>
  <done>`sync.py` is a thin runtime shim; `db.py` no longer contains inline OAuth/HTTP/rate-limit logic; `just test` passes including all Phase 2 and Phase 3 tests.</done>
  <acceptance_criteria>
    - `grep -E '^class RateLimiter' src/mcp_strava/sync.py` returns no matches.
    - `grep -E '^def _fetch_with_retry|^def _parse_rate_headers' src/mcp_strava/sync.py` returns no matches.
    - `grep -E '^def _sync_kudos|^def _insert_streams|^def _replace_streams|^def _stream_payload|^STREAM_KEYS|^def _is_iso_day|^def _safe_quick_sync_start_day' src/mcp_strava/sync.py` returns no matches (D-17 — those names live in refresh/_sync_ops.py only).
    - `grep -E 'import urllib' src/mcp_strava/sync.py` returns no matches.
    - `grep -E 'from mcp_strava.db import .*api_request|from mcp_strava.db import .*load_env' src/mcp_strava/sync.py` returns no matches.
    - `src/mcp_strava/sync.py::sync_activities` accepts a `quick: bool = False` argument and calls `refresh.runtime.run_once(..., force=quick, mode='quick' if quick else 'daily')` (verifiable by reading the function body and asserting both `force=quick` and `mode=` keyword arguments are present).
    - `src/mcp_strava/sync.py::backfill_activities` calls `refresh.runtime.run_backfill(...)` and does NOT contain `run_once` (D-16).
    - `grep -E '^def refresh_token|^def api_request|^def load_env|^def save_env|^def _parse_rate_headers' src/mcp_strava/db.py` shows at most thin shim definitions (no inline OAuth URL construction).
    - `grep -E 'https://www.strava.com/oauth/token|api/v3' src/mcp_strava/db.py` returns no matches (string literals moved to adapter).
    - `just test` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire `db-refresh` CLI command and extend security boundary tests</name>
  <files>src/mcp_strava/cli.py, tests/test_security_guards.py, tests/test_smoke.py</files>
  <read_first>
    - src/mcp_strava/cli.py
    - tests/test_security_guards.py
    - tests/test_smoke.py
    - src/mcp_strava/refresh/__init__.py
    - src/mcp_strava/adapters/strava/__init__.py
    - .planning/phases/03-strava-adapter-refresh-runtime/03-PATTERNS.md
  </read_first>
  <action>Add `'db-refresh': cmd_db_refresh` to `COMMANDS` in `src/mcp_strava/cli.py`. Implement `cmd_db_refresh(args)`:
  - Parses `--force` (boolean; D-15).
  - Builds production collaborators (same shape as `sync_activities`, including `TokenRefreshTransport` per D-18).
  - Calls `refresh.runtime.run_once(..., force=args.force, mode='daily')`.
  - Prints a brief structured JSON summary to stdout: `{"status": "ok"|"failed"|"skipped", "reason": ..., "checkpoint_stage": ..., "forced": <bool>}`.
  - Exits with code `0` on `RefreshResult(status='ok')`, `1` on `RefreshResult(status='failed')`, `2` on `RefreshSkipped` (operator can distinguish from log messages).

Add `db-refresh` help text describing `--force` semantics (mid-day refresh; bypasses daily idempotency but honours lease/backoff).

Update `cli.py`'s imports from `db` so that `refresh_token`/`api_request` continue to import successfully (via the shims) but new operator paths preferentially use the adapter directly.

Extend `tests/test_security_guards.py` with:
  1. `test_cli_includes_db_refresh_command` asserting `'db-refresh' in _command_registry_names()`.
  2. `test_cli_db_refresh_accepts_force_flag_per_D15` asserting that the CLI's argparse subparser for `db-refresh` includes a `--force` flag, and that invoking it (with a mocked `run_once`) propagates `force=True` to the runtime call.
  3. `test_read_modules_do_not_import_strava_or_refresh` walking the AST of `report.py`, `analytics.py`, `trends.py`, `training.py`, `metrics.py`, `cardiac_drift.py` and asserting they do not import any name under `mcp_strava.adapters.strava` or `mcp_strava.refresh`.
  4. `test_urllib_lives_only_in_strava_adapter` walking all `src/mcp_strava/**.py` files and asserting `import urllib*` appears only in `src/mcp_strava/adapters/strava/`.
  5. `test_refresh_does_not_import_sync_per_D17` walking AST of every `src/mcp_strava/refresh/**.py` file and asserting no `from mcp_strava.sync` or `import mcp_strava.sync` statements exist. The test reports the offending file and line on failure.
  6. `test_sync_does_not_define_moved_helpers_per_D17` walking AST of `src/mcp_strava/sync.py` and asserting it does NOT define `_sync_kudos`, `_insert_streams`, `_replace_streams`, `_stream_payload`, `STREAM_KEYS`, `_is_iso_day`, `_safe_quick_sync_start_day` at the module level.
  7. `test_sync_activities_quick_invokes_run_once_with_force_true_per_D15` — patch `refresh.runtime.run_once` to a spy, call `sync_activities(quick=True)` with a hermetic SQLite fixture, and assert the spy received `force=True` and `mode='quick'`.
  8. `test_backfill_activities_invokes_run_backfill_per_D16` — patch `refresh.runtime.run_backfill` to a spy, call `backfill_activities(since='2024-01-01')`, and assert the spy was called with `since='2024-01-01'` and `owner='refresh-backfill'`; also patch `refresh.runtime.run_once` and assert it was NOT called.
  9. Update `test_sync_never_calls_init_db_and_db_init_db_has_no_ddl` to additionally assert that `sync.py` no longer defines `RateLimiter` or `_fetch_with_retry`.

Update `tests/test_smoke.py` only as needed to use the new operator entrypoints (preserve existing assertions; this is a smoke pass, not a behavior change).</action>
  <verify>
    <automated>python3 -m pytest tests/test_security_guards.py tests/test_smoke.py -q</automated>
  </verify>
  <done>CLI `db-refresh` is registered and wired; boundary tests cover read-module isolation and `urllib` containment; smoke test still passes; `just test` passes.</done>
  <acceptance_criteria>
    - `grep -E "'db-refresh'" src/mcp_strava/cli.py` returns at least one match.
    - `grep -E "'--force'" src/mcp_strava/cli.py` returns at least one match (D-15 CLI flag).
    - `tests/test_security_guards.py` contains `test_cli_includes_db_refresh_command`, `test_cli_db_refresh_accepts_force_flag_per_D15`, `test_read_modules_do_not_import_strava_or_refresh`, `test_urllib_lives_only_in_strava_adapter`, `test_refresh_does_not_import_sync_per_D17`, `test_sync_does_not_define_moved_helpers_per_D17`, `test_sync_activities_quick_invokes_run_once_with_force_true_per_D15`, and `test_backfill_activities_invokes_run_backfill_per_D16`.
    - `python3 -m pytest tests/test_security_guards.py tests/test_smoke.py -q` exits 0.
    - `just test` exits 0 end-to-end.
    - No test introduces live network access; existing `urllib.request.urlopen` monkeypatches in adapter/runtime tests remain in place.
    - Running `python3 -m mcp_strava db-refresh --help` shows the `--force` flag with a one-line description referencing mid-day refresh.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Read-runtime modules -> Strava | Must be unable to reach Strava at all (no imports). |
| CLI operator -> refresh-runtime | Single sanctioned path to trigger sync via `db-refresh`. |
| Compatibility shims -> adapter | Legacy callers continue working but route through the adapter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of Privilege / DoS | read modules accidentally importing Strava | mitigate | AST/import boundary tests in `tests/test_security_guards.py` walk the source tree and fail CI if a forbidden import lands. |
| T-03-09 | Tampering | CLI `db-refresh` wiring | mitigate | `db-refresh` builds collaborators identically to `sync_activities`; no live test runs unless the operator invokes the command. |
| T-03-SC | Tampering | package installs | accept | No package-manager installs are planned for this plan. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_security_guards.py tests/test_smoke.py -q`
- `just test`
- Grep guard: `grep -R 'from mcp_strava.adapters.strava\|from mcp_strava.refresh' src/mcp_strava/report.py src/mcp_strava/analytics.py src/mcp_strava/trends.py src/mcp_strava/training.py src/mcp_strava/metrics.py src/mcp_strava/cardiac_drift.py` returns no matches.
- Grep guard: `grep -R 'import urllib' src/mcp_strava/` shows results only inside `src/mcp_strava/adapters/strava/`.
- Grep guard (D-17): `grep -RE 'from mcp_strava\.sync|import mcp_strava\.sync' src/mcp_strava/refresh/` returns no matches.
- Grep guard (D-15): `grep -E 'force=quick|force=True|force=args\.force' src/mcp_strava/sync.py src/mcp_strava/cli.py` shows at least one match in each file.
- Grep guard (D-16): `grep -E 'run_backfill' src/mcp_strava/sync.py` shows at least one match in `backfill_activities`.
- Manual operator confirmation (out of default test loop): `python3 -m mcp_strava db-refresh` against the real `.env` updates `refresh_state.last_success_at` and reports a structured summary.
- Manual operator confirmation (D-15): `python3 -m mcp_strava db-refresh --force` after a same-day successful refresh re-runs the cycle and updates `last_success_at` again (NOT skipped).
</verification>

<success_criteria>
- STRAVA-01, STRAVA-03, REFRESH-01, REFRESH-02, and TEST-02 are observable end-to-end: the operator entrypoint runs the refresh-runtime, which uses the adapter, which talks to Strava through `urllib.request.urlopen` only inside `adapters/strava/`.
- Phase 2 guarantees are preserved: no direct sqlite3 outside the allowed list, no implicit DDL on read paths, no test mutates `data/strava.db`.
- The Phase 3 boundary is testable: any future PR that imports Strava code from a read module fails `tests/test_security_guards.py`.
</success_criteria>

<output>
Create `.planning/phases/03-strava-adapter-refresh-runtime/03-04-SUMMARY.md` when done.
</output>
