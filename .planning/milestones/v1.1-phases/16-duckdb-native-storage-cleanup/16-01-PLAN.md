---
phase: "16"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_duckdb_repository.py
autonomous: true
requirements:
  - "Storage-layer modernization to DuckDB-native types (post-v1.1 maintenance)"
must_haves:
  truths:
    - "test_activities_missing_kudos_with_window_days exists and is RED (fails) before Task 1 rewrites the kudos branch"
    - "The test seeds activities at relative day offsets and verifies window_days=7 filters correctly once native SQL is in place"
  artifacts:
    - path: "tests/test_duckdb_repository.py"
      provides: "window_days regression guard for kudos branch"
      contains: "test_activities_missing_kudos_with_window_days"
  key_links:
    - from: "tests/test_duckdb_repository.py"
      to: "src/mcp_strava/adapters/duckdb/kudos_store.py"
      via: "activities_missing_kudos(repo, window_days=7) call"
      pattern: "window_days"
---

<objective>
Add the missing window_days test for the kudos store and verify Assumption A1
(RepositoryActivityRow.date field) before Task 1 touches any production code.

The kudos `window_days` branch has never been tested — that is exactly why the
SQLite `date('now', ?)` bug hid undetected. This plan writes the failing test
first so the Task 1 rewrite has a regression guard.

The test is expected to FAIL when written against the current dead/broken SQL
(or be skipped via pytest.xfail). After Plan 16-02 rewrites the branch, the
same test must turn GREEN — that is the proof.

Purpose: Nyquist Dimension 8 — every conversion has automated verify; Wave 0
must exist before the Task 1 commit.
Output: One new test function in tests/test_duckdb_repository.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-duckdb-native-storage-cleanup/16-CONTEXT.md
@.planning/phases/16-duckdb-native-storage-cleanup/16-RESEARCH.md
@.planning/phases/16-duckdb-native-storage-cleanup/16-VALIDATION.md
@src/mcp_strava/adapters/duckdb/kudos_store.py
@tests/test_duckdb_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task W0-1: Confirm Assumption A1 and add window_days kudos test</name>
  <files>tests/test_duckdb_repository.py</files>
  <read_first>
    - tests/test_duckdb_repository.py lines 630-680 (existing kudos tests, fixture pattern)
    - src/mcp_strava/adapters/duckdb/kudos_store.py (full file — window_days branch at lines 51-54)
    - src/mcp_strava/adapters/duckdb/activity_rows.py (to_activity_row mapping — confirms date field)
    - src/mcp_strava/types_repository.py lines 188-203 (RepositoryActivityRow — confirms date: str field)
    - src/mcp_strava/adapters/duckdb/repository.py lines 278-330 (upsert_activity_summary — notes date param and ON CONFLICT UPDATE)
  </read_first>
  <action>
  **Step 1 — Verify Assumption A1 (do this before writing any test):**

  Grep `src/mcp_strava/` for `.date` attribute access on `RepositoryActivityRow`
  instances and on row variables returned by activity query functions:

      grep -rn "row\.date\b\|\.date\b" src/mcp_strava/ | grep -v "__pycache__" | grep -v "\.date()"

  Confirm:
  - `RepositoryActivityRow` has `date: str` field at `types_repository.py:193`
  - `activity_rows.py:12` constructs it as `date=str(row["date"])`
  - `projection_services.py:197` reads `row.date` on a `RepositoryActivityRow`
  - `repository.py:302` has `date=excluded.date` in ON CONFLICT UPDATE and inserts `date` in the INSERT

  Record these findings inline as a comment in the test file (a one-line NOTE
  near the top of the new test: `# A1-confirmed: RepositoryActivityRow.date exists
  at types_repository.py:193; cleaned in 16-02`).

  **Step 2 — Add the test:**

  Add `test_activities_missing_kudos_with_window_days` immediately after the
  existing `test_activities_missing_kudos_filters_and_returns_typed_ids` test
  (around line 669).

  Test shape (copy the existing fixture pattern — `_create_duckdb_fixture` +
  `DuckDBRepository.from_path`):

  1. Seed three activities with `start_date_local` values relative to today:
     - activity_id=301: today minus 1 day (format as ISO datetime string
       `(date.today() - timedelta(days=1)).isoformat() + "T06:00:00"`)
     - activity_id=305: today minus 5 days
     - activity_id=320: today minus 20 days
     All three must have `kudos_count=0` in summary_json so they appear as
     "missing kudos" candidates.

  2. Call `activities_missing_kudos(repo, window_days=7)`.
     Assert only activity IDs 301 and 305 are returned (320 is outside the 7-day
     window). Order is not guaranteed — compare as sets.

  3. Call `activities_missing_kudos(repo, window_days=None)`.
     Assert all three IDs are returned.

  **Mark the window_days=7 assertion with `pytest.xfail` and a message
  `"SQLite date('now') branch is dead/broken — rewritten in 16-02"`** so the
  test suite stays GREEN in Wave 0 but the intent is recorded. The xfail must be
  `strict=False` (i.e., if the current broken SQL somehow returns the right answer
  by coincidence the suite still passes).

  The `window_days=None` assertion must pass without xfail — it exercises the
  existing None-path which is already working.

  Import `datetime` and `timedelta` at the top of the test if not already
  present.
  </action>
  <verify>
    <automated>cd /home/j2h4u/repos/j2h4u/mcp-strava && uv run pytest tests/test_duckdb_repository.py -q -x -k "kudos"</automated>
  </verify>
  <done>
  - test_activities_missing_kudos_with_window_days exists in tests/test_duckdb_repository.py
  - The window_days=7 assertion is xfail (expected failure before 16-02 rewrites the SQL)
  - The window_days=None assertion passes
  - Assumption A1 is confirmed with inline comment
  - No production code is changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only change | No production code touched; no trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | test correctness | mitigate | xfail marks the broken branch explicitly; window_days=None path verifies the working path; no false-green risk |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new packages; DuckDB and pytest already in dependencies |
</threat_model>

<verification>
```
cd /home/j2h4u/repos/j2h4u/mcp-strava
uv run pytest tests/test_duckdb_repository.py -q -x -k "kudos"
```

All kudos tests pass (window_days xfail is expected, not a failure).
</verification>

<success_criteria>
- test_activities_missing_kudos_with_window_days added to tests/test_duckdb_repository.py
- window_days=7 branch is xfail with clear message pointing to 16-02
- window_days=None assertion passes cleanly
- Full kudos test block still green
- No production code changed
</success_criteria>

<output>
Create `.planning/phases/16-duckdb-native-storage-cleanup/16-01-SUMMARY.md` when done.
</output>
