---
phase: 05-mcp-http-surface-docker-hardening
plan: 05-03
type: tdd
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/mcp_strava/application/metric_services.py
  - src/mcp_strava/application/__init__.py
  - tests/test_metric_services.py
  - tests/test_metric_registry.py
autonomous: true
requirements:
  - MCP-01
  - MCP-03
  - TEST-03
must_haves:
  truths:
    - "D-03: this plan implements service backends for `compare_periods` and `project_fitness_state`."
    - "D-12: `compare_periods` compares synthetic metrics, not just volume/load basics."
    - "D-13: comparisons include period A/B values, delta, delta_pct, trend direction where meaningful, sample size, coverage/confidence, and missing reasons."
    - "D-14: comparison services return metric facts only and do not claim medical/coaching improvement."
    - "D-15/D-17: `project_fitness_state` preserves useful planning logic as model simulation output, not recommendation output."
    - "D-16: projection supports `rest`, `easy`, `maintain`, and `custom` scenarios."
    - "D-18: weekly plan/recommendation interpretation fields are not exposed, while load bonus, projected form, post-weekend simulations, trend inputs, and safety metric flags remain available."
    - "Review feedback: distribution metrics, sport-filter semantics, scenario templates, and custom scenario validation are explicit contracts, not executor discretion."
  artifacts:
    - path: "src/mcp_strava/application/metric_services.py"
      provides: "Period comparison and fitness projection service functions"
    - path: "tests/test_metric_services.py"
      provides: "Comparison/projection tests for synthetic metric trends and no-coaching contract"
---

<objective>
Add period comparison and fitness projection metric services so agents can compare training periods and simulate target-date fitness state without server-side recommendations.

Output: `compare_periods_service` and `project_fitness_state_service` with registry-driven metric comparison, scenario simulation, metadata, and no coaching labels.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-RESEARCH.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-PATTERNS.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-01-PLAN.md
@.planning/phases/05-mcp-http-surface-docker-hardening/05-02-PLAN.md
@src/mcp_strava/application/metric_registry.py
@src/mcp_strava/application/metric_services.py
@src/mcp_strava/training.py
@src/mcp_strava/analytics.py
@src/mcp_strava/report.py
@src/mcp_strava/types.py
@tests/test_metric_services.py
</context>

<decision_traceability>
- D-12 through D-14 are implemented by registry-driven `compare_periods_service`.
- D-15 through D-18 are implemented by `project_fitness_state_service`.
- MCP-01 and MCP-03 are covered for the remaining two tools.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for period comparison synthetic trends</name>
  <files>tests/test_metric_services.py, tests/test_metric_registry.py</files>
  <read_first>
    - tests/test_metric_services.py
    - tests/test_metric_registry.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/training.py
    - .planning/phases/05-mcp-http-surface-docker-hardening/05-CONTEXT.md
  </read_first>
  <action>Extend `tests/test_metric_services.py` with fixture data spanning two date windows and call `compare_periods_service(period_a_start="2026-05-01", period_a_end="2026-05-07", period_b_start="2026-05-08", period_b_end="2026-05-14", sport=None, connection=conn, signal_first_use=False)`. Assert the envelope contains freshness and completeness metadata. Assert `data["metrics"]` or equivalent contains comparison records keyed by registry ids including `trimp`, `distance_km`, `moving_time_min`, `elevation_m`, `fitness`, `fatigue`, `form`, `acwr`, `cardiac_cost`, `cardiac_cost_adjusted`, `cardiac_drift_pct`, `hr_recovery_median_bpm_per_min`, `hrr_pct`, `vertical_speed_m_per_h`, and `time_in_hr_zones_min` when fixture data supports them. For each comparison record assert fields `period_a`, `period_b`, `delta`, `delta_pct`, `trend_direction`, `sample_size`, `coverage`, and `missing_reasons` exist. Add assertions that global metrics appear in a global section and `per_sport` metrics appear under a sport key rather than averaged across all sports. Add sport-filter tests: when `sport="Run"`, global load/body metrics are computed from the filtered running activities only and the per-sport section contains only `Run`; no all-sport body values are mixed into a sport-filtered request. Add distribution tests for `time_in_hr_zones_min`: output must contain `period_a.buckets`, `period_b.buckets`, `bucket_deltas`, `bucket_delta_pct`, and `distribution_overlap_pct` or `None` with `missing_reasons`. Extend `tests/test_metric_registry.py` to assert every `comparison_mode != "none"` registry entry is either compared by `compare_periods_service` or explicitly skipped with a `missing_reasons` code. Add negative assertions rejecting phrases or keys `heart_improved`, `vessels_improved`, `ready`, `should`, and `recommendation`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q</automated>
  </verify>
  <done>Tests fail before `compare_periods_service` exists.</done>
  <acceptance_criteria>
    - Tests exercise both global and per-sport comparison paths.
    - Tests exercise sport-filtered comparison behavior and distribution metric output.
    - Tests require `delta`, `delta_pct`, `sample_size`, `coverage`, and `missing_reasons`.
    - Tests reject medical/coaching interpretation keys and phrases.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement registry-driven period comparison</name>
  <files>src/mcp_strava/application/metric_services.py, src/mcp_strava/application/__init__.py</files>
  <read_first>
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/application/metric_services.py
    - src/mcp_strava/analytics.py
    - src/mcp_strava/training.py
    - src/mcp_strava/metrics.py
    - tests/test_metric_services.py
  </read_first>
  <action>Add `compare_periods_service(*, period_a_start: str, period_a_end: str, period_b_start: str, period_b_end: str, sport: str | None = None, now: datetime | None = None, signal_first_use: bool = True, connection=None) -> ServiceEnvelope` to `src/mcp_strava/application/metric_services.py`. Build period aggregates through small helpers named `load_period_activities`, `aggregate_metric_values`, `compare_scalar_metric`, `compare_distribution_metric`, and `route_metric_by_sport_scope`, all driven by `METRIC_REGISTRY` metadata. For `sport_scope="global"`, aggregate over training sports as one body/load series when `sport is None`; when `sport` is provided, compute the same global metric ids from only the filtered sport's activities and set `scope_filter="sport"`. For `sport_scope="per_sport"`, compare only within each sport present in both the requested period and the registry-supported metric data; when `sport` is provided, include only that sport. For `sport_scope="both"`, return both global and per-sport sections when valid. Implement comparison modes: `sum`, `avg`, `median`, `last`, `min`, `max`, `trend`, and `distribution`. For `distribution`, return period bucket maps side by side plus `bucket_deltas`, `bucket_delta_pct`, and `distribution_overlap_pct` where computable; do not collapse distributions to one scalar. If data is insufficient, return a comparison record with `period_a.value` or `period_b.value` as `None` plus a `missing_reasons` value such as `insufficient_history`, `missing_hr`, `missing_streams`, `metric_not_applicable`, or `no_activity_in_period`. Compute `delta_pct` only when both values are numeric and period B is non-zero. Compute `trend_direction` as `up`, `down`, `flat`, or `unavailable` without saying whether up/down is good. Do not reuse or expose old recommendation text.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q</automated>
  </verify>
  <done>Period comparison tests pass.</done>
  <acceptance_criteria>
    - `src/mcp_strava/application/metric_services.py` defines `compare_periods_service`.
    - Returned comparison records include all fields required by Task 1.
    - Per-sport metrics are not averaged across multiple sports unless the registry marks them `both`.
    - `comparison_mode="distribution"` records use bucket-level deltas and never pretend a zone map is a scalar.
    - With `sport` set, body/load metrics are filtered to that sport and marked as sport-filtered rather than all-body values.
    - `python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add and implement fitness projection scenarios</name>
  <files>src/mcp_strava/application/metric_services.py, tests/test_metric_services.py</files>
  <read_first>
    - src/mcp_strava/training.py
    - src/mcp_strava/report.py
    - src/mcp_strava/constants.py
    - src/mcp_strava/application/metric_registry.py
    - src/mcp_strava/application/metric_services.py
    - tests/test_metric_services.py
  </read_first>
  <action>Extend `tests/test_metric_services.py` with tests for `project_fitness_state_service(target_date="2026-05-25", scenarios=["rest", "easy", "maintain"], custom_daily_trimp=None, connection=conn, signal_first_use=False)` and `project_fitness_state_service(..., scenarios=["custom"], custom_daily_trimp=[{"date": "2026-05-22", "trimp": 40.0}, ...])`. Assert each scenario returns daily rows with `projected_daily_trimp`, `projected_fitness`, `projected_fatigue`, and `projected_form`, plus `target_date_form`, `model_assumptions`, and freshness/completeness metadata. Assert scenarios are not ranked and no field is named `best_scenario`, `recommended_scenario`, `should_train`, `ready`, or `on_track`. Add validation tests for `custom_daily_trimp`: dates must be ISO `YYYY-MM-DD`, rows must be within today..target_date inclusive, dates must be unique and monotonic after sorting, TRIMP must be numeric and non-negative, horizon must be at most 90 days, and missing dates are filled as zero-load days. Implement `project_fitness_state_service` using existing `calc_banister`, `forward_simulate`, and `Config.Model.Banister` alpha values. Define `rest` as zero TRIMP through target date. Define `easy` as a deterministic numeric template by reading existing `Config.Plan` TRIMP constants if present; if those constants are absent, use the existing weekly-plan helper's numeric activity template TRIMP outputs and record `model_assumptions.template_source`. Define `maintain` as the mean nonzero daily TRIMP from the last 28 days applied on the same weekday pattern where history exists, with no-activity days filled as zero. Define `custom` as caller-provided date/TRIMP rows validated by this task. Include old useful facts as model facts: `progressive_load_bonus`, `activity_template_trimp`, and optional `post_weekend_monday_form` only when `target_date` is Friday, Saturday, or Sunday; otherwise omit it and include `missing_reasons=["target_not_weekend_context"]` in the scenario metadata. Export the function from `application/__init__.py`.</action>
  <verify>
    <automated>python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q</automated>
  </verify>
  <done>Projection services pass tests and remain non-prescriptive.</done>
  <acceptance_criteria>
    - `src/mcp_strava/application/metric_services.py` defines `project_fitness_state_service`.
    - Supported scenario names are exactly `rest`, `easy`, `maintain`, and `custom`.
    - `custom_daily_trimp` schema and validation behavior are covered by tests.
    - `easy` records whether its numeric template came from `Config.Plan` constants or old weekly-plan numeric template outputs.
    - `post_weekend_monday_form` has a documented absence condition when target date is not weekend-adjacent.
    - Projection output includes target-date form and daily projected fitness/fatigue/form rows.
    - Projection output contains no best/recommended/should/ready/on_track fields.
    - `python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Comparison engine -> agent | Engine returns numeric facts and deltas, not medical or coaching claims. |
| Projection engine -> agent | Engine simulates scenarios without choosing a plan. |
| Registry -> aggregation | Sport-sensitive metrics must not be mixed across incompatible sports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Integrity | Period comparison | mitigate | Registry-driven `sport_scope` controls global vs per-sport aggregation and tests cover both. |
| T-05-08 | Information Integrity | Projection scenarios | mitigate | Tests reject best/recommended/on_track/should fields and verify scenario outputs are parallel facts. |
| T-05-09 | Repudiation | Missing metric data | mitigate | Every comparison record includes sample size, coverage, and missing reasons. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_metric_services.py tests/test_metric_registry.py -q`
- `just test`
</verification>

<success_criteria>
- `compare_periods` compares registry-supported synthetic metrics with coverage and missing reasons.
- `project_fitness_state` simulates rest/easy/maintain/custom scenarios without recommendations.
- Period and projection services preserve freshness/completeness metadata.
- No medical/coaching claims are emitted by the service layer.
</success_criteria>

<output>
Create `.planning/phases/05-mcp-http-surface-docker-hardening/05-03-SUMMARY.md` when done.
</output>
