---
phase: 07-materialized-metrics-read-model
plan: 07-01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/mcp_strava/types.py
  - src/mcp_strava/adapters/sqlite/backup.py
  - src/mcp_strava/adapters/sqlite/schema.py
  - src/mcp_strava/adapters/sqlite/migrations.py
  - tests/test_sqlite_safety.py
  - tests/test_read_model_materialization.py
autonomous: true
requirements:
  - READMODEL-01
  - READMODEL-02
  - TEST-06
requirements_addressed:
  - READMODEL-01
  - READMODEL-02
  - TEST-06
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Phase 7 follows expert-panel defaults with no new product decision checkpoint."
    - "D-03/D-04: SQLite holds replaceable read-model facts beside the raw Strava mirror source of truth."
    - "D-05: runtime schema advances to user_version=5 with inventory for read-model tables and indexes."
    - "D-06: hot MCP metrics use wide fact tables."
    - "D-07 through D-13: source state, dirty queue, activity facts, daily facts, model facts, rolling facts, and run audit tables exist."
    - "D-10: `daily_load_facts` stores per-day/per-scope aggregates including activity counts, stream/HR coverage, observed/effective TRIMP, distance, moving time, elevation, high-zone totals, anomaly totals, and observed/partial/unknown/rest status."
    - "D-11: `training_model_daily` stores recursive daily model state including fitness, fatigue, form, ATL/CTL, ACWR, effective TRIMP, and related model inputs."
    - "D-36 through D-39: v5 migration uses preflight, pinned backup, post-check, idempotent DDL, and parity reporting."
    - "D-37: the last known-good pre-Phase-7 backup is pinned outside normal retention until migration, materialization, parity, performance, and Docker smoke are accepted."
    - "D-38: migration v5 creates read-model tables/indexes idempotently without rewriting source rows unless required; initial materialization/backfill is a separate resumable step."
  artifacts:
    - path: "src/mcp_strava/adapters/sqlite/schema.py"
      provides: "v5 required tables, columns, indexes, and row-count inventory"
    - path: "src/mcp_strava/adapters/sqlite/migrations.py"
      provides: "backup-protected migration to user_version=5"
    - path: "tests/test_read_model_materialization.py"
      provides: "Wave 0 read-model schema and provenance contract tests"
  key_links:
    - from: "src/mcp_strava/adapters/sqlite/migrations.py"
      to: "src/mcp_strava/adapters/sqlite/backup.py"
      via: "pinned pre-Phase-7 backup before v5 migration"
      pattern: "create_.*backup.*pre_phase_7"
    - from: "src/mcp_strava/adapters/sqlite/schema.py"
      to: "src/mcp_strava/adapters/sqlite/migrations.py"
      via: "required inventory validation after migration"
      pattern: "REQUIRED_.*5"
---

<objective>
Create the SQLite read-model schema foundation for Phase 7.

Purpose: establish user_version=5 tables, indexes, row-count inventory, and pinned backup behavior before any source writes or MCP services depend on materialized facts.
Output: v5 migration, read-model dataclass contracts, pinned pre-Phase-7 backup handling, and RED/GREEN tests for schema safety.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-materialized-metrics-read-model/07-CONTEXT.md
@.planning/phases/07-materialized-metrics-read-model/07-RESEARCH.md
@.planning/phases/07-materialized-metrics-read-model/07-PATTERNS.md
@.planning/phases/07-materialized-metrics-read-model/07-VALIDATION.md
@.planning/phases/02-sqlite-safety-repository-layer/02-01-SUMMARY.md
@.planning/phases/06-full-fidelity-strava-mirror/06-03-SUMMARY.md
@src/mcp_strava/adapters/sqlite/backup.py
@src/mcp_strava/adapters/sqlite/schema.py
@src/mcp_strava/adapters/sqlite/migrations.py
@src/mcp_strava/types.py
@tests/test_sqlite_safety.py
</context>

<decision_traceability>
- D-01/D-02 are honored by using the researched expert-panel defaults without requesting a new business decision.
- D-03/D-04/D-06 are implemented by SQLite wide fact tables, not request caches or external stores.
- D-05/D-07/D-08/D-09/D-10/D-11/D-12/D-13 are implemented by the exact v5 table set and inventory checks.
- D-36/D-37/D-38/D-39 are implemented by migration safety, pinned backup, idempotent DDL, and parity tests.
- READMODEL-01, READMODEL-02, and TEST-06 are covered by schema/provenance table creation and migration tests.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing v5 read-model schema and backup tests</name>
  <files>tests/test_sqlite_safety.py, tests/test_read_model_materialization.py</files>
  <read_first>
    - .planning/phases/07-materialized-metrics-read-model/07-CONTEXT.md
    - .planning/phases/07-materialized-metrics-read-model/07-VALIDATION.md
    - src/mcp_strava/adapters/sqlite/backup.py
    - src/mcp_strava/adapters/sqlite/schema.py
    - src/mcp_strava/adapters/sqlite/migrations.py
    - tests/test_sqlite_safety.py
  </read_first>
  <action>Create `tests/test_read_model_materialization.py` and extend `tests/test_sqlite_safety.py` with failing tests for Phase 7 user_version=5. Tests must assert that `run_migrations()` creates `activity_source_state`, `metric_dirty_activities`, `activity_metric_facts`, `daily_load_facts`, `training_model_daily`, `rolling_period_facts`, and `read_model_refresh_runs`. Tests must assert required columns by exact name, including `source_hash`, `source_revision`, `metric_version`, `computed_at`, `completeness_status`, `missing_reasons_json`, `attempt_count`, and `last_error` where applicable. Tests must assert required indexes by exact name: `idx_activity_source_state_day`, `idx_metric_dirty_lookup`, `idx_activity_metric_day_sport_version`, `idx_activity_metric_activity_version`, `idx_daily_load_day_scope_sport_version`, `idx_training_model_day_scope_sport_version`, `idx_rolling_period_asof_window_scope_sport_version`, `idx_activities_date_id`, and `idx_activities_sport_date_id`. Add backup tests proving the pre-Phase-7 migration backup is marked as pinned and retained by `enforce_backup_retention()` even when more than five regular backups exist. Add idempotency and source row-count parity tests proving a second `run_migrations()` keeps user_version=5 and does not rewrite `activities`, `streams`, `stream_channels`, `athlete_zones`, `sync_log`, `kudos`, `refresh_state`, or `refresh_requests` rows.</action>
  <verify>
    <automated>uv run pytest -q tests/test_sqlite_safety.py tests/test_read_model_materialization.py</automated>
  </verify>
  <done>Tests fail because v5 read-model tables, indexes, and pinned backup behavior do not exist yet.</done>
  <acceptance_criteria>
    - `tests/test_read_model_materialization.py` uses only `tmp_path` SQLite fixtures.
    - Tests assert all seven required read-model tables by exact table name.
    - Tests assert the exact v5 index names listed in the action.
    - Tests assert pinned pre-Phase-7 backups are excluded from normal retention deletion.
    - Tests assert `run_migrations()` remains idempotent at user_version=5.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement v5 schema inventory, migration, and pinned backup support</name>
  <files>src/mcp_strava/types.py, src/mcp_strava/adapters/sqlite/backup.py, src/mcp_strava/adapters/sqlite/schema.py, src/mcp_strava/adapters/sqlite/migrations.py</files>
  <read_first>
    - tests/test_read_model_materialization.py
    - tests/test_sqlite_safety.py
    - src/mcp_strava/adapters/sqlite/backup.py
    - src/mcp_strava/adapters/sqlite/schema.py
    - src/mcp_strava/adapters/sqlite/migrations.py
    - src/mcp_strava/types.py
  </read_first>
  <action>Add Phase 7 dataclass contracts in `src/mcp_strava/types.py` for `ActivitySourceState`, `MetricDirtyActivity`, `ActivityMetricFact`, `DailyLoadFact`, `TrainingModelDailyFact`, `RollingPeriodFact`, and `ReadModelRefreshRun`. Extend `backup.py` with pinned backup support using filenames or metadata that `enforce_backup_retention()` can recognize and preserve; migration code must create a pinned pre-Phase-7 backup before applying user_version=5. Extend `schema.py` required inventory for user_version=5 with the seven read-model tables and required indexes from Task 1, plus row-count reporting for each table. Add migration target `5` in `migrations.py` using idempotent `CREATE TABLE IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS` statements. The migration must not rewrite source rows. Use wide columns for hot facts: activity facts include TRIMP, HR zone seconds, HR recovery, vertical speed, cardiac cost, adjusted cardiac cost, cardiac drift, HRR, Z5 seconds, anomaly count, distance, moving time, elevation, HR sample counts, and stream sample counts. Daily/model/rolling tables must include scope fields (`scope`, `sport_type`) and `metric_version` indexes. Run `PRAGMA optimize` or `ANALYZE` after index creation in the migration path.</action>
  <verify>
    <automated>uv run pytest -q tests/test_sqlite_safety.py tests/test_read_model_materialization.py</automated>
  </verify>
  <done>v5 migration, inventory, pinned backup, and read-model dataclass tests pass.</done>
  <acceptance_criteria>
    - `MIGRATIONS` contains key `5`.
    - `run_migrations()` on a v4 fixture creates a pinned pre-Phase-7 backup before DDL.
    - `PRAGMA user_version` is `5` after migration.
    - `REQUIRED_TABLES_BY_VERSION` and `REQUIRED_INDEXES_BY_VERSION` include v5 read-model inventory.
    - Source mirror row counts are unchanged by the v5 migration.
    - The migration creates tables and indexes idempotently with no raw Strava API calls.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration gate -> live SQLite mirror | DDL crosses into durable local mirror state. |
| Backup retention -> restore path | Retention logic must not delete the last known-good pre-Phase-7 backup. |
| Raw mirror -> read model | Derived tables must remain rebuildable projections, not source-of-truth replacements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `run_migrations()` v5 | mitigate | Idempotent DDL plus row-count parity tests prove source rows are not rewritten. |
| T-07-02 | Information Disclosure | Backup files | mitigate | Backup helper preserves permissions from existing safety tests and does not expose token files. |
| T-07-03 | Denial of Service | SQLite fact indexes | mitigate | Required indexes and `PRAGMA optimize`/`ANALYZE` prevent unindexed hot fact scans. |
| T-07-SC | Tampering | package installs | accept | No new package-manager install tasks exist in this plan; research found no new dependency requirement. |
</threat_model>

<verification>
- `uv run pytest -q tests/test_sqlite_safety.py tests/test_read_model_materialization.py`
- `uv run pytest -q tests/test_full_fidelity_mirror.py tests/test_repository_boundary.py`
- `just test`
</verification>

<success_criteria>
- Runtime schema migrates to user_version=5 through the existing safety gate.
- All required read-model tables and indexes exist and are validated.
- The pre-Phase-7 backup is pinned outside normal retention.
- Existing raw mirror rows remain unchanged by schema creation.
</success_criteria>

<output>
Create `.planning/phases/07-materialized-metrics-read-model/07-01-SUMMARY.md` when done.
</output>
