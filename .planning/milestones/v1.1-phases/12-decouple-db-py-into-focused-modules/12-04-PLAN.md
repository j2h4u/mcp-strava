---
phase: 12-decouple-db-py-into-focused-modules
plan: 04
type: execute
wave: 3
depends_on: ["12-03"]
files_modified:
  - src/mcp_strava/cli.py
  - src/mcp_strava/refresh/bootstrap.py
  - src/mcp_strava/sync.py
  - tests/test_repository_boundary.py
  - tests/test_smoke.py
  - tests/test_phase01_validation.py
  - tests/test_security_guards.py
  - tests/test_cli_surface.py
autonomous: true
requirements:
  - "Core/domain separation — residual db.py coupling"
must_haves:
  truths:
    - "cli.py imports MirrorConn + StravaClient + SystemClock/SystemSleeper from the new homes and uses direct repository factories; no db.py import (top-level or local) remains in cli.py"
    - "the RealClock/RealSleeper aliases kept on bootstrap (and re-exported by sync) through wave 3 are removed once cli.py no longer imports them (alias cleanup)"
    - "the CLI-specific test monkeypatch sites are retargeted (the application/refresh/sync sites + conftest + the two sync.DbConn sites were already retargeted in 12-03): cli.api_request/cli.DbConn (test_cli_surface.py L182/185), legacy_db.api_request/refresh_token (test_repository_boundary.py L353/354 -> StravaClient home), DbConn->MirrorConn imports in test_smoke.py/test_phase01_validation.py/test_repository_boundary.py L369 — none left pointing at removed symbols"
    - "no monkeypatch.setattr(..., \"DbConn\"/\"api_request\", ...) in the CLI-scoped files targets a renamed/removed attribute (would fail at setattr) or silently no-ops via raising=False (would pass vacuously)"
    - "test_repository_boundary.py L353-354 patches the CONCRETE StravaClient network methods — `mcp_strava.adapters.strava.client.StravaClient.api_request` and `.refresh_token` (the path the repository must never invoke) — so the 'repository must never touch Strava' invariant cannot pass vacuously"
    - "security-guard negative-import literals reference the new StravaClient home; the now-dead init_db DDL guards are removed/retargeted (the sync.DbConn->MirrorConn retarget was done in 12-03)"
  key_links:
    - from: "tests/test_cli_surface.py"
      to: "src/mcp_strava/adapters/strava/client.py"
      via: "cli network guard patches the StravaClient lookup cli now resolves, plus cli.MirrorConn"
      pattern: "StravaClient"
---

<objective>
Migrate the last consumers of `db.py` (D-10 steps 4-5): `cli.py` and the test suite. After this plan, NOTHING in the repo imports `mcp_strava.db` — clearing the way for deletion in 12-05.

- `cli.py`: import `MirrorConn` + `StravaClient` from new homes; `refresh_token()` and the `cmd_strava_raw` `api_request` go through `StravaClient` (D-08); use direct repository factories (D-04); pick up `SystemClock`/`SystemSleeper` (D-07) directly from `adapters/strava/clock` (or the sync re-exports). Then remove the temporary `RealClock`/`RealSleeper` aliases left on `bootstrap.py` (and their `sync.py` re-export) in 12-03 — nothing imports them anymore.
- Tests (CLI-scoped only): repoint imports AND the CLI-path monkeypatch-by-path targets to the relocated module objects. **Wave-placement split (review HIGH — Codex; replan 2026-05-30):** the application/refresh/sync test retargets (`conftest.py`, `test_metric_services.py`, `test_refresh_runtime.py`, `test_application_services.py`, and the two `sync.DbConn`->`sync.MirrorConn` sites in `test_security_guards.py`) were MOVED INTO 12-03, alongside the production code they exercise, so 12-03's own full-suite gate lands green. THIS plan owns only the CLI-specific retargets: `test_cli_surface.py` (cli.api_request/cli.DbConn L182/185), `test_repository_boundary.py` (legacy_db.api_request/refresh_token L353/354 + the `DbConn`/`repository_from_connection` import L369), and `DbConn`->`MirrorConn` in `test_smoke.py`/`test_phase01_validation.py`. In `test_security_guards.py` this plan still retargets the negative-import literals to the new home and removes/retargets the dead `init_db` DDL guards (init_db is dead per RESEARCH RI-03/Open Question 1) — but NOT the sync.DbConn sites (done in 12-03).

Purpose: complete the hard-cut migration with the regression net intact (GP-02 — no shim left behind). A missed monkeypatch retarget would either FAIL at setattr (renamed attribute, no raising=False) or PASS VACUOUSLY (raising=False no-op bypassing the real object) — both verified as live risks against the checkout.
Output: cli + bootstrap-alias-cleanup + the CLI-scoped tests on new homes; `db.py` referenced by nothing; suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-CONTEXT.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md
@.planning/phases/12-decouple-db-py-into-focused-modules/12-PATTERNS.md
@src/mcp_strava/db.py
@src/mcp_strava/cli.py
@tests/conftest.py
@tests/test_security_guards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate cli.py to new homes + clean up bootstrap/sync RealClock aliases (D-04, D-07, D-08)</name>
  <files>src/mcp_strava/cli.py, src/mcp_strava/refresh/bootstrap.py, src/mcp_strava/sync.py</files>
  <read_first>
    - src/mcp_strava/cli.py (imports L15-23: DbConn, refresh_token, repository_from_connection, repository_from_path; RealClock/RealSleeper from refresh.bootstrap L19-23; cmd_strava_raw local `from mcp_strava.db import api_request` L122; clock/sleeper use L281-282; cmd_refresh)
    - src/mcp_strava/adapters/strava/client.py (StravaClient.api_request(path) — no token param — / refresh_token from 12-02)
    - src/mcp_strava/adapters/strava/clock.py (SystemClock/SystemSleeper)
    - src/mcp_strava/refresh/bootstrap.py (the RealClock=SystemClock / RealSleeper=SystemSleeper aliases added in 12-03)
    - src/mcp_strava/sync.py (the RealClock/RealSleeper re-export + __all__ entries L9-18)
    - src/mcp_strava/adapters/duckdb/connection.py (MirrorConn)
  </read_first>
  <action>
    Replace cli.py's `from mcp_strava.db import DbConn, refresh_token, repository_from_connection, repository_from_path` with: `MirrorConn` from `adapters/duckdb/connection`; `DuckDBRepository.from_connection`/`from_path` directly (D-04); and route `refresh_token()` (cmd_refresh) + the `cmd_strava_raw` `api_request` (local import L122) through a `StravaClient` instance (D-08) — preserve the call shapes (`token = refresh_token()` becomes a `StravaClient().refresh_token()`; `data, _rate = api_request(path)` becomes `StravaClient().api_request(path)` with NO token arg). Replace cli's `RealClock`/`RealSleeper` import (L19-23) and usage (L281-282) with `SystemClock`/`SystemSleeper` imported directly from `adapters/strava/clock`. Then, since nothing imports them anymore, DELETE the temporary `RealClock = SystemClock` / `RealSleeper = SystemSleeper` aliases from `bootstrap.py` and remove `RealClock`/`RealSleeper` from `sync.py`'s imports, re-exports, and `__all__` (L9-18). No db.py import — local or top-level — may remain in cli.py.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_cli_surface.py tests/test_security_guards.py -x && ! rg -q "mcp_strava\.db" src/mcp_strava/cli.py && ! rg -q "RealClock|RealSleeper" src/mcp_strava/cli.py src/mcp_strava/refresh/bootstrap.py src/mcp_strava/sync.py</automated>
  </verify>
  <done>cli.py imports MirrorConn + StravaClient + SystemClock/SystemSleeper from new homes, uses direct repository factories, calls StravaClient.api_request(path) with no token arg, and contains no `mcp_strava.db` import; RealClock/RealSleeper aliases removed from bootstrap.py and sync.py (no occurrences remain in src). Both grep checks are fail-closed (`! rg -q`) so a leftover reference fails the gate non-zero rather than printing FAIL and exiting 0.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget the CLI-scoped test sites — imports + cli/repo-boundary monkeypatches (review HIGH split + MEDIUM repo-boundary target)</name>
  <files>tests/test_repository_boundary.py, tests/test_smoke.py, tests/test_phase01_validation.py, tests/test_cli_surface.py</files>
  <read_first>
    - tests/test_repository_boundary.py (L348 `import mcp_strava.db as legacy_db`; L353-354 monkeypatch legacy_db.api_request/refresh_token in `test_repository_module_does_not_call_strava_network` — invariant: repository read path must NOT invoke Strava network; L369 `from mcp_strava.db import DbConn, repository_from_connection`)
    - tests/test_cli_surface.py (L182: `monkeypatch.setattr(cli, "api_request", forbidden, raising=False)`; L185: `monkeypatch.setattr(cli, "DbConn", forbidden, raising=False)`)
    - tests/test_smoke.py (L21), tests/test_phase01_validation.py (L9): `from mcp_strava.db import DbConn`
    - src/mcp_strava/adapters/strava/client.py (StravaClient.api_request / refresh_token — the network methods the repository must never call, from 12-02)
    - src/mcp_strava/cli.py (post-12-04 imports: which module object cli resolves StravaClient + MirrorConn through)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Test couplings table + Pitfall 1/2)
  </read_first>
  <action>
    Patch at the LOOKUP SITE (the module object that resolves the name at call time) so interception still works. The application/refresh/sync sites + conftest + the two sync.DbConn sites are already retargeted in 12-03 — do NOT touch them here. Retarget only the CLI-scoped sites:
    - test_cli_surface.py L182/L185: `cli.api_request` and `cli.DbConn` (both `raising=False`). After cli migration these names no longer exist on `cli`, so the guards no-op vacuously. Retarget to the symbols cli actually uses now: guard that cli does not call the network path via `StravaClient` and that it uses `MirrorConn` — patch `cli.MirrorConn` and the `StravaClient.api_request` lookup the test intends to forbid, dropping `raising=False` where the attribute should exist so a miss fails loudly.
    - test_repository_boundary.py L353-354 (review MEDIUM — both reviewers, concrete target now named): the test's invariant is "the DuckDB repository read path must never touch Strava." The code under test (`DuckDBRepository.from_path(...).recent_activities()`) does not import `db` after migration; the network methods now live on the StravaClient facade. Replace `monkeypatch.setattr(legacy_db, "api_request", _boom)` / `refresh_token` with `monkeypatch.setattr(mcp_strava.adapters.strava.client.StravaClient, "api_request", _boom)` and `monkeypatch.setattr(mcp_strava.adapters.strava.client.StravaClient, "refresh_token", _boom)` (import the class/module at the top of the test). This patches the CONCRETE network methods on the facade — if the repository ever routes through StravaClient the `_boom` fires; since the pure read path never constructs StravaClient, the assertion holds without passing vacuously. Drop the `import mcp_strava.db as legacy_db` line. Change L369 `from mcp_strava.db import DbConn, repository_from_connection` -> `MirrorConn` from `adapters/duckdb/connection` + `DuckDBRepository.from_connection`.
    - test_smoke.py + test_phase01_validation.py: `DbConn` -> `MirrorConn` from `adapters/duckdb/connection`.
    Note (review): `get_settings` patches in test_security_guards come from `settings`, not `db`, so are unaffected; that file is handled in Task 3 (literals + init_db only).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_repository_boundary.py tests/test_smoke.py tests/test_phase01_validation.py tests/test_cli_surface.py -x && ! rg -q "import mcp_strava\.db as legacy_db" tests/test_repository_boundary.py</automated>
  </verify>
  <done>CLI-scoped monkeypatch targets (cli network guards, repository-boundary StravaClient.api_request/refresh_token _boom, DbConn->MirrorConn imports) resolve to live module objects with no dead-attribute or vacuous-no-op sites; the four listed suites green; no `import mcp_strava.db as legacy_db` remains in test_repository_boundary.py (fail-closed grep).</done>
</task>

<task type="auto">
  <name>Task 3: Update security-guard literals + retire dead init_db guards</name>
  <files>tests/test_security_guards.py</files>
  <read_first>
    - tests/test_security_guards.py (L32-38 _assert_no_schema_ddl_in_init_db; L200-205 test_sync_never_calls_init_db_and_db_init_db_has_no_ddl; L468-485 negative-import literals mcp_strava.db.api_request/refresh_token — these are THIS plan's scope. The L637 + L700 `setattr(sync, "DbConn", ...)` sites were already retargeted to `sync.MirrorConn` in 12-03 Task 3 — do NOT modify them here, only confirm they are clean.)
    - .planning/phases/12-decouple-db-py-into-focused-modules/12-RESEARCH.md (Open Question 1 + Pitfall 2)
    - src/mcp_strava/adapters/strava/client.py (the new home for api_request/refresh_token)
    - src/mcp_strava/sync.py (now imports MirrorConn, not DbConn — from 12-03)
  </read_first>
  <action>
    Two retargets in this file (semantic, not just string-swap — review MEDIUM Codex). NOTE: the `sync.DbConn`->`sync.MirrorConn` monkeypatches (L637/L700) were ALREADY retargeted in 12-03 Task 3 (same wave as the sync.py migration) — do NOT redo them here; just confirm they are clean via the fail-closed grep.
    1. **Negative-import literals (L468-485):** retarget `mcp_strava.db.api_request` / `mcp_strava.db.refresh_token` to the new `StravaClient` home so the "metric/read modules must not import network code" guard keeps biting (PATTERNS Pitfall 2). This is a SEMANTIC retarget: the guard must forbid the read/product modules from importing the actual `StravaClient` network lookup path, and where product-handler guards previously forbade `DbConn`, they should now forbid `MirrorConn` — not merely swap the literal and pass vacuously.
    2. **init_db guards (L32-38 helper + L200-205 test):** init_db is dead and deleted in 12-05 (RESEARCH Open Question 1). Default per GP-02: remove `_assert_no_schema_ddl_in_init_db` and `test_sync_never_calls_init_db_and_db_init_db_has_no_ddl` (they assert on a function that ceases to exist). If the "runtime runs no schema DDL" invariant still matters, RETARGET it to assert against the live sync/runtime/bootstrap modules instead — but leave NO test referencing the symbol 12-05 deletes.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_security_guards.py -x && ! rg -q 'setattr\(sync,\s*"DbConn"' tests/test_security_guards.py && ! rg -q "mcp_strava\.db\." tests/test_security_guards.py</automated>
  </verify>
  <done>negative-import literals semantically forbid the StravaClient network path and MirrorConn in read/product modules; no test references the to-be-deleted init_db; the sync.DbConn sites (retargeted in 12-03) are confirmed absent via fail-closed grep; test_security_guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → code under test | monkeypatch interception must target the real lookup site after relocation |
| read-path modules → network code | security guard enforces no network import in metric/read paths (literal updated to new home) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | stale security-guard literal passes vacuously | mitigate | Repoint literals to StravaClient home in the same plan that finishes the facade migration (Task 3) |
| T-12-06 | Tampering | monkeypatch retarget misses real lookup site | mitigate | Patch where the symbol is looked up; targeted suites in Tasks 2-3 prove interception still works |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `! rg -q "from mcp_strava\.db import|import mcp_strava\.db|mcp_strava\.db\." src tests` passes fail-closed (no matches — the canonical gate finalized in 12-05; replaces the brittle AST walk, review LOW Codex). After 12-03 + this plan, the application/refresh/sync sites (12-03) and the CLI-scoped sites (this plan) are all retargeted, so the repo is db.py-reference-free entering 12-05.
- No `setattr(..., "DbConn"` / `setattr(..., "repository_from_connection"` / `setattr(cli, "api_request"` monkeypatch site remains anywhere under tests/ (every one retargeted across 12-03 + 12-04).
- monkeypatches intercept the migrated call paths; the repository-boundary guard patches the concrete `StravaClient.api_request`/`refresh_token` so it cannot pass vacuously.
- `uv run pytest -q` green; ruff + pyright clean.
</verification>

<success_criteria>
cli + all tests migrated to new homes; security guards updated; dead init_db guards retired; nothing references db.py; suite green.
</success_criteria>

<output>
Create `.planning/phases/12-decouple-db-py-into-focused-modules/12-04-SUMMARY.md` when done.
</output>
